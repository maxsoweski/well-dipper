# Untriaged findings from Steps 5 and 6 — the fresh session's FIRST task

**Written 2026-08-09 at the session seam, on Max's instruction:** *"First up in that fresh session
should be checking for bugs and unresolved issues, looking for anything we need to address before
proceeding."* This document is that list.

⛔ **PROVENANCE, AND THE HONEST CAVEAT.** These 27 findings were produced by adversarial verify
agents during the Step 5 and Step 6 workflows. They were classified **material** — meaning not
blocking for the commit they were found in — and were NOT acted on. They lived only in
`/tmp` workflow outputs, which do not survive a session boundary, which is why they are being
written down here rather than lost.

⚠ **I have personally verified TWO of them** (marked ✅ VERIFIED below). The other 25 are
**agent-reported and UNCONFIRMED**. Do not treat them as established defects; treat them as leads
with evidence attached. This program's own history says some will not reproduce — §2's headline
tidal figures did not, the plan's own 225-body limb population did not, and five canary
superlatives did not. **Reproduce before repairing.**

## How to work this list

1. Reproduce the finding first. If it does not reproduce, say so IN THIS FILE and strike the row —
   a finding that evaporates is worth recording as evaporated.
2. Anything that reproduces and is a live defect gets a `CARRIED.md` row with a named clearing step.
3. Anything that is a measurement limit gets written into the gate's own source, not only a ledger
   (§11.9 — "a limit recorded only in a ledger has been forgotten, not accepted").

---

## ⭐ TIER 1 — the four with the largest blast radius, read these first

| id | finding | evidence | proposed fix |
|---|---|---|---|
| **S6-M2** | Every boot now compiles the 363,566-byte lab fragment shader and allocates a full 351-uniform lab material with six placeholder textures — while the lab pipeline is OFF by default, so nothing draws that program. | src/rendering/ShaderWarmup.js:103 `const VARIANT_ORDER = ['gas', 'rocky', 'exotic', 'lab'];` is the default, and src/main.js:7036 `warmPlanetPrograms(retroRenderer.renderer, camera, { target: retroRenderer.sceneTarget });` passes no `variants` — I read the surrounding block (:7025-7040) and the call is unconditional, fire-and-forget on the title screen. `grep -rn 'labGasBodiesEnabled\|labGasBodiesFlag\|__wdLabGasBodies\|wd.labGasBodies' src/` returns ZERO hits in src/rendering/ShaderWarmup.js — there is no flag gate anywhere in the warm-up path. Meanwhile src/objects/Planet.js:2127 `export con | Gate the lab warm-up on the flag it serves. Either make the `lab` entry conditional — `if (labGasBodiesEnabled()) …` around its inclusion in the resolved variant list (ShaderWarmup.js already imports from ../objects/Planet.js, so no new cycle) — or drop `'lab'` from VARIANT_ORDER's default and have the flag-on path opt in via main.js:2591 `_lab.warmShaders({variants:[...,'lab']})`. Either way the  |
| **S6-M9** | With the 6e flag OFF — the shipped default — every boot now hands a 366,262-byte lab fragment shader and a full 356-uniform lab material to the driver. This is the one place flag-OFF does not reproduce legacy behaviour. | src/rendering/ShaderWarmup.js:180 `const VARIANT_ORDER = ['gas', 'rocky', 'exotic', 'lab'];` consumed at src/rendering/ShaderWarmup.js:176 `const { target = null, variants = VARIANT_ORDER } = opts;`, and the only production caller is src/main.js:7036 `warmPlanetPrograms(retroRenderer.renderer, camera, { target: retroRenderer.sceneTarget });` — no `variants` key. EXECUTED: `buildProbeMaterial('lab')` returns `isLabPlanetMaterial === true`, 356 uniforms, fragmentShader 366262 bytes; the game's three variants measure 27887 / 46146 / 34841. Confirmed `grep -rn setLabGasBodiesOverride src/` has exa | Make the default warm set flag-aware: in `_warmPlanetPrograms`, default to `labGasBodiesEnabled() ? VARIANT_ORDER : VARIANT_ORDER.filter(k => WARMUP_VARIANTS[k].kind !== 'lab')`, and record the decision in `report.requested` so a reader sees WHY lab is absent. Reading the flag at warm time is correct because localStorage/`__wdLabGasBodies` are both set before boot. Keep `warmShaders({variants:['ga |
| **S5-M1** | `bandRough` drag-persistence regression: the `_driverTouched` guard is now dead. `rebakeE5Bands` honours it, then `applyDrivers` overwrites `state.bandRough` unconditionally through the pack mirror. | Guard: `planet-lod-lab.html:1787` `if (!_driverTouched.has('bandRough')) state.bandRough = _deck.drivers.uBandRough;`. Clobber: `planet-lod-lab.html:2302` `Object.assign(state, giantDeckLabState(_deck));`, because `src/worldengine/drivers/giantDeck.js:106` `uBandRough: 'bandRough',` is inside LAB_STATE_BINDING and `giantDeckLabState` copies every emitted key. EXECUTED simulation of the exact lab sequence on a real Jovian deck: dragged value 1.77 → after the rebakeE5Bands guard `state.bandRough = 1.77` (guard held) → after `Object.assign` `state.bandRough = 1.0559544676914812` (drag lost). appl | Remove `uBandRough: 'bandRough'` from LAB_STATE_BINDING in src/worldengine/drivers/giantDeck.js — rebakeE5Bands already owns that write and already applies the correct policy. (Alternative, if the game needs the binding: have the lab call `giantDeckLabState(_deck)` and delete touched keys before the assign.) Add a test that drives the sequence with `_driverTouched` non-empty; today nothing does. |
| **S5-M8** | The lab mirror silently destroys a user's `bandRough` override — the `_driverTouched` guard is defeated one line after it runs. | Structure: `git show 4e864bc:planet-lod-lab.html | awk 'NR>=1933 && NR<=2740' | grep 'state.band'` returns 8 fields, `bandRough` NOT among them; the old guarded write was at :1788. Executed proof (node, real modules): `giantDeckLabState(deck)` returns keys `bandStrength, bandContrast, bandWarp, bandTint, bandRough, jetStrength, jetSpeed, jetShearTurb, jetFestoon`; simulating `state.bandRough = 1.75` with `_driverTouched = new Set(['bandRough'])` -> after the guarded write 1.75 (preserved) -> after `Object.assign(state, mirror)` 1.173453753441572 (LOST). Full suite of 552 neighbour tests stays  | Filter the mirror through `_driverTouched` before assigning, in planet-lod-lab.html at the `Object.assign` site: `const _m = giantDeckLabState(_deck); for (const k of Object.keys(_m)) if (_driverTouched.has(k)) delete _m[k]; Object.assign(state, _m);`. Minimum change is `delete _m.bandRough` only (the other eight were unguarded before this step, so guarding them would itself be a behaviour change) |

**✅ VERIFIED BY THE AUTHOR (2026-08-09):** `src/rendering/ShaderWarmup.js:3` imports
`buildLabProbeMaterial` from `LabPlanetMaterial.js` and builds a lab probe at boot. That is the
mechanism S6-M2 and S6-M9 describe — a full lab material allocated on the shipped default path with
the 6e flag OFF. What is UNCONFIRMED is the cost figure and whether it is on every boot or only
under warm-up; measure before repairing.

---

## TIER 2 — real mechanisms, smaller blast radius

| id | finding | evidence | proposed fix |
|---|---|---|---|
| **S5-M4** | `resolveDriver` — the exported resolver, and the one the shipped lab path actually uses — never validates the display policy. It reads `ctx.displayRadiusEarth` raw. Only `writePackUniforms` calls `assertDisplayPolicy`. | src/worldengine/port/writePackUniforms.js:185 `v = featureFrequencyFromKm(ctx.displayRadiusEarth, d.featureSizeKm, d.cFeature);` vs src/worldengine/port/writePackUniforms.js:225 `assertDisplayPolicy(ctx);`. Executed probe on `sizeKm(398, 1.0)`: no `displayRadiusEarth` -> **NaN, no throw**; `'4'` -> **64.03015075376885** (the game value, coerced); `0` -> 0; `-4` -> -64.03; `null` -> 0. All five are shapes tests/pack-contract.test.js:152 asserts are rejected — but it asserts them only through `writePackUniforms`. TOCTOU control: a ctx with `get displayRadiusEarth(){ return n++===0 ? 4 : undefine | One line, verified: at src/worldengine/port/writePackUniforms.js:185 replace `ctx.displayRadiusEarth` with `assertDisplayPolicy(ctx)`. I applied it and re-ran: all five probe cases now throw `PackContractError`, the TOCTOU getter throws, and tests/pack-contract.test.js + tests/driver-pack-giantdeck.test.js stay **103/103 green**. Restored after measuring. |
| **S5-M7** | `LAB_MIRROR_CTX` hardcodes `displayRadiusEarth: 1` inside the shared tree — a default display policy living in exactly the place 5a's thesis forbids one. | src/worldengine/drivers/giantDeck.js:118 `displayRadiusEarth: 1, animRate: 1, relevance: {},` consumed at src/worldengine/drivers/giantDeck.js:287 `out[stateField] = resolveDriver(uName, deck.drivers[uName], LAB_MIRROR_CTX);`. Measured: a km-shaped `uBandRough` (which IS in `LAB_STATE_BINDING`) mirrors as **16.0075** where the lab-correct value on a 4 R⊕ body is **32.0151** and the game-correct is **64.0302**. Mitigation confirmed loud: making `uBandRough` km-shaped in the real file reds **21 tests** including tests/driver-pack-giantdeck.test.js:209 `expect(hasKmShapedDriver(deck.drivers), …). | Make the mirror derive its ctx from the caller's rather than fabricating one: `giantDeckLabState(deck, ctx)` resolving with `{ ...ctx, animRate: 1, gates: <every gate true>, relevance: <every key 1> }`, so `displayRadiusEarth` is carried through from the front-end and cannot be silently wrong. Delete `LAB_MIRROR_CTX`. planet-lod-lab.html:2301 already has `_dctx`… no it does not — it is in `applyDr |
| **S5-M5** | The lab's real pack call site is REQUIRED but UNPINNED. Nothing in the repo asserts it uses the lab's display policy — swapping it to the game's policy is completely invisible. | CONTROL, executed on the real file: planet-lod-lab.html:1742 `displayRadiusEarth: visScaleOf(state.planetRadiusEarth ?? 1),` changed to `(state.planetRadiusEarth ?? 1)` — i.e. the GAME's policy at the LAB's call site. Ran tests/driver-pack-giantdeck.test.js, tests/pack-contract.test.js, tests/vis-scale-fence.test.js, tests/lab-surface-ratchet.test.js, tests/radius-live-feed.test.js, tests/radius-live-feed-fence.test.js: **274 passed, 0 failed**. Restored (md5 verified). Note the repo already owns the counter-idiom for the frame() sites: tests/vis-scale-fence.test.js:221 `expect(lab).toMatch(/f | Add a source pin in tests/driver-pack-giantdeck.test.js, which already reads the lab at tests/driver-pack-giantdeck.test.js:61 `const LAB_RAW = readFileSync(join(ROOT, 'planet-lod-lab.html'), 'utf8');` — assert `LAB_RAW` matches `/displayRadiusEarth:\s*visScaleOf\(/` inside the `_dctx` literal, with a comment saying the game's counterpart is `gameDisplayRadiusEarth(R)`. That is one line in a file  |
| **S5-M10** | GATE 3's `>= 3 of 5 regimes represented` half is inert — random labels pass it with a better margin than the real classifier. | Executed over the same 200-seed / 354-gas-body population the test uses: (A) real classifier -> regimes=3, interior=86, PASSES. (B) labels cyclically permuted so EVERY body is mislabeled -> regimes=3, interior=79, PASSES. (C) per-body RANDOM label `KEYS[fnv1aString(id) % 5]`, zero physics -> regimes=5, interior=44, PASSES with a wider margin than the truth. (D) the mutant the pack's own comment forbids — classifying the PERTURBED condition after `drawGiantConditions` -> regimes=4, interior=101, PASSES. | Either drop the regime-coverage clause (the interior clause carries the gate on its own) or make it discriminating: pin the per-regime POPULATION SHARE against a fixture, or assert per-body agreement with the classifier's own pinned expectations. Also record the measured fact — only `sub-neptune`, `saturnian`, `neptunian` are reachable from generated bodies; `gas-giant` and `hot-jupiter` never occ |
| **S5-M6** | `Object.assign(state, …)` smuggles brand-new state fields past the 5f shrink-only ratchet — and that is the exact idiom Step 5c introduced into the file the ratchet watches. | Scan rule: tests/lab-surface-ratchet.test.js:199 (the `state\s*\.\s*([A-Za-z_$]…)…=` regex). Live idiom: planet-lod-lab.html:2301 `Object.assign(state, giantDeckLabState(_deck));`. CONTROL, executed: appended `Object.assign(state, { __smuggledField: 0.5, __smuggledField2: 1 });` immediately after that line in a copy, ran via `WD_LAB_SURFACE_SRC` — **17/17 PASS, no growth reported**. Positive control on the same harness: injecting `state.__fakeRatchetField = 0.5;` into `applyDrivers` DOES red (`applyDrivers state field GREW by 1: __fakeRatchetField`, 2 failed / 15 passed), so the harness is liv | Extend `measureLabSurface` with a fourth entry class: collect every `Object.assign(\s*state\s*,` occurrence in a watched region, keyed by the second argument's source text. An object-literal second argument (a hand-written field list) should be treated as growth outright; a named helper (`giantDeckLabState`) should be pinned by name so swapping in a different mirror is growth. Add the smuggle case |
| **S5-M9** | The shrink-only ratchet is defeated by one helper function, which is the lab's own idiom — the KNOWN LIMITS block understates the residual by a lot. | Executed against the REAL harness via the shipped `WD_LAB_SURFACE_SRC` hook: injected `function applyBypassFeature(){ state.__bypassField = 1.0; uniforms.uBypassUniform.value = state.__bypassField; }` immediately above `function applyDrivers(){` plus a call `applyBypassFeature();` as the first statement inside `applyDrivers`. Result: `Test Files 1 passed / Tests 17 passed` — the whole ratchet green on a brand-new un-packed feature. Compare the lane's CONTROL A/B/B2, which all inject INTO the two watched bodies and correctly red. | Make the watched region the transitive call graph rather than two lexical extents, or (cheaper and adequate) add a fourth pinned set: the set of function names CALLED from `applyDrivers()`/`frame()`. A new callee then reds with the same route-out message, and `applyStormState`/`rebakeE5Bands` are already in the baseline so they cost nothing. Also correct the KNOWN LIMITS text at tests/lab-surface- |
| **S5-M3** | New uncaught-throw path on `macroSeed === 0`, widened from gas bodies to every body class. | Throw site: `src/worldengine/port/writePackUniforms.js:126` `if (!Number.isInteger(macroSeed) || macroSeed === 0) {`, called from `src/worldengine/drivers/giantDeck.js:161` `const macroSeed = assertMacroSeed(ctx.macroSeed);` — which runs BEFORE `src/worldengine/drivers/giantDeck.js:163` `const gas = compositionClass(condition) === 'gas';`. Lab passes it raw at `planet-lod-lab.html:1747` `macroSeed: state.macroSeed | 0,`. Unclamped restore path: `planet-lod-lab.html:4680` `// ▸ Presets — lil-gui save/load round-trips the whole panel as JSON`. Clamped entries are only the slider (:4677), newPlan | Clamp at the read site as well as the entry points: `macroSeed: Math.max(1, state.macroSeed | 0),` at planet-lod-lab.html:1747 (line-count-neutral). Keep `assertMacroSeed` strict — it is the contract for the game — but do not let the lab's one un-clamped restore path turn a stale saved preset into a dead lab. |
| **S6-M7** | On a swapped planet-class moon the lab material's `uCameraPosObj` is never written for the life of the body, so the whole view-dependent channel renders against the lab's fixed fake camera at (0,0,3). No row rules this, and it is a swap-induced loss the legacy material does not have. | src/main.js:7373 `lodManager.register(planet);` and src/main.js:7437 `lodManager.register(moon);` are the only two registrations, and :7437 sits in the `else` arm (non-planet moons) at src/main.js:7432 `} else {`. src/rendering/LabPlanetMaterial.js:562 `if (Number.isFinite(opts.distanceRadii)) {` and src/rendering/LabPlanetMaterial.js:572 `if (opts.cameraWorldPos && opts.mesh && u.uCameraPosObj) {` are the two guards. planet-lod-uniforms.js:29 `uCameraPosObj: { value: new THREE.Vector3(0, 0, 3) },` is the frozen default; planet-lod-shaders.glsl.js:511 `vec3 V = normalize(uCameraPosObj - vPos); | Two acceptable closes, and the ledger should record which was chosen: (a) register planet-class moons with `lodManager` at src/main.js:7437's sibling so `setReliefDetail` reaches them, or (b) if that is out of scope for Step 6, make `labPipelineAdmits` refuse a body that is not on the per-frame LOD path, and rule the refusal in the ledger. Either way add a row to §3 naming `uCameraPosObj`/`uLodRam |
| **S6-M14** | The `_ordinal` clause — added specifically to block the 5d `'undefined:undefined'` macroSeed collapse — is defeated by string interpolation on the planet-class-moon route: a missing ordinal becomes the present string `'pm-undefined'`. | src/main.js:7416 `_ordinal: \`pm-${moonData._ordinal}\`,`. EXECUTED on real Sol data: raw `m._ordinal` is `undefined` → `scenePMData._ordinal` is the string `"pm-undefined"`; `worldEngineProvenance` reports no `no _ordinal` blocker for it. Re-seeded to a non-Sol system, two such moons produce the IDENTICAL `labMacroSeed` 2963621119 (src/objects/Planet.js:2218 `return fnv1aString(String(d && d._systemSeed) + ':' + String(d && d._ordinal)) || FNV_OFFSET_BASIS;`). The clause's rationale is at src/objects/Planet.js:2101-2104. Procedural safety verified at src/generation/StarSystemGenerator.js:597  | Guard the interpolation at the source: `_ordinal: moonData._ordinal == null ? null : \`pm-${moonData._ordinal}\`` so absence stays absent, and add a test that feeds a planet-moon with `_ordinal: undefined` through the main.js transform and asserts `worldEngineProvenance(...).blockers` contains `'no _ordinal'`. |

---

## TIER 3 — gates that may not bite, and scope/consistency defects

⚠ Several of these are the SAME shape this session kept finding: a guard that is green because it
cannot fail. That shape has been wrong three times and right three times here, so it earns
reproduction rather than assumption.

| id | finding | evidence | proposed fix |
|---|---|---|---|
| **S6-M1** | The mount-site fence — compose's central ratchet against 'the lab material appears somewhere nobody ruled on' — is bypassed by a one-line indirection, and this very commit-set already uses that indirection. | The fence pins a TOKEN: tests/gas-body-lab-material.test.js:754 `expect(liveCallers('buildLabPlanetMaterial(').sort()).toEqual(EXPECTED_MOUNT_SITES);` with EXPECTED_MOUNT_SITES = {src/main.js, src/objects/Planet.js}. But src/rendering/ShaderWarmup.js:96 `lab:    { kind: 'lab',  build: () => buildLabProbeMaterial(), bytes: () => _labProbeBytes() },` mounts a real lab material on a warm-up mesh at every boot, and the fence is green. EXECUTED CONTROL: I prepended `import { buildLabProbeMaterial } from '../LabPlanetMaterial.js'; export function __sneakyMount() { return buildLabProbeMaterial(); }`  | Scan the transitive set, not one token. Either add `buildLabProbeMaterial(` to the scanned token list (a one-line change that closes today's hole), or compute the reachability closure from LabPlanetMaterial.js's exported builders so any future re-export is covered by construction. Then add src/rendering/ShaderWarmup.js to EXPECTED_MOUNT_SITES with its role ('the 6c warm-up probe — a throwaway mesh |
| **S6-M12** | The mount-site fence is blind to the third mount site these four lanes add. ShaderWarmup.js now builds a full lab material on every boot through the alias `buildLabProbeMaterial`, and the fence — which greps the literal `buildLabPlanetMaterial(` — stays green. | tests/gas-body-lab-material.test.js:754 `expect(liveCallers('buildLabPlanetMaterial(').sort()).toEqual(EXPECTED_MOUNT_SITES);` with EXPECTED_MOUNT_SITES at tests/gas-body-lab-material.test.js:745 listing only src/main.js and src/objects/Planet.js. The unseen mount: src/rendering/ShaderWarmup.js:97 `lab:    { kind: 'lab',  build: () => buildLabProbeMaterial(), bytes: () => _labProbeBytes() },` calling src/rendering/LabPlanetMaterial.js:263 `return buildLabPlanetMaterial().material;`. EXECUTED: `npx vitest run tests/gas-body-lab-material.test.js` → 53 passed, with ShaderWarmup mounting a materia | Scan for BOTH tokens: `liveCallers('buildLabPlanetMaterial(')` ∪ `liveCallers('buildLabProbeMaterial(')`, and add `'src/rendering/ShaderWarmup.js'` to EXPECTED_MOUNT_SITES with its role ('warm-up probe — builds the material to link the program, never mounts it on a body'). Update the stale :742 comment in the same edit. |
| **S6-M11** | 6c's entire deliverable — the `lab` entry existing in the warm-up registry — is watched by nothing. EXECUTED CONTROL: I deleted it and the whole relevant suite stayed green. | MUTANT (applied to the shipped file, suite run, file restored — md5 40a7780a5e6067b496bb8b0166c16e6d before and after): removed the line src/rendering/ShaderWarmup.js:97 `lab:    { kind: 'lab',  build: () => buildLabProbeMaterial(), bytes: () => _labProbeBytes() },` and rewrote src/rendering/ShaderWarmup.js:180 to `['gas','rocky','exotic']`. `npx vitest run tests/shader-warmup-source-parity.test.js tests/instrument-d-frame-survival.test.js tests/swap-ledger.test.js tests/gas-body-lab-material.test.js tests/lab-surface-ratchet.test.js tests/material-parity-list.test.js tests/pack-contract.test. | In tests/shader-warmup-source-parity.test.js replace the hardcoded `['gas','rocky','exotic']` literal with `Object.keys(WARMUP_VARIANTS)`, and add two assertions: `expect(Object.keys(WARMUP_VARIANTS)).toContain('lab')` and `expect(VARIANT_ORDER).toEqual(expect.arrayContaining(Object.keys(WARMUP_VARIANTS)))`. Add the source-parity exemption for `lab` explicitly (it has no second expression by const |
| **S6-M4** | 6c's own deliverable is unwatched — I confirmed the declared hole moves ZERO tests, not 'few'. | EXECUTED: removed the `lab:` entry from `WARMUP_VARIANTS` (src/rendering/ShaderWarmup.js:96) AND `'lab'` from src/rendering/ShaderWarmup.js:103 `const VARIANT_ORDER = ['gas', 'rocky', 'exotic', 'lab'];` in the isolated tree, then ran the FULL suite: 26 failed / 5095 passed. Unmutated same tree: the identical 26 (2 in driver-pack-giantdeck — GATE 5 CONTROL, an artifact of the non-git tree, plus the 5e tripwire — and 24 in ProcgenSnapshot/byteSafety). Not one test moved. File restored, md5 verified. `tests/shader-warmup-source-parity.test.js` iterates a hardcoded `['gas','rocky','exotic']` and n | In tests/shader-warmup-source-parity.test.js replace the three-element literal with `Object.keys(WARMUP_VARIANTS)`, add `expect(Object.keys(WARMUP_VARIANTS)).toContain('lab')` and `expect(VARIANT_ORDER).toEqual(expect.arrayContaining(Object.keys(WARMUP_VARIANTS)))`. Same file should pick up the two other untested importables the lane named: `labShaderSource()`'s cache-bust and `buildLabProbeMateri |
| **S6-M8** | §4's closure claim — 'an unclaimed symbol fails the test, which is what makes a new hardcoded effect in the legacy shader an undeclared loss the build catches' — is false, and the shader it measures contains two live counterexamples right now. The extractor collects only DECLARATIONS, and §8's control injects a declaration, so the control is shaped to the gate rather than to the risk. | tests/material-parity-list.test.js:121 `export function extractBranchLocals(fragmentShader) {` collects matches of a declaration regex only. I ran it against the shipped `PLANET_SHADER_VARIANTS.gas.fragmentShader`: branch 6 = {bands, glowColor, hotspot, lat, nightSide, starFacing, swirl}, branch 10 = {bands, haze, lat} (dump at /home/ax/projects/well-dipper/scratchpad/verify/gas-symbols.json). The two unruled effects are src/objects/Planet.js:495 `ambient = 0.04;` (the hot-Jupiter ambient lift, inside `if (planetType == 6) {`) and src/objects/Planet.js:532 `sunFacing = smoothstep(-0.3, 0.2, di | Three edits, all inside lane C's files. (1) Reword §4's sentence to what the gate actually does: it catches a new hardcoded effect that DECLARES a local. (2) Add rows for the two instances — G-05b (pt6 ambient 0.035→0.04) and G-08b (pt10 wrapped terminator on the atmosphere glow, 166 of 341 bodies) — with rulings. (3) Strengthen the control so it moves on the real risk: inject an assignment-only e |
| **S6-M3** | Compose's Instrument A verification method manufactures the failure it then reports as pre-existing. Its isolation harness cannot distinguish a real regression in that file from its own artifact. | tests/driver-pack-giantdeck.test.js:450 `const prior = execFileSync('git', ['-C', ROOT, 'show', \`${CAPTURED_FROM}:planet-lod-lab.html\`],` with tests/driver-pack-giantdeck.test.js:60 `const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');` — a `git archive` scratch tree has no `.git`, so `git show` throws and the CONTROL always fails there. Measured: pristine-HEAD archive → 1 failed / 72 passed; the REAL repo → `npm run test:baseline` lists only the 5e tripwire as NEWLY RED, and my full-suite JSON extraction shows driver-pack-giantdeck with exactly 1 failure (5e). So in the real tr | Re-run compose's Instrument A claim in the real repo (as I did — result: nothing of compose's is newly red, so the conclusion survives). For future isolation runs, either `git init` the scratch tree with the blob available, or copy `.git` in; a suite that shells out to git cannot be verified in a tree without one. |
| **S6-M10** | The flag's live value is unreadable on the OFF twin — the exact frame §12.5 fact 6 exists for. `window._lab` exposes no flag hook, and both advertised read paths only exist on a body that already swapped. | src/objects/Planet.js:2136 `export function labGasBodiesFlag() {` is exported but `grep -n "labGasBodies\|labPipeline\|labSkips" src/main.js` returns NOTHING, while `window._lab` is defined at src/main.js:2318 `window._lab = {`. The null paths: src/objects/Planet.js:1994 `if (!decision.admitted) return null;` (no userData written) and src/rendering/objects/BodyRenderer.js:255 `get labPipeline() {` returning `surface?.userData?.wd?.lab || null`. | Add to `window._lab` in src/main.js: `labGasBodies: () => labGasBodiesFlag()` and `setLabGasBodies: (v) => setLabGasBodiesOverride(v)`, importing both from './objects/Planet.js'. Then the OFF caption reads the same record shape as the ON caption, and §12.5 fact 6 is satisfiable on both twins. |
| **S6-M13** | The ⛔ 6d-LIMIT is recorded three mutually inconsistent ways, and the two prose versions are false against the object the pipeline actually receives. | tests/gas-body-lab-material.test.js:421 `it('⛔ 6d-LIMIT — exactly ONE Sol body is refused for a reason that is not about Sol', () => {` — its comment claims the Titan-slot moon 'carries no `profileId`, no `_systemSeed` and no `_ordinal`' and is 'refused today ONLY by the seed-key clause', while its own assertion at tests/gas-body-lab-material.test.js:439 reads `expect(worldEngineProvenance(notSolBlocked[0].d).blockers).toEqual(['no _systemSeed', 'no _ordinal']);` — two clauses, neither of which is the seed-key clause. MEASURED against real Sol data: the body's `planetData.profileId === 'sol-ti | State the limit against BOTH objects with measured blockers: (a) the moon wrapper as `BodyRenderer.createMoon` will receive it at Step 10 → `['no _systemSeed','no _ordinal']`, no Sol clause; (b) the `scenePMData` the planet-moon route builds today → `['profileId=sol-titan','_systemSeed=sol']`, two Sol clauses. Delete the 'carries no profileId' clause (false) and reconcile the test comment with its |
| **S6-M5** | One red test remains and it is outside every lane's file set: the giantdeck byte-inert tripwire. Leaving it red is a false signal; deleting it is a hole. | tests/driver-pack-giantdeck.test.js:713 `it('and it is BYTE-INERT today, because nothing forwards metallicity yet', () => {`, asserting `expect(conditionFromPlanet(e.planetData).metallicity).toBeUndefined()` over 40 generated systems. Its comment at :714 reads 'If this ever goes red, the adapter started forwarding and the shellDepthFrac re-bless in tests/port-condition-contract.test.js is DUE'. I confirmed the re-bless IS done: withdrawing `metallicity: d.metallicity` from the adapter reds 9 tests in tests/port-condition-contract.test.js (restored, md5 verified). | Retire it BY NAME and INVERT it, the same way the fwd lane inverted the adapter-side fence — replace the two `toBeUndefined()` loops with an assertion that generated planets DO carry metallicity while the 18 lab presets still do not (that half is still true and still worth gating). Also fix the two prose statements the forwarding falsified, both outside every lane's set: src/worldengine/base/giant |
| **S6-M6** | The ledger's swapped population is measured over `sys.planets` only, so it excludes planet-class moons — a body class the swap actually admits. Measured on the ledger's OWN corpus (lab-procedural-0…199): 17 planet-class moons exist, 4 are claimed by giantDeck's predicate AND pass `worldEngineProvenance`, i.e. they swap. The ledger's headline population number (341) is wrong for the game: it is 341 planets + 4 planet-class moons. | tests/material-parity-list.test.js:171 `for (const e of (sys.planets || [])) {` — the census iterates planets only. src/main.js:7422 `const planetMoon = new Planet(scenePMData, pmStarInfo);` routes planet-class moons through the same `_createSurface` → `_createLabSurface`. src/main.js:7415 `_systemSeed: systemData.seed,` and src/main.js:7416 with the `pm-` ordinal make `worldEngineProvenance` return isWorldEngine. MEASURED by me over lab-procedural-0…199 with `conditionFromPlanet` + `selectPacks` + `worldEngineProvenance`: {planetClassMoons: 17, packClaimed: 4, admitted: 4}; the four are lab-p | Extend the census in tests/material-parity-list.test.js to walk `e.moons` for `isPlanetMoon` and rebuild the `scenePMData` shape main.js:7405-7417 builds, then re-state §1's table as `341 planets + 4 planet-class moons = 345`. Add a row (or a §1 note) ruling the planet-class-moon route explicitly. The gate that keeps this closed is the same shape as §5.5's exotic gate: derive the swapped-body set  |

---

## DOC ROT — citations that are wrong AND ungated

⛔ **This is the class that has cost 71 repairs across two steps, and the ungated half is the
dangerous one.** A ref that is wrong reads as freshly verified; a ref that is wrong in an UNSCANNED
file cannot even be caught. `check:instruments` reported 27 broken during Step 6 when the true
number was 31, for exactly this reason.

**✅ VERIFIED BY THE AUTHOR (2026-08-09):** `docs/FEATURES/pack-authoring-path.md` is NOT in
`CITE_SOURCES` — `grep -n pack-authoring-path tools/port-uniform-delta.mjs` returns nothing. So the
three refs S5-M2/M11 name are ungated by construction, and the fence's green is silent about them.
**Fix is two parts and both are needed: repair the refs AND add the file to `CITE_SOURCES`,** or the
next edit re-breaks them invisibly.

| id | finding | evidence | proposed fix |
|---|---|---|---|
| **S5-M2** | Three §10 line+symbol citations in `docs/FEATURES/pack-authoring-path.md` are broken by lane 2's concurrent edit, and Instrument C cannot see it because that file is not in `CITE_SOURCES`. | Verified by opening each cited line in planet-lod-lab.html. (1) cites `planet-lod-lab.html:2233 `state.bandStrength = _gas ? 1.0 : 0.0;`` — line now reads `// that OTHER blocks below read, plus the jet stripe ladder that has no uniform to be a driver of.` (statement deleted by lane 2). (2) cites `planet-lod-lab.html:2274 `state.bandContrast = 0.08 + 0.92 * _vigor;`` — line now reads `// FALSE and measurement refutes it: this ladder is LINEAR in R (12·R/rotH) while the Rhines` (statement deleted). (3) cites `planet-lod-lab.html:1788 `if (!_driverTouched.has('bandRough')) state.bandRough = drawB | Re-point the three citations in docs/FEATURES/pack-authoring-path.md at their current homes (the first two now describe the pack: src/worldengine/drivers/giantDeck.js:178 and :180; the third is planet-lod-lab.html:1787 with its new text). Then add docs/FEATURES/pack-authoring-path.md, tests/lab-surface-ratchet.test.js and tests/fixtures/lab-surface-baseline.mjs to CITE_SOURCES in tools/port-unifor |
| **S5-M11** | Three line+symbol citations in `docs/FEATURES/pack-authoring-path.md` are now broken, and the shipped citation fence reports 292/292 green because the file is outside `CITE_SOURCES`. | Resolver run over the nine new/changed files, root-preferring the basename: 23 line+symbol refs, 20 resolve, 3 miss. `pack-authoring-path.md` cites `planet-lod-lab.html:2233 \`state.bandStrength = _gas ? 1.0 : 0.0;\`` -> that line now reads `// that OTHER blocks below read, plus the jet stripe ladder that has no uniform to be a driver of.`; `planet-lod-lab.html:2274 \`state.bandContrast = 0.08 + 0.92 * _vigor;\`` -> now `// FALSE and measurement refutes it: this ladder is LINEAR in R (12·R/rotH) while the Rhines`; `planet-lod-lab.html:1788 \`if (!_driverTouched.has('bandRough')) state.bandRoug | In the doc's §6 "Before" block, re-anchor the three refs to the pinned blob (`planet-lod-lab.html @ 4e864bc:2233`) rather than to live line numbers — the block is deliberately historical, so a commit-anchored form is the honest one. Same for the bare trailing `// :2279`, `// :2281`, `// :2292` comments in that code block, which my scanner skips but which are stale by the same edit. Then add the th |
| **S6-M15** | Four symbol-anchored citations written by the compose lane are broken by exactly 3 lines, and the citation fence cannot see them because the lane's files are not in CITE_SOURCES. | src/objects/Planet.js:2062 and src/worldengine/drivers/index.js:13 both cite `src/main.js:7419 \`const planetMoon = new Planet(scenePMData, pmStarInfo);\`` — that line actually reads `// (Planet.js uses smooth diffuse vs Moon.js's contrasty smoothstep)`; the symbol is on src/main.js:7422. src/objects/Planet.js:2091 and tests/gas-body-lab-material.test.js:447 both cite `src/main.js:7412 \`_systemSeed: systemData.seed,\`` — that line reads `// Propagate stable id-deriving metadata for the inspection layer.`; the symbol is on src/main.js:7415. Confirmed by `sed -n '7412p;7419p' src/main.js` and ` | 7419→7422 in both places, 7412→7415 in both places (I opened the file at each new line and confirmed the symbol). Then the orchestrator adds `src/worldengine/drivers/index.js`, `src/objects/Planet.js`, `src/rendering/objects/BodyRenderer.js`, `tests/gas-body-lab-material.test.js`, `docs/FEATURES/step6-parity-ledger.md` and `tests/material-parity-list.test.js` to `CITE_SOURCES` in tools/port-unifor |
| **S6-M16** | The harness lane's proposed repair for PLAN §6b's `main.js:9836`/`:9847` points at a SECOND wrong line — the failure §10 names and the same lane warned about elsewhere. | `git show HEAD:src/main.js | sed -n '9836p;9847p'` → `// Warp rift projection` and `let _portalTestMode = false;` — already wrong before any of this work. `sed -n '10411p;10422p' src/main.js` (the harness lane's proposed 'New: 10411 / 10422') → the SAME two wrong lines. The real guarded shadow writers are src/main.js:11062 `if (pu.shadowMoonCount) {` and src/main.js:11073 `if (pu.shadowPlanetCount) {`. The same wrong refs are load-bearing in docs/FEATURES/step6-parity-ledger.md:123 (row P-03, `main.js:9836`/`:9847`). | Repair to `main.js:11062 \`if (pu.shadowMoonCount) {\`` and `main.js:11073 \`if (pu.shadowPlanetCount) {\`` in PLAN §6b and in step6-parity-ledger.md:123, and strike the 10411/10422 entry from the harness handoff so nobody applies it. Add the backticked symbol so the fence can check them. |

---

## What is NOT in this list

- The **ledger** (`one-pipeline-two-frontends-CARRIED.md`) carries C1–C22 separately, with named
  clearing steps. This file does not duplicate it.
- **Instrument E has never taken a real paired shot.** The harness is built and its hooks are live,
  but no OFF/ON capture has been produced, so §12's whole apparatus is unexercised end to end.
- ⚠ **Ledger row C21 is WRONG about its own subject and needs correcting.** It says `shotState()`
  "reports whatever body is currently selected". Measured live 2026-08-09: `selectBody('planet', 3)`
  returned `{ok: true}` targeting the gas giant and `shotState()` still reported `p=0`. So it does
  not follow the selection EITHER — it reports something else, likely the first body in the scene
  walk. The row understates the defect.
