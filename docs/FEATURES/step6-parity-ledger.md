# Step 6b — the FEATURE-parity ledger

**What this is.** PLAN §4 Step 6b, under Max's 2026‑08‑09 ruling: *this is a FEATURE ledger, not a
uniform‑NAME diff*, and **an accepted loss is allowed; an UNDECLARED loss is blocking.** Every row
below enumerates something the game draws today on the bodies Step 6 swaps, and rules it
**carried / accepted-loss / blocking**.

**How it was produced.** By RUNNING, never by reading. `tests/material-parity-list.test.js` builds
**both materials on the same body** — `setLabGasBodiesOverride(false)` then `(true)` on identical
`planetData` — over 200 generated systems, feeds the pair to `_lab.swapLedger()`'s decision core
(`swapLedgerOf`), and extracts the hardcoded-GLSL half straight out of the shipped legacy shader
source. The test carries the same row table this document does and **fails if the two disagree**, so
this file is machine-checked rather than transcribed. Reading is what left `uLimbMix` out of the
plan's own list (ledger C20); nothing here was typed from a document.

⛔ **Nothing in this file was measured on Sol.** Sol renders 18 NASA textures through a different
renderer with no world-engine condition fields, and PLAN §4 Step 6d excludes it in code. The corpus
is `StarSystemGenerator.generate('lab-procedural-N', null)` for **N = 0…199**.

---

## 0. The headline, before the rows

Three measured numbers say more than the forty rows below, and none of them appears in the plan.

| | ⭐ **re-measured at HEAD 2026-08-20**, over the post-Step-10a pass — **266 bodies** (103 gas + 163 solid) | as first written, over the 103 gas ones |
|---|---|---|
| **How much of the game's material varies from body to body** | **45 of 72** uniforms ⭐ **71 → 72 AT B2P, 2026-08-20** — `uPosterizeLevels`, the colour quantum. The DENOMINATOR moved and the numerator did not, deliberately: it is one global display value, identical on every body, so it enters the declared set without entering the varying one. Pinned at tests/material-parity-list.test.js:428 `expect(LEDGER.gameSize).toBe(74);`. | 37 of 71, as measured then — a historical column, left at the count the material had that day |
| **How much of the lab material varies from body to body, after the swap** | **48 of 356** uniforms | 16 of 356 |
| **How many uniforms the packs actually write** | **47**, across FOUR packs — 11 `uBand*` + 4 `uJet*` (giantDeck), 3 `uLimb*` (limbDeck), 8 `uPolar*` (polarDeck), 21 surface names (rockySurface) — + the `aBand`/`aShear`/`aMush` bake | 15, one pack |

⭐ **And the finding that a three-bucket name diff structurally cannot report.** `swapLedgerOf`
puts **28** names in **CARRIED**. Carried means *the name exists on both materials* — it does not
mean the value survives. Measured over **266** swapped bodies at HEAD: **20 of those 28 carry a different value
after the swap, 10 of them on every single body** — 17 before Step 10a, and the seven that left are the ones
`rockySurface` now writes on part of the population; the rest still answer with `makeUniforms`' constant default. The eight that genuinely match are eight
constants (`uCraterDensity` `uCraterRelaxation` `uEjectaLump` `uEjectaRampart` `uEjectaStrength`
`uFwClamp` `uTerraceCount` `uVoroCells`) — they agree because **both sides are zero or a shared
constant**, which is agreement by absence.

**So "CARRIED: 28" reads as 28 surviving features and is 8 at most.** That is this codebase's
signature failure — a measurement entirely true and entirely misleading — and it is the reason this
ledger rules the *value*, not the *name*. The control is executed in
`tests/material-parity-list.test.js` (`the CARRIED bucket is a NAME bucket`): the same pair of
materials is fed to `swapLedgerOf`, which reports the 20 as carried, and to a value comparison,
which reports them as diverged. Two answers, one input. ⭐⭐ **AND THE SAME FAILURE RECURS ONE INSTRUMENT DEEPER — measured 2026-08-20, and every re-ruling below is taken on the deeper reading.** The value comparison is `encodeValue`, which encodes a `THREE.Vector3` as `v:x,y,z` and a `THREE.Color` as `c:r,g,b`, so a game uniform stored as a Vector3 and a lab uniform stored as a Color **never compare equal however identical their three floats**. Four of the ten every-body divergences are exactly that container split — `uFreshColor` `uWeatheredColor` `uSedColor` `uBioGroundColor`, game `Vector3` → lab `Color`. Compared COMPONENT-WISE at 1e-6 they agree on **163 of 163** of the bodies `rockySurface` claims and disagree on **103 of 103** gas ones: a wire that landed on one half of the population and a real loss on the other, not the uniform every-body loss the container reading prints. ⛔ **BOTH READINGS ARE KEPT AND NEITHER IS RETIRED.** `encodeValue` is the SUBJECT-SET instrument — it decides which names this ledger must rule at all — and narrowing it to "numerically equal" would drop names out of the partition silently, which is the failure §0 exists to name. So the test keeps its pin on the container reading and P-12 keeps its `blocking`; what the component-wise number changes is the DENOMINATOR and the named cause, not the verdict.

---

## 1. The populations, re-measured

⚠ **Re-measured, not copied.** PLAN §4 Step 6b tabulates five rows with figures over an unnamed
corpus of "223 gas bodies". Step 4 taught that the plan's own tabulated numbers do not always
reproduce, so every figure below was taken again on the corpus named at the top of this file. **The
two sets of numbers are over different populations and must not be compared as a delta.**

| | ⭐ **re-measured at HEAD 2026-08-20** on `lab-procedural-0…199` | as first written (pre-Step-10a) |
|---|---|---|
| systems | 200, of which **64** are binary | same |
| bodies claimed by a registered pack predicate | **852** — every planet in the corpus. `rockySurface`'s predicate is the exact COMPLEMENT of `giantDeck`'s, so the two together leave nothing unclaimed | 343, when `giantDeck`'s `=== 'gas'` was the only one |
| …refused by Step 6d's provenance test | **6** — 4 × `crystal`, 1 × `ecumenopolis`, 1 × `shattered`, all `exotic`-variant, every one blocked on `no _systemSeed` + `no _ordinal` | 2, both `crystal` |
| **bodies that actually swap** | **846** | 341 |
| legacy program those 846 render **today** | **`gas` 229 · `rocky` 617** | `gas` 227 · `rocky` 114 |
| `gas` branch census | `planetType` **1**: 53 · **6**: 5 · **7**: 5 · **10**: 166 | 1: 53 · 6: 5 · 7: 3 · 10: 166 |
| `rocky` branch census | `planetType` **0**: 181 · **2**: 131 · **3**: 52 · **4**: 6 · **5**: 13 · **8**: 130 · **9**: 104 — ⭐ three branches newly live at Step 10a (`3` lava, `4` ocean, `8` venus), which is what R-05/R-06/R-07 rule | 0: 21 · 2: 66 · 5: 1 · 9: 26 |
| swapped bodies with ≥1 moon | **420** of 846, **663** moons — ⭐⭐ NOW PINNED, 2026-08-21, AND THE EARLIER "left stale and red on purpose" RULING IS SUPERSEDED RATHER THAN IGNORED. That ruling was right that pinning a LIVE population absorbs another lane's movement; it was wrong that a red serves as the marker, because this one sat inside the blessed 32 and Instrument A reported it as expected drift for weeks while both B3 and B4 published 420/663 in prose beside an assertion still reading 228/456. tests/material-parity-list.test.js:347 `expect(CENSUS.withMoons).toBe(420);        // 228 -> 420, predicate-dependent BY CONSTRUCTION` now carries the measured value, and the concern that motivated the old ruling is answered by SPLITTING the pin rather than by leaving it red: `corpusWithMoons`/`corpusMoons` (422/665) are counted BEFORE any admission test, so they are invariant under every pack-predicate change. ⚠ THEY ARE NOT INVARIANT UNDER THE MOON PROGRAM — the formation window (34b502d) is still open and both pairs move when it closes. That is the residual the old ruling was pointing at, and it is now stated instead of encoded as a hidden red | 228 of 341, 456 moons |
| swapped bodies in a binary system | **269** of 846 | 125 of 341 |
| swapped bodies under a **non-white** primary | **846** of 846 | 341 of 341 |

⭐ **The line the plan does not contain — and at HEAD it is the MAJORITY: 617 of the 846 swapped bodies do not render the gas
program today** (114 of 341 when this was written). `giantDeck`'s predicate is condition-derived — src/worldengine/drivers/index.js:100
`applies: (condition) => compositionClass(condition) === 'gas',` — while the legacy program is
chosen by the `type` *label*, src/objects/Planet.js:1422 `const GAS_TYPES = new Set(['gas-giant', 'hot-jupiter', 'eyeball', 'sub-neptune']);`.
The two disagree on most of the population: bodies labelled `ice`, `carbon`, `rocky`, `lava`, `ocean`, `venus` and
`terrestrial` whose condition says gas. Step 6 is titled **SHIPS GAS GIANTS** and §6b's loss table
enumerates the gas branch only, so the features those 617 bodies draw today were outside the plan's
list by construction. §3 below rules them. ⛔ **THE DENOMINATOR FENCE, NAMED ROW BY ROW RATHER THAN WAVED AT.** Re-measured in this pass and carrying HEAD figures: §0's three headline numbers, this table, §1.1's five rows, rows **P-10, P-12, P-13, P-14, P-15** in §3, and the branch populations of **G-02, G-04, G-05, G-06, G-07, R-01, R-02, R-03, R-04** in §4. ⚠ **NOT re-measured in this pass, and still carrying a pre-Step-10a `of 341` denominator: P-01, P-02, P-03, P-04, P-05, P-06, P-11, G-08.** Their MECHANISM is unaffected — not one of them is a population claim — but their populations were taken over the gas-only 341 and the corpus now swaps 846. ⭐ Two were left alone deliberately rather than guessed: P-05's *"20 of 341 carry a live aurora"* and P-06's *"21 of 341 draw clouds"* need a predicate this pass did not execute — a truthy `aurora`/`clouds` record over the 846 counts **197** and **103**, which is a DIFFERENT quantity from "live" — and writing a number I had not measured to the row's own definition is the reading-not-running failure this whole document exists against.

### 1.1 The plan's five tabulated rows, re-measured

| plan row | plan's figure | re-measured here |
|---|---|---|
| Moon transit + planet shadows | 177 of 223 gas bodies have moons (448 moons) | ⭐ **420 of 846** swapped bodies, **663** moons (228 of 341 / 456 pre-Step-10a) |
| Second-star lighting | 79 of 223 in 72 binary systems | ⭐ **269 of 846**, in **64** binary systems of 200 (125 of 341 pre-Step-10a) |
| Primary star **colour** | "**all** giants… implicit white light" | ⭐ **846 of 846** (341 of 341 pre-Step-10a) — and stronger: **every** primary in the corpus is non-white, so there is no body for which the loss is invisible |
| LOD1 procedural colour match | Sol's Jupiter/Saturn while textures load | ⭐ **0 of 846, and structurally 0.** `_applyLOD1Overrides` returns at src/rendering/objects/BodyRenderer.js:298 `if (!profileId) return;` and `worldEngineProvenance` refuses any body that HAS a `profileId`. The two guards are complements: no swapped body can ever reach that path. |
| `lodLevel` | "near-harmless — read by no shader" | **reproduces.** `lodLevel` occurs exactly twice in `src/objects/Planet.js`: the declaration src/objects/Planet.js:118 `uniform int lodLevel;` and the material entry src/objects/Planet.js:1739 `lodLevel: { value: 1 },`. No shader body reads it. The writer already announces its own no-op: src/rendering/objects/BodyRenderer.js:212 `if (!surface?.material?.uniforms?.lodLevel) return this._noteLabSkip('lodLevel', surface);` |

---

## 2. How each row is ruled

- **carried** — the feature still reaches the pixel after the swap. The row names the mechanism.
- **blocking** — the feature stops reaching the pixel **and the lab material already declares the
  uniform that would carry it**, so the loss is an omission in the port's uniform mapping rather
  than a missing capability. These must close before anyone is shown a parity claim.
- **accepted-loss** — the feature stops reaching the pixel and closing it is real work (no lab
  mechanism, or a producer the plan has already fenced out of pack #1). Declared here, in front of
  Max, on this step — which is the whole of what Max's ruling requires of a loss. ⭐⭐ **THE RULE-2 GATE, LANDED 2026-08-20 (D-13), AND IT SPLITS THIS RULING IN TWO.** The split is the difference between a re-parameterisation and a shader change, and it is decided by the LAB, not by how large the loss looks. **`[lab-has-it]`** — the lab HAS a mechanism for the feature; it is parameterised differently, or its producer is fenced out of the pack, so closing it is authoring against something that already exists. **`[lab-lacks-it]`** — the lab has NOTHING; closing it is a shader change. ⛔ **THE GATE ITSELF: every accepted-loss row added after this line must be `[lab-has-it]`, and exactly ONE new `[lab-lacks-it]` row is permitted — F52's**, the star-colour / second-star / transit-shadow restoration, which is a single feature spread over four rows and is D-4's declared B4 scope. **Measured at the gate's landing: 22 accepted-loss rows across Channels 1 and 2 — 15 `[lab-has-it]`, 7 `[lab-lacks-it]`.** Four of the seven (P-01, P-02, P-03, S-01) ARE F52; the three that are not are named here so the count is a fact and not a slogan — **P-17** (`lodLevel`, read by no shader on EITHER side, so no pixel is at stake and no shader change would restore one), **G-05** (day-side thermal hotspot and night-side glow, no substellar-anchored emission term anywhere in the lab) and **G-06** (the eyeball's substellar climate rings, against a lab climate that is latitudinal). ⚠ **The count is stated as a gate and is NOT machine-asserted, and that is a scope fact rather than an oversight.** The sub-class lives in each row's evidence cell — `grep -c '\[lab-lacks-it\]'` is the count — because the ruling COLUMN is pinned to §2's three legal strings by tests/material-parity-list.test.js:56 `const RULINGS = new Set(['carried', 'accepted-loss', 'blocking']);`, and the lane that landed this gate may not edit that file. ⭐ Making it fatal is ONE line in that test, and naming which line is the difference between a deferral and a hole. ⛔ And the three rulings stay three: `[lab-has-it]`/`[lab-lacks-it]` are a SUB-CLASS of `accepted-loss`, not a fourth and fifth verdict — the same fence that reddened on `deferred (Max, 2026-08-19)` at P-10 would redden on either of them in the ruling column, correctly. ⭐ Channel M inherits rather than repeats: every M row names the P row it mirrors and takes that row's sub-class, and the one moon-NATIVE row, **M-04** (`moonType`), is `[lab-lacks-it]` and grandfathered with the other seven.

⛔ **This ledger cannot mark anything `carried` on the strength of a name.** Every `carried` row
below names either a uniform the pack demonstrably writes, or a per-frame seam that demonstrably
writes it. The 20 name-carried/value-diverged rows are ruled `blocking`, not `carried`. ⭐ **AND `blocking` IS NOT WIDENED BY A DECLARED NAME EITHER — re-measured 2026-08-20, twice over.** §2's second conjunct asks whether the lab declares *the uniform that would carry it*, which is a question about the QUANTITY and not about the spelling: `uDispDomainScale` is declared on both materials and carries a gradient DIVISOR in the game against a domain MULTIPLIER in the lab, so it fails the test and left P-14 for P-15 (P-08's shape, one uniform over). `noiseDetail` has no lab counterpart of any spelling and left P-10 for the same reason. ⛔ Both were SPLIT OUT, not stretched: keeping a subject inside a `blocking` row it fails the test for is the P-08 error running the other way, and P-08's own correction records that the original ruling was mine to correct rather than Max's to decide.

---

## 3. Channel 1 — the uniform-shaped features

Union of `swapLedgerOf`'s `lost` and `lostAtZero` buckets over the corpus: **44 names** (43 before B2P), which with
the 28 name-carried ones accounts for all 72 uniforms the game material declares. ⭐ **B2P, 2026-08-20:** `uPosterizeLevels` raised both the union (43 → 44) and the total (71 → 72), and it lands in `lost` rather than `carried` because the SPELLING leaves the material while the QUANTITY does not — the lab holds the same quantity under its own name `uLevels`, in a SECOND object kept in step by the single writer `setPosterizeLevels` (P-18, M-11).

⚠ **`lostAtZero` is not "lost but it was off anyway".** The diff runs at material-creation time, so
every uniform the game writes *per frame* reads zero there — `shadowMoonCount`, `shadowPlanetCount`,
`starPos1/2`, `time`, and `lightDir2` (bound **by reference** to the vector src/main.js:11027
`entry.planet._lightDir2.copy(_sunDir2);` mutates). A ledger that ruled only the `lost` bucket would
declare the transit-shadow and second-star features unaffected. Both buckets are ruled below.

<!-- LEDGER-CH1 -->
| id | uniforms | ruling | evidence |
|---|---|---|---|
| P-01 | `starColor1` `starBrightness1` | carried | **CLOSED AT B4-1, 2026-08-21, AND IT WAS A SHADER CHANGE EXACTLY AS THIS ROW PREDICTED.** ⛔ **THE ROW'S OWN DENOMINATOR WAS STALE AND IS CORRECTED, NOT QUIETLY RE-FITTED.** It said *"341 swapped bodies … visible on 341/341"*; `341` is the pre-Step-10a population. **RE-MEASURED IN THIS SESSION over `lab-procedural-0…199`, by a faithful re-run of `census()` at tests/material-parity-list.test.js:157 `function census(n) {`: 200 systems, 64 binary, 852 claimed by a pack predicate, **846 swapped**, 6 provenance-blocked, **269** swapped bodies in a binary, and **846 of 846** under a non-white primary — 0 of the 200 systems draws a white primary, so there is still no body on which the loss would have been invisible.** The MECHANISM half of the old cell reproduced exactly and is what closed. ⭐ **THE MECHANISM, AND IT IS THREE LINES.** src/worldengine/shaders/uniforms.js:137 `uStarColor1: { value: new THREE.Color(1, 1, 1) }` declares the star set; the fragment spends it at src/worldengine/shaders/planetShaders.glsl.js:491 `vec3 starLight = uStarColor1 * diff1 * uStarBrightness1 * shadow1 + uStarColor2 * diff2 * uStarBrightness2 * shadow2;` — ⭐ **THE CITED SYMBOL WAS REPAIRED AT B4-2 BECAUSE THE LINE MOVED UNDER IT, AND THE REPAIR IS TO THE NEW SYMBOL, NOT TO A NEW LINE NUMBER.** B4-2 added the two `* shadowN` factors to this same statement, so the string this row quoted stopped existing and the citation fence reddened it. The P-01 content is untouched: all four star names, in this order, multiplying the albedo.; and it reaches the pixel at src/worldengine/shaders/planetShaders.glsl.js:787 `albedoCol * (starLight + vec3(ambient))` — src/objects/Planet.js:498's `finalColor = surfaceColor * (starLight + vec3(ambient));` verbatim, which is what keeps the tint on the ALBEDO ONLY and leaves limb, terminator, aurora and cloud optics untinted additive terms as the game leaves them. The value is carried at CONSTRUCTION (src/objects/Planet.js:1618 `Planet._createLabSurface(geometry, d, condition, this._lightDir, this._lightDir2,`), because star colour has no per-frame writer anywhere in the engine to widen. ⭐ **ASSERTED LIVE, NOT BY NAME.** tests/material-parity-list.test.js pins that `uStarColor1` and `uStarColor2` now appear in `LEDGER.labVarying` — they take DIFFERENT values on different bodies of the real corpus, which is the actual content of this row. The four names are also pinned by an INVERTED assertion (P-04's mechanism): the line that asserted the lab declared no `/star/i` uniform now names all four, so un-declaring one reddens it instead of letting the feature leave silently. Six severing mutants were run and each reddened at least one assertion. ⚠ **`carried` IS NOT `equal`.** ⭐ **TRUE UNTIL B7, 2026-08-21 — CORRECTED, NOT DELETED:** this row read *"reaches 0 pixels today… no body renders through this program, so the close is invisible until the flip"* against what the flag then read `export const LAB_GAS_BODIES_DEFAULT = false;`. B7 flipped the flag ON — src/objects/Planet.js:2158 `export const LAB_GAS_BODIES_DEFAULT = true;` — so this row now DOES reach a player, on every one of the 846 admitted planets; the same standing caveat M-09 carries is now a caveat about SOL specifically, which stays on the legacy program by provenance at any flag value, not about the flag. ⛔ **INSTRUMENT C CANNOT WITNESS THIS ROW AND MUST NOT BE QUOTED AS IF IT COULD.** `starColor1`/`starBrightness1` are on the tool's UNWATCHED list at tools/port-uniform-delta.mjs:549 `{ game: 'starColor1',      reason: 'harness-blind',` — its harness passes no starInfo, so every body would record the same white fallback. A green C is silent about P-01, not supportive of it. |
| P-02 | `lightDir2` `starColor2` `starBrightness2` | carried | **CLOSED AT B4-1, 2026-08-21, alongside P-01 and by the same three lines.** ⛔ **THE DENOMINATOR WAS STALE AND IS CORRECTED.** This cell said *"125 of 341 swapped bodies sit in a binary"* and split the buckets 125/216. **RE-MEASURED IN THIS SESSION on the same corpus and by the same census: **269 of 846** swapped bodies sit in a binary, so the complementary bucket is 577, not 216.** ⚠ I re-measured the POPULATION only; I did **not** re-run the bucket pass, so `125/216` is WITHDRAWN rather than replaced by `269/577` as a bucket claim — the population figures are mine, the bucket split is not, and the two must not be conflated a second time. ⭐ **THE MECHANISM, AND ITS TWO HALVES LIVE IN DIFFERENT PLACES — WHICH IS THE WHOLE SUBTLETY OF THIS ROW.** `starColor2`/`starBrightness2` are CONSTRUCTION-time, exactly as at P-01. `lightDir2` is NOT: `uLightDir2` is OBJECT space where the legacy `lightDir2` was world space and bound by reference, so it must be re-derived every render tick or a body would hold its build-time direction while the mesh spins under it — one false terminator sweep per body-day, the identical bug uLightDir already had. The seam does it at src/rendering/LabPlanetMaterial.js:558 `_light2Obj.copy(opts.lightDirWorld2).applyQuaternion(_invQuat).normalize();`, on the same inverted quaternion the primary just computed, fed from src/objects/Planet.js:1976 `lightDirWorld2: this._lightDir2,` and the matching line in `Moon.updateRender`. ⛔ **CONSEQUENCE FOR EVIDENCE, AND IT IS WHY THIS ROW IS FENCED IN TWO FILES.** A construction-time pass structurally CANNOT see `uLightDir2` — every body is built at (0,0,0) and only the per-frame seam ever writes a real direction — so `uLightDir2` correctly does NOT appear in `LEDGER.labVarying` while `uStarColor2` and `uStarBrightness2` do. Its direction half is fenced at the seam instead, in tests/lab-shader-perframe-seam.test.js, with a severing mutant that reddens it. ⚠ **`uStarBrightness1` ALSO DOES NOT VARY, AND THAT IS A CONTROL RATHER THAN A GAP:** src/generation/StarSystemGenerator.js:486 `brightness1: 1.0,` is a literal on every primary, so a build in which it started varying would mean the generator moved, not the port. ⛔ **INSTRUMENT C IS BLIND HERE TOO** — `lightDir2` is UNWATCHED as tools/port-uniform-delta.mjs:530 `{ game: 'lightDir2',           reason: 'runtime',` and the two star names as `harness-blind`. |
| P-03 | `shadowMoonCount` `shadowMoonPos` `shadowMoonRadius` `shadowPlanetCount` `shadowPlanetPos` `shadowPlanetRadius` `starPos1` `starPos2` | carried | **CLOSED AT B4-2, 2026-08-21.** ⛔⛔ **THIS ROW'S CITATION WAS WRONG — NOT STALE, WRONG — AND IT IS CORRECTED HERE RATHER THAN DROPPED.** The cell said every writer of these eight is guarded *(`main.js:9865`/`:9847`)*. **I READ BOTH LINES IN THIS SESSION. src/main.js:9845-9850 and :9863-9867 are ORRERY boot-reveal and autopilot-arming code** — `_boot.startAutopilot`, the HELM boot tour, `SupercruisePilot` — and neither line writes, reads or mentions a shadow or star uniform. `grep -n` over src/main.js puts **every writer of the eight in the band :11372-:11439**. The claim the citation was attached to (that they are all guarded) is TRUE; the evidence for it pointed at unrelated code. The citation fence could not catch this because a bare `main.js:9865` is in its UNCHECKED column. ⭐ **THE GUARDS, BY SYMBOL, SO THIS CANNOT DRIFT AGAIN:** src/main.js:11422 `if (pu.starPos1) pu.starPos1.value.copy(_star1Pos);`, src/main.js:11427 `if (pu.shadowMoonCount) {`, src/main.js:11438 `if (pu.shadowPlanetCount) {`, and the plain-moon arm at src/main.js:11486 `if (mu?.shadowPlanetPos) mu.shadowPlanetPos.value.copy(entry.planet.mesh.position);`. ⛔ **THE DENOMINATORS WERE STALE AND ARE CORRECTED, NOT QUIETLY RE-FITTED.** This cell said *"228 of 341 swapped bodies carry 456 moons"*; 341 is the pre-Step-10a population. **RE-MEASURED BY ME IN THIS SESSION over `lab-procedural-0…199`: 200 systems, 64 binary, 852 claimed, **846 swapped**, 6 provenance-blocked, **420 swapped bodies carrying 663 moons between them**, 269 swapped bodies in a binary, 846/846 under a non-white primary, and **837 of the 846 sit in a system with at least one planet-neighbour**, which is the planet-shadow population as distinct from the transit one.** ⭐ **AND ONE NEW FACT THE OLD CELL COULD NOT HAVE HAD: the 6-moon cap NEVER TRUNCATES on this corpus.** The measured per-body moon histogram is 0→426, 1→281, 2→69, 3→50, 4→10, 5→6, 6→4, and **0 of 846 bodies carry more than 6 moons**, so `Math.min(moons, 6)` is an identity here and no caster is being silently dropped. ⚠ **THE PLAIN-MOON FIGURE NEEDS ITS POPULATION STATED OR IT CONTRADICTS THE ONE M-01 CARRIES.** I measured **632 plain moons in the corpus**, of which **630 hang off a parent that swaps** and **2 hang off the six provenance-blocked bodies**. The 632 published elsewhere and my 630 are the same measurement over two different populations, not a disagreement. ⛔ I did NOT measure which plain moons THEMSELVES swap, which is a third quantity again, and no figure here should be read as it. ⭐⭐ **THE MECHANISM, AND THE SPACE IS THE WHOLE OF IT.** The game's test is world-space — src/objects/Planet.js:216 `float sphereShadow(vec3 fragPos, vec3 starPosition, vec3 casterPos, float casterRadius) {` applied at src/objects/Planet.js:485 `float shadow1 = totalShadow(vWorldPos, starPos1);` — and **the lab fragment shader has no world-space varying and provably cannot be given one**: a `vWorldPos` varying needs a FIFTH read of bare `position` in LAB_VERTEX_SHADER and tests/instrument-tap-fence.test.js:242 makes `deriveTapVertex` THROW on exactly that, killing the AC-SAMPLER instrument. So the CASTERS were transformed into the fragment's object space instead, on the transform the seam already uses for the camera operand. ⭐ **THAT SUBSTITUTION IS LICENSED BY sphereShadow BEING SIMILARITY-INVARIANT, AND I MEASURED IT RATHER THAN ASSUMING IT:** rotating, translating and uniformly scaling `fragPos`, `starPosition` and `casterPos` together and scaling `casterRadius` by the same factor moved the returned value by at most **2.5e-12 absolute / 1.5e-11 relative over 200,000 random float64 samples, 3,043 of them inside the smoothstep penumbra** — the only branch that computes anything. ⚠ **THAT IS FLOAT ROUNDING, NOT ZERO. This row claims an equality UP TO ROUNDING and does not claim bit-identity with the game's world-space evaluation; in float32 GLSL the rounding is larger.** ⚠ **AND IT ASSUMES UNIFORM MESH SCALE** — the seam reads one component of `getWorldScale`, so a non-uniformly-scaled body would break the invariance. No body in the engine is non-uniformly scaled today. ⭐ **THE ARITHMETIC IS FORWARDED, NOT RE-DERIVED, AND THAT IS ASSERTED AS A TOKEN COMPARISON RATHER THAN IN PROSE:** the lab's `sphereShadow` is src/objects/Planet.js:216-228 character-for-character once whitespace is normalised, and its `totalShadow` is src/objects/Planet.js:230 `float totalShadow(vec3 fragPos, vec3 starPosition) {` with six names re-spelled and nothing else changed — same caps, same break-on-count loops, same moons-then-planets multiply order, which matters because float multiplication is not associative. ⭐ **THE TRANSPORT IS THE GENUINELY NEW HALF:** src/objects/Planet.js:1524 and src/objects/Moon.js:24 allocate a world-space caster record once in the constructor, src/main.js:11369 and src/main.js:11406 fill it on the SIM tick, and src/rendering/LabPlanetMaterial.js:577 transforms it on the render tick. ⛔ **THE GAME-SIDE GUARDS ARE UNTOUCHED AND STILL GUARDED** — an unguarded write there once stopped the render loop permanently — so the record is a SECOND path rather than a widening of the first. ⛔ **THAT LEAVES TWO COPIES OF THE CASTER-SELECTION LAW IN src/main.js, DELIBERATELY, AND THE AGREEMENT IS FENCED:** de-duplicating them would rewrite lines tools/port-uniform-delta.mjs pins BY SYMBOL as its UNWATCHED evidence — two of them quoting a whole assignment expression — so tests/lab-shader-shadows.test.js slices both fragments out of the shipped text, runs one synthetic system through both, and asserts they select the same casters, with a control that reddens when one is severed. ⭐ **HELD CLOSED BY INVERSION, on P-01's and P-04's pattern:** tests/material-parity-list.test.js's `/shadow/i` assertion read `.toEqual([])` and now NAMES the six, and the `uShadow` corpus assertion flipped `false` -> `true`, so un-declaring any of them reddens this row instead of letting the feature leave silently. **Twelve severing mutants were run and every one reddened at least one assertion; every source file was md5-verified byte-restored afterwards.** ⛔ **INSTRUMENT C CANNOT WITNESS THIS ROW EITHER, AND MUST NOT BE QUOTED AS IF IT COULD.** All eight game names are on the tool's UNWATCHED list as tools/port-uniform-delta.mjs:530 `{ game: 'lightDir2',           reason: 'runtime',`'s neighbours — `starPos1` and the six shadow names are all `reason: 'runtime'` — because the renderer overwrites them every frame. C came back 55 uniforms / 633 bodies / 0 moved / exit 0, which is **silence about P-03, not support for it**. ⚠ **`carried` IS NOT `equal`.** ⭐ **TRUE UNTIL B7, 2026-08-21 — CORRECTED, NOT DELETED:** this row read *"reaches 0 pixels today — at … no body renders through this program, so the close is invisible until the flip"* against what the flag then read `export const LAB_GAS_BODIES_DEFAULT = false;`. B7 flipped the flag ON — src/objects/Planet.js:2158 `export const LAB_GAS_BODIES_DEFAULT = true;` — so this row now reaches a player on every admitted body. Same standing caveat P-01, P-02 and M-09 carry: SOL stays on the legacy program by provenance, not by flag, so it alone is still untouched by this row. |
| P-04 | `uLimbMix` | carried | **Ledger C20, and it is a real loss as shipped, not a resolved one.** src/objects/Planet.js:1642 `uLimbMix: { value: LIMB_MIX },` with src/objects/Planet.js:1401 `const LIMB_MIX = 1.0;` — the game draws the limb ON, on every body. The lab's master gate is the differently-named planet-lod-uniforms.js:40 `uLimbStrength:   { value: 0.0 },` and the pack does not write it. §12.4 speculated that ruling 4 (`gates = ALL_ON`) might open it; measured, ALL_ON resolves only the pack's own declared gate names — `{bands, jets}` — and touches no uniform default. One mapping line. **⭐ CLOSED at the limbDeck/polarDeck registration commit:** `LIMB_DECK_ENTRY` is in the runtime `PACKS` array, so `uLimbStrength` is written on every admitted gas body and is no longer a loss. Verified by inversion — tests/material-parity-list.test.js now asserts `LEDGER.written` CONTAINS it, so un-registering the pack turns this row red instead of letting the feature leave silently. |
| P-05 | `hasAurora` `auroraColor` `auroraIntensity` `auroraRingLat` `auroraRingWidth` | carried | The pure alias shape, four times over: the lab declares `uAuroraColor` `uAuroraIntensity` `uAuroraRingLat` `uAuroraRingWidth`, so a name diff sees four orphans on each side rather than one broken feature. The pack writes none, so `uAuroraIntensity` stays at its `0.0` default. 20 of 341 swapped bodies carry a live aurora today. ⭐ **INVESTIGATED 2026-08-19 — this is a WIRING row, not the law-choice it was filed as, and no ruling from Max is owed.** PLAN §2 records "two divergent aurora laws", which is true and overstates the gap: **ring latitude, ring width and colour do not differ at all.** `PlanetGenerator.js:501-502` is `0.7 + magneticField*0.2` / `0.15 - magneticField*0.08`; `world-engine-lab.html:2613-2614` is the same two expressions, the width merely floored at 0.07 — a no-op over the field's own 0-1 range. The 5-entry composition colour table is verbatim identical and the lab's comment says so (*"the PlanetGenerator auroraColors table"*). The laws differ in exactly TWO places: (1) the game scales intensity by stellar wind, `min(1, magneticField*min(uvFlux,50)*0.15)`, where the lab uses the field alone via `labCore.js:1045`; (2) the lab floors the field at 0.6 for gas bodies >= 3.5 R⊕ (`_giantDynamo`), which the game has no equivalent of. ⛔ **And (1) is not choosable: `uvFlux` appears NOWHERE under `src/worldengine/` — it never crosses the condition seam, so the game's intensity law is not expressible by a pack at all.** With standing constraint 3 (REPLACE, not graft) deciding the rest, the lab's law wins by default and this row reduces to forwarding four values. Its visible consequence — which bodies show an aurora — is a UAT look, not a scoping decision. ⭐⭐ **CLOSED AT B3 LEG 1, 2026-08-21 — the pack exists and writes all four.** `src/worldengine/drivers/solidOptics.js` claims non-gas conditions and forwards `uAuroraIntensity` `uAuroraColor` `uAuroraRingLat` `uAuroraRingWidth` from `src/worldengine/base/auroraOptics.js`. The intensity is not re-typed at all — it is read straight off the shipped `deriveUniforms` bundle, so labCore keeps the only expression of it; the ring and colour laws are the lab's post-process, moved verbatim. The four names move from *nothing writes them* into `LEDGER.written`, and `tests/material-parity-list.test.js`'s P-05 assertion is INVERTED to match — it now reds if the pack is un-registered, which is the shape P-04 and M-09 already use. ⛔ **A FIFTH THING THE ROW'S OWN PROSE DID NOT NAME, AND IT IS NOT SMALL.** The lab's law includes the Venus override `world-engine-lab.html:2637` — cloud regime 3 (`co2` above 10 bar) zeroes the intensity outright. **MEASURED over `lab-procedural-0…199`: that predicate is true on 130 of 852 generated planets and ALL 130 are non-gas**, i.e. every one of them is inside this pack's domain. Forwarding four values WITHOUT it would have authored a fifth law that lights an aurora on 130 bodies the lab leaves dark, so it is carried. ⚠ **The gas-only `_giantDynamo` field floor is deliberately NOT carried**, for two reasons recorded in the module: its first conjunct is *is a gas body*, so it is unreachable on this pack's population; and expressing it needs `compositionClass`, which `tests/worldengine-e1-shadow-audit.test.js` forbids every file in `src/worldengine/base/` from importing. ⚠ **Also measured, and it changes what a UAT would see:** `condition.magneticField` (a data-only passthrough of the GAME's field) and labCore's own `ironFraction × lock-factor` disagree on **21 of 852** planets, up to 0.290 apart. The bundle's value wins, per this row's own ruling that the lab's law wins. ⭐ Live-aurora population after the wire, MEASURED over `lab-procedural-0…59`: **111 of 266** swapped bodies carry a non-zero `uAuroraIntensity`, against 0 before. ⭐ **RULING MOVED `blocking` → `carried` IN THIS PASS.** §2's `blocking` test is *the feature stops reaching the pixel*; it now reaches it, through the lab's own four names rather than the game's five, which is the alias shape resolving exactly as P-04's did. `hasAurora` has no counterpart name and needs none — its whole function is subsumed by the shader's `uAuroraIntensity > 0.0` guard, which the intensity law's own zeros already drive. ⚠ **ONE RESIDUAL, NAMED SO THE `carried` IS NOT READ AS PIXEL-PERFECT: `uMagAxis` IS STILL UNWRITTEN.** The lab writes it at `world-engine-lab.html:5057` (spin axis tilted ~11°, azimuth hashed from `macroSeed`); no pack does, so a swapped body keeps the material default `(0, 1, 0)` and its oval sits on the geometric pole instead of a tilted dipole. It carries no game counterpart, so it never enters this channel's measured subject set and this row cannot claim it — recorded here rather than left for a UAT to rediscover. |
| P-06 | `hasClouds` `cloudColor` `cloudDensity` `cloudScale` | accepted-loss | `[lab-has-it]` The lab has a cloud system (`uCloudCoverage` `uCloudRegime` `uCloudRelief` `uCloudOpticsIntensity`) but **no cloud-colour uniform**, and none of the four is written by the pack. 21 of 341 swapped bodies draw clouds today. Not a mapping line: the game's `cloudDensity`/`cloudScale` do not map onto `uCloudCoverage`/`uCloudRegime` without a decision about what the lab's regime enum means for a generated body. |
| P-07 | `atmosphereStrength` `atmosphereColor` | accepted-loss | `[lab-has-it]` The legacy rim glow. Superseded in intent by the lab's limb optics, which P-04 and P-11 rule; recorded separately because it is a *second* atmosphere term in the legacy shader and folding it silently into the limb row would hide a decision. |
| P-08 | `baseColor` `accentColor` | accepted-loss | `[lab-has-it]` ⭐ **RE-RULED 2026-08-19, and the original ruling was mine to correct, not Max's to decide.** This row was `blocking` on the reasoning that "on the 114 rocky-variant swapped bodies these two uniforms ARE the surface palette". **They are not.** `PlanetGenerator.js:718-722` states it in source, directly above the world-engine palette it introduced: *"⚠ THIS IS BEDROCK, NOT A WHOLE-BODY COLOUR. It is deliberately NOT used for oceans, ice caps, clouds or gas bands — those are separate layers with their own colours, in the game as in the lab. `baseColor`/`accentColor` below are untouched and still drive them. Substituting this palette for baseColor turns every ocean brown and every gas giant tan (measured)."* So these two drive OCEAN, ICE-CAP, CLOUD and BAND colour — layers the lab parameterises separately and differently (`uCloudCoverage`/`uCloudRegime` per P-06, `uIcenessAlbedo`/`uFrostAlbedo` per P-09, `uBandTint` for gas). §2's own definition of `blocking` requires that **the lab material already declares the uniform that would carry it**; `uniforms.js:138` records the opposite — *"⛔ Do not reintroduce `uBaseColor`: the game's legacy record field `baseColor` is a DIFFERENT quantity the u-prefix rule would wrongly pair with it."* No counterpart name exists, so this row fails the `blocking` test and belongs with P-06 and P-09. ⛔ Step 9 does NOT close it and must not be read as having tried. |
| P-09 | `uIceColor` `uLavaGlow` `uLavaCrust` | accepted-loss | `[lab-has-it]` Per-body ice and lava tints. The lab has `uIcenessAlbedo` `uFrostAlbedo` `uLavaScale` `uLavaGlowRate` `uLavaAxis` — a different parameterisation, not a renamed one, so this is not a mapping line. |
| P-10 | `noiseScale` `uNoiseScale` | carried | ⚠ **This row's stated mechanism is wrong in a way that matters, and the correction changes what closing it means.** It says `uNoiseScale` "diverges because nothing writes it" — true of the GAME, but `grep -c uNoiseScale world-engine-lab.html` is **0**: the lab does not write it either. It sits at the `uniforms.js:10` factory default 4.0 on both sides, so there is no lab law to port. ⭐ **What IS true is stronger:** the two front-ends run the IDENTICAL four-octave stack — `Planet.js:340-345` and `height.glsl.js:690-693` share weights 0.5/0.35/0.2/0.1 and multipliers 0.3/1/2/4, differing only by the P-13 offsets — and `heightNoise.glsl.js:27` names the quantity as shared: *"uNoiseScale — per-planet base feature frequency (the game mirrors its own noiseScale)"*. ⛔ **AND `uNoiseScale` IS THE ONE FREQUENCY IN THE ENGINE WITH NO PHYSICAL SIZE BEHIND IT.** The lab km-keys SIXTEEN feature families through `featureFrequencyFromKm` (chaos, crack, crater, dune, ecuBlock, ecuDistrict, edifice, facet, fluvial, hex, karstDoline, lava, outflow, shat, subPit, subPoly) and `writePackUniforms.js:8` makes that the pack contract — *"Packs emit sizeKm-shaped drivers… the ONE place that turns a size in km into a shader frequency"*. A grep for `macroSizeKm`/`baseSizeKm`/`terrainSizeKm` returns nothing on either side: the BASE TERRAIN field alone still carries a raw frequency, and the game's is `rng.range(2.0,5.0)` off a type-keyed archetype table (`PlanetGenerator.js:346-356`) — a random draw unrelated to how big the body is. ⭐ **THE RULING STAYS `blocking` AND THAT IS DELIBERATE — corrected 2026-08-19 within the hour.** This row was briefly re-ruled `deferred (Max, 2026-08-19)` and `tests/material-parity-list.test.js:772` reddened on it within one run: *illegal ruling "deferred (Max, 2026-08-19)"*. ⛔ **That fence was right and the edit was wrong.** §2 defines exactly three rulings, and `blocking` is the one this row satisfies — the feature does not reach the pixel and the lab material does declare the uniform. **WHEN a blocking row gets fixed is SCHEDULING; it is not a fourth verdict**, and inventing one to record a date would have widened the legal set for every future row that found the three inconvenient. The deferral therefore lives here, in evidence, where it belongs. ⭐ Worth recording that this is the ledger's designed behaviour working on its author: this document is asserted by a test rather than read, which is the only doc-rot defence in the repo that has ever actually fired. **MAX'S SCHEDULING RULING 2026-08-19:** give the base field a characteristic wavelength in km, in the engine's own established shape — ⭐ **but AFTER moons ship.** It is not a Step 9/10 blocker, it moves the surface of ~970 bodies including ones already UAT-passed, and stacking it with the moon change would leave neither with a clean read. `uNoiseScale` stays at 4.0 through Steps 9-10, **deferred, not accepted-loss**. The wavelength comes to Max calibrated against real bodies, not chosen mid-wiring. ⭐⭐ **RE-MEASURED AT HEAD 2026-08-20, AND THE ROW IS SPLIT RATHER THAN STRETCHED.** `uNoiseScale` diverges on **266 of 266** bodies of the ledger pass (the cell said 103 of 103, which was the gas-only pass): the lab holds the factory 4.0 on every one while the game draws a real per-body value — measured over the 846 swapped bodies, `noiseScale` runs **1.5010 … 4.9970 across 846 distinct values**, and not one of them is 4.0. The ruling and Max's scheduling both stand unchanged. ⛔ **`noiseDetail` HAS LEFT THIS ROW AND IS RULED AT P-15 AS `accepted-loss`.** It is a game-side per-body gain — src/objects/Planet.js:383 `n += snoise(pos * noiseScale * 2.0) * noiseDetail * 0.5;`, the second octave of the surface-PATTERN stack rather than the relief one, measured **0.3005 … 0.7996 across 846 distinct values** — and the lab declares **no counterpart of any spelling**. §2's `blocking` test needs BOTH conjuncts; this subject meets the first and fails the second, so carrying it here spent a `blocking` verdict on a row it does not satisfy. That is P-08's error running the other way, and P-08's own correction is the precedent for fixing it without asking Max. ⭐⭐ **RE-RULED `carried` AT B2 LEG 3, 2026-08-20 — AND THE RULING IS THE SOLID HALF ONLY, WHICH THIS CELL SAYS RATHER THAN LETS THE ONE-WORD COLUMN IMPLY.** Max's scheduling ruling above landed: the base field now has a characteristic wavelength in km, calibrated against eight real bodies BEFORE it was chosen, in the engine's own established `featureFrequencyFromKm` shape. The mechanism is ONE ungated driver line — src/worldengine/drivers/rockySurface.js:268 `    uNoiseScale: sizeKm(macroWavelengthKm(condition), C_MACRO),` — resolved at the front-end's display radius by the writer, with every constant, the eight-body table and the Io-anchored process term in src/worldengine/base/macroWavelength.js:184 `export function macroWavelengthKm(condition) {`. **MEASURED on `lab-procedural-0…199`**: `rockySurface`'s `!== 'gas'` predicate claims **1160 of 1160** non-gas bodies; the driver comes back with **985 distinct values at raw float64, 844 at 9 significant figures and 780 at float32** — the precision convention is stated because a raw float64 count splits one physical value across ULP-adjacent doubles, and float32 is what a uniform reaches the shader at — **across 83 distinct 5 % bins**, where the LAB factory default was exactly ONE, and **0 of 1160** still answer 4.0. ⛔⛔ **TRUE UNTIL B7, 2026-08-21 — CORRECTED, NOT DELETED, BECAUSE THE CLAIM WAS RIGHT WHEN WRITTEN.** This cell read *"THIS CELL MEASURES THE POST-SWAP MATERIAL BY CONSTRUCTION AND IT IS NOT A CLAIM ABOUT TODAY'S PIXELS… the pack that carries it is admitted only behind a flag that is OFF"* — the flag then read `export const LAB_GAS_BODIES_DEFAULT = false;` with src/objects/Planet.js:2199 `    admitted: flag.enabled && provenance.isWorldEngine && packs.length > 0,`; MEASURED at that default `rockySurface` SELECTED 1160 of 1160 and **0 of 1160 were ADMITTED**, so every body rendered the legacy program. **B7 flipped the flag ON** — src/objects/Planet.js:2158 `export const LAB_GAS_BODIES_DEFAULT = true;` — so §2's `carried` test IS now a claim about today's pixels: admission still needs `provenance.isWorldEngine` (never Sol, at any flag value) and `packs.length > 0`, both of which `rockySurface`'s SELECTED set already satisfies, so this evidence is no longer the post-flip reading — it is the shipped one. ⚠ **AND THE ONE SHARED VALUE THIS REPLACES IS THE LAB'S, NOT THE GAME'S.** src/objects/Planet.js:1681 `        uNoiseScale: { value: d.noiseScale },` writes the generator's draw and MEASURED that is **1160 distinct over the same 1160 bodies at all three precisions**. Post-flip the swap is 1160 distinct → 844 (9 sig figs): FEWER values, deliberately, because Max's ruling was that the value be a PHYSICAL wavelength rather than a random draw. ⛔ Do not read this row as a differentiation win — it is a RE-CALIBRATION. ⛔ **THE GAS HALF IS NOT CARRIED, AND IT IS NOT RULED ANYWHERE YET.** ⛔ An earlier draft of this cell said it was "ruled at P-15 as `accepted-loss`, on P-15's own precedent". THAT WAS FALSE and is struck here: P-15's subject list at `:135` does not contain this name, so no ruling column carries the gas half at all. It is an OPEN RESIDUAL, which is why this row may not be read as closed outright.** Leg 3 ruled explicitly against writing the name into `giantDeck`: on a gas body the lab spends that spelling as a **band-warp frequency** reached through `fbmd` — src/worldengine/shaders/heightNoise.glsl.js:129 `        float freq = uNoiseScale * 0.3 * uDispDomainScale;` — not as a terrain wavelength, and none of the eight reference bodies behind the law is a gas giant while its process term's anchor (Io) is a solid-body anchor. Same spelling, two quantities: **P-08's shape, and P-15 is exactly where `uDispDomainScale` and `noiseDetail` already went for it**, so §2's `blocking` second conjunct FAILS on the gas half and `blocking` was never the right verdict for it. **MEASURED: 0 of 357 gas bodies in the corpus are claimed by any rocky pack, so all 357 hold src/worldengine/shaders/uniforms.js:10 `      uNoiseScale: { value: 4.0 },`** — on this ledger's own mounted pass that is the **103 gas bodies of 266**. ⚠ **THE NAME COULD NOT MOVE WITH THE POPULATION, AND THAT IS A FACT ABOUT THE INSTRUMENT RATHER THAN A DODGE:** tests/material-parity-list.test.js:675 `    const claimed = CH1_ROWS.flatMap((r) => r.subjects);` pins every measured subject to EXACTLY ONE row, so a split by POPULATION cannot be expressed by moving a NAME. It is declared in both cells instead — P-15's evidence carries the gas half in the same commit — and ⛔ no new row was created, because a nineteenth Channel-1 row reds tests/material-parity-list.test.js:964 `    expect(CH1_ROWS.length).toBe(19);`. ⭐ Held closed by INVERSION on P-04's pattern: tests/material-parity-list.test.js:789 `toContain('uNoiseScale')` asserts that `LEDGER.written` CONTAINS the name, so un-registering the pack reds this row instead of letting the feature leave silently. ⚠ **`carried` IS NOT `equal`, and nothing here claims it is** — §2's test is whether the feature reaches the pixel. The game still draws `noiseScale` as a random 1.5010 … 4.9970 and the lab now DERIVES its value from a size in km, two different questions by construction, so `uNoiseScale` stays on the every-body divergence list at tests/material-parity-list.test.js:590 `    expect(everyBody.sort()).toEqual([` — with a NEW cause, and §0's instrument note applies to it unchanged. |
| P-11 | `uLimbColor` `uTermColor` `uTermStrength` `uTermWidth` | carried | The air optics the game already computes per body from `atmosphereOpticsOf(condition)`. Same names on both materials; `uLimbColor`/`uTermColor`/`uTermStrength`/`uTermWidth` diverge on 103/103 bodies and `uLimbExponent` on 99/103. The values exist and are already in hand at material-creation time — the port simply does not carry them across. **⛔ `uLimbExponent` LEFT THIS ROW at the limbDeck/polarDeck registration AND CAME BACK AT STEP 10a, and the round trip is the finding.** It left because limbDeck writes it, moving from the diverged bucket into `agreeing` (20 -> 19) with the measured subject set falling 63 -> 62. Step 10a registers a fourth pack whose predicate is the COMPLEMENT of limbDeck's, so 163 solid bodies enter the ledger pass that limbDeck never claims; measured over `lab-procedural-0…59`, `uLimbExponent` diverges on **59 of those 163 and on 0 of the 103 gas ones**, and the two figures return to 20 and 63. **That is a real per-body loss on the newly-admitted half, not an instrument artefact** — the game computes an exponent for a solid body and the lab material does not receive it. ⛔ It must not be closed by scoping the ledger pass to the gas half; that would suppress the loss rather than rule it. Closing it means a pack that writes limb optics for non-gas conditions, which is not Step 9's or Step 10's declared scope. ⚠ **`uLimbColor` STAYS, and that is a measured finding rather than an oversight:** limbDeck writes it too, yet it still diverges from the game material's value on the compared bodies — so writing a uniform is not the same as agreeing with the game's own derivation of it. The three `uTerm*` names remain because nothing writes them at all. ⭐⭐ **B3 LEG 1, 2026-08-21 — THE NON-GAS HALF IS CLOSED AND `uLimbExponent` HAS LEFT THIS ROW FOR GOOD.** `src/worldengine/drivers/solidOptics.js` registers with the predicate `compositionClass(condition) !== 'gas'`, character-identical to `rockySurface`'s, and forwards five names from the two shared modules `atmosphereOpticsOf` and `terminatorOpticsOf`. ⛔ The terminator law was NOT re-authored: `TERM_STRENGTH` and `termWidthFor` were extracted out of `src/objects/Planet.js`, where they were module-private, into `src/worldengine/base/terminatorOptics.js`, and the game imports them BACK — one function object, both front-ends. **MEASURED over `lab-procedural-0…59` (266 both-materials bodies, 163 solid + 103 gas), before and after the registration, by the ledger pass in `tests/material-parity-list.test.js`:** `uLimbExponent` 59/266 → **0/266**; `uLimbColor` 59/266 → 266/266; `uTermStrength` 266/266 → **103/266**; `uTermWidth` 266/266 → **103/266**; `uTermColor` 266/266 → 266/266. ⭐ The `59` reproduces this row's own "59 of those 163" exactly, and it is now zero, so the name leaves the subject cell and `measured()` shrinks 68 → 67 — the first SHRINK any leg of this program has produced. ⚠ **THREE THINGS THIS DOES NOT CLOSE, EACH MEASURED RATHER THAN ESTIMATED, AND THE ROW STAYS `blocking` FOR THEM:** **(1) the GAS half of the terminator.** The residual 103 is the gas population exactly; `limbDeck` writes no `uTerm*` and this pack's predicate excludes gas, so nothing writes the band on a gas body. **(2) `uLimbColor` and `uTermColor` still diverge on all 266** — and NOT because they are unwritten. Both are written on every body this pack claims. They diverge through the `encodeValue` CONTAINER split this document already records for the palette four: the game hands a `THREE.Vector3`, the lab a `THREE.Color`. That is an instrument fact, and closing it is a change to the comparison, not to a wire. **(3) `uLimbStrength` IS NOT WRITTEN AND THE FORWARDED LIMB PAIR IS THEREFORE INERT ON PIXELS.** It is not in this row's subject list and the game's solid material has no counterpart for it (the game gates its rim on the differently-named `uLimbMix`), so a swapped solid body now carries the correct exponent and hue behind a rim master gate still at its `0.0` factory default. Turning it on is a visible change to every solid body — a UAT decision, not a wiring one, and a wiring commit does not make it. ⭐⭐ **CLOSED 2026-08-21 — THE GAS HALF BY A WIRE, THE TWO COLOURS BY THE INSTRUMENT, AND THOSE ARE NOT THE SAME KIND OF CLOSURE.** **(1) THE WIRE.** `src/worldengine/drivers/giantSurface.js` registers over `compositionClass(condition) === 'gas'` — the exact complement of `solidOptics`' predicate, so no body runs both and the collision throw stays inert — and forwards the SAME `terminatorOpticsOf` call the game makes at src/objects/Planet.js:1610. MEASURED over `lab-procedural-0…59` (266 both-materials bodies): `uTermStrength` **103/266 → 0/266**, `uTermWidth` **103/266 → 0/266**. The residual 103 this row named was the gas population exactly, and it is gone. ⚠ **AND THE AGREEMENT ON `uTermStrength` IS THE WEAK KIND, SAID HERE RATHER THAN LEFT TO BE FOUND.** Both materials now write the CONSTANT **0.15** on all 266 — `columnFraction` saturates above 0.3 bar and every body in this corpus is above it — so the name also LEFT `labVarying` (73 → 72 names). ⛔ That is not a regression and the direction is the tell: the lab's old 'variation' WAS the defect — it wrote 0 on the 103 gas bodies and 0.15 on the 163 solid ones, and two values one of which is a factory default is not per-body character. Instrument C's own caveat applies (green is weak evidence on a constant), and it is recorded rather than counted as a win. ⭐ **THE PIXEL CHANGE IS REAL AND IS THE ONE THIS ROW EXISTED FOR:** `uTermStrength` goes 0 → 0.15 on 343 gas-class bodies, so src/worldengine/shaders/planetShaders.glsl.js:953 stops early-outing and the twilight band renders — which is what the legacy material already draws on those same bodies. Closing this row REMOVES a regression B7 would have shipped rather than adding a feature. **(2) THE INSTRUMENT.** `uLimbColor` and `uTermColor` were never unwritten — both packs write them on every body they claim — and they diverged **266/266 through the `encodeValue` CONTAINER SPLIT** this row's own earlier text names. ⛔ **PROBED, NOT INHERITED:** on a gas body the game's `THREE.Vector3` and the lab's `THREE.Color` carry components that are equal at **maxComponentDelta = 0**, i.e. bit-for-bit at float64, and both upload to the same GLSL `vec3` slot. `encodeValue` now encodes a Color as a 3-component vector, so the comparison stops measuring the writer's choice of JS class. **EXACTLY SIX NAMES FLIP AND ALL SIX ARE ENUMERATED IN §0's instrument note** — `uBioGroundColor` `uFreshColor` `uLimbColor` `uSedColor` `uTermColor` `uWeatheredColor`, each 266/266 at delta 0. No name flipped for any other reason, which is the check that makes this a comparison fix rather than a loosening. |
| P-12 | `uFreshColor` `uWeatheredColor` `uSedColor` `uBioGroundColor` `uBioGroundCover` `uIcenessMix` | carried | ⭐⭐ **RE-MEASURED AT HEAD 2026-08-20 OVER THE POST-STEP-10a SUBJECT SET — 266 bodies, 103 gas + 163 solid — AND THE ROW SPLITS CLEANLY DOWN THE PACK PREDICATE.** `rockySurface` writes all six on the 163 solid bodies it claims, and compared COMPONENT-WISE at 1e-6 the written half **agrees on 163 of 163 for every one of the six**. What remains is the 103 gas bodies no pack claims: the four colours diverge on **103 of 103**, `uIcenessMix` on **56 of 103**, `uBioGroundCover` on **5 of 103** (7 under exact string equality — two differ by less than 1e-6, which is the reason the number is quoted with its tolerance). Measured, the three ground endmembers all sit at the factory default on every gas body — src/worldengine/shaders/uniforms.js:142 `uFreshColor:   { value: new THREE.Color(0.46, 0.40, 0.34) },` — while the game draws a per-body value there. ⛔ **THE OLD CELL'S `103/103` WAS NOT A STALE DENOMINATOR, IT WAS A STALE NUMERATOR TOO:** the identical figure now names the opposite population, which is exactly why a re-measure was owed rather than a re-read. ⚠ **AND THE HALF THAT AGREES IS NOT RE-RULED `carried`, DELIBERATELY.** The pass's `encodeValue` compares CONTAINERS, and these four are `THREE.Vector3` in the game against `THREE.Color` in the lab, so `tests/material-parity-list.test.js` still lists them among the ten that diverge on every body — and it is right to, because that instrument decides the SUBJECT SET (§0). Splitting a row on a tolerance the subject-set instrument does not share would drop names out of the partition silently. **`blocking` stands, with a corrected denominator of 103 of 266 and a named cause: the loss is now precisely the gas half, and closing it is a `giantDeck` line, not a `rockySurface` one.** ⭐⭐ **CLOSED 2026-08-21.** `giantSurface` writes all six on the 103 gas bodies no pack claimed, from the SAME producers the generator itself calls unconditionally — src/generation/PlanetGenerator.js:809 and src/generation/PlanetGenerator.js:827 — so this is a wire and not a law. MEASURED: `uBioGroundCover` **5/103 → 0/266**, `uIcenessMix` **56/103 → 0/266**; the four colours close with P-11's two on the `encodeValue` container fix, at maxComponentDelta 0 on 266/266. ⚠⚠ **AND THE HONEST HALF OF THIS ROW: SIX OF THE SEVEN NAMES CHANGE NO PIXEL ON A GAS BODY, WHICH IS MEASURED AND IS NOT A REASON TO HAVE SKIPPED THEM.** src/worldengine/shaders/planetShaders.glsl.js:634 derives `bandMask` 1.0 on every gas body (giantDeck writes `uBandStrength` 1.0, the province weight is neutral at its 1.0 floor), and src/worldengine/shaders/planetShaders.glsl.js:652 then REPLACES the whole solid-surface albedo story the palette feeds. `uFreshColor`, `uSedColor`, `uCratonColor`, `uBioGroundColor`, `uBioGroundCover` and `uIcenessMix` are read only inside that discarded chain. ⭐ `uWeatheredColor` IS THE EXCEPTION and it is why the family was not skipped: it survives into the `litSurf` Rayleigh term and into src/worldengine/shaders/planetShaders.glsl.js:975 `          vec3 emissive = uWeatheredColor * uEmissive * (1.0 - blanketMask);`. ⛔ The six are written because B7 DELETES the legacy fallback, so a name left at the factory default after that is a permanent loss rather than a recoverable one — and because a row closed by declaring the loss acceptable is the 'suppress rather than rule' move this document exists against. **This row closes on VALUES. No look change is claimed from it.** |
| P-13 | `uMacroOffset` `uDetailOffset` `uCraterOffset` | carried | ⭐⭐ **RE-MEASURED AT HEAD 2026-08-20 — the forwarding LANDED on the half it can reach, and the row is corrected rather than closed.** All three are written by `rockySurface` and agree component-wise with the game on **163 of 163** solid bodies; the stale evidence this row carried (`value-defaulted on 103/103`) is retired on measurement, not on argument. They diverge on **103 of 103** gas bodies, where the lab answers src/worldengine/shaders/uniforms.js:158 `uMacroOffset:  { value: new THREE.Vector3() },` — literally `(0,0,0)` — against a per-body game value (sampled: `(-78.8825, -8.3232, -3.6071)`). ⛔ **SO THE DISTINCTNESS DEFECT THIS ROW NAMES IS NOT RESOLVED, IT IS CONCENTRATED: every one of the 103 swapped gas bodies now draws its base height field from the SAME three offsets.** They are read with no composition gate between the default and the pixel — src/worldengine/shaders/heightNoise.glsl.js:143 `vec3 off = (i < 3) ? uMacroOffset : uDetailOffset;` — so this is the 5d hex-collapse defect alive on the gas half, and no algebraic gate on the pack sees it. **`blocking` stands with a corrected denominator of 103 of 266; the missing writer is `giantDeck`.** ⚠ The cell's companion claim — *"the per-body variation left on the whole lab material is 16 uniforms of 356"* — is superseded: **48 of 356** at HEAD (§0), because the registration that closed the solid half of this row added 32 varying names. ⭐⭐ **CLOSED 2026-08-21, AND IT IS THE ONE OF THE THREE WHOSE CLOSURE IS PURELY A WIRE AND WHOLLY PIXEL-LIVE.** `giantSurface` FORWARDS the front-end's own three vectors — it derives none of them, exactly as `rockySurface` refuses to — and the two packs now share ONE expression of the block (`offsetDriverBlock`), so the refuse-don't-default rule cannot be repaired in one pack and left in the other. MEASURED: all three go **103/103 → 0/266**. ⭐ **THE 5d HEX-COLLAPSE THIS ROW NAMED IS GONE RATHER THAN CONCENTRATED.** src/worldengine/shaders/heightNoise.glsl.js:143 reads the offsets with no composition gate between the default and the pixel, so until today all 343 gas-class bodies drew their base height field from the SAME `(0,0,0)` domain; they now draw distinct ones, and because `zonalBandCol` reads the perturbed `N` and `vPos`, the bands ride the domain too. ⛔ **THE ROW'S OWN RESIDUE IS UNCHANGED AND IS NOT SILENTLY ABSORBED:** the 20 remaining feature-domain offsets (dune, dust, karst, glacial, fluvial, tessera, …) are still identical on every swapped body. The legacy material writes exactly these three and no others, so that residue is the state the game already ships — an offset-FAMILY decision with 20 producers to find, not three more driver lines. |
| P-14 | `uCraterAmp` `uCraterComplexD` `uCraterScale` `uEjectaAmp` `uCraterDensity` `uCraterRelaxation` `uEjectaRampart` `uEjectaStrength` | carried | ⭐⭐ **RE-MEASURED AT HEAD 2026-08-20 — P-13's shape and P-13's correction.** `rockySurface` writes all four and they agree component-wise on **163 of 163** solid bodies; they diverge on **103 of 103** gas ones, where the game holds craters OFF (`uCraterAmp` 0, `uCraterScale` 1, `uEjectaAmp` 0) and the lab answers with the factory schedule (0.9, 6.0, 0.35) — the lab is LOUDER than the game here, which is the opposite of every other row in this section. ⚠ **The gas-half divergence is ruled, not dismissed, and it is not a pixel today:** `uCraterDensity` is the gate AND the negative control — src/objects/Planet.js:314 `// across this game's bodies. The gate is a uniform, so at uCraterDensity 0 this function is` — and it is one of the eight that agree by absence, zero on both sides on every gas body. So this is a loud default behind a shut gate, and it becomes a pixel the moment anything opens that gate; declaring it now is cheaper than discovering it then. **`blocking` stands with a corrected denominator of 103 of 266.** ⛔ **`uDispDomainScale` HAS LEFT THIS ROW AND IS RULED AT P-15 AS `accepted-loss`** — it is not an impact term, nothing writes it on either side, and it fails §2's `blocking` second conjunct for the P-08 reason (the lab declares the NAME and not the QUANTITY). The remaining crater names (`uCraterDensity` `uCraterRelaxation` `uEjectaLump` `uEjectaRampart` `uEjectaStrength` `uTerraceCount` `uVoroCells` `uFwClamp`) agree on every body — because both sides are zero or the same constant, which is agreement by absence and is recorded as such rather than as parity. ⭐⭐ **FOUR NAMES JOINED THIS ROW AT B2 LEG 1, 2026-08-20, AND THIS ROW PREDICTED THEM.** The sentence above — *"a loud default behind a shut gate, and it becomes a pixel the moment anything opens that gate; declaring it now is cheaper than discovering it then"* — was written about `uCraterDensity`, and leg 1 is what opened the gate: re-deriving src/worldengine/port/craterUniforms.js:71 `export const CRATER_VIS_FLOOR_RAD = 9.6e-4;` and retiring the fixed density floor into src/worldengine/port/craterUniforms.js:79 `export const CRATER_MIN_VISIBLE = 1.0;` took the crater-live population from **8 of 526 to 289 of 526** on the port-contract corpus and from **485 to 761 of `lab-procedural-0…199`'s 1160 non-gas bodies**. **MEASURED at that commit over this ledger's own 266 compared bodies: `uCraterDensity` diverges on 64, `uEjectaStrength` on 64, `uCraterRelaxation` on 56, `uEjectaRampart` on 42** — they leave the "agree by absence" list in §1 and enter this row. ⚠ **THE DIRECTION IS THE ONE THAT COSTS A PIXEL, and it is measured rather than inferred: on every diverging body the GAME writes the live value and the LAB writes 0** (`rocky: uCraterDensity 0.0008051676964833844 → 0`; `sub-neptune: uEjectaStrength 1 → 0`). **The cause is a GATE difference that leg 1 neither created nor closes:** src/worldengine/drivers/craterDeck.js:126 `uCraterDensity: scalar(cu.density * rel, { gate: CRATER_GATE }),` (⭐ RE-POINTED 2026-08-21: the line moved OUT of `rockySurface.js` with the whole block at B3 leg 2 — same text, new file) multiplies the crater terms by src/worldengine/drivers/craterDeck.js:109 `const rel = craterRelevanceOf(condition);` (⭐ RE-POINTED 2026-08-21, same move), and the pack's predicate excludes gas-class bodies, while the legacy material's crater path is keyed on the TYPE LABEL — so the two gates disagree, and while both sides were zero the disagreement was invisible. ⛔ **`blocking` is inherited, not newly ruled** — same family, same row, same §2 test (the loss stops reaching the pixel and the lab material already declares every one of these uniforms). No fourth verdict was invented, and the four names are NOT re-blessed into agreement. ⭐⭐⭐ **RE-RULED `carried` AT B3 LEG 2, 2026-08-21 — THE CRATER HALF IS CLOSED AND SO IS THE REST OF THE ROW, AND THE SECOND HALF OF THAT SENTENCE IS THE PART TO READ.** The mechanism is one pack over the COMPLEMENT predicate: src/worldengine/drivers/craterDeck.js:96 `export function craterDriverBlock(condition) {` — the ten impact drivers, MOVED out of `rockySurface.js` rather than copied, so `rockySurface` (`!== 'gas'`) and `craterDeck` (`=== 'gas'`) emit the identical map from ONE expression and every body in any corpus has exactly one writer of the family instead of none on the gas half. ⭐ **MEASURED over `lab-procedural-0…199`, 343 gas-class planets, game legacy value against the pack's resolved value, all ten names:** `uCraterScale` 343→**0**, `uCraterAmp` 343→**0**, `uCraterComplexD` 343→**0**, `uEjectaAmp` 343→**0**, `uCraterDensity` 204→**0**, `uEjectaStrength` 204→**0**, `uCraterRelaxation` 184→**0**, `uEjectaRampart` 145→**0**, with `uTerraceCount` and `uEjectaLump` already at 0. On this ledger's own 266-body pass all eight subjects leave `divergedCarried` (23→**15**) and `measured()` goes **67→59** — the second SHRINK this program has produced and eight times the size of the first. ⚠ **AND THE CLOSURE IS NOT 'CRATERS OFF ON GAS WORLDS', WHICH IS WHAT THE CELL ABOVE WOULD LEAD A READER TO EXPECT.** `craterUniformsFrom` is TOTAL and condition-derived, and MEASURED on the same corpus **285 of 343 gas bodies have `craterRelevanceOf > 0` and 204 have a schedule that FIRES**, reaching `uCraterAmp` 0.0310 and `uCraterDensity` 1.0837e-3. The pack forwards the producer the game's own material already calls at src/objects/Planet.js:1596 `    const craters = craterRelevanceOf(condition) > 0`; it never chooses a number. That is what makes this a WIRING closure. ⚠ **ONE RESIDUAL SHAPE, DECLARED RATHER THAN CLOSED, AND IT IS INHERITED NOT INTRODUCED:** the block writes `cu.amp`/`cu.scale`/`cu.complexD` ungated while the game writes the frozen `CRATERS_OFF` whenever `craterRelevanceOf` is 0, so the two would disagree on a body whose schedule FIRED with relevance 0. MEASURED: **0 of 343** gas bodies are in that state, and `rockySurface` has carried the same shape on the solid half since Step 10a. It is a corpus fact, not an identity. ⛔ **`uDispDomainScale` WAS ALREADY SPLIT OFF BEFORE THIS LEG AND THIS LEG DID NOT MOVE IT** — the cell above records the split and P-15's subject list at `:135` carries the name. Verified, not re-done. |
| P-15 | `uReliefMix` `uReliefOctaves` `uReliefGain` `uReliefGainCont` `uReliefNormalGain` `uCraterReliefGain` `noiseDetail` `uDispDomainScale` | accepted-loss | `[lab-has-it]` The game's own noise calibration — the fbmd relief gains, plus the two terms that have no carrier at all. The lab has its own displacement stack (`uOctaves` `uRedistribution` `uRidgeGain` `uPerturb` …) and replaces this wholesale rather than renaming it, so the game's calibrated gains have nowhere to go. Declared because "the lab does its own relief" is a decision, and an undeclared one would read as an oversight later. ⭐⭐ **TWO SUBJECTS JOINED THIS ROW 2026-08-20, EACH SPLIT OUT OF A `blocking` ROW WHOSE TEST IT FAILED — and neither landed here for convenience.** **(1) `uDispDomainScale` is the sixth member of this row's own const block, and the source says so:** src/objects/Planet.js:1381 `const RELIEF_DOMAIN_SCALE = 1.0 / 0.3;` sits inside the `RELIEF_*` calibration whose comment nine lines up already ties the two together, and the game spends it in exactly ONE place — src/objects/Planet.js:317 `float baseFreq = uNoiseScale * 0.3 * uDispDomainScale;   // fbmd's octave-0 frequency` — as a DIVISOR that normalises the analytic relief gradient. The lab spends the same spelling as a MULTIPLIER of the sample domain, src/worldengine/shaders/height.glsl.js:689 `pos *= uDispDomainScale;`, written by the lab's frame loop and by **no pack** — src/worldengine/shaders/uniforms.js:16 `// frame loop is the ONLY writer; display scale 1 (radius 1 R⊕) → uDispDomainScale=1.`. Measured over the 266: game **3.3333** on every body, lab **1.0** on every body, neither varying. ⛔ Writing the game's number into the lab's name would multiply every continent's domain by 3.33; it would not normalise a gradient. **Same spelling, two quantities — P-08's exact shape one uniform over** — so the lab declares the NAME and not the CARRIER, and §2's `blocking` second conjunct fails. **(2) `noiseDetail`** is a per-body game gain on the surface-PATTERN stack (P-10 carries the citation and the measured 0.3005 … 0.7996 over 846 distinct values) with no lab counterpart of any spelling. ⚠ This row's title widens with them: it was *"the game's fbmd relief calibration"* and is now the game's noise calibration entire, relief and pattern, because the two arrivals are pattern-side and calling them relief would be the tidier lie. ⭐ **A THIRD SUBJECT IS RULED HERE AT B2 LEG 3, 2026-08-20 — BY POPULATION RATHER THAN BY NAME.** `uNoiseScale`'s **GAS HALF** belongs in this row for this row's exact reason. The lab spends that spelling on a gas body as a **band-warp frequency**: `bandWarpField` and `jetsDisp` reach it through `fbmd`, whose octave 0 is src/worldengine/shaders/heightNoise.glsl.js:129 `        float freq = uNoiseScale * 0.3 * uDispDomainScale;`. A zonal-flow warp has no macro-relief structure to calibrate, none of the eight reference bodies behind the km law is a gas giant, and the law's process term is anchored on Io — a solid body — so writing it into `giantDeck` would give the same spelling two quantities. That is (1) above, one uniform over. **MEASURED: 0 of 357 gas bodies in `lab-procedural-0…199` are claimed by a rocky pack, so all 357 keep the factory 4.0.** ⛔ The NAME is NOT added to this row's subject cell, and the reason is the instrument: tests/material-parity-list.test.js:675 `    const claimed = CH1_ROWS.flatMap((r) => r.subjects);` pins each measured subject to exactly one row. P-10 keeps the name and rules the SOLID half `carried`; this cell rules the gas half, and the two cells are the whole accounting. ⭐⭐ **`uDispDomainScale` IS NOW RULED BY MAX, 2026-08-21 — THE VERDICT IS UNCHANGED AND ITS STANDING IS NOT.** `accepted-loss` recorded that nobody had chosen the lab's 1.0; it is now chosen. He compared the shipped result against a clean feature-size A/B in the live game on `lab-procedural-6` (17 swapped bodies, ~2.2 radii) and picked the SHIPPED larger features. ⛔ **THE FIRST A/B PUT TO HIM WAS INADMISSIBLE AND IS RECORDED HERE BECAUSE THE ROW ITSELF PREDICTED WHY:** it drove `uDispDomainScale` on the lab material, so the B arm carried the legacy fbmd octave-0 frequency AND a 3.33x province/domain change together — this cell's own *"Same spelling, two quantities"* warning, walked into. The admissible dial is `uNoiseScale`, which keys every octave in the lab stack and nothing else; proven live rather than assumed at 10.0% of pixels differing (150,635 of 1,508,800), max channel delta 255. ⭐ **AND THE STRUCTURAL FACT THE FIRST INSTRUMENT HID, WHICH OUTLIVES THIS RULING: there is no uniform-level "pre-B7 look" on a swapped body at all.** The two materials are different shader stacks, not one stack with a dial, so no single uniform reproduces the legacy appearance and any future A/B that claims to is making the same error. ⚠ This ruling is about the SHIPPED rendering, not about porting the game's 3.3333 into the lab's name — that remains what this cell says it is. |
| P-16 | `planetType` | carried | The legacy branch selector. `carried` here means **the mechanism is replaced by design** — the lab draws from the condition, not from a type label, which is the port's whole thesis — and it must not be read as "nothing was lost at this row". What each branch actually DREW through this switch is ruled loss-by-loss in §4; this row rules only the switch. |
| P-17 | `lodLevel` | accepted-loss | `[lab-lacks-it]` Read by no shader (§1.1). The writer already reports its own skip. Nothing reaches a pixel through this uniform on either side. |
| P-18 | `lightDir` `planetRadius` `time` `uPosterizeLevels` | carried | The four that genuinely survive (three until B2P, 2026-08-20), each by a named mechanism — and ⚠ **THREE of the four are verified to VARY across bodies on the post-swap material; `uPosterizeLevels` deliberately does NOT**, which the star note below states outright and which this clause used to contradict: `lightDir`→`uLightDir` and `planetRadius`→`uBodyRadius` are written by `buildLabPlanetMaterial`, and `time`→`uTime` by the per-frame seam `updateLabPlanetMaterial`. ⭐ **`uPosterizeLevels` JOINED THIS ROW AT B2P, 2026-08-20, AND IT IS THE ONE SUBJECT HERE THAT DOES NOT VARY ACROSS BODIES — deliberately.** It is the colour quantum: a global DISPLAY setting, not a per-body derived quantity, so sameness across bodies is the feature and not a collapse (`port-uniform-delta.mjs` files it UNWATCHED for the same reason). The mechanism this row demands is named, and it is a DIFFERENT mechanism from the rename the other three rely on. ⛔ **CORRECTED 2026-08-20 — IT IS NOT OBJECT IDENTITY ACROSS THE SWAP, and the round-3 arithmetic fix is why.** `src/rendering/posterizeLevels.js` holds TWO objects on purpose: `POSTERIZE_QUANTUM`, a `THREE.Vector2(levels, Math.fround(1/levels))`, is what the six game fragment programs hold under `uPosterizeLevels`, because the reciprocal has to be CPU-carried; the lab program declares `uniform float uLevels`, a SCALAR that cannot hold a vec2, so `buildLabPlanetMaterial` substitutes the OTHER object, `POSTERIZE_LEVELS`. Pre-swap and post-swap therefore hold DIFFERENT objects and one assignment does not move both BY IDENTITY — `setPosterizeLevels` is the single writer of both and sets them together, so the carry is by SINGLE-WRITER CONSTRUCTION. This cell is the planet-side twin of M-11 and carried M-11's pre-correction wording until this edit. ⛔ Which is exactly why the name still lands in `lost`: the SPELLING leaves the material and the QUANTITY does not, which is §2's `carried` in its own words. |
| P-19 | `uCoarseCut` | carried | ⭐⭐ **NEW PHYSICS, 2026-08-26 — NOT A PORT, AND THE ONLY ROW IN THIS LEDGER WITH NO LEGACY COUNTERPART AT ALL.** The game has never had a term for *how much LARGE-SCALE relief tidal resurfacing has erased*, so there is nothing here for the lab to reach byte-identity WITH; the row is `carried` because BOTH materials now declare the name (`src/worldengine/shaders/uniforms.js` for the lab-surface material, `src/objects/Planet.js` for the legacy one), not because the two sides agree in value. ⛔ THEY DELIBERATELY DIVERGE: the legacy material holds the identity 0.0 (no flattening, i.e. exactly today's look) while the lab-surface material takes the `rockySurface` pack's `coarseReliefCut(condition)`. That divergence IS the feature — it is what makes this additive, since a body the pack does not claim renders bit-unchanged. ⭐ WHY IT EXISTS: `macroShortening` used to express tidal drive as a shorter WAVELENGTH, which measured out at 75 macro structures per body radius — 5.1x finer than Io, the finest body in the law's own calibration table — and left the renderer 0–1 usable LOD octaves against Earth's 6. Io's table rows record *features on Io*, i.e. an absence of LARGE-SCALE RELIEF, which is an AMPLITUDE fact that had been encoded as a FREQUENCY. Full derivation: `docs/FEATURES/lod-architecture-rootcause-2026-08-26.md`; the law is `coarseReliefCut` in `src/worldengine/base/macroWavelength.js`. |
<!-- /LEDGER-CH1 -->

---

## 4. Channel 2 — the features drawn in hardcoded GLSL, per `planetType` branch

⚠ **§12.4 channel 3: the branches are mutually exclusive, so no single body witnesses more than
one.** The rows below are therefore per branch, and only branches with **≥1 swapped body on the
measured corpus** are ruled. The live set is derived from the population at test time, so a body
class that becomes swappable in an unruled branch fails the test rather than passing silently.

**How the subject set is derived.** `tests/material-parity-list.test.js` strips comments from the
shipped `PLANET_SHADER_VARIANTS[key].fragmentShader`, isolates `getSurfacePattern` and `main`,
brace-matches every `planetType == N` block, and collects every `float`/`vec2`/`vec3`/`vec4`/`mat3`
local declared inside it. The extraction reproduces all six lines PLAN §6b names by hand —
src/objects/Planet.js:434 `float stormMask = smoothstep(0.78, 0.88, bandVal);`, :437
`float polarDark = smoothstep(0.6, 1.0, abs(vPosition.y) / planetRadius);`, :446
`float hotspot = pow(starFacing, 3.0);`, :451
`float nightSide = max(-dot(normalize(vWorldPos), lightDir), 0.0);`, :407
`float ringNoise = snoise(pos * noiseScale * 2.0) * 0.15;`, :413
`float haze = snoise(pos * noiseScale * 0.7) * 0.08;` — **and 93 more**. Every extracted symbol must
be claimed by exactly one row below; an unclaimed symbol fails the test, which is what makes a new
hardcoded effect in the legacy shader an undeclared loss the build catches.

<!-- LEDGER-CH2 -->
| id | variant | branch | symbols | ruling | evidence |
|---|---|---|---|---|---|
| G-01 | gas | pt1 | `bands` `lat` `turb` `bandVal` `zoneMask` | carried | Zonal banding and its zone/belt split — the one feature the pack genuinely replaces. 11 `uBand*` uniforms written per body plus the `aBand` bake, and 10 of the 16 uniforms that vary across swapped bodies are these. |
| G-02 | gas | pt1 | `storm` `stormColor` `stormMask` | accepted-loss | `[lab-has-it]` The great spot. The lab declares the mechanism — planet-lod-uniforms.js:417 `uStormPosSize:    { value: Array.from({ length: 8 }, () => new THREE.Vector4()) },  // xyz center, w angular radius (rad)` and `uStormParams` `uStormColor` `uStormAux` `uStormCount` — but PLAN §5 fences the storm producer out of pack #1 **by name**, and `aStorm` is the one attribute `_createLabSurface` deliberately zero-fills. Declared here rather than left to be discovered on 53 gas giants. |
| G-03 | gas | pt1 | `polarDark` | carried | **Ledger C19.** planet-lod-uniforms.js:425 `uPolarStrength:   { value: 0.0 },` is the lab's gate and it is not written. §12.4 measured that `state.polarStrength`'s only producer lives inside the fenced `applyStormState`, so ALL_ON would open a gate onto a value the game cannot compute — C19 survives ruling 4, and this row is that finding on the game side. **⭐ CLOSED at the registration commit:** `POLAR_DECK_ENTRY` is in `PACKS` and polarDeck writes the eight-name `uPolar*` family from `resolvePolarVortex`, which is shared, three-free, ported code — so the game CAN compute the value §12.4 said it could not. ⚠ `uPolarStrength` is the per-seed PRESENCE coin, not a master gate: it is 0 on roughly 40% of gas bodies BY DESIGN, so a capless giant is not a failed wire. |
| G-04 | gas | pt6 | `bands` `lat` `swirl` | carried | Hot-Jupiter banding, same mechanism as G-01. |
| G-05 | gas | pt6 | `starFacing` `hotspot` `glowColor` `nightSide` | accepted-loss | `[lab-lacks-it]` Day-side thermal hotspot and night-side thermal glow. Neither `uHotspot` nor `uNightSide` occurs in the corpus, and the lab has no substellar-anchored emission term. Population **5 of 846** (5 of 341 pre-Step-10a — this branch is one of the two the Step-10a widening did NOT move). ⭐ Re-measured, it is now TIED-rarest with `gas pt7` rather than second-rarest: the branch it used to be rarer than, `rocky pt5`, went 1 → 13. The reason it is ruled anyway is unchanged — "rare" is not "declared". |
| G-06 | gas | pt7 | `angDist` `ringNoise` `oceanColor` `oceanMask` `landColor` `landMask` `iceColor` `iceMask` `frozenColor` `frozenMask` | accepted-loss | `[lab-lacks-it]` The eyeball's concentric climate rings, centred on the substellar point, and the four-way ocean/land/ice/frozen palette they drive. No lab counterpart: the lab's climate is latitudinal, not substellar. Population **5 of 846** (3 of 341 pre-Step-10a). ⭐ The two extra are rocky-CONDITION eyeballs: `eyeball` is inside `GAS_TYPES` so `shaderVariantFor` says gas while the condition says otherwise, and they swap for the first time under `rockySurface`'s complement predicate. ⚠ PLAN §12.4's committed seed for this branch, `lab-procedural-20`, carries an eyeball that the predicate does **not** claim — see §6. |
| G-07 | gas | pt10 | `bands` `lat` | carried | Sub-Neptune's subtle banding, same mechanism as G-01. It was the **largest** swapped branch at 166 of 341; at HEAD it is **166 of 846** and no longer the largest — R-01's `rocky pt0` carries **181**. The branch itself did not move; the population around it did. |
| G-08 | gas | pt10 | `haze` | accepted-loss | `[lab-has-it]` The sub-Neptune haze term. `uHazeColor` and `uHazeMute` exist on the lab material and are not written, so the lab draws its own default haze rather than this one; ruled a loss rather than a mapping line because the game's `haze` is a *pattern* contribution and the lab's is a *tint*. |
| R-01 | rocky | pt0 | `dust` `dustPos` `dustMask` `dustLight` | accepted-loss | `[lab-has-it]` The rocky dust mantle. `uDustColor` `uDustTint` `uDustFlatK` `uDustRegionFreq` exist and are unwritten. Population **181 of 846** (21 of 341 pre-Step-10a) — ⭐ the LARGEST swapped branch in the corpus at HEAD, and one of the 617 the plan's §6b table does not cover. |
| R-02 | rocky | pt2 | `cracks` `h` `ground` `rock` | accepted-loss | `[lab-has-it]` Ice cracking and the ground/rock mix. `uCrackScale` `uCrackWidth` `uFrostAlbedo` exist and are unwritten. Population **131 of 846** (66 of 341 pre-Step-10a) — still the second-largest swapped branch in the whole corpus, and entirely absent from the plan's list. |
| R-03 | rocky | pt5 | `cOld` `terrainNoise` `warpX` `warpZ` `warpedPos` `height` `seaLevel` `landMask` `landElev` `deepOcean` `shallowOcean` `ocean` `oceanDepth` `land` `lowland` `midland` `highland` `peak` `vegElev` `iceNoise` `iceColor` `iceMask` `polar` `lat` `latitude` `latBias` `itcz` `stormTrack` `cloudPos` `cloudMask` `cloudLight` `cn` | accepted-loss | `[lab-has-it]` The whole terrestrial world: continents, sea level, the elevation palette, polar caps, the ITCZ and storm-track cloud bands. The lab has its own continent/sea system, so this is replacement rather than deletion — but nothing the pack writes reaches any of it, so the *body's own* continents are gone. Population **13 of 846** on this corpus (1 of 341 pre-Step-10a); ruled in full when it was a single body, which is the reason the ×13 needed no re-ruling — the population is a property of the seed range, not of the code. |
| R-04 | rocky | pt9 | `base` `crystal` `glint` `val` | accepted-loss | `[lab-has-it]` Carbon-world crystalline facets and their glint. `uFacetAmp` `uFacetCoverage` `uGlintDensity` `uGlintExp` `uGlintTint` exist and are unwritten. Population **104 of 846** (26 of 341 pre-Step-10a). |
| R-05 | rocky | pt3 | `crackGlow` `crust` `h` `melt` | accepted-loss | `[lab-has-it]` The lava world's crust/melt split and the glow in its cracks. **Newly live at Step 10a — 52 bodies**, and live for the first time because `rockySurface`'s predicate is `compositionClass(condition) !== 'gas'`; before it, no `lava`-typed body swapped at all. Ruled by §2's own test rather than by choice: the lab HAS a lava mechanism (`uLavaScale` `uLavaGlowRate` `uLavaAxis` `uLavaCoverage`) but it is **a different parameterisation, not a renamed one** — the same reasoning under which P-09 already rules this feature's uniform half (`uLavaGlow` `uLavaCrust`) an accepted loss. `rockySurface` writes none of the four. ⚠ **The SCHEDULING of this row is still Max's, reserved 2026-08-09**; per P-10's precedent the deferral lives here in evidence, because §2 defines exactly three rulings and "deferred" is not one of them. |
| R-06 | rocky | pt4 | `deepOcean` `height` `land` `landElev` `ocean` `oceanDepth` `seaLevel` | accepted-loss | `[lab-has-it]` The ocean world's own continents, sea level and depth palette. **Newly live at Step 10a — 6 bodies.** Same subject family and same ruling as R-03, which rules the identical seven symbols on `pt5`. ⛔ **CORRECTED 2026-08-20 — R-03's REASON DOES NOT TRANSFER AND THIS ROW INHERITED IT VERBATIM.** R-03 says "the lab has its own continent/sea system, so this is replacement rather than deletion". **Measured on these bodies there is no replacement: an ocean world does not swap to a different ocean, it swaps to a DRY ROCK.** Live on the mounted lab material, `lab-procedural-4` p=2: `uSeaLevel` **-1.0** — the shader's own no-liquid sentinel, src/worldengine/shaders/planetShaders.glsl.js:374 `if (uSeaLevel > -1.0){` — so the sea cut never runs, `liquidMask` stays 0 for every fragment, and the sunglint that multiplies by that mask goes with it. ⭐⭐ **AND IT CANNOT BE CLOSED BY WIRING**, which is the schedule-relevant half: recomputing src/worldengine/base/labCore.js:706 `const liquidStability = clamp01(retentionGate * volatileGate * tempWindow);` over all 6 gives 0, 0.0081, 0, 0, 0, 0 — **above 0.01 on 0 of 6**. All six carry a retained atmosphere (0.77–2.43 bar) and all six sit inside the water window (T_eq 261–318 K), so temperature and pressure are NOT the problem; their volatile fractions are 0.0276–0.0579 against src/worldengine/base/labCore.js:693 `const volatileGate = smoothstep(0.05, 0.2, volatileFraction);` whose dry floor is 0.05, which zeroes the product on 5 of 6 outright. Wiring the lab's sea driver into `rockySurface` tomorrow would still render all six dry. **R-06 is a world-generation item wearing a port-mapping costume.** The population is a property of the seed range, not of the code, so it is ruled in full at 6. ⚠ Scheduling reserved to Max as in R-05. |
| R-07 | rocky | pt8 | `bands` `lat` `swirl` `val` | carried | Venus's zonal banding, the same three-symbol mechanism G-04 rules `carried` on the gas `pt6` branch. **Newly live at Step 10a — 130 bodies, the largest of the three newly-live rocky branches.** ⛔ **`blocking`, not `accepted-loss`, and the difference is §2's test rather than a preference:** the lab material declares the whole `uBand*` family and `giantDeck` demonstrably writes it — so the carrier exists and the loss is an omission in the port's mapping. It is unwritten HERE because **TWO independent gates** each key on `=== 'gas'` and a venus-typed body's condition is not: the pack predicate at src/worldengine/drivers/index.js:115 `applies: (condition) => bandedEnvelopeOf(condition),` (⛔ RE-POINTED 2026-08-21 — that line READ `applies: (condition) => compositionClass(condition) === 'gas',` when this row was written, and B3 leg 2 is the commit that changed it; the historical text is preserved here rather than in a ref that no longer resolves) AND giantDeck's own internal src/worldengine/drivers/giantDeck.js:201 `const gas = compositionClass(condition) === 'gas';`, which feeds src/worldengine/drivers/giantDeck.js:225 `uBandStrength: scalar(banded ? 1.0 : 0.0, { gate: 'bands' }),` (⛔ RE-POINTED 2026-08-21 — it READ `scalar(gas ? 1.0 : 0.0, ...)` when this row was written; the `gas`→`banded` swap on that line IS gate 2's half of the closure). ⛔ **CORRECTED 2026-08-20** — this row read "only because `giantDeck`'s predicate", which named a repair that is a MEASURABLE NO-OP: widening the predicate alone still writes 0.0 at the second gate. So a body that draws bands today swaps to a material whose band deck sits at its 0.0 default. ⚠ Scheduling reserved to Max as in R-05; `blocking` is the conservative direction and claims no closure. ⭐⭐⭐ **RE-RULED `carried` AT B3 LEG 2, 2026-08-21, AND BOTH GATES MOVED BECAUSE ONE OF THEM ALONE IS A MEASURED NO-OP.** The fix is a single condition-derived predicate read at both sites: src/worldengine/drivers/giantDeck.js:89 `export function bandedEnvelopeOf(condition) {` — an h2-he envelope OR an opaque CO2 shroud — wired into the registry entry (gate 1) and into `uBandStrength`/`uJetStrength` (gate 2). ⛔ **A THIRD SITE HAD TO MOVE AND IT IS NOT IN THIS ROW'S OWN PROSE:** the pack's `if (!gas) return` early exit. Leaving it would have set `uBandStrength` to 1.0 with the E5 bake never run, so `aBand` stays zero-filled while src/worldengine/shaders/planetShaders.glsl.js:634 `        float bandMask = uBandStrength * provinceWeight(PROV_BANDS);` hands `mix(albedoCol, zonalBandCol(...), 1.0)` a flat field — the rocky albedo REPLACED by a degenerate deck. A regression wearing the closure's clothes; named here so nobody rediscovers it. ⭐ **THE POPULATION IS 130 AND IT IS THE SAME 130 THE LEGACY LABEL SELECTS, MEASURED AS A SET RATHER THAN AS A COUNT.** Over `lab-procedural-0…199`: `d.type === 'venus'` is true on 130 of 852 planets, `opaqueCO2ShroudOf(condition)` is true on 130, and the two sets are IDENTICAL — 130 in both, 0 in either alone — all of composition class `rocky`. After the change **130 of 130 carry `uBandStrength` > 0 and a real baked `aBand`**, and `applyDriverPacks` throws on none of the 852. ⚠ **THE COLLISION GUARD IS NOW LIVE RATHER THAN INERT** on that slice — the deck co-applies with `rockySurface` and `solidOptics` — and the three name sets are disjoint, asserted by name lookup in all three suites. ⚠ **WHAT IT COSTS, MEASURED AND NOT GLOSSED:** a body admitted through the shroud disjunct runs the same E5 chain a gas giant does, and `giantRegimeOf` answers `sub-neptune` on all 130 while `drawGiantConditions` overrides `surfaceGravity` by a factor of **5.22 … 20.06**. That is inside the spread the deck already applies to the population it claims today — **0.31 … 359.83, median 0.60 over 104 gas bodies** — so it is what this deck does to every body, not something new done to Venus. ⭐ **AND THE MAGNITUDES WERE NOT AUTHORED.** On the lab's own `Venus (sulfuric shroud)` preset the ONLY fields that move are the two master gates 0→1, the deck tint [0,0,0]→**[0.93, 0.87, 0.65]** (the preset's own CO2 atmosphere colour — the cream/yellow the legacy branch at src/objects/Planet.js:745 already names), `bandRough`, and the three baked attributes. `bandContrast` was **already 1** in the pre-pack fixture and is unchanged: the T_eq ramp saturates on a 737 K world and always did, behind a zero gate. Pinned field by field in tests/driver-pack-giantdeck.test.js instead of re-recording that fixture. ⛔ **WHETHER IT LOOKS RIGHT IS MAX'S EYES.** No pixel was inspected in this leg. |
| S-01 | both | shared | `diff1` `diff2` `diffuse` `shadow1` `shadow2` `starLight` `ambient` | carried | **CLOSED AT B4-2, 2026-08-21 — AND THIS ROW COULD NOT CLOSE BEFORE ITS THREE CONSTITUENTS DID, WHICH IS THE WHOLE POINT OF A ROLL-UP ROW.** It rolls up P-01 (star colour), P-02 (the second light) and P-03 (the shadow accumulation); P-01 and P-02 closed at B4-1 and P-03 closes above. ⭐ **THE ACCUMULATION IS NOW ONE STATEMENT IN THE LAB AND IT IS THE GAME'S OWN, TERM FOR TERM:** src/worldengine/shaders/planetShaders.glsl.js:491 `float shadow1 = totalShadow(vPos, uStarPos1);` opens it and the pair it feeds — a coloured `starLight` and a scalar `diff` — is src/objects/Planet.js:489-491 with the lab's spellings. ⛔ **`ambient` IS THE ONE SUBJECT ON THIS ROW THAT B4 DID NOT UNIFY, AND SAYING SO IS WHAT KEEPS `carried` HONEST.** The lab holds src/worldengine/shaders/planetShaders.glsl.js:492 `float ambient = 0.035;`, the game holds 0.035 with a 0.04 special case for hot jupiters at src/objects/Planet.js:494 `if (planetType == 6) {`, and src/objects/Moon.js:543 floors moon night with `max(starLight, vec3(0.025))` instead. **Three night floors, and B4 changed none of them** — the two shipped by the two front ends still differ on hot jupiters. That is a real residual on this row, it is not a defect introduced here, and it is recorded rather than absorbed into the one-word column. ⭐ **WHY THE SCALAR AND THE COLOUR ARE BOTH KEPT, which is the shape that stopped this row from spilling into the neighbouring block:** the game carries a `vec3 starLight` AND a `float diffuse` side by side, and the lab now does too. `diff` is read as a gate a dozen times below and as a magnitude inside the F34 limb's `(diff + 0.15)` — widening it to a vec3 would have silently re-coloured the limb and every night gate, which are another workstream's pixels. tests/lab-shader-shadows.test.js asserts it stays a `float`, and a severing mutant that widens it reddens two files. ⚠ **`carried` IS NOT `equal`** — the `ambient` residual above is the named gap, and like its three constituents this row reaches 0 pixels at today's `LAB_GAS_BODIES_DEFAULT = false`. |
| S-02 | both | shared | `cloudPos` `cloudMask` `cloudLight` `cloudSpeed` `cn` | accepted-loss | `[lab-has-it]` The shared cloud layer. Rolls up P-06. |
| S-03 | both | shared | `fresnel` `muTerm` `viewDir` `sunFacing` | accepted-loss | `[lab-has-it]` The rim/fresnel term and the terminator band. Rolls up P-07 and P-11. |
| S-04 | both | shared | `pattern` `n` `nOld` `mixFactor` `finalColor` `tt` `val` `shadingNormal` `perturbStrength` `terrainLandMask` | carried | Composition plumbing, not features: the pattern hand-off, the relief mix, the normal perturbation and the final write. The lab performs the equivalent composition in its own shader; there is no per-body quantity here that the other rows do not already carry. |
<!-- /LEDGER-CH2 -->

---

## 5. Named limits — what this ledger does NOT see

⛔ **Recorded with the construct that produced each one.** A false claim of closure is worse than an
open hole, because it tells the next reader not to look.

1. **The diff runs at material-creation time.** Any per-frame divergence is invisible to it. The
   consequence is already visible inside the data: `shadowMoonCount` reads 0 at creation on every
   body and lands in `lostAtZero`, when the live game writes it every frame. §3 rules both buckets
   for exactly this reason, but a future uniform that only ever differs *mid-flight* is not seen at
   all.
2. **`swapLedgerOf`'s CARRIED bucket is a NAME bucket.** Measured: 20 of 28 carried names diverge in
   value. This ledger closes that by ruling value, and the test pins the divergent set. ⚠ **The
   expensive pass runs over the 60-system prefix, not all 200** — building two `Planet`s per body
   costs a subdivision-5 icosahedron each. Measured, not assumed: the same pass over the full 200
   systems returns the identical bucket membership (37 `lost`, 13 `lostAtZero`), so the prefix is
   not lossy today. It could become lossy for a uniform that only ever takes a non-zero value on a
   body in systems 60–199, and that is the limit.
3. **Channel 2 extracts *declared locals*, not features.** A hardcoded effect written without a
   named local — `surfaceColor *= 1.0 - smoothstep(...)` inline — declares no symbol and is
   invisible to the extraction. The six the plan names all happen to be declarations, which is why
   the extraction reproduces them; that is a property of this shader's style, not a guarantee.
4. **Channel 2 covers `getSurfacePattern` and `main` only.** Locals inside the header's helper
   functions (`snoise`, `fbmd`, `craterEjectaCombiner`, the noise scaffolding) are excluded
   deliberately: over the whole fragment source the extraction returns 102 (gas) / 138 (rocky)
   symbols, almost all of them noise-math scratch (`a0` `b1` `gf` `x_`), and a ledger of those would
   be an unranked list of the kind Max's ruling was issued against. The limit is real: a feature
   implemented inside a helper is not ruled here.
5. **The `exotic` variant is unruled, and it is one field away from being live.** Zero of its 20
   branch blocks have a swapped body — but only because the **2** `crystal` bodies the predicate
   claims both lack `_systemSeed` and `_ordinal`, so Step 6d's provenance test refuses them. Scanned
   over 500 systems: 6 exotic-variant bodies claimed, **0** provenanced. If a `crystal`, `hex`,
   `fungal`, `machine` or `ecumenopolis` body ever carries provenance, its branch swaps with no
   ruling. The test derives its live-branch set from the population, so that day is a red build
   rather than a silent loss — and that is not a promise: the situation is **constructed and
   executed** in `⭐⭐ CONTROL THAT MOVED — a newly swappable branch DEMANDS rows`, which re-runs the
   census with the provenance refusal ignored, shows `exotic:13` entering the live set, shows no row
   exists for it, and counts the 9 symbols that would arrive unruled.
6. **Nothing here is a picture.** Every row is a uniform value or a GLSL symbol. Whether the swap
   *looks* acceptable is Instrument E's job and Max's call; this ledger only guarantees that no loss
   reaches him undeclared.

---

## 6. Two corrections the plan needs, which this lane may not make

⛔ Lane C's file set is `tests/material-parity-list.test.js` and this document. Both items below are
edits to `docs/FEATURES/one-pipeline-two-frontends-PLAN.md`, which the orchestrator owns.

1. **§12.4's committed seed for the `planetType == 7` eyeball branch does not swap.**
   `lab-procedural-20`'s eyeball is not claimed by `giantDeck`'s predicate, so at Step 6 it renders
   the legacy program in both frames and the branch's declared losses (G-06) go unwitnessed. The
   LOSS triptych for that branch needs a seed whose eyeball is *claimed*; the corpus contains 3 such
   bodies in 200 systems. ⚠ §12.4 also states the seeds were chosen so the rare branches are
   reachable — this one is reachable and not admissible, which is a different property and the one
   the shot needs.
2. **§6b's five-row loss table is scoped to the gas branch and the swap is not.** 114 of 341 swapped
   bodies render the `rocky` program today (§1). Rows R-01 through R-04 are that gap. The plan's
   "SHIPS GAS GIANTS" heading is accurate about intent and inaccurate about population.
3. ⭐ **DONE — RE-CHECKED AT HEAD 2026-08-20 AND THIS ITEM IS NOW A RECORD, NOT A REQUEST.** Both files are in the array (`step6-parity-ledger.md` and `tests/material-parity-list.test.js`), so every `line + symbol` citation written into this document is GATED, and the numbers below are superseded: the instrument is **green**, `npm run --silent port-uniform-delta:citations` exits 0 at **531 CHECKED**, 0 UNRESOLVED, 0 MALFORMED. ⛔ Line 251 below is left byte-identical on purpose — CARRIED.md's C24 cites it by line. The original text, kept because its reasoning is the record: **§11.3.4 wants every file a step edited inside `CITE_SOURCES`, and this step's two are not in
   it.** `tools/port-uniform-delta.mjs` is outside lane C's file set, so this is reported rather
   than done. The addition is `'docs/FEATURES/step6-parity-ledger.md'` and
   `'tests/material-parity-list.test.js'` appended to the array at
   tools/port-uniform-delta.mjs:1023 `const CITE_SOURCES = [`. ⚠ **Adding a source and GATING a
   source are different acts** (the comment already in that array says so). All 14 `line + symbol`
   citations in this lane's two files were verified against their target lines by hand before this
   was written — 14 hold, 0 broken — so the addition lands them in the checked column rather than
   the unchecked pile. ⛔ The instrument is **already red** at 27 broken citations, all in
   `PLAN.md` and `port-uniform-delta.mjs` and all pre-existing; none is this lane's.** ⚠ That last sentence is the one the re-check overturns — the 27 are repaired and the fence is green.

---

## 7. What is open for Max

⭐ **RE-DERIVED FROM THE ROWS 2026-08-20, BECAUSE THIS LIST WAS REMEMBERED AND TWO OF ITS ENTRIES HAD ALREADY MOVED.** Everything ruled **blocking** is an omission in the port's uniform mapping, not a missing
capability — ⭐⭐⭐ **SIX → THREE AT B3, 2026-08-21: the list is now P-11, P-12, P-13**, with **NONE in Channel M**. Leg 1 closed **P-05** (`solidOptics` writes the four aurora names for non-gas conditions) and leg 2 closed **P-14** (`craterDeck` gives the impact family a writer on the gas half — all ten names go from diverging on up to 343 of 343 gas planets to 0) and **R-07** (a condition-derived banding predicate at BOTH gates plus the pack's early return — 130 of 130 opaque-CO2 bodies now carry a live `uBandStrength` and a real bake). ⛔ **P-11 IS STILL HERE AND IT IS HALF-CLOSED, WHICH THE ONE-WORD COLUMN CANNOT SAY:** its non-gas terminator half closed at leg 1 and its GAS half (103 of 266) did not — `limbDeck` writes no `uTerm*` and `solidOptics`' predicate excludes gas. So what the three remaining rows share is narrower than what the six did: **P-12 and P-13 are the GAS half of the palette and the offsets, and P-11 is the gas half of the terminator.** Every one of them now needs the same thing — a gas-side writer — which is the shape `craterDeck` just took for P-14. The historical list read: **P-05, P-11, P-12, P-13, P-14, R-07** in the planet channels — ⭐ **eight → SIX at B2 leg 3, 2026-08-20.** ⛔ Five corrections against the list this paragraph used to carry: **P-04 LEFT** (closed at the limbDeck/polarDeck registration, ruled `carried`, with an inverted assertion holding it closed); **P-08 LEFT** (re-ruled `accepted-loss` 2026-08-19 — no counterpart name exists, so it fails §2's second conjunct); and **R-07 was never in it**, though it has been `blocking` since Step 10a and is the largest of the three newly-live rocky branches at 130 bodies. ⭐ They no longer share one cause, and saying they do was the error that let the list rot: **P-12, P-13 and P-14 are now the GAS half only** — `rockySurface` closed their solid half on 163 of 163 bodies (measured 2026-08-20) and `giantDeck` is the missing writer — while **P-11** and **R-07** need a pack that does not exist yet. ⭐⭐ **AND P-10 AND M-09 HAVE NOW LEFT TOO, WHICH IS THE FOURTH AND FIFTH CORRECTION:** Max's scheduled base-frequency work landed at B2 leg 3 and both rows are ruled `carried` — P-10 on its SOLID half only, with the gas half declared at P-15 as `accepted-loss` on the same-spelling-two-quantities ground, and M-09 outright on all 632 plain moons. What the six still share is that the game's own material holds the value, derived, at the moment the lab material is built. The old sentence read: they share one cause, the pack writes
15 uniforms and the game's own material already holds the other values, derived, at the moment the
lab material is built. Closing them is a mapping pass in the lane that owns
`src/rendering/LabPlanetMaterial.js` and `src/objects/Planet.js`, not a shader change.

Everything ruled **accepted-loss** is declared, here, on this step, which is what Max's ruling
requires — but the declaration is not the decision. The ones with a population large enough to
notice are **P-01** (star colour, **846/846**), **P-03** (transit shadows, **420 bodies / 663 moons**),
**P-02** (second star, **269** bodies), **R-01** (the dust mantle, **181** bodies), **R-02** (ice cracking, **131** bodies), **R-04** (carbon facets, **104** bodies) and **G-02** (the great spot,
53 gas giants). ⭐ The four rocky rows are the shape of the whole Step-10a move: three of them were single- or double-digit populations when this paragraph was written and none of them was in it. ⛔ Ranked by population these now OUTWEIGH every gas-branch loss except the great spot, and §2's Rule-2 gate says why that matters — **all four are `[lab-has-it]`**, so each is a re-parameterisation against a mechanism the lab already has rather than a shader change, which is a different kind of ask than P-01/P-02/P-03's.

---

## 8. The controls — every claim above has one that MOVED

§11.3.3 is the clause with no cheap exit: a zero with no control that moved is not evidence of
anything. Each row below is executed, not described. Six live inside
`tests/material-parity-list.test.js`; four were run against the shipped document and are recorded
here because a document mutation cannot live in the file it mutates.

| claim | the control | it moved |
|---|---|---|
| 341 bodies swap | re-run the census with Step 6d's provenance refusal ignored | **343** vs 341, and the two extra are `crystal` — the unruled `exotic` variant |
| `CARRIED: 28` is not 28 surviving features | feed the identical material pair to `swapLedgerOf` and to a value comparison | carried **28**, value-diverged **20** — two answers, one input |
| `lostAtZero` is a real per-body value split | re-run the whole pass with `starInfo = null`, which is what a headless probe reaches for | `starColor2`/`starBrightness2` collapse to `lostAtZero` on every body; with the real `starInfo` they split **125 / 216**, and 125 is exactly the binary population |
| a new hardcoded effect is caught | inject `float wdNewEffect = …` into a copy of the shipped gas source, in the branch the plan's own six live in | the extractor returns it and the partition rejects it |
| the extractor is brace-matched | a branch containing a nested block, read both ways | the naive "up to the next `}`" reader loses the declaration after the nested block |
| a newly swappable branch demands rows | the provenance-ignoring census, re-run against the live-branch check | `exotic:13` enters the live set, **0** rows exist for it, **9** symbols would arrive unruled |
| `lodLevel` is read by no shader | count `lodLevel` in `Planet.js`, then count it again in a copy with one read added | **2** vs **3** |
| the doc's rows are load-bearing | delete `` `uLimbMix` `` from row P-04, run | 3 assertions red, incl. the partition |
| the rulings vocabulary is load-bearing | change P-01's ruling to `fine`, run | 2 assertions red |
| a live branch cannot go unruled | delete row R-02 (`rocky pt2`, 66 bodies), run | `every LIVE branch has rows` red |
| a dead branch cannot be ruled | add a row for `rocky pt3` (lava, 0 swapped bodies), run | `no row rules a branch that has no swapped body` red |

**Instrument status at the end of this lane.** C zero delta (55 uniforms × 526 bodies). B green.
A: one newly-red, `driver-pack-giantdeck 5e`, which is the metallicity lane's and predates this
work; this lane's file enters as NEW with 27 tests and reds nothing. Citations: 27 broken,
pre-existing, none in this lane's files (§6 item 3).

⛔ **What no part of this lane can establish.** Whether the swap LOOKS acceptable. Every row here is
a uniform value or a GLSL symbol; not one pixel was rendered, because this lane may not start a dev
server or drive a browser. Instrument E's DRIVE pair and the per-branch LOSS triptych are the
missing half, and the ledger's job was to make sure that when those shots are taken, **the caption
names losses the machine printed** rather than losses a document remembered.

---

## 9. Channel M — the MOON class (PLAN §4 Step 10)

**Why a Step-10 channel lives in a Step-6b document.** Because the machinery is the same and a
second document would be a second version of it. PLAN §4 Step 10's **What** paragraph ends *"Produce
a moon parity list on the Step-6 pattern"*, its **Gate** bullet reads *"Moon parity-list test
green"*, and risk 4's mitigation names Steps **6b and 10** by number. Appending here rather than
forking also keeps §2's three rulings, and the "an accepted loss is allowed; an UNDECLARED loss is
blocking" ruling that produced them, as one vocabulary rather than two.

⛔ **Channel 1 has never contained a moon.** `tests/material-parity-list.test.js` walks
`sys.planets` and builds `new Planet(...)`; a PLAIN moon is a `Moon`, so it is outside that
population by construction, and P-01…P-18's evidence cells each state their own population
explicitly (`341 of 341`, `125 of 341`, `228 of 341`) — none of which is a moon count. So the moon
losses are not "already ruled one class over"; they were undeclared until this channel, and
silently so: the four frame-loop writers that would otherwise have thrown were converted to guards
at src/main.js:11486 `if (mu?.shadowPlanetPos) mu.shadowPlanetPos.value.copy(entry.planet.mesh.position);`
and src/objects/Moon.js:650 `if (mu?.time) mu.time.value += renderDt;`, so every one of these losses
is a silent no-op rather than a crash.

**How it was produced.** Channel 1's derivation, one class over and by the same decision core: BOTH
materials built on the SAME moon record — `setLabGasBodiesOverride(false)` then `(true)` through
`new Moon(...)` with identical `lightDir`, `lightDir2` and `starInfo` — and the pair fed to
`swapLedgerOf`. `tests/moon-lab-mount.test.js` carries the same row table this section does and
**fails when the two disagree**, so this is machine-checked rather than transcribed. Corpus is the
one at the top of this file; nothing here was measured on Sol, which is structurally refused.

### 9.1 The moon populations, measured

| | measured on `lab-procedural-0…199` |
|---|---|
| plain moons — the class Step 10 ships | **632** |
| …that admit with the 6e flag forced ON | **632 of 632 (100%)** — the pack registered at index.js:182 `ROCKY_SURFACE_ENTRY,` applies on `compositionClass(condition) !== 'gas'`, the complement of `giantDeck`'s predicate at §1, and it claims every one; measured types `captured` 139 · `ice` 219 · `volcanic` 67 · `rocky` 207, and **no `gas` moon exists** |
| swapped moons in a binary system | **197** of 632, in **64** binary systems of 200 |
| swapped moons under a **non-white** primary | **632** of 632 — as at P-01, there is no body on which the star-colour loss is invisible |
| swapped moons carrying clouds / atmosphere / aurora | **0 / 0 / 0.** One gate produces all three: src/generation/MoonGenerator.js:211 `clouds: type === 'terrestrial' ? {` and the two literals under it, and **the corpus contains no terrestrial plain moon** |
| uniforms the legacy `Moon.js` material declares | **30** (29 before B2P — RE-MEASURED 2026-08-20: `Object.keys` on the built legacy moon material returns 30 on every one of the 632 swapped plain moons over `lab-procedural-0…199`, and the `uniforms:` literal at src/objects/Moon.js:71 `baseColor: { value: new THREE.Vector3(...d.baseColor) }, uPosterizeLevels: POSTERIZE_QUANTUM,` went 29 → 30 vs bed3235, the added key being `uPosterizeLevels`. B2P moved every planet-channel count — 71→72, 43→44, 63→64 — and left this one behind) |
| uniforms the lab material declares | **356** |
| …of the 30 in `swapLedgerOf`'s CARRIED bucket | **0** (the 0 is unchanged and re-measured: the lab spells the quantity `uLevels`, so the new name is LOST, not carried) |

⭐ **CARRIED IS EMPTY, and that is the structural difference from Channel 1.** There, 28 names sat
on both materials and §0's whole finding was that a name bucket reads as 28 surviving features and
is 8 at most. Here the two materials share **no name at all** — every legacy moon uniform is
lowercase and every lab uniform is `u`-prefixed — so `lost ∪ lostAtZero` is the complete 30 (RE-MEASURED 2026-08-20 — union size 30 at 20 seeds, at 60 and at all 200, with `carried` empty at every one; it was 29 before B2P) and
there is no name-carried/value-diverged class to mis-read. The FOUR features that DO survive
(M-11) survive by a **rename plus a named mechanism**, and each is asserted live — the first three in
`tests/moon-lab-mount.test.js`, and `uPosterizeLevels` (B2P, 2026-08-20) in `tests/posterize-levels-wiring.test.js` — rather than inferred from a name. ⚠ **THAT SECOND FILE WAS UNTRACKED WHEN THIS SENTENCE WAS FIRST WRITTEN**, so the assertion existed only in a working tree and shipped to no clone; it is `git add`ed now (`git ls-files --error-unmatch` resolves it) and runs 36 TESTS green (tests, not assertions — the quantity-swap this audit exists for). Naming a test is not the same act as shipping one.

⚠ **`lostAtZero` is not "lost but it was off anyway"** — the same caveat §3 records, and on moons it
has two clean controls. `starColor2`/`starBrightness2` split **197 lost / 435 lostAtZero**, and 197
is exactly the binary population. `moonType` splits **493 lost / 139 lostAtZero**, and 139 is
exactly the `captured` population, whose type index is 0. Both buckets are ruled below.

### 9.2 The rows

<!-- LEDGER-MOON -->
| id | uniforms | ruling | evidence |
|---|---|---|---|
| M-01 | `starColor1` `starBrightness1` | accepted-loss | P-01's mechanism on the moon population. **No lab uniform name contains `star` or `Star`** — measured over all 356. The legacy moon shader spends both in one expression, src/objects/Moon.js:541 `vec3 starLight = starColor1 * diff1 * starBrightness1 * shadow1`, so a swapped moon is lit implicit white. **632 of 632** sit under a non-white primary, so as at P-01 there is no body on which the loss is invisible: a red dwarf's moon and a B-star's moon become indistinguishable. Closing it is a shader change, not a mapping line. |
| M-02 | `lightDir2` `starColor2` `starBrightness2` | accepted-loss | P-02's mechanism on the moon population. `uLightDir2` occurs nowhere in `LAB_SHADER_CORPUS`; the lab has one light. **197 of 632** sit in a binary and lose the second star's diffuse term outright — src/objects/Moon.js:513 `float diff2 = max(dot(shadingNormal, lightDir2), 0.0);` and three further sites. `starColor2`/`starBrightness2` land in `lost` on exactly those 197 and in `lostAtZero` on the other 435; **the split IS the binary population**, which is the control that the bucket assignment is reading real values rather than defaults. |
| M-03 | `shadowPlanetPos` `shadowPlanetRadius` `starPos1` `starPos2` | accepted-loss | P-03's mechanism on the moon population, and the one loss whose *writers* had to be neutered for Step 10 to ship at all. **No lab uniform name contains `hadow`.** The parent planet eclipsing its own moon is src/objects/Moon.js:536 `shadow1 = sphereShadow(vWorldPos, starPos1, shadowPlanetPos, shadowPlanetRadius);` and it stops on **632 of 632**. All four writers are guarded — src/main.js:11487 `if (mu?.shadowPlanetRadius) mu.shadowPlanetRadius.value = entry.planet.data.radius;`, src/main.js:11488 `if (mu?.starPos1) mu.starPos1.value.copy(_star1Pos);`, src/main.js:11489 `if (mu?.starPos2) mu.starPos2.value.copy(_star2Pos);` and the `shadowPlanetPos` sibling above them — so the loss throws nothing and reports nothing. ⚠ This is the row a transit UAT will find first: a swapped moon no longer darkens when its parent passes in front of the star. |
| M-04 | `moonType` | accepted-loss | ⛔ **MOON-NATIVE — no planet row covers it and none can, because the quantity does not exist on the planet material.** P-16 rules `planetType` `carried` on the ground that the branches it selects are ruled loss-by-loss in Channel 2; **there is no Channel 2 for moons**, so the same ruling here would claim a coverage that does not exist. `moonType` is not only a branch selector: it drives the per-type surface pattern, the per-type `perturbStrength` and the per-type diffuse ramp across roughly forty lines of src/objects/Moon.js:133 `uniform int moonType;`. Neither `moonType` nor `uMoonType` occurs anywhere in the lab corpus — the lab draws from the CONDITION, not from a type label, which is the port's thesis — so §2's `blocking` test (the lab already declares the carrier) is not met and this is replacement rather than a mapping line. Population **632 of 632**, split 493 `lost` / 139 `lostAtZero` on the `captured` index-0 population. |
| M-05 | `hasClouds` `cloudColor` `cloudDensity` `cloudScale` | accepted-loss | P-06's mechanism on the moon population: the lab has a cloud system (`uCloudCoverage` `uCloudRegime` `uCloudRelief` `uCloudOpticsIntensity`) and **no cloud-colour uniform**, and `rockySurface` writes none of the four. ⚠ **MEASURED POPULATION: 0 of 632.** Clouds are terrestrial-only and the corpus carries no terrestrial plain moon, so this loss has no witness here — it is DECLARED rather than discovered, which is the point of the row. Across 2000 seeds a cloudy plain moon runs 8 in 6295 (0.13%). |
| M-06 | `hasAtmosphere` `atmosphereColor` `atmosphereStrength` | accepted-loss | P-07's mechanism on the moon population: the legacy rim glow, superseded in intent by the lab's limb optics. **No lab uniform name contains `Atmos`.** ⚠ **MEASURED POPULATION: 0 of 632**, same terrestrial-only gate as M-05. |
| M-07 | `hasAurora` `auroraColor` `auroraIntensity` `auroraRingLat` `auroraRingWidth` | accepted-loss | P-05's subject family, one class over, and the ruling differs from P-05's **because the population does**. The lab DOES declare the carriers (`uAuroraColor` `uAuroraIntensity` `uAuroraRingLat` `uAuroraRingWidth`, all unwritten by `rockySurface`), which is why P-05 is `blocking` at 20 of 341 live planets. ⚠ **MEASURED POPULATION HERE: 0 of 632** — same terrestrial-only gate as M-05 — so §2's `blocking` first conjunct ("the feature stops reaching the pixel") is not satisfied on any moon, and ruling it `blocking` would invent an obligation over an empty set. ⛔ **If `MoonGenerator` ever gives a non-terrestrial moon an aurora, §2's test moves this row to `blocking` on the same day**; that is a consequence of the test, not a fourth verdict, and it is recorded here for the same reason P-10 records its scheduling in evidence. |
| M-08 | `baseColor` `accentColor` | accepted-loss | P-08's mechanism on the moon population, and P-08's re-ruling applies verbatim: these are palette endmembers for layers the lab parameterises separately, and **no counterpart name exists** — src/worldengine/shaders/uniforms.js:138 `uWeatheredColor:    { value: new THREE.Color(0.46, 0.40, 0.34) },` carries the standing instruction not to reintroduce `uBaseColor`, because the game's record field is a different quantity. §2's `blocking` test therefore fails and this belongs with M-05. Population **632 of 632**. |
| M-09 | `noiseScale` | carried | P-10's row on the moon population, and it closes with P-10's solid half at **B2 leg 3, 2026-08-20**. ⭐ **THE MECHANISM, AND IT IS ONE LINE.** A plain moon builds its surface through src/objects/Moon.js:58 `Planet._createLabSurface(geometry, d, conditionFromBody(d), lightDir, lightDir2, starInfo)`, so it runs the SAME driver-pack path a planet does; `rockySurface`'s `!== 'gas'` predicate claims **632 of 632** plain moons — ⭐ and at B3 it is joined on that same predicate by `solidOptics` and `solidFeatures`, so a plain moon now runs THREE packs, not one — tests/moon-lab-mount.test.js:100 `    expect(wd.lab.packsApplied).toEqual(['rockySurface', 'solidOptics', 'solidFeatures']);` — and ONE ungated driver, src/worldengine/drivers/rockySurface.js:268 `    uNoiseScale: sizeKm(macroWavelengthKm(condition), C_MACRO),`, writes it on every one of them. **MEASURED over `lab-procedural-0…199`'s 632 plain moons: 632 distinct values at raw float64 (557 at 9 significant figures, 521 at float32 — the convention is named because a raw count splits one physical value across ULP-adjacent doubles) across 79 distinct 5 % bins, 2.8736 … 245.175, and 0 of 632 still answer the factory 4.0** — against exactly ONE shared value on the LAB material before the leg. ⛔⛔ **THAT ONE SHARED VALUE IS THE LAB'S FACTORY 4.0 AND NOT WHAT A MOON RENDERS TODAY**, and this evidence is the POST-SWAP reading by construction: the mount proof at tests/moon-lab-mount.test.js:100 `    expect(wd.lab.packsApplied).toEqual(['rockySurface', 'solidOptics', 'solidFeatures']);` runs under `setLabGasBodiesOverride(true)`, ⭐ **TRUE UNTIL B7, 2026-08-21 — CORRECTED, NOT DELETED:** this row read *"at the shipped default … 0 of 632 plain moons are ADMITTED … a RE-CALIBRATION onto a physical wavelength, not a differentiation win, and invisible until the flip"* — against what the flag then read `export const LAB_GAS_BODIES_DEFAULT = false;`, with every moon still rendering src/objects/Moon.js:73 `        noiseScale: { value: d.noiseScale },` (MEASURED **632 distinct values, 4.8319 … 510.6324**). **B7 flipped the flag ON** — src/objects/Planet.js:2158 `export const LAB_GAS_BODIES_DEFAULT = true;` — so, per that same line's own count, all 632 plain moons are now ADMITTED and the trade is live: 632 distinct raw `noiseScale` values fall to 557 at 9 significant figures. Still a RE-CALIBRATION, not a differentiation win — but no longer an invisible one. ⛔ The pre-leg measurement is kept as the thing CORRECTED rather than deleted: *"`uNoiseScale` sits at its factory default `4.0` on every swapped moon while the game draws a real per-moon value — measured range 4.83 … 510.6 over the 632, with 0 of 632 equal to 4.0, and the scene record rescales it further."* ⭐ **VERIFIED BY INVERSION, P-04's mechanism taken verbatim:** tests/material-parity-list.test.js:789 `toContain('uNoiseScale')` asserts that `LEDGER.written` CONTAINS the name, so un-registering the pack turns this row RED instead of letting the feature leave silently. ⚠ **`carried` IS NOT `equal`.** §2's test is whether the feature reaches the pixel, not whether the two front-ends agree: the game's per-moon value is a rescaled random draw and the lab's is derived from a size in km, so they answer different questions by construction and the value still diverges. ⚠ `noiseDetail` has no moon-side equivalent — the legacy moon material never declared one — so P-10's uncovered half does not recur here. ⚠ The moon population is LIVE under an open formation window (`34b502d`), so every count in this cell is a floor. |
| M-10 | `lodLevel` | accepted-loss | P-17's row, on the moon population: read by no shader on either side, and the writer already announces its own no-op at src/rendering/objects/BodyRenderer.js:212 `if (!surface?.material?.uniforms?.lodLevel) return this._noteLabSkip('lodLevel', surface);`. This is the **one** loss in the whole channel that is WITNESSED at runtime rather than merely declared — `labSkips.lodLevel` counts it, and `tests/moon-lab-mount.test.js` asserts the counter moves on a swapped moon. ⚠ The other ten rows have no such counter: `_noteLabSkip` has exactly two call sites in the whole tree, `lodLevel` and `baseColor`. |
| M-11 | `lightDir` `moonRadius` `time` `uPosterizeLevels` | carried | P-18's three, moon-side, each by a rename plus a named mechanism and each asserted LIVE rather than by name — which matters more here than at P-18 because the CARRIED bucket is empty, so a name argument was never available. `lightDir`→`uLightDir` and `moonRadius`→`uBodyRadius` are written by `buildLabPlanetMaterial`; `time`→`uTime` by the per-frame seam `updateLabPlanetMaterial`, which **Step 10 had to add to `Moon.updateRender` for this row to be true at all** — before it the clock stayed at 0 forever on every swapped moon. ⛔ `uLightDir` is OBJECT-space where the legacy `lightDir` was world-space and bound by reference, so "carried" here means re-derived every render tick, not shared. All three are fenced in `tests/moon-lab-mount.test.js` (uTime advances, uLightDir changes under rotation, uBodyRadius equals the geometry's radius) with a committed mutant on the seam. ⭐ **`uPosterizeLevels` joined at B2P, 2026-08-20, by P-18's mechanism verbatim.** The legacy plain-moon program spends it at its own `edgeWidth` 0.6 rather than the planet's 0.4, and the lab material a moon swaps to holds the same QUANTITY under the lab spelling `uLevels` — but ⛔ **since the B2P arithmetic fix this is a SECOND object, not the same one.** The SIX game fragment programs — gas, rocky, exotic, ring, moon, belt — declare `uniform vec2 uPosterizeLevels` and hold `POSTERIZE_QUANTUM` (levels, 1/levels), because the reciprocal must be CPU-carried. ⚠ **FOUR IS A DIFFERENT QUANTITY: the number of DECLARATION SITES** (FRAG_HEADER, the ring, the moon, the belt), because `PLANET_SHADER_VARIANTS` splices one shared header onto three bodies; four is ALSO the posterize() SOURCE-COPY count, and THREE is the file count. This cell said four PROGRAMS until 2026-08-20. the lab's `uniform float uLevels` is a scalar and cannot hold it, so the lab keeps `POSTERIZE_LEVELS`. `setPosterizeLevels` is the ONLY writer of either, so the carry is by single-writer construction, not by object identity. ⛔ **THIS CELL PREVIOUSLY CLAIMED IT WAS "asserted live by the same pass that measures the loss". THAT WAS FALSE WHEN WRITTEN AND IS CORRECTED HERE.** `tests/moon-lab-mount.test.js` never mentioned either spelling when this was written (`grep -c` returned 0; it returns 1 today only because this pass's own correction added the word), and `tests/material-parity-list.test.js` compares uniform NAMES and VALUES — it cannot observe object identity at all, which is the entire property this row rests on. The claim is now TRUE and carried by a TRACKED test written for it — ⚠ **and it was false a second time in between, because that file was still untracked when this retraction was first written**: naming a test proves nothing until `git ls-files` resolves it, which it now does. tests/posterize-levels-wiring.test.js:77 `expect(mat.uniforms[key] === want,` asserts identity per material path against the object that slot must hold, the moon and the lab material among them, and the row above it — tests/posterize-levels-wiring.test.js:67 `'lab material (world engine, uLevels)':` — is the lab slot by name. Both were proved by severing the wiring at each site and watching that assertion, and only that assertion, redden. |
<!-- /LEDGER-MOON -->

### 9.3 Named limits — what Channel M does not see

⛔ Recorded with the construct that produced each one, on §5's pattern.

1. **§5's limits 1-4 all recur unchanged.** The diff still runs at material-creation time, so a
   per-frame divergence is invisible; a feature implemented inside a GLSL helper or inline with no
   named local is still unclaimable.
2. **THERE IS NO CHANNEL-2 EQUIVALENT FOR MOONS.** Channel 2 brace-matches `planetType == N`
   branches out of the shipped planet shader and demands a row per symbol. The moon shader
   branches on `moonType` in a different shape and no extractor was written for it, so a new
   hardcoded effect in `Moon.js`'s GLSL is **not** caught the way a new one in `Planet.js`'s is.
   M-04 rules the switch and the per-type look together, which is coarser than Channel 2's
   per-symbol partition and is stated as coarser rather than presented as equivalent.
3. **The pass builds from the RAW generator record, not the scene record.** `src/main.js` rescales
   `radius`, `noiseScale` and `clouds` when it builds a moon's scene record, so the *values* here
   are one transform away from the shipped body. That does not move any name into or out of a
   bucket — which is what this channel rules — but no value quoted here should be read as the
   number a frame contains.
4. **Ten of the eleven rows are declared, not witnessed.** Only M-10 has a runtime counter. Nothing
   in the tree reports at run time that a swapped moon stopped drawing its eclipse or its second
   star; those losses are visible only in this document and in the test that re-derives it.
