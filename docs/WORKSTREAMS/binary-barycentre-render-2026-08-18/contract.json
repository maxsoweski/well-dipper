{
  "workstream": "binary-barycentre-render-2026-08-18",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "verified",
  "summary": "Draw the primary at the true barycentre of itself and all its moons instead of pinning it to its orbital point, and draw the orbit rings in the barycentric frame where one moon dominates. Pure renderer change in src/main.js: q is read from records that already exist as of B5.0 (34b502d), so no generation output moves by a single bit. Max ruled the term applies to EVERY moon with no mass-ratio cutoff, which means it also corrects 16 existing moon/parent pairs whose barycentre already sits outside the primary and visibly moves 111 more. Fixes the UAT failure recorded in docs/FEATURES/binary-planets-scoping-2026-08-17.md §9.",
  "acceptanceCriteria": [
    {
      "id": "AC-WOBBLE",
      "statement": "The primary is drawn at the barycentre of itself and ALL of its moons, not at its orbital point. Max's ruling: every moon contributes, with no mass-ratio cutoff and no flag on the record. On wd-10 planet 3 the primary must circle a point 5.53 of its own radii away instead of sitting still.",
      "verifyVia": {
        "input": "⛔ AMENDED 2026-08-18: `grep -c '^export' src/main.js` = 0 and the module evaluates `new THREE.PerspectiveCamera(...)` / `document.getElementById` at top level, so the per-frame sim step CANNOT be imported and the original 'advance the celestial sim headlessly' input was not achievable at this HEAD. The honest route, both under vitest with no GL context: (a) instantiate the shipped Moon / OrbitLine / PlanetMoonBody classes directly and drive updateSim — this reaches the REAL placement formula (precedent: tests/gas-body-lab-material.test.js:678); and (b) sentinel-fence the offset arithmetic in main.js and compile the slice, the Instrument-D pattern at main.js:4215 / tests/instrument-d-frame-survival.test.js:47, so the assertion runs over SHIPPED BYTES. Advance the celestial sim headlessly over a full companion orbit for wd-10 planet 3 and for a sample spanning FENCE-221, reading the planet mesh position P, the orbital point B = (cos(orbitAngle)*orbitRadius, 0, sin(orbitAngle)*orbitRadius), and every moon mesh position.",
        "observable": "P - B equals -(1/M_total) * sum_i m_i * (moon_i - P) to float tolerance at every sampled frame, where m_i is the moon's mass by the SAME rule GravityField._estimateMoonMass uses (planetData.massEarth when present) and M_total = M_planet + sum m_i. For wd-10 planet 3, |P - B| / radiusScene = 5.53 and is constant over the orbit. The y component is included, not dropped: a moon with non-zero inclination moves the primary off the y=0 plane."
      },
      "layer": "unit"
    },
    {
      "id": "AC-SEPARATION",
      "statement": "The pair stays exactly as far apart as it is generated, and the RING radii are the barycentric elements. ⛔ AMENDED 2026-08-18 after discovery: the drawn body separation CANNOT fail from this change — it is `r` by construction in both moon branches (Moon.js:591 -> :599, main.js:11244 -> :11249-11251) and neither formula is touched. The real Convention-B exposure is the RINGS: r2 = a/(1+q) is CORRECT for the companion's ring and FATAL for the body separation, and no test in the battery measures either. This AC now pins both halves so the trap cannot be satisfied by accident.",
      "verifyVia": {
        "input": "Instantiate the shipped Moon / PlanetMoonBody classes directly under vitest, advance them against a moved parent position, and separately read the constructed ring radii for a dominated planet.",
        "observable": "|moon mesh - planet mesh| == that moon's orbitRadiusScene for every moon including the companion — on wd-10 planet 3 that is 1.15200 scene units (25.1 primary radii), NOT 0.898. AND the two barycentric rings measure r1 = 5.53 / r2 = 19.55 primary radii, NOT 25.1 and not 5.53/25.1."
      },
      "layer": "unit"
    },
    {
      "id": "AC-NO-BODY-OFF-ITS-LINE",
      "statement": "No body is drawn off its own orbit line. This is the trap named in scoping §9: main.js:11265-11271 sources moon ring positions from the recomputed px/pz rather than the planet mesh, so offsetting the primary detaches every ring belonging to its other moons and parks them at the empty point. It must be fixed in the same change, and the fix must hold for BOTH ring branches.",
      "verifyVia": {
        "input": "⛔ AMENDED 2026-08-18: `grep -c '^export' src/main.js` = 0 and the module evaluates `new THREE.PerspectiveCamera(...)` / `document.getElementById` at top level, so the per-frame sim step CANNOT be imported and the original 'advance the celestial sim headlessly' input was not achievable at this HEAD. The honest route, both under vitest with no GL context: (a) instantiate the shipped Moon / OrbitLine / PlanetMoonBody classes directly and drive updateSim — this reaches the REAL placement formula (precedent: tests/gas-body-lab-material.test.js:678); and (b) sentinel-fence the offset arithmetic in main.js and compile the slice, the Instrument-D pattern at main.js:4215 / tests/instrument-d-frame-survival.test.js:47, so the assertion runs over SHIPPED BYTES. Over the headless run, for every moon of every sampled planet compute the distance from the moon mesh to the centre of its own orbit ring, and compare against that ring's radius.",
        "observable": "The two agree to float tolerance at every frame, for every moon, in both ring branches. Zero moons detached. ⛔ AMENDED: the TDD revert-proof must be defined against the ring WRITE (main.js:11265-11271), not against fenced arithmetic — reverting the write restores `px - _worldOriginVec.x` outside any fence, so a fenced-bytes test would stay green and prove nothing."
      },
      "layer": "unit"
    },
    {
      "id": "AC-PAIR-RINGS",
      "statement": "Max's words: two orbit lines around a shared empty gravitational center, not one line centred on the primary. Where one moon dominates the barycentric excursion (measured: all 27 companions are at share >= 0.99), the primary's ring and that moon's ring are both centred on the barycentre, at radii r1 = a*q/(1+q) and r2 = a/(1+q). Where several moons are comparable — 16 of the 44 visibly-wobbling planets, worst wd-133/4 at six moons and top share 0.626 — rings stay centred on the planet mesh and follow it, because a barycentric circle is not that body's path and drawing one would put bodies off their lines (see AC-NO-BODY-OFF-ITS-LINE). ⛔ CORRECTED 2026-08-18: the scoping doc's 'a new ring array is silently never drawn (CONIC_MAX = 64)' is GARBLED and this contract repeated it. OrbitConicField.updateFromSystem reads exactly three hardcoded property names (:524 starOrbitLines, :526 orbitLines, :531 moonOrbitLines), so a NEW array is invisible at ring count 3, not 65. CONIC_MAX is a separate, SYSTEM-WIDE truncation (:589) that is nowhere near: measured over FENCE-221 the worst system goes 25 -> 27 rings against a cap of 64. Push the extra ring onto moonOrbitLines and do not touch OrbitConicField.js (docs/PARKING_LOT.md:240 reserves it for a pending merge arc).",
      "verifyVia": {
        "input": "Headless: enumerate the ring set for wd-10 planet 3 and for wd-133 planet 4. Live: park on wd-10 planet 3 with orbits visible and the camera sunward, and drive a full companion orbit.",
        "observable": "wd-10 planet 3 carries exactly TWO rings, both centred on the same point, that point is empty (no mesh within r1 of it), radii are 5.53 and 19.55 primary radii, and both rings are STATIC while both bodies move along them. wd-133 planet 4 carries six planet-centred rings that translate with the wobbling primary and no barycentric ring. On screen at wd-10: two concentric circles about an empty point with a body on each."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-LIGHTING-AND-MOONS-FOLLOW",
      "statement": "Everything downstream of the planet write follows the moved primary and stays consistent: lighting, plain moons, planet-class moons, and the companion itself. The companion needs no change at all — placing the primary at bary - r1*u and the companion at primary + a*u lands it at bary + r2*u exactly, which the existing code at main.js:11247-11252 already does.",
      "verifyVia": {
        "input": "Headless over the sampled run plus a live sunward look at wd-10 planet 3 in a binary-star system; compare _lightDir/_lightDir2 and every moon position against the planet's mesh position.",
        "observable": "_lightDir is computed from the moved mesh position (not from px/pz), so the terminator stays correct in binary-star systems after a world-origin rebase — the WU5 property. Every moon's position is parent-mesh-relative. Nothing is computed from px/pz except the barycentre itself."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-ZERO-GENERATION-DRIFT",
      "statement": "This is a pure renderer change and must move no generation output by a single bit. The lane's instruments are RED BY DESIGN mid-window (handoff §3 lists every deliberate literal; B7 re-blesses), so the gate is not 'green' — it is 'bit-identical to the same command at the parent commit'.",
      "verifyVia": {
        "input": "Run tools/moon-census.mjs, tools/binary-yield-probe.mjs and the full npm test suite at the build commit and at its parent, and diff the outputs.",
        "observable": "Byte-identical census and probe output. The test suite's failing-file set and per-file assertion messages are identical to the parent's — no test moves from red to green, none from green to red, and no failure message changes a number. A newly-red generation test is a FAIL of this AC, not a re-bless candidate."
      },
      "layer": "unit"
    },
    {
      "id": "AC-FLIGHT-STILL-WORKS",
      "statement": "Flying there still works. Gravity, SOI, collision, supercruise, targeting and autopilot all chase live meshes, so they should follow the moved primary for free — this AC exists to prove it rather than assume it, since the primary now leaves the y=0 plane for the first time.",
      "verifyVia": {
        "input": "Live: at wd-10 planet 3, approach the pair, target each body, and let the autopilot run to each; check dominantBodyAt at 0.1*a from the companion and near the primary.",
        "observable": "Both bodies are targetable and reachable, the autopilot arrives at the body rather than at the empty barycentre, dominantBodyAt returns the companion near the companion, and no collision or SOI discontinuity fires at the empty point. No console errors."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-UAT",
      "statement": "Max's gate, two subjects. (1) The pair: does wd-10 planet 3 read as a binary planet — both bodies orbiting a shared empty gravitational center — rather than as a planet with a big moon? (2) The blast radius: the same term now moves 111 planets Max has already flown to, 44 of them by more than a full planet radius. Does the game still look right, or did correcting the physics make ordinary moon systems look wrong?",
      "verifyVia": {
        "input": "Max watches a recording of wd-10 planet 3 over a full companion orbit with orbits visible, and a second of wd-133 planet 4 (six moons, worst epicyclic case) and one ordinary moon system.",
        "observable": "Max says it reads as a pair, and that nothing else got worse. Never PASSed by an agent — deferred-to-max. Animated feature, so the Shipped gate takes a recording, not a screenshot."
      },
      "layer": "uat"
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "src/main.js:11197-11205 — the planet per-frame write; the single hardcoded-singleton seam (scoping §2). Today it writes the orbital point straight to the mesh with no offset term.",
      "src/main.js:11237-11259 — the moon loop; planet-class moons are placed here relative to entry.planet.mesh.position, plain moons inside Moon.updateSim (src/objects/Moon.js:587-602), which advances its own orbitAngle. The offset needs this frame's moon directions BEFORE the planet write, so the loop order changes: compute next angles, offset, write planet, then place moons.",
      "src/main.js:11265-11271 — moon orbit ring placement, currently from px/pz. The one thing scoping §9 says MUST land in the same change.",
      "src/main.js:7633-7695 — per-moon OrbitLine construction at spawn; ⚠ reuse this array, a NEW ring array is silently never drawn (CONIC_MAX = 64, src/objects/OrbitConicField.js:51).",
      "moon record fields as of B5.0: orbitRadiusScene (Convention A — the FULL parent-relative separation), planetData.massEarth for planet-class moons, radiusEarth for plain ones.",
      "src/physics/GravityField.js:179-189 _estimateMoonMass — the mass rule the renderer must match so physics and render agree on q."
    ],
    "outputs": [
      "Planet mesh world positions — consumed by lighting (main.js:11205+), moon placement, gravity (GravityField.tick, a blind mesh copy), collision, supercruise, targeting, brackets, autopilot, cinematic and the freezeFrame instrument.",
      "Moon orbit ring transforms, and one new barycentric ring per dominated planet.",
      "Nothing written back to systemData — the generator's output is untouched, which is what AC-ZERO-GENERATION-DRIFT pins."
    ],
    "mustStayWorking": [
      "World-origin rebasing: every position written in the rebased frame. Mixing a rebased star with a raw planet skews the light direction by _worldOriginVec far from origin (WU5, memory/well-dipper-rebasing-plan.md). The barycentre must be computed and applied INSIDE the write, before any consumer reads it — a POST-write offset is scoping §3 scenario 6, rated fatal.",
      "Planets are no longer pinned to y = 0. Moon inclination reaches 0.4982 rad, so the primary now moves vertically. Anything assuming a planet lies in the y=0 plane must be found and checked — orbit lines, brackets, minimap fallback, ring gaps, collision.",
      "The 27 B5.0 companions and the 51 planet-class moons stay exactly where generation puts them; the drawn separation stays the full a (AC-SEPARATION).",
      "Every existing moon stays on its own orbit line (AC-NO-BODY-OFF-ITS-LINE) — today zero moons are detached and that must remain true.",
      "The moon-formation window's instrument reds stay exactly as handoff §3 documents them. B7 is what re-blesses; this workstream must not move a single one in either direction.",
      "No dev server touched during the build: a server on :5173 serves Max's live session and any src edit fires HMR into it.",
      "⛔ SOL MUST NOT GO NaN. `grep -c massEarth src/generation/SolarSystemData.js` = 0 — Sol carries no mass anywhere, so the planet-mass rule MUST keep the `?? estimateMassEarth(pd.radiusEarth, pd.type)` fallback (GravityField.js:122). Without it M_total is undefined, the offset is NaN, and every planet in Sol takes its lighting, moons, rings and gravity to NaN with it — invisible on every procedural seed.",
      "The moon's live orbit angle lives in TWO places: on the wrapper for planet-class moons (PlanetMoonBody.js:53) and on the DELEGATE for plain ones (Moon.js:18, reached as moon._delegate — BodyRenderer exposes no orbitAngle getter). Reading `moon.orbitAngle` uniformly returns undefined for every plain moon and NaNs the whole planet silently. main.js:3213 already encodes the split.",
      "The pre-pass that computes each moon's direction MUST PREDICT, NEVER MUTATE. Moon.updateSim advances its own angle at Moon.js:589; pre-advancing and then calling it doubles every plain moon's orbital rate — plausible on screen and invisible to every current test. `celestialDt = 0` is not an escape hatch: Moon.js:609 uses the same dt for moon rotation.",
      "Ring radii are set at CONSTRUCTION, never per frame: OrbitLine bakes its hit-test perimeter from the constructor radius (OrbitLine.js:57-65) and hitTestOrbits prefers that baked attribute (main.js:7011), so a per-frame radius change would draw at r2 and click at a. q is constant, so construction is correct.",
      "Adding a ring to moonOrbitLines makes it a DEAD CLICK TARGET unless the registration loop at main.js:7879 is bounded by entry.moons.length instead of entry.moonOrbitLines.length — otherwise it highlights on hover and swallows the click (main.js:7072-7073 -> :14011).",
      "Spawn writes the un-offset position too (main.js:7606 planet, :7691 moon rings) and the opening hero shot takes a CLONE of it (main.js:7942), so a per-frame-only fix leaves the hero camera framed on the un-offset point permanently."
    ]
  },
  "verifiedPendingMax": "b201bc0"
}