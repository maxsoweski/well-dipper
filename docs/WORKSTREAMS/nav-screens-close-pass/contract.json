{
  "workstream": "nav-screens-close-pass",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "building",
  "acceptanceCriteria": [
    {
      "id": "AC-1",
      "statement": "⭐ MAX RULED 2026-09-07: *\"I like removing the negative space; the chunky cells of design1 today are good but there are too many cells that are not selectable, so the solution is simply to remove the negative/non-selectable space and redraw the cells from there.\"* So the cells DO move and DO resize — remove the dead footprint, then re-lay the grid over what is left — and the CHUNKY WEIGHT is kept, he asked for it by name. ⛔ This supersedes my earlier 'nothing on the glass may move' framing, which was my risk-minimising default and not his goal. MEASURED LIVE against the real 775: design 1 draws 8x8 over a 216-texel square and 32 of the 64 cells — exactly half — have a centre resolving to NO sector; 47.2% of the square's texels resolve to nothing; the dead cells are a complete RING (whole top row, whole bottom row, both side columns, plus the next ring's corners). The defect is AFFORDANCE, not function — pickSector already returns null out there correctly. ⚠ Design 2 is NOT this fix: it has no cell grid at GALAXY and fails the opposite way, cropping 20 of 775 sectors off the glass.",
      "verifyVia": {
        "input": "At GALAXY in design 1, click every drawn cell, including every edge and corner cell.",
        "observable": "Every drawn cell resolves to a real sector — zero dead cells — and no sector that was reachable before has become unreachable. The cells keep the chunky weight of today's design."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ BUILT AND MEASURED 2026-09-07, live: S.mapProj is now size 36 (was 44), sq 216 / n 8 / cell 27 UNCHANGED so the chunk Max named is intact, 52 of 64 cells drawn, 0 drawn cells resolving to nothing (was 32), 0 sectors lost, dead ground in the drawn square 47.5% -> 21.5%. The reachable radius (18.000 kpc) is derived by bisecting 32 rays through the picker's OWN getSectorAt, not a radius of our choosing. ▶ AWAITING MAX'S EYES on the picture — shots/galaxy-design1-refitted.png against galaxy-design1-today.png. ⭐⭐ THE CLICK SWEEP THE AC'S INPUT NAMES WAS RUN 2026-09-07 (part 2), after the verify-workstream skeptic pointed out that '0 dead cells' had only ever been the paint's OWN predicate counted back (cells drawn = cells whose centre resolves), never a click. Instrument: the drawn set read OFF THE PIXELS (a cell is drawn iff all four of its rules are inked in INK.RULE), the resolving set from REAL pointer events at every cell centre through the pipeline a click uses (`nav._hoveredTile`). Result on Sol, design 1 at GALAXY (sq 216, n 8, cell 27, 36 kpc): 52 centres resolve, 0 drawn cells with a dead centre, 12 culled cells whose centres all resolve to nothing (the picker decides, not the paint). 47 cells pass the strict four-rule pixel test; the other 5 have an edge overdrawn by the YOU frame or a plate — an instrument limit, not a dead cell. ⛔ AND THE SKEPTIC'S SECOND POINT WAS REAL: probing every drawn cell's four INSET corners the same way found 28 of 208 dead (13.5%), in exactly the 20 rim cells — a square laid over the 18 kpc disc always has outer corners past it. That is the defect Max named at sub-cell size, so it is closed: `pickSector` now falls back, ONLY where the texel itself answered nothing, to the sector at the containing cell's centre — the paint's own predicate, so it cannot disagree with what was drawn. Re-swept live with the identical instrument: 208 corners, 0 dead; 12 culled centres still resolve to nothing; interior clicks keep the sector under them (65 of 208 corners share their centre's sector, the rest keep their own). ⚠ A MUTANT REMOVED A GATE: the first version also required the cell to be in a published `live` set, and deleting that gate broke nothing — a culled cell's centre is dead by definition, so snapping to it answers nothing anyway. The gate and its publication are gone; the comment says why. Mutant 'no fallback' goes red in two tests. ✅ MAX RULED ON THE PICTURE 2026-09-07 (part 2, close), shown the flip: *\"new one looks good.\"* CLOSED.",
      "status": "VERIFIED_BY_MAX"
    },
    {
      "id": "AC-2",
      "statement": "\"anything on screen should be clickable\" — nothing is drawn in either design that does not answer a click. Today the rail's detail block, the status line, the y-gauge, design 2's minimap, the density bars, the HZ band, the orbit ellipses, the tile-ID plates, the prism's index tags and the placed labels are all inert.",
      "verifyVia": {
        "input": "In both designs, at every level, click each drawn element that is not already pickable.",
        "observable": "Each one either performs its own action (selects the object it names, scrolls to it, or drills) or is a readout deliberately and visibly marked as non-interactive. A published-geometry entry on S exists for every newly pickable element — the geometry comes out of the paint, never restated in the hit-test."
      },
      "layer": "integration",
      "live": true,
      "progress": "◐ IN PROGRESS. `S.labelHits` shipped 2026-09-07 (AC-14). **`S.orbitRings` shipped 2026-09-07 part 2** — design 2's SYSTEM orbit ellipses, the largest inert thing the orrery draws, now answer a click by selecting the body the ring belongs to. Published out of `dottedEllipse`'s own two arguments at the draw site, so the grab band cannot drift from the drawn band; cleared in the DRIVER's `resetPicks` rather than in design 1, because a field only design 2 publishes needs an owner that runs for both (without it one press of V left design 2's rings live under design 1's ladder). `pickOrbitRing` measures the gap IN TEXELS along the ray, so a 3-texel band stays 3 texels wide on the outer ring as well as the inner one, and NEAREST ring wins because the sqrt(AU) spacing crowds the outer orbits. Tested LAST of the level-4 candidates: a mark and a label both beat it. ⭐ VERIFIED LIVE 2026-09-07 on Sol, design 2 at SYSTEM: 15 rings published; a click on a bare texel of Azcai's ring (289,120 — chosen by walking the ring for a texel clear of every mark and every plate) hovered `{planet,6}` and selected `{planet,planetIndex:6}`. **CONTROL:** the same walk 14 texels further out, clear of every ring and mark, hovered nothing and selected nothing — so the ring is what answered and the band has an edge. ⭐ FOUR MUTANTS, ALL CAUGHT: dropping the publication (4 tests red), never consulting a ring in `pickBody` (2 red), testing the ring FIRST instead of last (the mark-beats-ring test red), and removing the driver's clear (the design-1 test red). Suite `src/ui` 532 passed / 28 files; `extract-nav-designs --check` green; 0 mark-guard violations over both designs x five levels live. ▶ STILL INERT, and each is its own slice: `S.pagerRect`, `S.yGaugeRect` (which is AC-9's), `S.minimapRect`, `S.locatorRect`, `S.listHeaderRects`, `S.companionRect`. ✅ PART 3 (2026-09-08, 88510cb + the correction commit the same day): THE REMAINING RECTANGLES. `S.pagerRect` — design 1's `N-M OF T   - = PAGE` row pages back on its left half and forward on its right (live at 417x240: right half 0 → 27 = rows, left half back to 0, a click 3 texels above the row changed nothing, the click never drilled). `S.listHeaderRects` — design 2's list headers sort the list: NAME/SP/PC/PLANE/SYSTEM → name/class/dist/plane/catalog, the last two NEW level-3 keys appended after the existing four so DIST stays first; N is the row ordinal and has no key (live: DIST → NAME with the first three rows alphabetical, PLANE with wy ascending, N a no-op, a row click still selects the row's star and changes no sort). `S.locatorRect` — `HERE · SECTOR` re-centres on the player: an eased frame at GALAXY (live: _viewCenter −33.02 → 8.000 exactly over 350 ms of the host's own _viewEase), the player's sector/region at SECTOR/REGION (stack rebuilt, then eased), the camera and the loader's block at PRISM (live: _localCenter back to {8, 0.025, 0}, 11,511 stars reloaded); ▶ at SYSTEM it is deliberately NOT eaten and does nothing — 'centre on the player' has no agreed meaning there (a foreign system re-entered is resolveArrivalSystem territory) — MAX'S CALL. `S.companionRect` — the `» STAR B nnAU` strip eats a press so it cannot fall through to a ring (measured: no ring can reach above y≈21, so today it guards nothing the paint produces; kept as the plate rule stated where a taller orrery would need it). ⛔ `S.minimapRect` WAS BUILT AND THEN WITHDRAWN by the adversarial pass: the widget is four brackets, a dot and a scale mark over a starfield drawn before it — not a plate — so eating the press turned a visible, hoverable star into a dead click, the opposite of this AC; the starfield under it answers as before. ⚠ THE ESCAPE CLAUSE ('a readout deliberately and VISIBLY marked as non-interactive') IS NOT MET for the minimap, the companion strip or the N header: each eats or ignores a click with no marking on the glass. A marking is a picture change → Max. ⚠ STILL INERT, outside this pass: design 1's rail detail block, both hint rows, design 2's status line, the rail's density bars, the HZ band (the tile-ID plates already resolve through S.mapProj). EVIDENCE: 33+ headless cases driving real clicks (navClosePass3.test.js), the live walk on :5175 (checks A-D), and a 14-agent adversarial pass — every builder mutant reproduced red; the survivors were test gaps (rect edges, other levels, search-open, self-referential probes) closed in the correction commit with absolute, paint-derived assertions."
    },
    {
      "id": "AC-3",
      "statement": "\"selecting one thing should not prevent a second selection, as is currently the case on the system screen\" — at SYSTEM, clicking a body and then a different body moves the selection to the second one, in both designs. MEASURED ROOT CAUSE: selecting a planet that has moons sets _systemMode='planet', and NavComputer.js:4512-4522's 'planet' branch then writes { planetIndex: this._selectedPlanetIdx } — the already-selected planet — instead of this._hoveredBody.index. The legacy planet-detail view draws only the selected planet so the two agreed; the designs keep drawing all 15 bodies and keep publishing them all as pickable, so the selection sticks forever.",
      "verifyVia": {
        "input": "At SYSTEM in each design, click body A, then body B, then body C — including at least one body that has moons, since that is what arms the bug.",
        "observable": "nav._selectedBody follows each click. Measured today in design 2: three clicks on Sol b, Sol g and Sol b left _selectedBody frozen at planetIndex 3 every time."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ VERIFIED LIVE 2026-09-07, design 1 at SYSTEM on Sol. Five clicks on five different planets — indices 1, 3, 1, 5, 0 — and _selectedBody followed EVERY one (1,3,1,5,0), with _systemMode staying 'system' throughout. Before the fix the same walk froze at planetIndex 3 in both designs."
    },
    {
      "id": "AC-4",
      "statement": "\"it's important to be able to rotate it in 3D. The previous system for the prism view was more functional in that sense\" — dragging on the PRISM screen turns the star column in 3D, in both designs, WITHOUT losing the low-fi look Max asked for by name. The rotation applies to the existing texel-quantised marks; it is not a switch back to the legacy renderer's arcs and strokes. MEASURED: _handleMouseMove already writes _localRotX/_localRotY on drag and the designs are blind to them — with a live control (moving _localCenter.z moved every published prism mark), changing _localRotX/_localRotY moved none.",
      "verifyVia": {
        "input": "At PRISM in each design, press and drag across the map pane.",
        "observable": "The star column turns with the drag: the published S.prismHits coordinates move as _localRotX/_localRotY change, and the marks stay texel-quantised (no anti-aliased arcs or sub-texel strokes appear)."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ VERIFIED LIVE 2026-09-07 with the same instrument and the same control as the pre-change measurement. The prism enters at _localRotX = 0.6521714117570698 (the derived default — the seed lands) and _localRotY = 0. Control still live (moving _localCenter.z moves every published mark). Rotating now MOVES the picture where before it was byte-identical, and returning the rotation restores the marks exactly."
    },
    {
      "id": "AC-5",
      "statement": "\"clicking on a cell from the grid should highlight it, then zoom into it, resulting in the next screen (galaxy > sector > region)\" — the clicked cell lights up FIRST, then the map flies into it. MEASURED: the zoom half is already live — a real GALAXY drill walks S.view.size 44 -> 43.98 -> 43.88 -> 43.63 -> 43.16 -> 42.46 -> 40.23 -> ... -> 0.5 and lands at SECTOR. What does not exist is any click-highlight state at all.",
      "verifyVia": {
        "input": "At GALAXY in each design, click a grid cell and watch the frames between the click and the arrival at SECTOR.",
        "observable": "The clicked cell is drawn highlighted before the view starts moving, and the highlight is visible for the first frames of the zoom."
      },
      "layer": "integration",
      "live": true,
      "progress": "◐ PARTLY VERIFIED LIVE 2026-09-07. At SECTOR, a cell click set S.pick to '1:3,2' and it held across all 8 sampled frames, then cleared exactly when the drill landed (level 1 -> 2, viewSize 1 -> 0.125). ⛔ STILL MISSING AT GALAXY, which is the level Max named: at level 0 the cell is not the drill target (the identity is the containing sector, 1 of 775 and irregular, flying to its own centre and size), so framing the cell would promise the wrong destination. Needs the sector's own rect published out of the paint. ✅ GALAXY HALF BUILT 2026-09-08 (88510cb + correction): `notePick` records `{ level: 0, sector }` off the very hover object `_handleClick` drills (one identity, not two lookups); both designs frame that sector's rect in INK.KEY through their OWN toX/toY, clipped to the painted square (design 1) or band (design 2), drawn last, and it goes out on the frame the drill lands. LIVE (check E, 417x240): design 1, sector Phi Ara-5 — the probe texel read 81,79,76 before the click and 216,251,255 (INK.KEY) two rAF after it, `_anim` running, level 1 and S.pick null 900 ms later; design 2, Phi Musca-83 — 172,173,182 → 216,251,255, same landing. ⭐⭐ AND THE ZOOM HALF WAS NOT LIVE AT GALAXY AFTER ALL — the earlier '44 → 0.5' walk was the FIELD, not the picture: the lab's `levelView(0)` returned a fixed `{0,0,44}` disc, so a GALAXY drill CUT to SECTOR after 500 ms while `S.view` zoomed unseen (and the host's own 2D pan at GALAXY moved a frame nothing drew — measured: a 60-texel drag moved _viewCenter.x 0 → −16.5 with a byte-identical canvas hash). Level 0 now reads S.view like levels 1-2; the entry frame is unchanged because `_viewStack[0]` IS that literal; so the sector lights, the square/band closes on it, and SECTOR appears — Max's sentence in full. ⚠ Fixed on the way: a click with no preceding hover frame (a tap) framed a sector and did not drill, because the drill reads the previous frame's `_hoveredTile`; `remapClick` now resolves hover at the click point before recording the pick.",
      "status": "VERIFIED_PENDING_MAX"
    },
    {
      "id": "AC-6",
      "statement": "\"We have also lost much of the animation between screens\" — the transitions that still SNAP animate too. MEASURED: _handleClick's tab-strip branch (NavComputer.js:4450) only calls _startDrillAnim when `idx <= 2 && this._levelIndex <= 2`; every transition touching PRISM (3) or SYSTEM (4) takes the else branch and jumps.",
      "verifyVia": {
        "input": "In each design, move between every adjacent pair of levels via the tab strip and via a drill click — REGION->PRISM and PRISM->SYSTEM especially.",
        "observable": "Each transition is animated rather than an instant cut."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ OUTBOUND HALF at c8954eb (2026-09-07): PRISM/SYSTEM → 2D by tab eases through the host's `_viewEase` (:1419, armed at :4476). ✅ INBOUND HALF 2026-09-08 (88510cb + correction). MEASURED FIRST, and it narrowed the scope: the drill click into PRISM (`_startDrillAnim` to level 3) and the star click into SYSTEM (`_systemZoomAnim`) ALREADY animated under a mode because both move state the designs read (`_viewCenter/_viewSize` → S.view, `_localRadius/_localCenter` → S.cam); only the TAB STRIP and the Tab KEY into PRISM/SYSTEM snapped (:4475 moves the index synchronously). The driver now holds S.level for 350 ms and eases S.view (2D → 3/4: the exact mirror of :4476's outbound `fromSize / (gridN*2)`, onto _localCenter for PRISM and the player for SYSTEM) or S.cam (3 → 4: radius ×0.1 toward the system star, the mirror of _systemZoomAnim; 4 → 3: back up), armed ONLY by the driver's own tab paths so a direct `_levelIndex` assignment and the drill clicks never lag; map clicks during the ease are eaten and hover is null; a REAL tab press cancels the ease and moves on (the first build swallowed a quick double-Tab; a press in the strip's band past the last tab no longer cancels either). LIVE (check F): REGION → PRISM by Tab — `_levelIndex` 3 on the first sample, S.level held at 2, S.view.size 0.671 → 0.577 → 0.260 → 0.036 then 3 and lag null; PRISM → SYSTEM by Tab (warm, 13-17 ms frames) — S.level held at 3 for 357 ms, radius 0.0015 → 0.00015 (×0.1 exactly), then 4; a second Tab 120 ms in → 4 with a prism-kind lag, not swallowed. ⚠ ON THE FIRST PRISM ENTRY frames are ~450 ms (the synchronous 32,689-star load), so the 350 ms sim-clock ease visibly stretches over ~2 s once; warm, it is clean. ⚠ GALAXY → PRISM/SYSTEM by tab is visible only since `levelView(0)` reads S.view (see AC-5). Every adjacent pair is now either the host's own animation or the driver's ease.",
      "status": "VERIFIED_PENDING_MAX"
    },
    {
      "id": "AC-7",
      "statement": "\"I want to be able to grab and drag the system line view to look at the further planets from the star, rather than having to press a button exclusively\" — at SYSTEM in design 1, dragging sideways scrolls the ladder. Today S.ladderScroll moves only on ',' / '.' and on a click in the two '...' cap zones. The drag must land on a published stop (S.ladderStops), never on a continuous offset, or it scrolls to a position with no body in it.",
      "verifyVia": {
        "input": "At SYSTEM in design 1, press on the ladder and drag left and right.",
        "observable": "The ladder scrolls with the drag and comes to rest on a body, not between bodies. The '1-14 OF 15' counter tracks it. The ',' / '.' keys and the cap clicks still work."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ VERIFIED LIVE 2026-09-07, design 1 at SYSTEM. A leftward drag on the ladder took S.ladderScroll 0 -> 4 (its max on Sol) and left _systemRotX/_systemRotY untouched — so the gesture routes by design exactly as intended."
    },
    {
      "id": "AC-8",
      "statement": "\"On the orbits view of the system screen, I want to be able to rotate the view in 3D like I could with the previous one\" — at SYSTEM in design 2, dragging turns the orrery in 3D. _handleMouseMove already writes _systemRotX/_systemRotY on drag at level 4; d2System ignores them and uses a fixed TILT=0.42. ⛔ ONE GESTURE, TWO MEANINGS: the same drag pans design 1's ladder (AC-7) and rotates design 2's orrery. Which one runs is chosen by the active design, deliberately, not by whichever handler happens to run first.",
      "verifyVia": {
        "input": "At SYSTEM in design 2, press and drag across the orbit view.",
        "observable": "The orrery's tilt and heading follow the drag; the published S.bodyHits coordinates move with _systemRotX/_systemRotY. Dragging in design 1 at the same level scrolls the ladder instead and does not rotate anything."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ VERIFIED LIVE 2026-09-07, design 2 at SYSTEM. Entered at _systemRotX = asin(0.42) = 0.43344532006988595, _systemRotY = 0. A drag took them to 0.6254 / 0.5743, S.sysCam followed both, and the published body marks moved with them."
    },
    {
      "id": "AC-9",
      "statement": "\"The indicators on the prism and system screens should be grabbable\" — the readouts that show a position along a range are handles, not decoration. On the prism that is the y-gauge and the zoom/index indicators; at SYSTEM it is the ladder's scroll indicator and the counter.",
      "verifyVia": {
        "input": "At PRISM and at SYSTEM in each design, press on each indicator and drag along its axis.",
        "observable": "The indicator moves under the pointer and the view it reports moves with it. Releasing leaves the view where the indicator was dropped."
      },
      "layer": "integration",
      "live": true,
      "progress": "◐ THE PRISM'S Y-GAUGE IS DONE (2026-09-07 part 2); the prism's zoom/index indicators and SYSTEM's counter are not. ⭐ AND THE GAUGE TURNED OUT TO BE A RESTORATION, NOT A NEW CONTROL. It reports `_localCenter.y`, the height R and F already move (`NavComputer:1392-1393`) and the LEGACY prism already DREW — a camera mark at `:3618`, a height in pc at `:3669`, and a literal `WASD move · R/F up/down` hint. Design 1's gauge kept the selected star's offset and dropped the camera, so the readout for Max's own UAT item of 2026-08-01 (*\"I still can't use the up/down controls to rise and lower below the galactic plane on the prism menu\"*) went with it. BUILT: `d1Prism` declares the mapping once (`GAUGE_HALF_KPC`, `gaugeCy`, `gaugeSpan`), draws the star mark through it — algebraically the line it replaces, so the ruled picture is reproduced texel for texel — draws the CAMERA's mark through the same function, and publishes `S.yGaugeRect = {x,y,w,h,cy,span,halfKpc,base}` so the drag inverts the paint's own arithmetic instead of restating it. `gaugeGrab` / `gaugeDragTo` in the driver; four folded clauses in the line-frozen `NavComputer.js` (still 4711) arm at mousedown, own the move, and release at mouseup. ⛔ THE CAMERA MARK IS DRAWN ONLY ONCE THE CAMERA HAS LEFT THE PLAYER'S PLANE, which is what keeps AC-11 true: at prism entry `_localCenter` IS the player (`:1186`), so the default picture gains nothing. ⭐ VERIFIED LIVE 2026-09-07, design 1 at PRISM on Sol. Gauge published at x252 y6 6x216, cy 114, span 108, halfKpc 0.002, base 0.025 — and camY == playerY at entry, so no mark. A real mousedown-drag on the strip took the camera 0.025 -> 0.0259943 kpc (the residue is the client→canvas rounding of the pointer, against an expected 0.026), left BOTH rotations untouched, and moved ALL 22 published prism marks — the view it reports moved with it. Releasing left it where it was dropped and a further move with the button up changed nothing. ⭐⭐ AND THE MARK WAS READ OFF THE GLASS, NOT INFERRED: sampling the strip's own pixels, parked shows one 4-wide mark at row 114 (the YOU marker, 255,176,58) and NOTHING else; with the camera a third of a span up a second 4-wide mark appears at row 78 in INK.KEY (216,251,255), against a predicted row of 78. Exact. ⛔ CONTROL: a press 20 texels left of the strip rotated the prism and left the camera height alone — the grab takes no ground it does not draw. ⭐ FIVE MUTANTS, AND THE FIFTH REWROTE A COMMENT. Not arming at mousedown, not drawing the mark, drawing it unconditionally, and dropping the clamp all went red. Deleting the RELEASE did not — because the level-3 mousedown reassigns the flag on every press — so the claim that it was load-bearing was FALSE as written. The path it actually guards (armed at PRISM, next press taken at SYSTEM, Tab back to PRISM) is now pinned by its own test and the comment says what is true. ⚠ THE DRAG'S RANGE IS THE STRIP'S OWN ±halfKpc: one scale for the readout and the handle. R and F still go further and the mark pegs, exactly as a star's mark already does. ✅ THE COUNTER (2026-09-08, 88510cb + correction): `S.ladderCounterRect` published by d1Ladder where `N-M OF K` is drawn (only when the ladder overflows); `counterGrab` / `counterDragTo` in the driver invert it (texels, clamped 0..ladderMax); three folds in the line-frozen host (:4405 press, :4355 move with a load-bearing return so the pan does not also run, :4415 release) mirror the y-gauge exactly. LIVE (check G, at a 227-wide buffer where a ladder overflows): a press on the readout and five moves to its right edge took ladderScroll 0 → 13 → 15 → 17 → 19 → 21 = ladderMax and ladderVisible [0,12] → [1,14]; release held; a press 20 texels off the readout panned 1:1 instead (AC-7) — the two are cleanly distinguishable. ⛔ FOUND AND FIXED ON THE WAY: d1Ladder padded its virtual axis `+ 8` where `vpx` already carries a 4-texel margin on both ends, so EVERY ladder with a body overflowed by exactly four texels — a `...` cap promising four texels of nothing and a `1-N OF N` counter on every system (probed one planet to forty). Now `+ 4`. ⚠ CONSEQUENCE, MEASURED LIVE: SOL FITS at Max's 417x240 (15 stops end at 222 against a 226 window), so on Sol there is no cap, no counter and nothing to scroll; the control appears on denser systems or narrower windows. The design-1 SYSTEM hint now names the scroll controls only when the ladder overflows. ⚠ THE READOUT'S OWN PIXELS DO NOT MOVE — the AC's 'indicator moves under the pointer' is met by the readout changing as the window it reports moves; a drawn thumb is a new element on the SYSTEM picture → Max. ⚠ THE PRISM'S 'ZOOM/INDEX INDICATORS' DO NOT EXIST ON THE GLASS: neither design draws a zoom gauge (design 2 prints the zoom as a number in its status line, design 1 prints nothing) and the index tags are labels, not a position along a range (already clickable, AC-14). There is nothing to grab; a zoom gauge would be a new element → Max.",
      "status": "VERIFIED_PENDING_MAX"
    },
    {
      "id": "AC-10",
      "statement": "FOUND IN PASSING, and it is a thing on the glass that is wrong: every body name in both designs is a silent error fallback, and past ten planets it reads 'Sol undefined'. state.js:109's makeRng returns { next, child } while NameGenerator's generatePlanetName / generateMoonName call rng.float() and rng.pick() — so every call throws, state.js:258-259's catch swallows it, and the fallback alphabet 'bcdefghijk' runs out at index 10. Sol has 13 planets. The comment at state.js:107 claims this rng is \"the same shape\" as NavComputer._makeRng (:315-327), which has float/int/pick/bool/chance; it is not.",
      "verifyVia": {
        "input": "At SYSTEM on Sol in design 1, read the ladder's body labels and the status line, scrolling to the outermost bodies.",
        "observable": "No body is named 'Sol undefined'. Names come from the real generator, not the catch-block fallback — verifiable headlessly by asserting makeRng exposes float/int/pick/bool/chance and that generatePlanetName does not throw."
      },
      "layer": "unit",
      "progress": "✅ VERIFIED LIVE 2026-09-07 on Sol. Zero names contain 'undefined'. The fifteen laddered bodies now read Veli, Sol Minor, Raupo, Sol e, BELT A, Sol f, Kothionura, Sol-7, Sol i, Sol j, Kezex, BELT B, Felfefeon, Flevonein, Zamioxkaho — the real generator's output. Before: Sol b..Sol k plus three literal 'Sol undefined'."
    },
    {
      "id": "AC-11",
      "statement": "THE REGRESSION GUARD, and it is the discipline that made the camera safe to wire last session: every rotation, drag and highlight added here leaves the DEFAULT picture exactly where Max ruled on it. Each view opens at the rotation it opens at today (_localRotX=0.5/_localRotY=0.3 at PRISM, _systemRotX=0.5/_systemRotY=0 at SYSTEM, against the designs' fixed rx=0.92/tilt=0.42), and only a real gesture changes it. ⚠ _localRotY is unclamped and unwrapped — it grows without bound across drags and must be wrapped now that the designs read it.",
      "verifyVia": {
        "input": "Enter each level in each design and render without touching the mouse or keyboard; then drag, then re-enter.",
        "observable": "The entry frame is byte-identical to the pre-change build's entry frame at every level in both designs. viewMode === null is today's nav, byte for byte. _localRotY stays bounded across many drags."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ HELD, AND MEASURED THREE WAYS. 2026-09-07: rotation defaults derived from the gains (`PRISM_ROT_X0 = atan2(0.42, 0.55)`, `SYSTEM_ROT_X0 = asin(0.42)`), the host re-seeds its own camera under a mode, `wrapTau` bounds the azimuth COPIES the designs read while the raw `_localRotY` is left alone; 64,000 prism projections and 20,000 orrery bodies bit-identical at the default with a live probe (rotY 1e-9 diverges). 2026-09-08: the LAB's op-stream diff of 20 frames (both designs × five levels × list on/off) byte-identical to HEAD's with S.pick null; live (check H) cam.rotX 0.6521714117570698 / rotY 0 and sysCam.rotX 0.43344532006988595 / rotY 0 at every fresh entry in both designs, no pick, no lag. ⚠ ONE RECORDED EXCEPTION, DELIBERATE: design 1 at SYSTEM on a ladder that FITS (Sol at Max's window is one) no longer draws the right `...` cap, the `N-M OF K` readout or the hint's `SCROLL , . OR CLICK ...` clause — all three were artifacts of the `+ 8` axis padding (AC-9), promising a scroll that did not exist. For Max's eyes.",
      "status": "VERIFIED_PENDING_MAX"
    },
    {
      "id": "AC-13",
      "statement": "⭐ MAX RULED 2026-09-07, shown the A/B: *\"I want the nav screen to reflect the actual orientation of the planets in the game, so if B is correct then that's what we use.\"* B STAYS. And his words set a HIGHER bar than the A/B asked about: not merely 'a real angle once', but the planets' ACTUAL ORIENTATION IN THE GAME. Background: d2System placed bodies at `a = i * 1.7 + 0.6` where `i` is the DRAW-LOOP INDEX, and state.js re-sorts D.bodies on [ / ], so the sort key teleported every planet around its ring. The angle exists upstream (StarSystemGenerator.js:550/609, SolarSystemData.js:723/846) and the LEGACY orrery already reads it (NavComputer.js:2853); buildBodies never copied it across. ⚠ OPEN SUB-QUESTION HIS RULING CREATES: the game ADVANCES orbitAngle every tick (main.js:3145, `entry.orbitAngle += entry.orbitSpeed * celestialDt`), and there is a pin/thaw mechanism at :3216. The nav is handed the live `system._systemData` (main.js:5781), so the plumbing is right — but a 4-second live sample showed the nav's angles STATIC. Either the planet objects are a snapshot, or orbits are pinned in ORRERY. Must be settled by measurement: does the nav's mark sit where the planet actually is RIGHT NOW?",
      "verifyVia": {
        "input": "At SYSTEM in design 2, watch a fast inner planet over time, and separately press [ and ] to cycle the sort key.",
        "observable": "The nav's mark sits where that planet actually is in the game at that moment, and keeps tracking it as it orbits. Every body stays on its own ring across every sort."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ Real angles built and live; ▶ MAX RULED FOR B. ⚠ NOT YET CLOSED: his 'actual orientation' bar needs the live-tracking question answered. Measured 2026-09-07: nav angles static over 4s and D.bodies faithfully mirrored them, so the adapter is honest — but whether its SOURCE tracks the rendered planet is unsettled. ⚠ ALSO OPEN: B clusters Sol's inner planets so numerals II/III/IV/VI/VII overlap where A's are clean. placeLabel tries 7 vertical offsets against other labels only and cannot solve an angular cluster; unreadable numerals are a defect, not a polish item, so the placer needs a pass. ✅ BOTH OPEN SUB-QUESTIONS CLOSED. Live tracking: AC-16 (green, measured off the running sim with a mutation control). The overlapping numerals: e801dc9 (AC-14) — they were not overlapping each other, they were sitting on planet marks and their knockout plates erased the dots; the placer now refuses slots over foreign marks and can go left as well as up and down: 4,854 → 0 foreign-mark hits, 507 → 0 erased glyphs, more labels drawn (6,909 → 7,155). Max on the after picture: 'That's better, I like the after version better.'",
      "status": "VERIFIED_BY_MAX"
    },
    {
      "id": "AC-14",
      "statement": "FOUND IN PASSING, and it is a live MIS-SELECTION, not an inert region: placeLabel (lab:296-306) walks 7 vertical offsets up to 18 texels from the star it labels and checks each candidate slot only against OTHER ALREADY-PLACED LABELS — never against star dots. So a label can land squarely on an unrelated star's mark, and clicking it selects the wrong star. The prism's index tags have the same drift at a fixed +3,-6 offset, over a field of up to 240 marks with a 3-texel hit radius.",
      "verifyVia": {
        "input": "At PRISM in design 2, find a placed star label that has drifted away from its own mark, and click the label.",
        "observable": "The star the label NAMES is selected — not whatever mark happens to lie under the label's plate. S.labelHits is tested before S.prismHits, so the plate's texels belong to the label's own object."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ CLOSED at e801dc9 — `S.labelHits` is published by `plated()` itself and tested BEFORE the mark lists, so a click on a label selects the object the label names; `placeLabel` is a 14-candidate solver rejecting slots over other labels AND over drawn marks. Approved by Max the same day ('looks good to me').",
      "status": "VERIFIED_BY_MAX"
    },
    {
      "id": "AC-15",
      "statement": "FOUND BY THE NEW MARK GUARD, and nothing else could have seen it: design 2's moon pips are a SCREEN-SPACE strip drawn at `x + 4 + m*2, y - 4` (d2System:1416), so a moon-rich planet sitting on an outer orbit near cos(a) ~ 1 runs the strip off the right edge of the map. It was silently CLIPPED before, because rect/sprite/dottedEllipse were unguarded — assertFits is reached only through T() and therefore plated(). Zero firings at the default tilt, which is exactly why no suite is red today; 30 firings across a 1200-frame rotation sweep. The pips are correct as a screen-space badge and must stay one — the fix is to place the strip on whichever side of the body has room, not to tilt it into an ellipse.",
      "verifyVia": {
        "input": "Sweep the orrery through its full rotation range on a system with a moon-rich outer planet (the lab's own Q/E and arrow keys drive it).",
        "observable": "assertMark reports zero overflows at every angle, and the moon pips stay on the glass rather than being clipped at the pane edge."
      },
      "layer": "integration",
      "live": false,
      "progress": "✅ 2,880-frame sweep across both designs and all five levels: ZERO guard firings, against the old budget's overflows at the same angles, with a sabotage control proving the violation channel is live so the zero is not a dead wire. At the default tilt old and new both compute 205.50 — the picture cannot move."
    },
    {
      "id": "AC-16",
      "statement": "⭐ MAX'S BAR ON AC-13, AND IT WAS NOT MET BY THE REAL ANGLE ALONE: *\"I want the nav screen to reflect the actual orientation of the planets in the game.\"* Planets MOVE. main.js:7875 copies orbitAngle into the scene entry as a NUMBER, and from then on the sim advances only the copy (:11358, `entry.orbitAngle += entry.orbitSpeed * celestialDt`). The nav is handed system._systemData — the raw generation data, and main.js:7950's own comment says so — so reading p.orbitAngle draws every planet WHERE IT STARTED, forever. ⛔ AND buildBodies RUNS ONCE PER SYSTEM (generating ~39 names is not cheap), so reading the live value inside it is not enough on its own: the row must be re-read every frame. Both halves are required. Fixed by a `_live` back-reference folded onto main.js:7878 (line count still 15161) plus a per-frame angle refresh in state.js that leaves the cached names alone.",
      "verifyVia": {
        "input": "At SYSTEM in design 2, watch a fast inner planet for several seconds while the game runs.",
        "observable": "Its mark travels around its ring, staying where the planet actually is. Pinned by a test that moves the live angle between two renders WITHOUT changing the system object, so the cache is exercised; both halves mutation-proved — disabling the per-frame refresh fails with 'the orrery froze', ignoring _live fails with 'read the frozen generation angle'."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ VERIFIED LIVE 2026-09-07 (part 2), AND THE PREVIOUS SESSION'S BLOCKER WAS THE BOOT STATE, NOT THE FIX. The referent needs a genuinely spawned system, so the walk was `_lab.enterSol()` — `_debugEnterKnownSystem` → `spawnSystem({forWarp:false})` → the SAME `_applyNavArrival` call site a warp arrival uses (main.js:8084, deliberately above `if (forWarp) return;`), which is also Max's own D-hold route. State after it: `nav._systemData === window._systemData` (the SPAWNED data, not a nav preview), `_isCurrentSystem()` true, level 4, viewMode 'bars' (design 2, the orrery), `_live` an object on every planet. MEASURED with `celestialTimeMultiplier` at 200000 — the game's OWN time lever, so the sim advances itself and nothing is hand-written into the angle: over 6 s the innermost planet's live angle ran 0.30006 → 1.29620 rad (~57°) and `D.bodies[0].ang` equalled it to 5 dp at EVERY one of 7 samples, while `p.orbitAngle` (generation) stayed frozen at 0.3 throughout. ON THE GLASS: 30 of the 42 published `S.bodyHits` marks physically moved (the star's and the belts' correctly did not). ⭐ MUTATION CONTROL, RUN LIVE IN THE SAME SESSION: deleting `_live` off the planets froze `D.bodies[0].ang` at 0.3 while the sim ran on to 1.74323 — the exact frozen-snapshot defect this AC names — and restoring `_live` resumed exact tracking (1.84257, 1.94467). So the marks are driven by the scene entry and by nothing else. ⚠ RESIDUAL, STATED RATHER THAN GLOSSED: the warp-arrival path was not flown live. It shares the one `_applyNavArrival` call site measured here and is pinned by `src/cockpit/__tests__/mainNavWiring.test.js`, but no live warp was driven this session. ⚠ AND THE BOOT STATE IS REAL, MEASURED, AND A QUESTION FOR MAX: clicking ORRERY on the splash and letting the intro run to the end leaves the pilot with NO system spawned — 36 s after the click `window._systemData` was still undefined and `_lab.systemInfo()` returned `{name:'', planetCount:0}`. `_currentSystemData` is null there, so `NavComputer.js:2458` falls through to `resolveArrivalSystem` and the SYSTEM screen draws a system the nav generated FOR ITSELF. That is not a defect in this fix — there are no in-game planets to track — but 'the actual orientation' has no referent on that path, and the same preview is what a BROWSED foreign star shows by design. Nobody has ruled on it out loud."
    },
    {
      "id": "AC-17",
      "statement": "FOUND IN PASSING AND IT IS THE WORST FAILURE MODE THIS SURFACE HAS: d1Rail dereferences D.playerSector.name unguarded, and state.js can legitimately hand a null playerSector — getSectorAt returns null for any point past R > GALAXY_RADIUS * 1.2 = 18, so a player out there freezes the nav. ⛔ It does not throw visibly: PanelHost catches a painter throw ONCE and then stops uploading, so the glass keeps showing the last good frame and LOOKS ALIVE. A dead screen that looks fine is worse than a crash.",
      "verifyVia": {
        "input": "Render both designs at every level with D.playerSector null.",
        "observable": "Every painter completes, the glass keeps updating, and no field a design reads is left without a default in state.js."
      },
      "layer": "unit",
      "progress": "✅ Four sites guarded, not one — d1Rail, d2Status, d3Left, d3Right. 10 paints (2 designs x 5 levels) with a null playerSector, 0 painter throws, and a control proves the OLD dereference really does throw TypeError on the same value. ⚠ The guards draw em-dashes, never a fabricated sector — a placeholder would put four plausible numbers on the glass for a sector that does not exist."
    },
    {
      "id": "AC-12",
      "statement": "Max walks both nav menus again, screen by screen, and each one holds up close.",
      "verifyVia": {
        "input": "ORRERY -> N -> V, then walk GALAXY / SECTOR / REGION / PRISM / SYSTEM in both designs, clicking and dragging everything.",
        "observable": "Max's judgment that the screens hold up. NEVER driven or PASSed by an agent."
      },
      "layer": "uat"
    },
    {
      "id": "AC-18",
      "statement": "⭐ MAX RULED 2026-09-07 (part 2), asked whether the nav should draw a system it generated for itself when the ship is in no system: *\"disable the system screen when not in a system.\"* So with no spawned system (`_currentSystemData` null — the ORRERY splash boot with the intro played out, and nothing else, since every arrival sets it) the SYSTEM screen is not reachable in either design: the SYSTEM tab is drawn dimmer than an inactive tab and does not answer a click, Tab skips it in both directions, and a prism star click SELECTS the star without drilling — so Enter and the rail's chip still warp the pilot out of deep space. The screen is disabled; the function is not. ⭐ WIDENED THE SAME DAY on Max's word (*\"3 yes\"*) to TODAY'S NAV and the COCKPIT PANEL: one predicate for every surface.",
      "verifyVia": {
        "input": "Boot ORRERY from the splash, let the intro play out, open the nav, in each design AND in today's nav (V to null): press Tab from PRISM and Shift+Tab from GALAXY; click the SYSTEM tab; click a star in the prism.",
        "observable": "Tab lands on GALAXY and Shift+Tab on PRISM; the SYSTEM tab is visibly dimmer and the click changes nothing; the star click selects the star (Enter warps to it) and the SYSTEM screen never appears. In a spawned system all four behave exactly as before."
      },
      "layer": "integration",
      "live": true,
      "progress": "✅ BUILT AND VERIFIED LIVE 2026-09-07 in the genuine no-system state (splash → ORRERY, intro played out, `_lab.systemInfo()` 0 planets, `_currentSystemData` null). One predicate, three consumers: `S.noSystem` is published by the paint's `refresh` and read by both tab rows (SYSTEM in INK.RULE instead of INK.DIM); the driver's `tabLevel` skips level 4 and `remapClick` eats a SYSTEM-tab click, both reading `nav._currentSystemData` directly because the mirror is a frame stale between an arrival and its first paint; and one folded clause in the line-frozen `NavComputer.js` (still 4711) makes the prism star click select-and-stop ahead of the drill sound and the zoom. LIVE, design 1: GALAXY tab glyphs 47,107,122 (DIM), SYSTEM tab glyphs 29,58,74 (RULE); Tab from PRISM → 0, Shift+Tab from GALAXY → 3; a real click on the SYSTEM tab left the level at 3; a real click on an isolated prism mark hovered, selected and armed seed 163760118 (`_externalTarget` set) with `_systemZoomAnim` null and the level still 3. Design 2 via `S.tabRects[4]`: SYSTEM ink RULE only, GALAXY DIM. THREE MUTANTS, ALL RED: Tab never skipping (2 tests), the tab click not eaten (1), the star click drilling anyway (1). Three inherited ring tests now state their precondition (`_currentSystemData` set) instead of relying on the harness's default, which IS the no-system state. Suites: `src/ui` 548/548, `src/cockpit` 698/698. ▶ MAX'S WALK: boot ORRERY, let the intro finish, N, then Tab round the levels and click a star. ⭐ WIDENED 2026-09-07 (close) to today's nav and the cockpit panel — Max: *\"3 yes.\"* Two more folds in `NavComputer.js` (still 4711): the legacy tab strip returns on a SYSTEM click with no system (the branch below it would auto-select the nearest star and open a preview), and `_renderTabs` draws SYSTEM at alpha 0.15 against an inactive tab's 0.4 off the same field; the prism star-click guard lost its `viewMode` gate so the cockpit panel's pointer takes the same path. LIVE, today's nav in the genuine no-system state (1981x1122 canvas): SYSTEM tab max luminance 170 against GALAXY's 325; a real click on the SYSTEM tab left the level at 3 with no preview generated; a real pointer sweep found a star under the legacy prism (seed 3073183202), and the click selected it, set `_externalTarget`, and did not drill. TWO MORE MUTANTS RED (the legacy tab fold removed; the star guard re-gated on `viewMode`); the cockpit-panel case is pinned with `chromeless = true`. Suites: `src/ui` 551/551, `src/cockpit` 698/698.",
      "status": "VERIFIED_PENDING_MAX"
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "nav-240p-lab.html — THE SPEC. Every design change goes here and is re-extracted; designs.js is GENERATED and must never be hand-edited.",
      "src/ui/NavComputer.js — the host. LINE-COUNT-FROZEN at 4711; ~700 line-anchored citations ride it, so new statements FOLD onto existing lines and every comment on a folded line must be /* */ terminated. Lines 586, 4345, 4420 and 4442 end in // and are poisoned. The keyboard fold line is 349.",
      "NavComputer's live drag state — _dragging, _dragStartX/Y, _dragStartRotX/Y, _localRotX/_localRotY (PRISM), _systemRotX/_systemRotY (SYSTEM). All already written at 0.008 rad/px on drag; the designs simply never read them.",
      "src/ui/navViewModes/state.js — the adapter that builds S and D. S.cam and S.view are the pipes the camera and the 2D frame already travel down; rotation is one more field on S.cam.",
      "src/ui/navViewModes/index.js — the driver: modes, buffer, the render-tail hover picker, the keyboard methods, remapClick.",
      "src/ui/navViewModes/picking.js — hit-testing over what the paint published; branches on S.mapProj.kind in exactly one place.",
      "src/generation/NameGenerator.js — generatePlanetName / generateMoonName, whose rng contract state.js does not currently satisfy (AC-10)."
    ],
    "outputs": [
      "The two 240p nav view modes ('rail' and 'bars') as the player drives them — both ship, per Max's 'I like both versions... let's keep both'.",
      "S.cam gains rotX/rotY; d2System gains a real camera; new published-geometry fields on S for every element AC-2 makes pickable and for the AC-5 click-highlight.",
      "The diegetic cockpit nav panel eventually represents these designs — ⛔ but it MUST NOT change here. Two NavComputer instances exist (main.js:4700 and :5895); the gate is activate(), which _openCockpitNav never calls. NOT _bare, which is permanently false on the panel."
    ],
    "mustStayWorking": [
      "wc -l src/ui/NavComputer.js == 4711 and wc -l src/main.js == 15161.",
      "node scripts/extract-nav-designs.mjs --check exits 0 — designs.js matches the lab.",
      "viewMode === null is today's nav, byte for byte.",
      "Suites at baseline: tests/ 20 failed across the same 8 files (agent-camera-api, driver-pack-giantdeck, gas-body-lab-material, lab-shader-perframe-seam, moon-condition-contract, moon-rng-stream-identity, port-condition-contract, relief-octave-lod-ramp); src/cockpit 698 passed; src/ui 520 passed (baseline moved 480 -> 520 when the three build agents landed 40 new tests). ⚠ navViewModes.test.js's 'rail: every level tab changes level when clicked where it is drawn' failed once under a full src/ui run and passed 3/3 in isolation — cross-test pollution, pre-existing, not caused here.",
      "AC-1..AC-11 of the closed nav-menu-elements-functional workstream: the map is a picker at all five levels in both designs; Tab / Enter / [ ] / - = / / stay bound; the prism follows the camera on WASD/R/F and the wheel; the drawn search reuses the whole existing search pipeline.",
      "Hover resolves at the TAIL of the driver's render() — the legacy painters rewrite all three hover fields every frame from _mouseX/_mouseY against LEGACY geometry, so any new pointer state must be written there too or the legacy pass wins.",
      "The legacy renderer keeps running underneath every mode frame ON PURPOSE — it is also the lazy loader (_renderLocal -> _ensureStarsLoaded; _renderSystem resolves _systemData).",
      "_autopilotButtonRect is a live invisible button that _handleClick tests FIRST; any new interactive rect competes with that ordering.",
      "A painter throw freezes the glass — PanelHost catches once, then stops uploading, and the screen keeps showing the last good frame and looks alive. Every new field the designs read needs a default in state.js.",
      "S and D are MUTATED, never replaced — a fresh object leaves every design painting the frame the factory was built on, forever, and it repaints happily."
    ]
  }
}
