# Code review — the new 240p nav menu system (a10adc7^..HEAD), 2026-09-18

Nine finders (inputs, state, host-folds, picking, painters, tests, search, integration, claims) over the 20 files the three nav workstreams added; every non-nit finding put to a refuter and a reproducer (opus), a tie-breaker on disagreement. 93 agents; two verifier calls failed on API safeguards (findings 2 and 5 carry one vote). Raw result: session scratchpad review/result.json.

**Counts:** 37 confirmed · 3 refuted · 13 nits unverified.

## Confirmed

| # | sev | where | finding | pilot-facing | pre-existing |
|---|---|---|---|---|---|
| C1 | defect | `src/ui/navViewModes/index.js:896` | Every design chrome control fires on the release of a drag — worst case a drag released on a search row arms a warp | yes | no |
| C2 | defect | `src/ui/NavComputer.js:4503` | The drawn [ WARP ] button is dead at GALAXY/SECTOR/REGION/PRISM in both designs while ENTER commits there | yes | no |
| C3 | defect | `src/ui/NavComputer.js:4370` | 2D drag-pan moves the map 1.35x too far under design 1 — the legacy navMapSize is used against the design's own square | yes | no |
| C4 | defect | `src/ui/NavComputer.js:620` | The drawn search survives closing the nav computer; reopening lands in the field with the old query and eats every click | yes | no |
| C5 | defect | `src/ui/NavComputer.js:4707` | The mouse wheel is dead at SYSTEM under both designs — _systemZoom is read by nobody the designs draw from | yes | no |
| C6 | risk→nit | `src/ui/NavComputer.js:4488` | Right-click-to-go-back almost certainly never fires, and under a design remapClick would eat it first anyway | yes | yes |
| C7 | risk | `src/ui/NavComputer.js:349` | V pressed mid-drag leaves the gesture's start point in the previous canvas coordinate space | yes | no |
| C8 | risk→defect | `src/ui/NavComputer.js:4413` | _dragStartLadder is never cleared on mouse-up, so a gesture that starts at another level can scrub the SYSTEM ladder from a stale snapshot | yes | no |
| C9 | defect | `src/ui/NavComputer.js:4585` | The _systemMode pin is entry-only: a 'planet' mode acquired on CURRENT survives V into a design and restores the AC-3 second-selection freeze | yes | no |
| C10 | defect | `src/ui/NavComputer.js:589` | _seedViewModeCam is one-way, so V back to CURRENT leaves today's nav drawing the designs' camera angles | yes | no |
| C11 | defect | `src/ui/navViewModes/search.js:179` | The drawn search survives deactivate(), so the nav reopens with the field open and the whole keyboard captured | yes | no |
| C12 | risk→nit | `src/ui/navViewModes/index.js:777` | gaugeDragTo validates py and span but not base, and the caller's `!= null` test admits NaN into _localCenter.y | no | no |
| C13 | risk | `src/ui/navViewModes/state.js:769` | Every background star-load growth rebuilds and re-sorts the whole starRows array, and D.here/D.selStar rescan it every frame | no | no |
| C14 | defect | `src/style.css:1273` | Under a view mode the nav has no mouse close: the X is pushed off-screen and the backdrop disappears | yes | no |
| C15 | risk | `src/ui/NavComputer.js:4705` | The prism wheel clamp was widened to today's nav with no recorded ruling, while AC-3 still claims byte-identity | yes | no |
| C16 | defect | `src/ui/navViewModes/search.js:195` | Drawn-search row hit band sits half a lead above the drawn row: clicking the lower third of a result warps to the NEXT result | yes | no |
| C17 | defect | `src/ui/navViewModes/index.js:365` | Same Math.round off-by-half-row in listRowAt: the bottom 2 of every 6 rail/list texels pick the row below | yes | no |
| C18 | defect | `src/ui/navViewModes/designs.js:1476` | Design 2's SECTOR/REGION status line truncates "CLICK TO ENTER" off the glass at Max's own 417x240 buffer | yes | no |
| C19 | defect | `src/ui/navViewModes/designs.js:1503` | The YOU marker and target diamond are unguarded and paint over the chrome as soon as a 2D level is panned | yes | no |
| C20 | defect | `src/ui/navViewModes/designs.js:1112` | Design 1's SYSTEM screen gives one body two different index tags — the ladder and the rail disagree whenever the system has moons | yes | no |
| C21 | defect | `src/ui/navViewModes/designs.js:1421` | Both designs' chrome is laid out in absolute texels and breaks below ~205 (design 2) / ~241 (design 1) buffer texels — the SYSTEM tab leaves the glass | yes | no |
| C22 | defect | `src/ui/navViewModes/designs.js:127` | `fire()`'s dedup key is cleared every frame by the driver, so one persistent layout violation becomes a console.error at frame rate | no | no |
| C23 | risk→nit | `src/ui/navViewModes/designs.js:1545` | The SECTOR/REGION tile ranking is rebuilt twice per frame in both designs — 512 density evaluations and two 256-element sorts per frame, unmemoised | no | no |
| C24 | defect→risk | `src/ui/__tests__/navAffordances.test.js:183` | AC-10's five CLICK/SELECT probes never click — they assert a hover and stop | yes | no |
| C25 | defect | `src/ui/__tests__/navSearch.test.js:415` | navSearch's 'Shift+Tab at GALAXY wraps to SYSTEM' is green only from a state the pilot cannot reach | yes | yes |
| C26 | risk | `src/ui/__tests__/navViewModes.test.js:136` | The sabotage probe proves the violations plumbing, not the per-draw guard it claims to prove | no | no |
| C27 | risk | `src/ui/__tests__/navPicking.test.js:1535` | 'every published field has a default' is a hardcoded allowlist that never grew — 24 of 39 state fields are outside it | yes | no |
| C28 | risk→nit | `src/ui/__tests__/helpers/headlessNav.mjs:84` | headlessNav rebinds document.createElement globally, so an older nav's offscreen draws land in a newer nav's recorder | no | yes |
| C29 | defect | `src/ui/NavComputer.js:4420` | A drag that ends on a search row arms a warp — the 25-texel drag guard is downstream of remapClick | yes | no |
| C30 | defect | `src/ui/navViewModes/search.js:65` | Ctrl/Meta chords are typed into the query and preventDefaulted — no paste, and Ctrl+R is eaten | yes | no |
| C31 | defect | `src/ui/NavComputer.js:4499` | The drawn COMMIT bar / [WARP] chip is dead to a click at GALAXY, SECTOR, REGION and PRISM — Enter commits there, the button does not | yes | no |
| C32 | defect | `src/main.js:6161` | Backdrop click-to-close is unreachable under a design: the panel covers every pixel of the overlay | yes | no |
| C33 | risk | `src/main.js:6146` | A throw from a design painter permanently stops the overlay's render loop — the glass freezes on the last good frame and still takes input | yes | yes |
| C34 | risk | `src/main.js:5925` | In HELM the N key opens the cockpit panel, which can never take a design — V looks dead | yes | no |
| C35 | defect | `src/ui/NavComputer.js:4364` | The 2D map drag pans at the LEGACY scale under both designs — 1.35x to 2.61x the design's own kpc-per-texel | yes | no |
| C36 | defect | `docs/FEATURES/nav-menu-guide.html:332` | Walk sheet's first row tells Max the click-highlight is amber; the code frames it in INK.KEY (#d8fbff), and amber is the SELECTION colour | yes | no |
| C37 | risk | `docs/WORKSTREAMS/nav-screens-close-pass/contract.json:201` | AC-18's verifyVia and the walk sheet both ask for Tab behaviour in today's nav, where Tab is not bound at all | yes | no |

### Claims as verified

**C1 — Every design chrome control fires on the release of a drag — worst case a drag released on a search row arms a warp** (`src/ui/navViewModes/index.js:896`, inputs)

remapClick runs as the first statement of _handleClick (src/ui/NavComputer.js:4420) while the only "was a drag, not a click" rejection is 76 lines downstream (src/ui/NavComputer.js:4496), so every control remapClick owns fires on a click whose mousedown was hundreds of texels away. The search-row branch (src/ui/navViewModes/index.js:911) reaches _activateSearchHighlight -> _selectSearchResult -> _onCommit({type:'warp'}) — and that is not merely an armed target: src/main.js:5970 dispatchNavAction's warp branch calls _setWarpTargetFromNavStar and then either _enterSystemInstantOrrery() or beginWarpTurn() (src/main.js:6047-6058), so the pan gesture closes the nav and actually starts the jump to a system the pilot never chose. The claim's "arms a warp" is if anything understated on consequence; everything else in it is exact.

*Evidence:* NavComputer.js:4420 calls drv.remapClick(...) before anything else; NavComputer.js:4496 `if (dx*dx + dy*dy > 25) return;` is the only drag rejection and it is reached only after remapClick has already returned. Inside remapClick the acting branches are index.js:911 (search.activate), :928/:929 (scrollLadder), :977 (page), :995 (sortTo), :1002 (recentreOnPlayer) — none of them reads nav._dragStartX/_dragStartY. Only notePick (index.js:244-248) applies the 25-texel test, and it merely suppresses the click-highlight. Measured headless at 417x240, design 1, PRISM: mousedown (20,120) -> mousemove to the pager (340,175) -> mouseup -> click; drag distance^2 = 105745 (4200x the 25 threshold) and S.l

**C2 — The drawn [ WARP ] button is dead at GALAXY/SECTOR/REGION/PRISM in both designs while ENTER commits there** (`src/ui/NavComputer.js:4503`, inputs+picking)

The drawn commit control is dead to the click at levels 0-3 in both designs while ENTER commits there — but TWO conditions block it, not the one the finding names. `_handleClick`'s commit hit-test (NavComputer.js:4503) is guarded by BOTH `if (this._levelIndex === 4)` (:4502) AND `&& this._commitAction` (:4503), and `_commitAction` is structurally null below SYSTEM: `_buildCommitAction` (:1124-1125) returns null without `_selectedBody` && `_systemStar`, and both are only ever set at SYSTEM (assignments at :3048, :4518, :4524, :4568, :4581 are all inside the level-4 branch). Measured null at L0-L3 in all cases. So lifting the `_levelIndex === 4` gate alone would NOT fix this; the click path also needs drv.commit()'s `_selectedNavStar`/`_externalTarget` fallback (index.js:648-662), which is exactly the branch ENTER takes.

*Evidence:* NavComputer.js:4502 `if (this._levelIndex === 4) {` then :4503 `if (this._commitButtonRect && this._commitAction) {`; there is no other _commitButtonRect hit-test in the handler (grep: :267, :1145, :3079, :3088, :3119, :3521, :3527, :4503-4504 only). index.js:313-322 writes nav._commitButtonRect unconditionally every design frame. Measured headless at 417x240 with _selectedNavStar set and _onCommit counted, for mode in {rail,bars} x level in {0,1,2,3}: the published rect was rail {x:0,y:234,w:417,h:5} (from regions().commit, S.chipRect null) and bars {x:374,y:232,w:41,h:8} (S.chipRect, armed:true); a click at the centre of that rect produced 0 commits in all 8 cases, and the very next ENTER 

**C3 — 2D drag-pan moves the map 1.35x too far under design 1 — the legacy navMapSize is used against the design's own square** (`src/ui/NavComputer.js:4370`, inputs)

2D drag-pan uses the legacy navMapSize projection under BOTH designs, so the ground slides at the wrong gain at every 2D level: design 1 GALAXY 1.65x, design 1 SECTOR/REGION 1.35x, design 2 GALAXY 2.61x, design 2 SECTOR/REGION 1.40x. The pan branch of _handleMouseMove (src/ui/NavComputer.js:4362-4371, navMapSize call at :4364, scale at :4365 — not :4370, which is the _densityCacheKey line) has no viewMode awareness. Legacy (viewMode === null) is exact.

*Evidence:* NavComputer.js:4370-4377: `const drawSize = navMapSize(this._canvas.width, this._canvas.height); const scale = this._viewSize / drawSize;`. navLayout.js:154 navMapSize(417,240) = min(417,240) - navMapInset(240) = 240 - 80 = 160. geometry.js railGeometry(417,240,FACE{advance:6,h:5}) gives mapW 252, mapH 216 -> sq 216. Measured headless, design 1 at SECTOR: S.mapProj {ox:18, sq:216}, S.view.size 44 kpc; a 50-texel drag moved _viewCenter.x by 13.75 kpc = 0.275 kpc/texel, against the design's own 44/216 = 0.2037 kpc/texel — ratio exactly 1.35. Design 2's 2D projections are different again ('wide' at level 0, designs.js:1510; 'block' at 1-2, :1562) and are not 160 either. At GALAXY design 1 addit

**C4 — The drawn search survives closing the nav computer; reopening lands in the field with the old query and eats every click** (`src/ui/NavComputer.js:620`, inputs+search)

Closing the nav computer while the drawn search field is open leaves the field open on the driver, so reopening the nav lands the pilot back in the search field with the previous query drawn. Because search.js key() (src/ui/navViewModes/search.js:163-180) consumes every key and NavComputer._onKeyDown's first folded clause (src/ui/NavComputer.js:349) calls stopPropagation(), the reopened nav is keyboard-dead for nav control — V, Tab, Enter, [, ], -, =, , and . are typed into the ghost field, and so is N, so the pilot's normal close key does not close the nav. Recovery is one Escape, or one click on the map (which is eaten and then closes the field). The claim's "eats every click" is overstated — exactly one click is eaten — and the claim's own live repro step "press N to close the nav" is not possible while the field is open; the live trigger is a mouse close (the .overlay-close button or backdrop, src/main.js:6152-6168 -> closeNavComputer :5946 -> deactivate() :5954). No warp can be armed from the stale field in the live game.

*Evidence:* NavComputer.js:620-626 deactivate() calls detachKeys/_hideSearch/_resetPrismLoad and touches no driver state; activate() (:585-588) calls attachKeys, sets viewMode from loadViewMode(), _resizeCanvas, _seedViewModeCam, _showSearch — again nothing that reaches S.search. search.js close() is only called from key() Escape (:163-166), activate() (:143) and remapClick's miss (index.js:914). Measured headless: press '/' then 'S', deactivate(), activate(), render() — S.search.open stayed true with text "s".

**C5 — The mouse wheel is dead at SYSTEM under both designs — _systemZoom is read by nobody the designs draw from** (`src/ui/NavComputer.js:4707`, inputs)

At SYSTEM (level 4) under both designs the mouse wheel moves nothing on the glass — but the value is not discarded. `_systemZoom` persists across the V cycle, so a wheel turn at SYSTEM under a design silently re-zooms TODAY'S nav orrery, which the pilot sees the moment V lands back on CURRENT. Also: under design 1 there is no orrery at SYSTEM at all (it draws `d1Ladder`, a list), so the wheel there has no picture to act on rather than a picture it fails to move; under design 2 the orrery's radius budget is `min(W/2-8, (mapH/2-4)/sin(rotX))` (designs.js:1759) — pane geometry only, with no zoom term. The cited line NavComputer.js:4707-4708 is PRE-EXISTING (59ea3ffd, 2026-03-28, an ancestor of a10adc7^); what the reviewed range changed is that the designs overpaint the legacy orrery, so the wheel's effect stopped being visible. No AC in any of the three contracts asks for a SYSTEM wheel zoom (nav-menu-elements-functional/contract.json:57-58 and :146 name the wheel only at PRISM).

*Evidence:* NavComputer.js:4707-4709 `} else if (this._levelIndex === 4) { const factor = …; this._systemZoom = …}`. grep for sysZoom/systemZoom across src/ui/navViewModes/{state,designs}.js returns only comments about _systemZoomAnim plus state.js:395/730-731 (rotX/rotY only) and designs.js:1742 `const sc = S.sysCam || {}`. No design reads a SYSTEM zoom. For contrast the PRISM branch (:4704-4705) writes _localRadius, which state.js does pipe into S.cam.radius, so the wheel is alive at PRISM. The wheel is also inert at levels 0-2 in every look, but that is pre-existing legacy behaviour (the handler has no 0-2 branch).

**C6 — Right-click-to-go-back almost certainly never fires, and under a design remapClick would eat it first anyway** (`src/ui/NavComputer.js:4488`, inputs)

The `e.button === 2` escape branch at src/ui/NavComputer.js:4488-4491 is unreachable in any current browser, and it is the ONLY production caller of `handleEscape()` — so that method is dead from the mouse. This is PRE-EXISTING (the branch dates to b0f3cae, 2026-03-22; the main.js:13594-13596 comment asserting it survives dates to 1a9628d, 2026-07-29 — both outside a10adc7^..HEAD). The in-range half of the finding (remapClick pre-empting it under a design) is real as written but MOOT, because the click it would pre-empt never arrives. Nothing in the UI or the lab spec ever told the pilot right-click goes back (the only mention repo-wide is the code comment at :4487), and the pilot keeps two working level-back routes — the tab strip in every look, plus Tab/Shift-Tab under a design — so nothing is lost at the glass. What is actually wrong is documentation: main.js:13594-13596 and src/ui/__tests__/NavComputer.escape.test.js:184-190 both justify keeping `handleEscape()` on an affordance that does not exist.

*Evidence:* NavComputer.js:334 `canvas.addEventListener('click', this._handleClick.bind(this))`; :336 `canvas.addEventListener('contextmenu', e => e.preventDefault())`; no 'auxclick' or 'mouseup'-based escape anywhere (grep). _handleClick:4488 `if (e.button === 2) { this.handleEscape(); return; }` sits after the autopilot test and the tab-strip branch. main.js:13594-13596: "`handleEscape()` is NOT deleted: right-click-to-go-back still calls it (NavComputer._handleClick, `e.button === 2`)".

**C7 — V pressed mid-drag leaves the gesture's start point in the previous canvas coordinate space** (`src/ui/NavComputer.js:349`, inputs)

V pressed while the mouse button is held swaps the canvas backing store without rebasing the gesture's start point, so the next mousemove applies a several-hundred-texel delta — and the release is also read as a drag, so the click that ends the gesture is discarded. Measured headlessly on a 2021x1162 CSS box: at PRISM, CURRENT->RAIL with a completely stationary pointer at clientX 1400 moved _localRotY 0.3 -> -8.589 (delta -8.89 rad, ~1.4 revolutions); at SYSTEM, BARS->CURRENT at clientX 300 moved _systemRotY 0.2 -> 2.105 (+1.90 rad, ~109 deg); at REGION the 2D pan moved _viewCenter from {x:8,z:0} to {x:313.6,z:-130.9} with _viewSize 44 ly, i.e. the map is thrown ~340 ly outside its own window. RAIL->BARS is exactly 0 (both designs share the 417x240 buffer), confirming that two of the three V transitions cross the boundary. The claim's "~10 rad" estimate is accurate. Two additions the claim missed: (a) NavComputer.js:4496 (`if (dx*dx + dy*dy > 25) return`) differences the same stale _dragStartX, so press-hold, V, release with NO pointer motion at all discards the click (measured dist2 787301 vs threshold 25) — that path needs no mousemove; (b) _mouseX/_mouseY are named in the claim's evidence but are not part of the damage — the next mousemove overwrites them before anything reads them. Correction to the claim's repro note: a headless repro IS possible. helpers/headlessNav.mjs:getBoundingClientRect closes over the CONSTRUCTION-time width/height as constants while _resizeCanvas mutates canvas.width/height, which is exactly the browser's behaviour (CSS box fixed, backing store swapped), so the store swap reproduces in plain node. Damage is recoverable without a reload (a level change re-runs _applyLevelView; re-entering a mode re-runs _seedViewModeCam which zeroes _localRotY/_systemRotY), and nothing is persisted or lost.

*Evidence:* NavComputer.js:349, V clause: `{ this.viewMode = nextViewMode(this.viewMode); saveViewMode(this.viewMode); this._resizeCanvas(); this._seedViewModeCam(); }` — no _dragging/_dragStartX handling. _resizeCanvas (:612-618) assigns canvas.width/height from drv.bufferFor when a mode is on and from getBoundingClientRect when it is not. _handleMouseMove (:4342) recomputes p through _getCanvasPos against the NEW store and differences it against the stale _dragStartX (:4352-4353, :4372-4377). _handleMouseUp (:4413) clears _dragging but the damage is applied on the intervening moves. Two of every three V presses cross the CURRENT/design boundary, and Max cycles V constantly during an A/B.

**C8 — _dragStartLadder is never cleared on mouse-up, so a gesture that starts at another level can scrub the SYSTEM ladder from a stale snapshot** (`src/ui/NavComputer.js:4413`, inputs)

`_handleMouseUp` (src/ui/NavComputer.js:4413-4416) clears `_dragging`, `_panStartCenter`, `_gaugeDrag` and `_counterDrag` but not `_dragStartLadder`, which is assigned in exactly two places — the constructor default (:291, `null`) and the level-4 branch of `_handleMouseDown` (:4405). So after any press taken at SYSTEM under design 1 the field stays a number forever. If the level then becomes 4 while a *later* button-press is still held, the move handler's `viewMode === 'rail' && this._dragStartLadder != null` clause (:4355) is true and writes `S.ladderScroll = clamp(staleSnapshot - (p.x - this._dragStartX))` — a snapshot from the earlier gesture differenced against this gesture's start X — so the ladder window jumps, by up to the whole of `S.ladderMax`, before tracking the pointer normally. Two corrections to the original wording: (a) Tab does not "fire through `_handleClick`" from the key handler — `_onKeyDown` (:349) calls `driver.tabLevel()` (src/ui/navViewModes/index.js:582), which synthesises the click into `_handleClick`; same net effect, different path; (b) more importantly, the trigger is not keyboard-only. The PRISM→SYSTEM drill is a 400 ms `_systemZoomAnim` that sets `this._levelIndex = 4` mid-frame at :1336-1338, and neither `_handleMouseDown` nor `_handleMouseMove` is gated on it (`_handleClick`'s `if (this._anim) return` guards a different field), so press-and-hold during that zoom reaches the same stale write with no key pressed at all.

*Evidence:* NavComputer.js:4413-4414 `_handleMouseUp() { this._dragging = false; this._panStartCenter = null; this._gaugeDrag = false; this._counterDrag = false; }` — _dragStartLadder absent. :4405 is the only assignment outside the constructor default at :291. :4355 is the consumer. The mouse-up comment explicitly reasons about exactly this hazard for _counterDrag ("press the counter, press V to design 2 or Tab to PRISM, and nothing rearms it") and stops one field short.

**C9 — The _systemMode pin is entry-only: a 'planet' mode acquired on CURRENT survives V into a design and restores the AC-3 second-selection freeze** (`src/ui/NavComputer.js:4585`, state)

`_systemMode` is pinned only at the two entry sites (`if (!this.viewMode) this._systemMode = 'planet'`, NavComputer.js:4585 and :4590) and nothing resets it when V switches into a design (:349 sets only `viewMode`, `saveViewMode`, `_resizeCanvas`, `_seedViewModeCam`). A 'planet' mode entered on today's nav therefore survives into design 1/2. In the CURRENT system this restores the AC-3 freeze: `_handleClick`'s level-4 branch takes the `_systemMode === 'planet'` path at :4512 and its planet arm (:4522-4526) writes `planetIndex: this._selectedPlanetIdx` — the previously selected planet — ignoring `_hoveredBody.index`, for every subsequent body click, and `_buildCommitAction` builds a BURN to that frozen planet. In a FOREIGN system the same stale mode instead takes the `else` at :4536-4538, costing one dead click before it self-heals.

*Evidence:* NavComputer.js:4585 pins on entry only; :4512-4527 is the 'planet' branch, whose planet arm (:4522-4526) ignores `_hoveredBody.index` and writes `planetIndex: this._selectedPlanetIdx`. The only resets are the constructor (:190), `_applyNavArrival` (:605), the tab-strip level change (:4469), Escape (:1457-1458, :1474) and the prism drill (:4617) — none of which the V key touches (V's clause on :349 sets `this.viewMode`, calls `saveViewMode`, `_resizeCanvas()` and `_seedViewModeCam()` and nothing else). Under a design the designs still publish every body as pickable (`S.bodyHits`, designs.js:1145/1894) so the click resolves a `_hoveredBody` of type 'planet' and reaches :4512. state.js:856-863 

**C10 — _seedViewModeCam is one-way, so V back to CURRENT leaves today's nav drawing the designs' camera angles** (`src/ui/NavComputer.js:589`, state+host-folds+claims)

`_seedViewModeCam()` opens `if (!this.viewMode) return;` and then overwrites `_localRotX/_localRotY/_systemRotX/_systemRotY` with the designs' defaults, but no site anywhere restores the legacy values — so after a single V press the LEGACY nav draws its prism at rotX 0.6521714 / rotY 0 instead of 0.5 / 0.3 and its orrery at rotX 0.4334453 instead of 0.5, for the rest of the session. That falsifies 'viewMode === null is byte-identical to the pre-workstream nav' on exactly the gesture Max uses to A/B the three looks.

*Evidence:* NavComputer.js:589 is the whole setter; it is called from :349 (the V clause, after `this.viewMode = nextViewMode(...)`) and from :587 (activate, after `loadViewMode()`). The legacy seeds are :159-160 (`_localRotX = 0.5`, `_localRotY = 0.3`) and :188-189 (`_systemRotX = 0.5`, `_systemRotY = 0.0`); grep for assignments to those four fields finds only the constructor, `_tiltAnim` (:1348), the two drag clamps (:4354, :4360), the prism drill (:4685-4686) and :589. `deactivate()` (:620-626) restores nothing either, so closing and reopening the nav does not clear it. The drill at :4686 recovers `_localRotX` to 0.5 only when `viewMode` is null AND the pilot reaches PRISM by drilling; a Tab into PRI

**C11 — The drawn search survives deactivate(), so the nav reopens with the field open and the whole keyboard captured** (`src/ui/navViewModes/search.js:179`, state+integration)

`search.js:163-180` `key()` returns true for every keystroke while `S.search.open` is true — printable keys become query text and the trailing `return true` at :179 consumes named keys too. `NavComputer.js:349`'s first view-mode clause then calls `e.preventDefault(); e.stopPropagation(); return;`, and that listener is registered at `NavComputer.js:565` as a DOCUMENT-CAPTURE keydown handler, so it ends propagation before `main.js:13438`'s window-BUBBLE listener — which holds the N toggle at `main.js:13539` — is ever reached. While the drawn field is open, N, Tab, V, `[`, `]`, `-` and `=` are all inert; Escape (`search.js:169`) is the only route out. `deactivate()` (`NavComputer.js:620-626`) never closes the field and `_viewDriverInst` is nulled only in the constructor (`:255`), so closing the nav with the X button or the backdrop (`main.js:6152-6168` → `toggleNavComputer` :6064 → `closeNavComputer` :5946) and reopening it lands the pilot back in an open drawn search with the stale query still typed. The drawn ROWS survive too (not "no rows"), but `_hideSearch` (`:742-751`) has cleared `nav._searchResults` in the real overlay, so those visible rows select nothing until he types again.

*Evidence:* search.js:174-179 — printable keys are consumed and the trailing `return true` consumes every named key too ('KeyN' → e.key 'n' → printable). NavComputer.js:349's first view-mode clause is `searchActive?.() === true && searchKey?.(e) === true` → `e.preventDefault(); e.stopPropagation(); return;`. The listener is registered at NavComputer.js:565 as `document.addEventListener('keydown', this._onKeyDown, true)` (capture on document); main.js:13438 is `window.addEventListener('keydown', ...)` with no capture flag, so it runs in the bubble phase at window — which a stopPropagation during the document capture phase prevents from ever being reached. main.js:13539's N toggle lives inside that listen

**C12 — gaugeDragTo validates py and span but not base, and the caller's `!= null` test admits NaN into _localCenter.y** (`src/ui/navViewModes/index.js:777`, state)

`gaugeDragTo` (src/ui/navViewModes/index.js:777-781) guards `py` and `r.span` but not `r.base`, so a non-finite `base` returns a non-finite number instead of `null`, and NavComputer.js:4349's `if (ky != null)` admits it into `_localCenter.y` (R/F at :1392-1393 cannot climb back out of NaN). The mechanism is exact, but the finding's premise that `base` comes from "`nav._playerY` unchecked" is wrong: `_playerY` has exactly two writers — NavComputer.js:175 (`= 0`) and NavComputer.js:1180 `this._playerY = galacticPos.y || 0` — and `NaN || 0 === 0`, so NaN is scrubbed at the sole entry point. Only ±Infinity survives `|| 0`, and it yields Infinity (not NaN) from the clamp; its only production feeder is main.js:5778 `nav.setPlayerPosition(playerGalacticPos || {x:8,y:0,z:0})`, i.e. a ship position already broken upstream of the nav. So this is missing defensive validation with no reachable production trigger: adding `Number.isFinite(r.base)` at index.js:779 (and/or `Number.isFinite(ky)` at NavComputer.js:4349) is one-token hardening, not a fix for a live defect. The finding's secondary observation is separately correct and worth keeping: state.js:746 rebuilds `D.player` as a fresh object literal every frame, so `D.ready = !!(D.gm && D.player)` (state.js:864) reduces to `!!D.gm` and does not gate the player half it appears to.

*Evidence:* index.js:777-780 — `if (!r || !Number.isFinite(py) || !(r.span > 0)) return null;` then `const k = r.base + ((r.cy - py) / r.span) * r.halfKpc;` and `Math.max(r.base - r.halfKpc, Math.min(r.base + r.halfKpc, k))`, which is NaN for a NaN base. designs.js:1009 publishes `base: D.player.y`. state.js:746 `D.player = { x: nav._playerX, y: nav._playerY, z: nav._playerZ }` and :864 `D.ready = !!(D.gm && D.player)` — D.player is a fresh object every frame and is never null, so the `D.ready` gate at index.js:288 is effectively `!!D.gm` and does not guard the player half it appears to. NavComputer.js:4349 `const ky = this._viewDriverInst.gaugeDragTo(p.y); if (ky != null) this._localCenter.y = ky;`. Re

**C13 — Every background star-load growth rebuilds and re-sorts the whole starRows array, and D.here/D.selStar rescan it every frame** (`src/ui/navViewModes/state.js:769`, state)

Every in-place growth of `_localStars` makes the adapter re-spread and re-sort the whole star list, and at least one O(N) scan of that list runs on every design frame regardless of any cache. Reproduced headlessly at N≈27.5k: 60 growth batches → 60 rebuild frames, mean 4.5 ms, worst 10.7 ms (node, this machine); steady non-growth frames cost 0.26–0.72 ms. Correction to the original wording: `D.here` does NOT run "a find plus a reduce" every frame — the find is skipped entirely when `nav._currentSystemName` is null (the ternary yields null), and the reduce is skipped when the name lookup HITS. What is unconditional is exactly one full-array scan per frame (reduce on null-name or name-miss; find on name-hit), plus a second find when a star is selected. All of it only under a design (viewMode non-null); LEGACY never calls refresh.

*Evidence:* state.js:766-780 — `if (cache.starsRef !== D.stars || cache.starsLen !== D.stars.length) { ... cache.starRowsBase = D.stars.map((s) => ({ ...s, name: nameFor(s), pc: ..., ly: ... })); }` then an unconditional `slice().sort()` on the next key change. state.js:782-786 `D.here = (nav._currentSystemName ? D.starRows.find(...) : null) || D.starRows.reduce(...)` — the reduce is skipped only when the name lookup hits, so a hash-grid system whose generated name differs pays the full O(N) reduce every frame. state.js:789 `D.selStar = sel ? (D.starRows.find((r) => r.seed === sel.seed) || {...}) : null`. The background expander pushes into `_localStars` in place at NavComputer.js:3764 (identity stable,

**C14 — Under a view mode the nav has no mouse close: the X is pushed off-screen and the backdrop disappears** (`src/style.css:1273`, host-folds+integration)

Under a view mode (`viewMode` non-null) the nav overlay has no pointer route out on desktop: `.nav-computer-panel.nav-lowres { width:100vw; height:100vh }` (src/style.css:1273-1276, NEW in the range) pushes `.overlay-close` entirely above the viewport (0 visible rows, no hit anywhere in its box) and leaves no uncovered pixel of `#nav-computer-overlay`, so `if (e.target === navEl) toggleNavComputer()` (src/main.js:6161-6166) can never fire. Escape (src/main.js:13583) and N (src/main.js:13539) still close it, so this is a lost affordance, not a stuck state — but neither design's non-search hint row names ESC or N (src/ui/navViewModes/designs.js:649-656), so nothing on the glass tells the pilot how to get out once the X he has always used is gone.

*Evidence:* src/style.css:1213-1222 `#nav-computer-overlay { position:fixed; inset:0; display:flex; align-items:center; justify-content:center }` with no padding. src/style.css:1224-1230 legacy panel = `calc(100vw - 40px)` x `calc(100vh - 40px)`, so today the panel top is 20px and the button at src/style.css:1232-1236 (`position:absolute; top:-40px; right:0`, 40px tall per src/style.css:356-370) spans viewport y in [-20,20] — its bottom half is on screen and clickable, and the 20px gutter is live backdrop. src/style.css:1273-1276 (new) sets the panel to 100vw x 100vh, so panel top becomes 0, the button spans y in [-40,0] (zero visible rows), and the gutter is gone. Backdrop close: src/main.js:6161-6163 

**C15 — The prism wheel clamp was widened to today's nav with no recorded ruling, while AC-3 still claims byte-identity** (`src/ui/NavComputer.js:4705`, host-folds)

`_handleWheel`'s lower clamp moved from 0.002 to 0.0015 (src/ui/NavComputer.js:4705) with no `viewMode` gate, so the legacy nav AND the cockpit NAV panel change behaviour, and the change carries no Max ruling — only an agreement between code owners at docs/WORKSTREAMS/nav-menu-elements-functional/INTERFACE.md:176-178. The pilot-facing effect in legacy is strictly a fix, not a loss: at the PRISM entry radius 0.0015 (set at :1189 and :4680) one wheel-down used to land on 0.002 (+33% snap-out) and one wheel-UP also landed on 0.002 (scrolling IN zoomed OUT 33%); both now behave smoothly, and the deepest reachable zoom moves from 0.002 to 0.0015. CORRECTION to the claim's framing: this is not what first falsified nav-fullscreen-view-modes AC-3's "byte-identical" progress field. AC-18's ruled widening already did, visibly, at src/ui/NavComputer.js:4187 — in today's nav the SYSTEM tab now draws at alpha 0.15 instead of the inactive 0.4 when `_currentSystemData` is null, which is a pixel change on the very open-and-compare AC-3's verifyVia.input describes. So AC-3's progress field is stale for two reasons: one ruled by Max ("3 yes"), one not. What is unique to the wheel clamp is the missing ruling, not the breach of byte-identity.

*Evidence:* src/ui/NavComputer.js:4705 `this._localRadius = Math.max(0.0015, Math.min(this._localCubeSize || 0.01, this._localRadius * factor));` — no `this.viewMode` in the expression. Recorded only at docs/WORKSTREAMS/nav-menu-elements-functional/INTERFACE.md:176-178 ("Fix the clamp to `0.0015`"), which is an agreement between the three code owners, not a Max ruling. Contrast docs/WORKSTREAMS/nav-screens-close-pass/contract.json AC-18, whose statement carries "⭐ WIDENED THE SAME DAY on Max's word (*\"3 yes\"*) to TODAY'S NAV and the COCKPIT PANEL". nav-fullscreen-view-modes/contract.json AC-3 verifyVia.observable: "Byte-identical. viewMode defaults to null and null is today's path by construction".

**C16 — Drawn-search row hit band sits half a lead above the drawn row: clicking the lower third of a result warps to the NEXT result** (`src/ui/navViewModes/search.js:195`, picking+search)

`rowAt` (src/ui/navViewModes/search.js:195) inverts the paint's row formula with `Math.round`, which centres the hit band ON row i's glyph top instead of spanning the row's 6 drawn texels, putting the pickable band 2 texels ABOVE what the pilot sees. Measured headlessly at 427x240, both designs, query "tau": searchGeom {top:12, lead:6}; row 0's glyph ink is 18..22 and its selection plate 17..22, while `rowAt` resolves row 0 only for y 15..20. Two consequences, both confirmed by driving the real handlers: (a) a click at y=21 or 22 — the bottom 2 of the 6 texels the pilot reads as row 0 — resolves to row 1 and, via index.js:942-944 → search.activate → NavComputer._selectSearchResult:877 → `_onCommit({type:'warp'})`, arms a real warp to the WRONG star and closes the nav (measured: clicked row 0 "Alpha Centauri", got "Tau Gomerkaro 5538"); (b) a click at y=15 or 16 — texels of the QUERY LINE itself, which is drawn at top..top+4 = 12..16 — resolves to row 0 and warps, so the text field the pilot clicks to focus is a live warp button. Consequence (b) is not in the original claim. The same defect exists for the LIST picker, also not in the claim: makeViewModeDriver.listRowAt (src/ui/navViewModes/index.js:364, and the bars fallback at :367) uses the identical `Math.round((y - top - lead)/lead)` against `S.listGeom`, which designs.js:1293 (d1Rail) and designs.js:1686 (d2List) publish with the same `top + (i+1)*lead` row convention — measured at level 0 design 1: listGeom {top:6, lead:6}, row 0 ink at 12 (plate 11..16), band 9..14, so the bottom 2 texels of every drawn list row drill the row below. List mis-picks drill a wrong sector/star (recoverable with Escape); search mis-picks commit a warp.

*Evidence:* src/ui/navViewModes/search.js:195 `const i = Math.round((y - g.top - g.lead) / g.lead);` vs the draw site src/ui/navViewModes/designs.js:1345-1348 (`const yy = qy + (i + 1) * LEAD; if (sel) rect(g, x - 1, yy - 1, w + 2, LEAD, ...)`) and the publication designs.js:1362 (`top: qy, lead: LEAD`). Consumed at src/ui/navViewModes/index.js:942-944 (`const row = search.rowAt(p.x, p.y); if (row >= 0) search.activate(search.resultIndexOf(row));`). Measured headlessly at 427x240, design 1, PRISM, query "A": searchGeom {top:12, lead:6, rows:21}; drawn row 0 ink 18..22, plate 17..22; a click at y=21 (inside the plate) resolved to row 1 and fired `_onCommit` with star "Alpha Vramdabes 6240" instead of row

**C17 — Same Math.round off-by-half-row in listRowAt: the bottom 2 of every 6 rail/list texels pick the row below** (`src/ui/navViewModes/index.js:365`, picking+picking)

`listRowAt` (src/ui/navViewModes/index.js:365) centres its pick band on the row's text origin, while `d1Rail` draws the row's cell one texel above it: the band is [rowTop-3, rowTop+3) against a drawn cell of [rowTop-1, rowTop+5). The bottom 2 texels of every drawn rail row therefore hover and drill the row beneath it, and the 2 texels above the row (the previous row's own glyph area) drill this row. Confirmed for design 1's rail at PRISM and at GALAXY, for hover and for click. Design 2's `L` list shares the convention by code (designs.js:1668-1670 draws at `mapY+4+(i+1)*LEAD` with the plate at `y-1`, publishes `top: mapY+4` at :1686) but was not exercised headlessly — design 2 at PRISM publishes no `listGeom` until list mode is on. Legacy (viewMode === null) never calls this path.

*Evidence:* src/ui/navViewModes/index.js:365 (and the two geometry fallbacks at :370 and :374 use the identical `Math.round`); paint sites src/ui/navViewModes/designs.js:1257-1259 (`const yy = y + (i + 1) * LEAD; if (L.sel) rect(g, x - 1, yy - 1, w + 2, LEAD, ...)`) and the publication designs.js:1293. Measured headlessly at 427x240, design 1, PRISM: listGeom {top:6, lead:6}; sweeping `_handleMouseMove` across row 2 (rowTop=24) gave starRows[2] for dy=-3..+2 and starRows[3] for dy=+3..+5 — i.e. the last 2 texels of row 2's own glyphs name row 3. The existing suite only ever probes `lg.top + (i+1)*lg.lead` exactly (navPicking.test.js:102-106 `rowPoint`), which is the one texel the bug cannot show at.

**C18 — Design 2's SECTOR/REGION status line truncates "CLICK TO ENTER" off the glass at Max's own 417x240 buffer** (`src/ui/navViewModes/designs.js:1476`, painters)

At SECTOR and REGION under design 2, `d2Status` (src/ui/navViewModes/designs.js:1476) interpolates `rankMax(d1TileRows(...))` raw — the one count in either design not passed through `fmtK` — and the assembled 72-char clause list measures 431 texels against a 364-texel window at Max's 417-wide buffer, so `fit()` (designs.js:1452) eats the trailing affordance: the bar reads "... 22124140697 SYSTEMS IN BEST TILE · CL". `fmtK` alone does not close the gap (395 texels, still over); the clause list is over budget too.

*Evidence:* designs.js:1476 builds `` `${rankMax(d1TileRows(levelView(S.level), levelView(S.level).n))} SYSTEMS IN BEST TILE` `` — the one count in either design not passed through `fmtK` (compare designs.js:1474 at GALAXY, `fmtK(estStars(...))`, and designs.js:1185/1200/1225/1229 in design 1's rail). designs.js:1449-1452 joins the clauses and draws `fit(full, avail)` with `avail = W - 8 - chipW - 4` = 364 texels = 60 chars at W=417. Measured headless (makeHeadlessNav 417x240, bars, level 1/2, drawPixelText injected): level 0 draws "GALAXY · TAU VELA-94 · 69.6M SYSTEMS · CLICK TO ENTER" complete; level 1 draws "SECTOR · TAU VELA-94 · 22124140697 SYSTEMS IN BEST TILE · CL" and level 2 "REGION · TAU VELA-

**C19 — The YOU marker and target diamond are unguarded and paint over the chrome as soon as a 2D level is panned** (`src/ui/navViewModes/designs.js:1503`, painters)

Design 1 only: `d1TwoD` draws the YOU cell frame + 3x3 block (designs.js:826-828) and the target diamond (designs.js:829) through the map transform with no cull, so the host's 2D pan (live at levels 0-2) walks them out of the 216-texel square and onto the design's own chrome — the status row and the ranked rail — where they survive to the final frame. Design 2's half of the claim is false: `d2TwoD` does issue the same unguarded fills into the topbar/botbar band, but `drawDesign2` repaints the topbar background + rule (designs.js:1418) and the bottom bar (designs.js:1446) AFTER the map painter (called at designs.js:1403), so zero of those texels survive. The supporting observations about the guard are correct: `assertMark` is called from exactly 3 sites, all inside `d2System` (designs.js:1780, 1811, 1850; lab 2056/2087/2126), and every mark in d1TwoD/d1Prism/d1Ladder/d2TwoD/d2Prism is unguarded.

*Evidence:* design 2: designs.js:1502-1504 draws `frame(px-2, py-2, 5, 5)` plus four ticks at `toY(z) = mapY + mapH/2 + ((z - v.cz)/v.size) * W`, with no bounds test — while the sector dots two lines above (designs.js:1499) ARE culled. Measured at 417x240: map band is y=[8..232], BAR=8, and the five tab labels sit at y=1..6; `view.cz = 12` puts the marker at y = 6.3, i.e. inside the topbar. `_handleMouseMove`'s pan branch (NavComputer.js:4363-4370) scales by `navMapSize` = 160 at 240p against `_viewSize` = 44, so 12 kpc is a 44-texel vertical drag. design 1: designs.js:824-828 draws the cell `frame` and the 3x3 YOU block at `toX/toY` with the same absence; measured geometry cols=69, railC=25, square x=[

**C20 — Design 1's SYSTEM screen gives one body two different index tags — the ladder and the rail disagree whenever the system has moons** (`src/ui/navViewModes/designs.js:1112`, painters)

Design 1's SYSTEM screen spells the same 35-character tag alphabet twice and feeds it two different indices: the ladder (designs.js:1112) indexes `shown = D.bodies.filter(b => b.kind !== 'moon')` (designs.js:1040) while the rail (designs.js:1244) indexes `D.bodies` (moons included). state.js:530 pushes each moon immediately after its parent planet, so the two indices diverge at the first MOON — i.e. every non-moon body drawn AFTER the first moon carries a different tag on the two halves of one screen (the moon-bearing planet itself still agrees). Reproduced headless at 417x240, design 1, level 4, one drawDesign1 call: bodies Ulsakafa/Moony Ultima/Moony d/Moony d I(m)/Moony Minor/Moony Minor I(m)/Ar(m)/Moony f — ladder drew "1 2 3 4 5", rail drew "1 ULSAKAFA","3 MOONY D","4- MOONY D I","5 MOONY MINOR","7- AR","8 MOONY F"; MOONY MINOR is "4" on the ladder and "5" in the rail, MOONY F "5" and "8". Both painters run in the same frame (designs.js:633 ladder, :636 rail). The tag is display-only — picking resolves by object/index (index.js:395 pickFromRow, designs.js:1100 hits carry `ref: b`), so nothing mis-targets; what breaks is the pilot's cross-reference from a rail row to its ladder mark. Fix belongs in nav-240p-lab.html:1388/:1520, since `node scripts/extract-nav-designs.mjs --check` exits 0 and would break if designs.js alone were edited. Secondary claim (35-char alphabet vs ~39 Sol bodies producing '?') NOT verified: alphabet length confirmed by inspection and state.js:465's comment says "39 of 39 on Sol", but Sol was not loaded and counted; it also requires paging past index 35 in a ~13-row rail to become visible.

*Evidence:* designs.js:1112 `const tag = '123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ'[i]` inside `shown.forEach` where `shown = D.bodies.filter((b) => b.kind !== 'moon')` (designs.js:1040); designs.js:1244 uses the identical literal indexed `off + i` over `D.bodies.slice(...)`. state.js:494-533 pushes each moon immediately after its planet, so the two indices diverge at the first moon. Measured headless at 417x240 (design 1, level 4, bodies MERCURY/VENUS/EARTH/LUNA/MARS/PHOBOS/DEIMOS/JUPITER): the ladder drew tags "1","2","3","4","5" while the rail drew rows "1 MERCURY","2 VENUS","3 EARTH","4- LUNA","5 MARS","6- PHOBOS","7- DEIMOS","8 JUPITER" — MARS is "4" on the ladder and "5" in the rail, JUPITER "5" and "8

**C21 — Both designs' chrome is laid out in absolute texels and breaks below ~205 (design 2) / ~241 (design 1) buffer texels — the SYSTEM tab leaves the glass** (`src/ui/navViewModes/designs.js:1421`, painters)

Both designs lay their chrome out in absolute texels that never consult the buffer width. Design 2's tab strip advances by measured glyph widths from a fixed x=4 (designs.js:1421-1427), so the SYSTEM tab always occupies x=162..197: it clips progressively from W=197 down (5 texels lost at W=192, 29 at W=168) and is entirely off-glass at W<=162. The same fixed tx=203 collapses the locator's fit budget (`fit(loc, W - tx - 6)`, designs.js:1434), so for W<=209 the "HERE · SECTOR" readout — design 2's only statement of where you are — is fitted to the empty string and disappears, and S.locatorRect goes zero-width. Design 1 places its ident at 1, its sector at 1+17*CELL and right-aligns TGT against a 34*CELL fit (designs.js:620-626), all absolute, so the status row overprints for W<=240 with this data (12 texels of overlap at exactly 240) and the sector name overflows the region below W=156; with a longer sector name the overlap threshold rises to roughly W~290. Buffer width is round(panelAspect * lines) with lines >= 240 (index.js:139-146, renderLines.js:63), so a 900x1080 browser window already yields W=198. The pilot loses the SYSTEM tab as a CLICK target only — Tab/Shift+Tab still reach SYSTEM, because tabIndexAt reads barsGeometry's equally W-independent tab table and _getCanvasPos does not clamp (NavComputer.js:349/4334, index.js:582, geometry.js:63-86).

*Evidence:* Swept headless, W = 168..432 step 12 at 240 lines, both designs, levels 0/3/4, collecting onViolation. Design 2 fires at every level for W <= 192: "tab SYSTEM overflows topbar — right by 29.0 texel(s). ink 35.0x5.0 at (162.0,1.0); region 168x8 at (0,0)" (designs.js:1421-1427, `tx += w + 6` from `tx = 4`, total 203 texels for the five labels), and at W = 204 fires "tab strip vs locator collides inside topbar" (designs.js:1436). Design 1 fires at every level for W <= 240: "status ident/sector vs TGT collides inside status — the left block ends at 174 and the right block starts at 162, 12 texel(s) of overlap" — designs.js:620/622/626 place the ident at 14*CELL, the sector at 1 + 17*CELL and rig

**C22 — `fire()`'s dedup key is cleared every frame by the driver, so one persistent layout violation becomes a console.error at frame rate** (`src/ui/navViewModes/designs.js:127`, painters)

`fire()`'s per-condition dedup set is cleared every frame by `render()`, so one persistent layout violation produces a `console.error` on every animation frame for as long as the nav is open. Measured 1-2 `console.error` per frame, steady and undecaying, at narrow buffer widths. The dedup key still suppresses an exact-duplicate message WITHIN a single frame, so "its key is doing no work" is slightly overstated; its intended once-per-condition lifetime is what is lost. The port deviates from its own spec here: nav-240p-lab.html:2487 clears REGIONS and _violations per frame but deliberately does NOT clear _fired, and `scripts/extract-nav-designs.mjs --check` exits 0 because `resetViolations` is hand-written port plumbing outside the extracted region. Not reachable at Max's current window (417-texel buffer = 0 violations); it needs a panel aspect below ~1.0 (design 1) / ~0.83 (design 2).

*Evidence:* designs.js:125-132 dedups on `_fired`; designs.js:1900 `resetViolations: () => { _violations = 0; _fired.clear(); }`; index.js:290 calls `designs.resetViolations()` inside `render()`, i.e. once per painted frame, immediately before the painters run. The `onViolation` handler at index.js:113-116 pushes to `violations` and calls `console.error(line)`. Combined with the finding above, a window narrower than ~205/241 texels means a continuous 60+/s `console.error` stream for the whole time the nav is open; Chrome captures a stack per `console.error`, and the effect is worst with DevTools open, which is exactly when someone is looking. The dedup as written can only ever fire once per *frame*, nev

**C23 — The SECTOR/REGION tile ranking is rebuilt twice per frame in both designs — 512 density evaluations and two 256-element sorts per frame, unmemoised** (`src/ui/navViewModes/designs.js:1545`, painters)

`d1TileRows` (src/ui/navViewModes/designs.js:881) is called twice per painted frame at levels 1-2 in BOTH designs with identical arguments, so half of its 128 (level 1) / 512 (level 2) `potentialDerivedDensity` evaluations and one of its two 256-element sorts are a straight duplicate, unmemoised, next to two memoised helpers. MEASURED COST, which the original claim overstates: one `d1TileRows` at n=16 is 0.385 ms in node (0.347 ms density + 0.038 ms sort) and at n=8 is 0.125 ms, so the duplicated half is ~0.39 ms/frame at REGION and ~0.13 ms/frame at SECTOR — about 2.3% and 0.8% of a 16.7 ms budget, not the 12-24% the original evidence implies (that figure was the whole design paint, of which this work is roughly a tenth). Cleanup, not a performance risk.

*Evidence:* Instrumented `D.gm.potentialDerivedDensity` around one painted frame at 417x240: design 1 level 1 = 128 calls, level 2 = 512; design 2 level 1 = 128, level 2 = 512. Design 1's two call sites are designs.js:831 (`d1TileOrder(d1TileRows(v, n))` in `d1TwoD`) and designs.js:1209 (`d1TileRows(v, v.n)` in `d1Rail`); design 2's are designs.js:1545 (`d1TileRows(v, n)` in `d2TwoD`) and designs.js:1476, where `d2Status` calls `d1TileRows(levelView(S.level), levelView(S.level).n)` — building and sorting 256 objects to extract a single number, and calling `levelView` twice to do it. Half the work is a straight duplicate: both calls in a frame take the same `(v, n)`. `reachableRadius` (designs.js:721) an

**C24 — AC-10's five CLICK/SELECT probes never click — they assert a hover and stop** (`src/ui/__tests__/navAffordances.test.js:183`, tests)

All five of AC-10's CLICK/SELECT probes in src/ui/__tests__/navAffordances.test.js ('CLICK A SECTOR' :183, 'CLICK A TILE' :198, 'CLICK A STAR' :213, 'CLICK TO ENTER' :225, 'SELECT A BODY' :241) assert only a hover field after writing `_mouseX`/`_mouseY` as plain fields and calling `render()`. They enter neither `_handleClick` NOR `_handleMouseMove` — so the bypass is wider than the finding states: the file's own header at :29-35 ("EVERY PROBE DRIVES THE REAL ENTRY POINT — `_onKeyDown` for a key, `_handleMouseMove` + the render tail for a pick") is violated on both halves for these five, not just the click half. Mitigation stands and is stronger than claimed: navPicking.test.js drives the real `clickAt` entry 28 times, including the full mousedown→mouseup→click path at GALAXY (:208, asserting the drill sound and `_viewStack[1].center`) and at PRISM (:341, asserting `_selectedNavStar.seed`), in both 'rail' and 'bars'; navClosePass3.test.js drives clickAt across the pager, headers, locator and companion strip. So no shipped click path is unguarded today — what is one level short is AC-10's own promise-kept guarantee for these five phrases.

*Evidence:* navAffordances.test.js:192, :205, :219, :232, :254 write `_mouseX`/`_mouseY` as fields; the return statements at :194, :208, :221, :235, :256 read only a hover field. Nothing in the file calls `clickAt` or `_handleClick` — `grep -c clickAt navAffordances.test.js` is 0. The file header at :29-35 states "⭐ AND EVERY PROBE DRIVES THE REAL ENTRY POINT — `_onKeyDown` for a key, `_handleMouseMove` + the render tail for a pick", and the vocabulary comment at :63-67 states "⛔ A PROBE MUST ASSERT AN OBSERVABLE CONSEQUENCE". Under a mode the click path has three gates the hover path does not: `_handleClick`'s `if (this._anim) return` (NavComputer.js:4419), `remapClick` returning null (navViewModes/ind

**C25 — navSearch's 'Shift+Tab at GALAXY wraps to SYSTEM' is green only from a state the pilot cannot reach** (`src/ui/__tests__/navSearch.test.js:415`, tests)

navSearch.test.js:415 ('⛔ Shift+Tab at GALAXY wraps to SYSTEM') reaches GALAXY by assigning `nav._levelIndex = 0` after `loadedNav({level:3})`, which leaves `_localStars` populated — a state no pilot route to GALAXY produces (both the tab branch at NavComputer.js:4480 and handleEscape at :1495 clear it), so the case passes for a reason the running game does not supply. The underlying pilot-facing dead end is real but NARROWER than 'the ring is open backwards out of GALAXY': it fires only when `_currentSystemData` is set AND `_systemData` is null — i.e. the pilot is in a spawned system and left the SYSTEM view by ESCAPE (which nulls `_systemData` at :1472) or never entered it. With no spawned system the back-wrap retargets to PRISM (navViewModes/index.js:595) and lands; and if the pilot left SYSTEM by Tab/tab-strip (which does NOT null `_systemData`) the back-wrap also lands, because the `_findNearestStar` gate at :4459 is skipped. The dead end itself is pre-existing (today's legacy tab strip does the same nothing), but the Tab KEY is new in this range (Tab is unbound in legacy), and design 1's hint row prints 'TAB LEVEL' at GALAXY (designs.js:649), so the new key is the first thing that makes the old dead end reachable by keyboard.

*Evidence:* navSearch.test.js:415-422: `loadedNav({ level: 3 })` renders at PRISM (populating `_localStars`), then `nav._levelIndex = 0; nav.render(); press(nav,'Tab',{shiftKey:true}); expect(nav._levelIndex).toBe(4)`. navSearch.test.js:440-452 does the same journey by PRESSING Shift+Tab three times and asserts `expect(nav._localStars).toHaveLength(0)` then `expect(nav._levelIndex).toBe(0)`. The refusal is NavComputer.js:4459-4461 `if (idx === 4 && !this._systemData) { const nearest = this._findNearestStar(); if (!nearest) return; }` against the clear at :4480 `this._localStars = []; this._resetPrismLoad();`. The describe is titled 'TAB is a ring, not a clamp' (:404) and the workstream reports AC-6 as m

**C26 — The sabotage probe proves the violations plumbing, not the per-draw guard it claims to prove** (`src/ui/__tests__/navViewModes.test.js:136`, tests)

The sabotage probe at src/ui/__tests__/navViewModes.test.js:136 exercises assertFits's overflow arithmetic and the fire()/violations() accounting, but not the per-draw wiring it claims to prove: every violation it produces comes from the two sabotage-only call sites (designs.js:667, designs.js:1453), never from the `if (rgn) assertFits(...)` inside T() (designs.js:275) that covers every string on the glass. Verified two ways. (1) Scratch test rendering both modes at all five levels with S.sabotage=true: exactly one violation per (mode, level), all ten matching /^D[12] (hint|status) line \(unclipped\)/. (2) Mutation test in a copied tree with designs.js:275 deleted: navViewModes.test.js + navSearch.test.js pass 63/63 — the mutant survives. All other violations() assertions (navViewModes:123, :145, :318, navSearch:354) assert the EMPTY direction, so removing the guard cannot fail them; :148 is the only `> 0` assertion in the suite. assertMark/assertClear (designs.js:96-118) likewise have no `> 0` assertion anywhere (grep-confirmed; not mutation-tested).

*Evidence:* designs.js:664-667: the shipped path passes `fit(hintFull, W-2)` — already clipped — to `T()`, so `T()`'s `assertFits` cannot fire; the violation comes from the extra unclipped call on the next line, guarded by `S.sabotage`. Same shape at designs.js:1449-1453. `grep -n 'violations()' src/ui/__tests__/*.test.js` returns five sites: navViewModes:145 (expects 0), :123 (expects 0 after filtering), :318 (expects 0 matching), navSearch:354 (expects 0) and navViewModes:148 — the ONLY `> 0` assertion in the whole suite, and it is the sabotage one. PROVED: a scratch test rendered both designs with `S.sabotage = true` and asserted every violation message; all of them contain the literal '(unclipped)',

**C27 — 'every published field has a default' is a hardcoded allowlist that never grew — 24 of 39 state fields are outside it** (`src/ui/__tests__/navPicking.test.js:1535`, tests)

navPicking.test.js:1535 enforces trap (2) with a hardcoded allowlist of 15 field names that does not grow with `makeViewState()`, which now returns 39 fields. Reproduced: the guard's own assertion body passes on a state with all 24 unlisted fields deleted, and throws only when a listed one (prismHits) is removed; no derived check (Object.keys over the factory) exists anywhere in the nav suites. Of the 24 unlisted fields, 7 (design, level, lines, list, sabotage, zoomIdx, buf) are literal driver config above state.js's "⛔ EVERY FIELD BELOW" banner and are not what the test targets; the ~17 that matter are the paint-/driver-published ones (labelHits, orbitRings, yGaugeRect, pagerRect, ladderCounterRect, listHeaderRects, locatorRect, companionRect, searchGeom, ladderStops/Max/Caps/Visible/Scroll, levelLag, levelArm, noSystem). A missing default is also partly covered elsewhere — index.js:159-193 `resetPicks()` re-assigns 15 of them at the head of every frame, and navViewModes.test.js:112-133 renders both designs at all five levels asserting no throw — so the uncovered window is a read before the first paint, or a level whose paint does not write the field. No present defect: all 39 fields currently have defaults.

*Evidence:* navPicking.test.js:1537-1539 lists ['mapProj','prismHits','bodyHits','railTiles','listGeom','tabRects','chipRect','cam','view','sysCam','pick','sortIdx','sortLabel','listOffset','search']. `node -e "import('./src/ui/navViewModes/state.js').then(m => console.log(Object.keys(m.makeViewState().S).length))"` reports 39 keys; the 24 not in the list are: design, level, lines, list, sabotage, zoomIdx, ladderScroll, ladderStops, ladderMax, ladderCaps, ladderVisible, buf, labelHits, orbitRings, yGaugeRect, pagerRect, ladderCounterRect, listHeaderRects, locatorRect, companionRect, levelLag, levelArm, noSystem, searchGeom. state.js:305-309 states the rule the test is meant to enforce ("⛔ EVERY FIELD BE

**C28 — headlessNav rebinds document.createElement globally, so an older nav's offscreen draws land in a newer nav's recorder** (`src/ui/__tests__/helpers/headlessNav.mjs:84`, tests)

`installDom(ctx)` is called unconditionally by every `makeHeadlessNav` (headlessNav.mjs:127) and replaces `globalThis.document`, `globalThis.window` and `globalThis.localStorage` wholesale (headlessNav.mjs:92-104), so after a second nav is built, `document.createElement('canvas').getContext()` returns the SECOND nav's recording ctx. Reproduced: calling `navA._getTileImage()` pushed `putImageData` into nav B's `rec` and added zero ops to nav A's. BUT the only consumer of that offscreen canvas is dead code — `_getTileImage` (NavComputer.js:1510) has zero call sites repo-wide, rendering 2D levels 0/1/2 reaches it 0 times, and `this._tileImageCache` is never initialized so a first call throws `TypeError` at NavComputer.js:1512. The finding's stated trigger ("renders the first at a 2D level sends _getTileImage's offscreen draws into the second nav's rec") therefore cannot happen. No other `createElement('canvas')` exists in NavComputer.js. The file is pre-existing (last touched b5947f5, before the review base a10adc7) and not in the range under review. The live-today residue is the parallel `localStorage` rebind, which would silently drop a test-installed stub if a nav were built after it; navKeys.test.js:554 orders it the safe way by luck, not by construction.

*Evidence:* headlessNav.mjs:84-102 `installDom` sets `globalThis.document.createElement = (tag) => tag === 'canvas' ? { …, getContext: () => ctx } : el()` where `ctx` is the ctx passed in from that call's `makeRecordingContext`; makeHeadlessNav:127 calls it unconditionally. NavComputer.js:1509-1519 `_getTileImage` builds its tile cache through `document.createElement('canvas').getContext('2d')`, reached from the 2D levels. I found no test in the range that both holds two navs and asserts on the first one's `rec` (checked with an awk scan over navViewModes/navPicking/navClosePass3/navPrismCull), so this is latent rather than live — but navKeys:224-235 and navSearch:395-401 do build two navs in one case, 

**C29 — A drag that ends on a search row arms a warp — the 25-texel drag guard is downstream of remapClick** (`src/ui/NavComputer.js:4420`, search)

While the drawn search is open (either design), a press anywhere on the canvas that is released over a result row commits the warp for that row: NavComputer.js:4420 runs remapClick before the right-click branch (:4488) and before the `dx*dx+dy*dy > 25` drag rejection (:4494-4496), and the search branch at navViewModes/index.js:909-914 acts and returns null. Not design-2-only — design 1's rail rows show the same bypass. Correction to the claim's geometry: at 427x240 the design-2 field does not cover the whole map, it covers the top band (measured S.searchGeom for 'bars': x0=2, x1=425, top=12, lead=6, rows=12, i.e. y roughly 18-90); design 1's is x0=269, x1=426, same 12 rows. So the realistic live gesture is a press on the map (or, in design 1, anywhere left of the rail) dragged up/into the row band and released, not any pan whatsoever.

*Evidence:* NavComputer.js:4420 `const p = this.viewMode ? (...).remapClick(...) : this._getCanvasPos(e); if (!p) return;` — the search branch at index.js:909-914 returns null after acting, and every guard that would have rejected the gesture (button===2 at :4488, the 25 threshold at :4494) is 68+ lines later. Measured headless, design 2 level 1: _handleMouseDown at (10,200), _handleMouseUp, then _handleClick at (g.x0+4, g.top+2*g.lead) fires _onCommit once with 'Tau Gomerkaro 5538' — a 190-texel gesture treated as a row click.

**C30 — Ctrl/Meta chords are typed into the query and preventDefaulted — no paste, and Ctrl+R is eaten** (`src/ui/navViewModes/search.js:65`, search)

While the drawn search field is open, `printable()` (src/ui/navViewModes/search.js:65-68) tests only `e.key.length === 1` with no ctrlKey/metaKey/altKey guard, so Ctrl+V types 'v', Ctrl+A types 'a', Ctrl+R types 'r' — and because the search clause folded onto src/ui/NavComputer.js:349 ends `{ e.preventDefault(); e.stopPropagation(); return; }`, the browser's own Ctrl+R reload is suppressed for as long as the field is open. Cmd/meta chords behave identically. The claim's paste half is over-stated in its causation: a ctrlKey guard would NOT restore paste, because the drawn field is canvas text with no editable element and there is no `paste`/`clipboardData` listener anywhere in src/ (grep for paste|clipboardData|onpaste over src/ui/ and src/main.js returns zero hits). "Paste is a capability the drawn field drops" is true as a statement about replacing the DOM `<input>`, but it is a separate missing-feature item, not a consequence of this line. The composition half is confirmed only in the shape the finding stated it: keys named 'Dead'/'Process'/'Unidentified' fall past `printable()` to the unconditional `return true` at search.js:179 and are consumed with preventDefault and no text; whether a real dead-key/IME layout therefore types nothing live is still unmeasured, and I doubt the strong form of it — with no editable target Chrome typically delivers the composed character on the following keydown as a length-1 `key`, which would type normally.

*Evidence:* search.js:65-68 `return k.length === 1;`; search.js:174-177 appends e.key; NavComputer.js:349 searchKey clause ends `{ e.preventDefault(); e.stopPropagation(); return; }`. Measured headless: three events {code:'KeyV'|'KeyR'|'KeyA', key:'v'|'r'|'a', ctrlKey:true} while open -> S.search.text === 'vra', preventDefault called 3 times. Same path silently consumes composition keys ('Dead', 'Process', 'Unidentified' are all length > 1 and fall to the unconditional `return true` at search.js:179), so a dead-key or IME layout can type nothing at all with no feedback — that half is unmeasured, I had no IME to drive.

**C31 — The drawn COMMIT bar / [WARP] chip is dead to a click at GALAXY, SECTOR, REGION and PRISM — Enter commits there, the button does not** (`src/ui/NavComputer.js:4499`, integration)

Under both designs the driver publishes nav._commitButtonRect at every level (index.js:313-322) and both designs paint a filled commit control at every level (designs.js:687-691, 1456-1462), but _handleClick's only read of that rect (NavComputer.js:4503) sits inside `if (this._levelIndex === 4)` at :4499 — so at GALAXY/SECTOR/REGION/PRISM a click on design 1's full-width "WARP TO <NAME> · N LY · ENTER" bar or design 2's filled [WARP] chip does nothing (no warp, no sound), while Enter at the same moment commits. Measured 0 clicks vs 1 Enter at all 4 levels in both modes; level 4 with an armed action fires 1 on click. Correction to the original evidence: the level gate is NOT "the whole difference" — `_commitAction` is also null at levels 0-3 in both modes (measured), so the hit-test's `&& this._commitAction` would keep the button dead even with the gate removed; the click has to be routed through the driver's commit() (index.js:640-662), whose _selectedNavStar/_externalTarget fallback is what makes the Enter key work there.

*Evidence:* src/ui/NavComputer.js:4499 `if (this._levelIndex === 4) {` opens the only block that reads the rect (:4503 `if (this._commitButtonRect && this._commitAction)`), and grep shows :4503 is the sole read site. src/ui/navViewModes/index.js:313-322 writes nav._commitButtonRect from S.chipRect or regions().commit on every frame at every level. src/ui/navViewModes/designs.js:611 declares region('commit', …) unconditionally in drawDesign1 and :687-691 paints the filled bar; designs.js:1447-1462 paints design 2's chip, filling it with INK.TARGET when `armed`, and publishes S.chipRect. The key path, src/ui/navViewModes/index.js:640-662 `commit()`, explicitly handles the not-level-4 case from _selectedNa

**C32 — Backdrop click-to-close is unreachable under a design: the panel covers every pixel of the overlay** (`src/main.js:6161`, integration)

Under a design (viewMode 'rail' or 'bars'), the new rule `.nav-computer-panel.nav-lowres { width:100vw; height:100vh }` (src/style.css:1273-1276, added in this range) makes the panel the full viewport, and `#nav-computer-canvas` at width/height 100% (src/style.css:1246-1252, no pointer-events override) covers every pixel of it, so no pixel of #nav-computer-overlay is exposed and the backdrop handler `if (e.target === navEl) toggleNavComputer()` (src/main.js:6161-6163, touchend twin :6164-6166) can never fire. The same rule ALSO newly kills the close button on fine pointers, which the finding understated: `.overlay-close` is 40px tall (style.css:356-371) and `.nav-computer-panel .overlay-close` sets top:-40px (style.css:1232-1236), so under legacy (panel top = 20px) the button spans -20..+20px and its bottom half is clickable, while under full bleed (panel top = 0) it spans -40..0 and is entirely off-screen. On coarse pointers style.css:1238-1243 rescues the ✕, so touch loses only the backdrop. The finding's "leaves N and Escape as the only ways out" is correct, but the implied stuck-state risk is weaker than stated: NavComputer._onKeyDown (src/ui/NavComputer.js:349) stopPropagation's only for search-active, ,/. in rail, V/L and WASDRF — Escape and KeyN are not in that set — and when the drawn search is open search.js:168 makes Escape close the search only, so Escape is a two-press exit there. main.js:13595 then closes the nav. Severity defect (not blocker): a reliable keyboard exit remains.

*Evidence:* src/main.js:6154-6169 wires closeBtn click/touchend and then `navEl.addEventListener('click', (e) => { if (e.target === navEl) toggleNavComputer(); })` at :6161-6163 plus the touchend twin at :6164-6166. src/style.css:1273-1276 (panel 100vw/100vh) and :1246-1251 (#nav-computer-canvas width:100% height:100%). Under legacy the 20px letterbox from :1226-1227 is the only navEl-targeted area, and the new rule removes it.

**C33 — A throw from a design painter permanently stops the overlay's render loop — the glass freezes on the last good frame and still takes input** (`src/main.js:6146`, integration)

The overlay's render loop is unguarded and re-arms rAF only AFTER render() returns, so any throw out of NavComputer.render() — including one out of a design painter — permanently ends the loop for the rest of the overlay session while the canvas keeps its last frame and every mouse/key listener stays live. CORRECTION on attribution: this loop is NOT part of the range under review. src/main.js:6146-6150 is unchanged by a10adc7^..HEAD (the only main.js hunk in the range is at :7875, the `entry._live` fold) and dates to commit 85bc17d, 2026-07-30. The unguarded-loop exposure is therefore PRE-EXISTING and applies identically under legacy (viewMode === null) — verified by making NavComputer._renderHUD throw with viewMode null: rAF was likewise never re-armed. What the range under review adds is ~1900 lines of new painter surface (designs.js) plus the driver and adapter running inside that same unguarded loop, i.e. it enlarges an existing hazard rather than creating one. SECOND CORRECTION: I could not demonstrate a currently-reachable throw. Four plausible thin-state mutations (systemData without `zones`, planets null, _localStars emptied at prism, _selectedLocalStar = {}) all rendered without throwing under both 'rail' and 'bars'; the null-deref designs.js:1190-1197 documents is already fixed in the reviewed code (`D.playerSector?.id` plus a ternary). So this is a robustness risk conditional on a painter bug existing, not a present defect.

*Evidence:* src/main.js:6146-6150 `function _navRenderLoop() { if (!_navComputerOpen) return; _domNavComputer.render(); _navAnimFrame = requestAnimationFrame(_navRenderLoop); }`. src/ui/NavComputer.js:1434 dispatches into the driver with no guard. src/ui/navViewModes/index.js:281-340 render() has no try/catch; the only guarded call sites in the new code are inside search.js (:80-84, :101-104, :134-135) and the D.playerSector null notes in designs.js:1190-1197 record that an unguarded dereference in a painter is a real, already-hit failure.

**C34 — In HELM the N key opens the cockpit panel, which can never take a design — V looks dead** (`src/main.js:5925`, integration)

In HELM *with the cockpit actually rendering* (_scManual && _cockpitReady && rig && !loadError — src/main.js:4631), openNavComputer (src/main.js:5925) returns early into _openCockpitNav, which never calls activate(), the sole writer of _viewModesEnabled (src/ui/NavComputer.js:586; verified: only two writes in the file, ctor false at :255 and activate true at :586). The cockpit instance DOES get a live keyboard (_syncCockpitNavKeys → attachKeys, src/main.js:5709), so _onKeyDown runs, but every view-mode clause at src/ui/NavComputer.js:349 is gated on _viewModesEnabled and falls through. KeyV is bound nowhere else in main.js, so V is completely inert — no preventDefault, no stopPropagation, no feedback of any kind (verified headlessly: zero calls on the event). The same deadness applies to , . Tab Enter [ ] - = / . Additionally the persisted mode (localStorage well-dipper-nav-view-mode) is read only inside activate(), so a pilot who picks DESIGN 2 in ORRERY and swaps to HELM sees LEGACY with his saved choice silently ignored. Exception the original claim omits: if the cockpit GLB fails or has not loaded, _cockpitShouldRender() is false, the redirect does not fire, and HELM gets the DOM overlay WITH designs.

*Evidence:* src/main.js:5925 `if (_cockpitNavComputer && _cockpitShouldRender()) { _openCockpitNav(); return; }`; :4632-4634 _cockpitShouldRender returns `_scManual && _cockpitReady && !!_cockpitRig && !_cockpitRig.loadError`; :5912-5922 _openCockpitNav zooms the panel and never calls activate(). src/ui/NavComputer.js:586 activate() is the sole writer of _viewModesEnabled, and every view-mode key clause at :349 is gated on it.

**C35 — The 2D map drag pans at the LEGACY scale under both designs — 1.35x to 2.61x the design's own kpc-per-texel** (`src/ui/NavComputer.js:4364`, claims)

The 2D drag-pan scale is taken from the LEGACY map square under both designs, so at every 2D level the world slides 1.35x–2.61x further than the pointer travels. Measured headlessly at 417x240 (60-texel drag, _viewSize 44, navMapSize(417,240)=160 → 16.5 kpc every time): rail L0 ratio 1.65 (design square sq=216, size re-fit to 36 → 10.0 kpc expected), rail L1/L2 ratio 1.35 (sq=216, size 44 → 12.22), bars L0 ratio 2.606 (wide projection, kpc=0.10552 → 6.33), bars L1/L2 ratio 1.40 (block projection blk=224, size 44 → 11.79). Two corrections to the original wording: (a) design 2 spreads the frame across the full 417-texel width only at GALAXY — at SECTOR/REGION it publishes `kind:'block'` with blk=224 (designs.js:1562), which is where the 1.40 comes from, not W; (b) the two doc citations live in docs/WORKSTREAMS/nav-screens-close-pass/ (INTERFACE.md:344, contract.json:61), and neither doc asserts rate parity — INTERFACE.md:344 says only that a GALAXY pan "moves the picture in BOTH designs", and contract.json:61 offers the 0 → −16.5 measurement as proof the host pan had been running against a frame nothing drew, not as proof the rate is right. The defect is already disclosed: docs/FEATURES/nav-menu-guide.html:249 carries it as a KNOWN GAP ("the pan speed is Legacy's scale, not the design's, so it feels a little off"). What the finding adds is the magnitude — up to 2.61x at design 2 GALAXY — and the internal inconsistency: the click picker resolves through the design's own published S.mapProj (picking.js:181/218 via usableProj), while the pan alone still derives its scale from navLayout.js:154 navMapSize.

*Evidence:* src/ui/NavComputer.js:4364-4369 — `const drawSize = navMapSize(this._canvas.width, this._canvas.height); const scale = this._viewSize / drawSize; ... this._viewCenter.x = this._panStartCenter.x - dx;`. navLayout.js:154 `navMapSize(417,240) = 160`. Design projections published by the paint: designs.js:876 (design 1, `sq: 216`, `size` re-fit to 36 at GALAXY) and designs.js:1510 (design 2, `kpc: v.size / W` with W = 417). Doc claims: INTERFACE.md:344; contract.json:61 (AC-5 progress); docs/FEATURES/nav-menu-guide.html:249 ("feels a little off") and rows g-drag/s-drag/r-drag.

**C36 — Walk sheet's first row tells Max the click-highlight is amber; the code frames it in INK.KEY (#d8fbff), and amber is the SELECTION colour** (`docs/FEATURES/nav-menu-guide.html:332`, claims)

The walk sheet states the wrong colour for the GALAXY click-highlight in TWO places, not one: the shot legend at docs/FEATURES/nav-menu-guide.html:235 ("the clicked cell is framed in amber while the map flies into it") and the g-cell-click row at :332 ("The cell frames in amber"). The highlight is drawn in INK.KEY = '#d8fbff' (near-white cyan) in both designs; amber is INK.TARGET = '#ffb03a', the warp/selection ink. The row is tagged st:'ok' = "MAX SIGNED OFF" (:153), so a colour Max never saw is presented as already approved. It also contradicts a green, in-range test: navClosePass3.test.js:918 asserts the GALAXY sector frame is INK.KEY. Amber IS live on the same screen for a different meaning (designs.js:828 target sprite, :945 selected star, :1110 selected body, :1241 WARP ARMED), so the wrong word points at a real mark that means something else. Note nav-menu-guide.html is outside the stated diff paths (src/, scripts/, nav-240p-lab.html) but was added by d3db23f, one of the three unpushed lane-A commits — not pre-existing.

*Evidence:* docs/FEATURES/nav-menu-guide.html:332 (row exp text). Palette: src/ui/navViewModes/designs.js:177 `KEY: '#d8fbff'` and :179 `TARGET: '#ffb03a'`. Draw sites: designs.js:849 `if (pk) frame(..., INK.KEY)` (levels 1-2 cell) and :866 `frame(g, sx0, sy0, sx1-sx0, sy1-sy0, INK.KEY)` (GALAXY sector, design 1); designs.js:1512-1513 comment for design 2's frame "in INK.KEY". Contract: docs/WORKSTREAMS/nav-screens-close-pass/contract.json:61 — "the probe texel read 81,79,76 before the click and 216,251,255 (INK.KEY) two rAF after it".

**C37 — AC-18's verifyVia and the walk sheet both ask for Tab behaviour in today's nav, where Tab is not bound at all** (`docs/WORKSTREAMS/nav-screens-close-pass/contract.json:201`, claims)

AC-18's verifyVia (docs/WORKSTREAMS/nav-screens-close-pass/contract.json:201) and the walk sheet's row `p-nosystem` (docs/FEATURES/nav-menu-guide.html:429, d: '1 · 2 · L') both ask Max to check Tab behaviour in today's nav, where the Tab clause is gated on `this.viewMode` (src/ui/NavComputer.js:349) and therefore never runs. One correction to the original claim: in today's nav Tab is not inert. NavComputer's handler neither preventDefaults nor stopPropagations it (measured), so the event falls through to the game's own window keydown chain at src/main.js:14056, which cycles a body — `scControls.selectTarget(...)` in ORRERY, `focusPlanet(...)` (which FLIES the camera) in HELM — with no `_navComputerOpen` gate anywhere between the handler's start at src/main.js:13438 and that branch. So the sheet's instruction will produce a visible-but-wrong effect behind the open map rather than silence, which is a worse walk-sheet outcome than "nothing happens". The fall-through itself is pre-existing (legacy is byte-identical by AC); the defect introduced by this range is the two documents asking for a Legacy Tab behaviour that was never built — the widening the AC-18 progress note actually measured in today's nav was the tab DIM and the tab CLICK (contract.json:206), never the key. The walk sheet contradicts itself: its key table at docs/FEATURES/nav-menu-guide.html:185 says Tab is "Not bound in Legacy", and every other Tab row on the sheet (:365 s-tab, :388 r-tab, :428 p-tab-system, :489, :490) is correctly tagged '1 · 2' — only p-nosystem carries the 'L'.

*Evidence:* docs/WORKSTREAMS/nav-screens-close-pass/contract.json:201 (AC-18 verifyVia); docs/FEATURES/nav-menu-guide.html:429 (row p-nosystem, d: '1 · 2 · L') against :185 (key table: "Not bound in Legacy"). Code: src/ui/NavComputer.js:349 — `if (this._viewModesEnabled && this.viewMode && e.code === 'Tab') { ...tabLevel... }`; the legacy path has no Tab binding. The measured widening is contract.json:206 — "SYSTEM tab max luminance 170 against GALAXY's 325; a real click on the SYSTEM tab left the level at 3".

## Refuted

- **R1** `src/main.js:7878` — entry._live puts a live three.js scene graph onto generation data the repo keeps JSON-serialisable. *Why refuted:* HEADLESS REPRO (scratch file src/ui/__tests__/_scratch_review_v15.test.js, run with `npx vitest run <file> --root /home/ax/projects/well-dipper --exclude '**/.claude/**'`, now deleted; `ls src/ui/__tests__/ | grep scratch` shows only other reviewers' v17/v20/ref20/refuter17 files remain, and `git status --porcelain` shows no tracked-file change). WHAT I BUILT: generated a real system (`StarSystemGenerator.generate('system-N')`, first seed with planets — note `system-42` yields zero planets), con
- **R2** `src/ui/navViewModes/index.js:288` — resetPicks runs AFTER the !D.ready bail, so a frame that falls back to the legacy nav leaves the previous design frame's hit geometry published. *Why refuted:* TIE-BREAK: siding with the refuter. The ordering is real, the consequence is unreachable by construction, so the finding as filed (severity "risk", with an exposure window for a click) is false. Ordering confirmed: src/ui/navViewModes/index.js:286 refresh() -> :287 agePick() -> :288 `if (!D.ready) return false;` -> :289-292 violations/resetViolations/resetRegions/resetPicks(). resetPicks (index.js:159-198) is the only clearer of S.mapProj/S.tabRects/S.listGeom/S.chipRect/S.pagerRect and it sits 
- **R3** `src/ui/__tests__/navKeys.test.js:159` — `it.runIf(DRIVER_HAS.*)` turns a removed driver method into a silent skip, not a failure. *Why refuted:* TIE-BREAK: I side with the REFUTER, for the reason it gives, which I re-verified in the code rather than taking on trust. MECHANISM (both verifiers agree, and I confirm by reading): src/ui/__tests__/navKeys.test.js:44-48 builds `DRIVER_HAS` at module scope off a real driver (`Object.fromEntries(DRIVER_KEYS.map((k) => [k, typeof drv[k] === 'function']))`); :159 `it.runIf(DRIVER_HAS.commit)(...)` is the AC-7 end-to-end; :743-748 builds `DRIVER_HAS_COUNTER` from `typeof drv.counterGrab === 'functio

## Nits (not verified)

- **N1** `src/ui/navViewModes/state.js:596` — A quick double-Tab between two frames silently loses the inbound ease
- **N2** `src/ui/navViewModes/index.js:909` — With the drawn search open, a click on a level tab only closes the field
- **N3** `src/ui/navViewModes/index.js:288` — render() consumes the level-lag arm and ages the pick before the D.ready gate, and returns without clearing last frame's published geometry
- **N4** `src/ui/NavComputer.js:1434` — Four more poisoned fold targets than the traps list names
- **N5** `src/ui/NavComputer.js:620` — _viewEase outlives deactivate(), and _resizeCanvas measures the rect before applySurface changes the box
- **N6** `src/ui/navViewModes/index.js:364` — listRowAt's right-edge test is inclusive while every other published band is half-open
- **N7** `src/ui/navViewModes/designs.js:652` — Design 1's PRISM and SYSTEM hint rows omit keys that are bound at those levels
- **N8** `src/ui/navViewModes/index.js:730` — The driver's long note on the frozen clause order describes an ordering that no longer exists in NavComputer.js:349
- **N9** `src/ui/__tests__/navViewModes.test.js:61` — navViewModes leaves a throwing globalThis.localStorage installed after its storage round-trip case
- **N10** `src/ui/navViewModes/index.js:726` — The driver's searchOpen() doc block describes a clause order that is no longer shipped
- **N11** `src/ui/NavComputer.js:589` — _seedViewModeCam leaks the designs' camera angle into today's nav after a V round trip
- **N12** `src/ui/navViewModes/index.js:131` — The driver still documents the `nav-lowres` one-way-door defect as live, and the fix has shipped on NavComputer.js:613
- **N13** `src/ui/navViewModes/index.js:527` — `,` and `.` are gated on the design, not on the level: they scrub a SYSTEM ladder from GALAXY/SECTOR/REGION/PRISM against stops nothing cleared

## Completeness critic

Baseline reproduced and `--check` green. Here is the completeness read.

---

## A. FILES IN THE RANGE THAT DREW NO FINDING

Range confirmed: `git diff a10adc7^..HEAD -- src/ scripts/ nav-240p-lab.html` = 20 files, +12529/-178, across 48 commits from `a10adc7` to `783e32b`.

| file | lines now | diff | findings |
|---|---|---|---|
| `nav-240p-lab.html` | 2683 | +1492/-143 | **0** |
| `src/ui/__tests__/navClosePass3.test.js` | 1709 | +1709 | **0** |
| `src/ui/__tests__/navKeys.test.js` | 899 | +899 | 1, refuted → **0** |
| `src/auto/AutopilotNavSequence.js` | 687 | +7 | **0** |
| `src/ui/navViewModes/picking.js` | 384 | +384 | **0** |
| `scripts/extract-nav-designs.mjs` | 126 | +126 | **0** |
| `src/ui/navViewModes/geometry.js` | 87 | +87 | **0** |
| `src/ui/navViewModes/designs.header.js` | 68 | +68 | **0** |
| `scripts/cdp-driver.mjs` | 36 | +36 | **0** |

**Plausible (I read all four in full, found nothing pilot-facing):** `geometry.js` 87, `designs.header.js` 68, `cdp-driver.mjs` 36, `AutopilotNavSequence.js` +7. The autopilot diff is two guarded `this._nav._seedViewModeCam?.()` calls (`:330`, `:362`); both are no-ops under `viewMode === null` because `_seedViewModeCam` opens `if (!this.viewMode) return;` (NavComputer.js:589) — so they cannot be the mechanism of finding 10, though they are two more sites that write the designs' angles and never restore.

**Plausible but unaudited:** `scripts/extract-nav-designs.mjs` 126. I ran `node scripts/extract-nav-designs.mjs --check` → `designs.js matches nav-240p-lab.html`, exit 0. I did not audit the extractor's own logic, so "the gate passes" is verified and "the gate is correct" is not.

**NOT plausible — three gaps:**

**1. `nav-240p-lab.html` (2683 lines) is the generation source for every `designs.js` finding.** Six findings land in `designs.js`, which the extractor produces verbatim from the lab. I confirmed the twins exist: `_fired` is at nav-240p-lab.html:118/176/177, and `d1TileRows` and `CLICK TO ENTER` are both present in the lab. **A fix applied to `designs.js` alone will fail `--check`.** One correction to hand the owners: finding 22's actual fix site is *not* generated — `resetViolations: () => { _violations = 0; _fired.clear(); }` is at `designs.js:1900`, inside the extractor-appended returned API (`grep -c resetViolations nav-240p-lab.html` = 0). So `_fired.clear()` can be removed without touching the lab; the other five designs.js findings cannot.

**2. `src/ui/navViewModes/picking.js` (384 lines, wholly new) is the hit-test core and drew nothing.** It is not untestable and not trivially correct — it carries the z-flip, the three-projection branch, and the bounded nearest-scan. Coverage measured by grep over `src/ui/__tests__/`:

- imported by two files: `navPicking.test.js:35` and `navClosePass3.test.js:26`
- exercised: `projRect`, `worldAt`, `pickLabel`, `pickBody`, `pickPrismStar`, `pickOrbitRing`, `pickSector`, `gridNFallback`
- **named by no test at all: `pickTile`, `cellAt`, `tileOf`, `nearestHit`, `usableProj`, `insideProj`, `inRegion`, `bodyIdentity`**

`pickTile` is the SECTOR and REGION map drill — two of the five screens on Max's walk sheet, in both designs — and it has no direct test. I hand-verified `tileOf`'s z-flip and it is correct (`d1TileRows` at designs.js:881 gives `z = cz + (j+0.5-n/2)·(size/n)`, j↑ ⇒ z↑; NavComputer.js:4657 gives `newCz = viewCenter.z + ext - (row+0.5)·tileSize`, row↑ ⇒ z↓; so `row = n-1-j` ✓). I also traced `pickFromRow`'s levels 1-2 branch (index.js:406) and it is correct — `S.railTiles` is sliced off `ordered` *after* the page offset (`designs.js:1221 const tiles = ordered.slice(off, off + listRows)`), so the bare `[row]` index is right, not a rowBase bug. Both were candidates; both clear.

**3. `navClosePass3.test.js` (1709) + `navKeys.test.js` (899) = 2608 lines of new test with zero surviving findings**, while the four smaller test files (426/545/482/2080) drew one each. `navClosePass3.test.js` is the file that verifies AC-2, AC-5, AC-6 and AC-9 — the ladder counter, the galaxy click-highlight, the inbound ease — i.e. precisely the ACs whose hit-tests findings 1, 2 and 17 say are wrong.

---

## B. THE UNDER-COVERED DIMENSION

**Driver-instance lifetime and persisted state.** The search findings (4, 11) are one instance of a class nobody swept.

Two mechanisms, both verified:
- `NavComputer.js:586` — `activate()` does `this._viewModesEnabled = true; this.viewMode = loadViewMode();`. The design is restored from localStorage on **every** open, not only the first.
- `_viewDriverInst` is created once (`||=` at `:349` and `:587`, initialised `null` at `:255`) and **never rebuilt**. `deactivate()` does not null it.

So every field on `S` that a control writes and `resetPicks` does not clear survives a close-and-reopen and a level change. `resetPicks` (index.js:159-167) clears 15 fields. `makeViewState` returns 33. The 18 it does not clear:

`design level lines list sabotage zoomIdx ladderScroll buf labelHits levelLag levelArm cam sysCam view pick noSystem sortIdx search`

Most are intentional. `search` is findings 4/11. The ones with a pilot-visible consequence that nobody filed:

- **`S.listOffset`** (written by `page()`, index.js:705) — the pager position survives a close-and-reopen and a level change. `d1Rail` re-clamps its own `off` against that level's `total` (designs.js:1172), so it degrades rather than throws, but "reopen the nav and the rail is on page 4" is a walk-sheet-visible state.
- **`S.levelLag`** — while set, `remapClick` returns `null` for everything except a real tab (index.js:964-967), i.e. **every map click is eaten**. Its exits are all time- or level-driven (`stepLevelLag`, state.js:587-613); `deactivate()` clears nothing.
- **`S.sortIdx` / `S.list` / `S.zoomIdx`** — the `[`/`]` sort key, design 2's list-mode toggle and the zoom index all persist across a close.

I checked and **cleared** two adjacent hypotheses so nobody re-spends the time:
- *Does `S.labelHits` leak?* No. It is cleared at the head of both painters (`designs.js:579` in `drawDesign1`, `:1394` in `drawDesign2`).
- *Does the sim clock stall while the nav is open, freezing `agePick`/`stepLevelLag` forever?* No. The main rAF loop is independent of the overlay's `_navRenderLoop` and keeps calling `_advanceSimClock` (main.js:13211-13221). Likewise *window resize while the nav is open* is handled — `render()` calls `_resizeCanvas()` every frame (NavComputer.js:1320), and pointer coords go through `getBoundingClientRect()` at `:4335`.

---

## C. REFUTATIONS RE-CHECKED

**(1) `resetPicks` after the `!D.ready` bail — refutation CORRECT, and I verified its load-bearing claim directly.** `state.js:864` reads `D.ready = !!(D.gm && D.player);` and `state.js:746` builds `D.player = { x, y, z }` as an unconditional object literal — always truthy. So `D.ready` reduces to `!!nav._gm`, an instance invariant. The bail is unreachable-after-ready. Refutation stands.

One correction to the refuter's own residue, which it flagged as unverified and which I closed: the sub-frame V window is **real but not what the finding claimed**. `S.design = design` is assigned at `index.js:283`, the *first* statement of `render()`. So between the V keypress (which sets `nav.viewMode` synchronously on the frozen line 349) and the next rAF, `S.design` still names the *outgoing* design while `S.mapProj.design` also names it — `usableProj`'s stamp test (`picking.js:87`) therefore **passes on a stale projection**, and `_handleClick`'s `remapClick` gate is `this.viewMode` alone. The window is one frame (~16ms) and needs a click inside it. Not filable; worth a hygiene line.

**(2) `entry._live` / JSON serialisation — refutation CORRECT.** The mechanism claim in the original finding (three.js graphs are circular ⇒ `stringify` throws) is false; `Object3D.prototype.toJSON` exists in the pinned `three@^0.183.1`. I did not re-run the refuter's scratch test but I checked the one angle it left open — a non-`stringify` serialiser. `grep -rn "structuredClone" src/` returns **nothing**, and no save/share path in `src/main.js` touches `systemData` (`grep -niE "localStorage.setItem|saveGame|serializ" src/main.js` → one comment at `:1770`). So there is no clone path that would choke on the live subtree either. What survives is only what the refuter conceded: the size-shaped fact (224KB → 10.9MB) against `StarSystemGenerator.js:932`'s own note that `JSON.stringify(system)` is a saves/sharing path, and the parking-lot seed-tag/share work in `well-dipper-system-save-share-seedtags.md`. That is a debt row, not a defect.

**(3) `it.runIf(DRIVER_HAS.*)` — refutation CORRECT.** Its sibling-coverage argument is the right one and I spot-checked the predicate: the driver's returned API is a static object literal (`index.js:1057-1064`), so method *existence* cannot differ between the bare probe and a loaded nav, which is the only way the gate could diverge from `navPicking.test.js:982-985`'s ungated assertion of the same seven names. Refutation stands. Its residue is right too: the gates are dead weight and their justification comment (`navKeys.test.js:22-28`, "landing in parallel") is stale now that `index.js` is committed in the same range.

**None of the three refutations looks wrong to me.**

One range correction the finders missed: finding 28 (`headlessNav.mjs` rebinds `document.createElement`) is **outside the reviewed range**. `src/ui/__tests__/helpers/headlessNav.mjs` does not appear in `git diff --numstat a10adc7^..HEAD`; its last touch is `b5947f5`, which `git merge-base --is-ancestor b5947f5 a10adc7^` confirms is an ancestor. It should carry an explicit "pre-existing" tag.

---

## D. FIVE PLACES TO LOOK NEXT

**1. `src/ui/navViewModes/picking.js:113` (`cellAt`) against `designs.js:1562` (the `'block'` projection).** Hypothesis: design 2's SECTOR/REGION map publishes `kind:'block'` with `bx/by/blk/n/cell` laying `blk` texels over `v.size` kpc while the density *behind* it spends the full `W` — the failure `picking.js`'s own header (`:38-45`) names as "drills a tile roughly TWICE the size the pilot clicked, and it looks like an off-by-one in the grid rather than the wrong projection". `pickTile` and `cellAt` are named by no test. Two walk-sheet rows.

**2. `src/ui/__tests__/navPicking.test.js:102` — the `rowPoint` helper.** `rowPoint(drv, i) = { x: lg.x0 + 4, y: lg.top + (i+1)*lg.lead }` is exactly the point where finding 17's buggy `Math.round((y - top - lead)/lead)` and the correct band agree. Hypothesis: **every** row-pick probe in the range tests the band centre and none tests an edge, which is why findings 16 and 17 (the two `Math.round` off-by-half-row defects) are invisible to a 630-green suite. Check the same for `navClosePass3.test.js:194-260`'s pager probes.

**3. `src/ui/NavComputer.js:586` (`activate()` → `loadViewMode()`) with `_viewDriverInst` never rebuilt.** Hypothesis: the search field is one of ~6 driver-owned fields that survive a close-and-reopen; `S.listOffset`, `S.levelLag`, `S.sortIdx`, `S.list` and `S.zoomIdx` do too, and `S.levelLag` is the one that eats every map click (`index.js:964`). Sweep `deactivate()` (`:620`) against the 18 fields `resetPicks` does not clear.

**4. `nav-240p-lab.html` — locate the twin of each `designs.js` finding before any of them is patched.** Hypothesis: five of the six (`:1112` tag alphabet, `:1421` tab overflow, `:1476` d2Status truncation, `:1503` unguarded marker, `:1545` d1TileRows double-eval) must be fixed in the lab or `scripts/extract-nav-designs.mjs --check` goes red; only finding 22's `_fired.clear()` at `designs.js:1900` is outside the generated block. I confirmed the lab carries `_fired` (`:118/176/177`), `d1TileRows` and `CLICK TO ENTER`; I did **not** map the other three to lab line numbers.

**5. `src/ui/navViewModes/index.js:686` (`listBounds`).** `const totals = [D.sectorRows.length, 64, 256, D.starRows.length, D.bodies.length];` — the `64` and `256` are `gridNForLevel(1)²` and `gridNForLevel(2)²` restated as literals, in the file whose own comments argue hardest against restating geometry (the AC-4 defect shape). Hypothesis: low severity today (`d1Rail` re-clamps its own `off` at `designs.js:1172`), but it is the pager's end-stop and it is a second copy that can drift silently from `NavComputer.gridNForLevel`.

---

## E. SUITES AND BASELINE

**Which suites the finders ran: unknown to me.** No finder logs, run transcripts or exit codes were passed into this task — only the finding and refutation JSON. Two refutations state their own test posture explicitly (the `entry._live` refuter created and deleted `src/ui/__tests__/_scratch_review_refuter15.test.js`; the `resetPicks` refuter says "no test run, no scratch file"; the `runIf` refuter says "I ran no tests" but cites a prior verifier's "52 passed" on `navKeys.test.js`). For the other ~34 findings I have no evidence either way and will not infer it.

**What I ran, and the baseline:**

```
npx vitest run src/ui --root /home/ax/projects/well-dipper --exclude '**/.claude/**'
  → Test Files 29 passed (29)
  → Tests      630 passed (630)
  → Duration   273.19s, EXIT=0
```

**The `src/ui` baseline reproduced exactly: 630 passed / 29 files.** No skips reported, which independently corroborates the `runIf` refutation (both gated `it.runIf` blocks ran rather than skipping).

```
node scripts/extract-nav-designs.mjs --check
  → designs.js matches nav-240p-lab.html, EXIT=0
```

I did **not** run `src/cockpit` (baseline 698) or `tests/` (baseline 20 failed across 8 files, expected).

---

## F. NOTES / NOT CHECKED

- Nothing was modified. No scratch vitest file was created, so none needed deleting. Scratch output went to `/tmp/claude-1000/-home-ax/f54656ac-0fff-4640-bc3e-0748edc4fb32/scratchpad/ui-suite.log`.
- No live browser. Every determination above is static reading plus the two commands in §E. The findings that need a live pilot to confirm — 3/35 (the 1.35x–2.61x pan scale), 5 (dead wheel at SYSTEM), 14/32 (no mouse close under a design), 34 (dead V in HELM) — I did not reproduce.
- Two findings target files outside the stated range (`docs/FEATURES/nav-menu-guide.html:332`, `docs/WORKSTREAMS/nav-screens-close-pass/contract.json:201`). They are inside the commit range and both are walk-sheet correctness, so they matter to the next session — they just are not `src/`/`scripts/`/lab findings and should not be counted against source coverage.
- I did not audit `scripts/extract-nav-designs.mjs`'s extraction logic, `nav-240p-lab.html`'s 1492 added lines directly, or `navClosePass3.test.js` beyond its `listGeom`/`pagerRect` probes and its `picking.js` import line.
