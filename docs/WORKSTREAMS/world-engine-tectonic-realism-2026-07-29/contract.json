{
  "workstream": "world-engine-tectonic-realism-2026-07-29",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "building",
  "acceptanceCriteria": [
    {
      "id": "AC-SAMPLER",
      "statement": "The field sampler measures the field that is actually being rendered. Today createHeightSampler renders ROUTER_MAIN (bare fbmd, no baked-cube blend, no crater restore) while the lab's live default is uReliefBakeStrength = 1 -- a pure BAKED body. Every number downstream of the instrument currently describes a field nobody looks at. This blocks every other AC and lands first.",
      "verifyVia": {
        "input": "Run _lab.sampleField on Rocky (Earthlike) at default settings (bake strength 1), then plant a change in the baked relief cube and sample again.",
        "observable": "The first sample's statistics match the baked body, not bare fBm; the planted change moves the sampled field. A planted-defect control is mandatory -- a sampler that silently reads the wrong field is exactly the failure this AC exists to close, and it already fooled one prior session."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-KMUNIT",
      "statement": "The vertical axis carries a real unit: kmPerUnitOf(radiusEarth, surfaceGravity, path) exists as a DERIVED function, not a literal, because the quantity demonstrably is one -- it scales as reliefEnvelope(R,g)*R, differs 3.85x between the baked and synth paths, and ranges 10-107 km/unit across presets. It reproduces the two independent anchor routes (the 19.9 km design anchor, and reliefBudget's relic-lambda law plus MOLA hypsometry) within their measured spread at R=1 on the rocky plate path.",
      "verifyVia": {
        "input": "Call kmPerUnitOf across the radius band and the 8 in-scope presets; run its LAW_REGISTRY entry through auditLaw.",
        "observable": "Returns ~16 +/- 3 km/unit at R=1 rocky, matching both anchor routes; auditLaw resolves the claimed exponent rather than returning UNRESOLVABLE. The guard's expectations are HAND-DUPLICATED LITERALS, never read from the constants they guard, and the guard is mutation-tested -- five instances of that exact defect appeared in one prior session."
      },
      "layer": "unit",
      "live": false
    },
    {
      "id": "AC-MEASURE",
      "statement": "Max's words: \"We should be able to measure the height of mountains.\" A km figure for mountain relief that describes the rendered body. Today state.mountainHeightKm = 9.0 is a dead constant with zero consumers reaching a pixel, and craterDepthKm / edificeHeightKm / canyonDepthKm have none at all. Note this is FINISHING an unfinished path, not connecting a disconnected one: the intended formula at planet-lod-lab.html:2294 names K_* constants that were never written.",
      "verifyVia": {
        "input": "Sample the field on Rocky (Earthlike) at R = 1.00 (slider t = 0.303) and read the scale-readout panel.",
        "observable": "Mountain relief and total peak-to-trough report in km and land inside the Earth sanity bound of roughly 12-30 km total relief -- NOT the ~3200 km the current shading chain implies, and not a dead 9.0 that never changes. The readout tracks radius when the slider moves."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-HYPSO",
      "statement": "The elevation distribution is bimodal the way a two-crust world's is, because that -- not total relief -- is what makes coastlines and continental shelves land in the right places when the existing sea level cuts the field. Total peak-to-trough relief is explicitly NOT a criterion here: Moon ~19.9 km (one plate, airless) versus Earth ~19.8 km versus Mars ~29-30 km (one plate) means it has no relationship to tectonic regime.",
      "verifyVia": {
        "input": "Build the area-weighted elevation histogram (equal-area grid or cos-latitude weighting -- an unweighted lat/lon histogram will fake or destroy modes) at 50-100 m bins for a plate-path preset, then repeat after subtracting degree-0 and degree-1 spherical harmonics.",
        "observable": "Hartigan's dip test rejects unimodality; modal separation 4.3-5.0 km; upper mode centre 0.0 to +0.3 km holding 0.35-0.45 of surface area (the CRUSTAL fraction, not the 29% land fraction); inter-modal trough under 10% of area. The bimodality SURVIVES the centre-of-figure re-reference -- Earth's does, Mars's collapses (Aharonson et al. 2001), and that step is what separates a real two-crust world from one big volcanic province. Negative control REQUIRED: a despun-path preset must FAIL this test."
      },
      "layer": "integration",
      "live": false
    },
    {
      "id": "AC-PROVINCE",
      "statement": "Max's words: \"The tectonics system exists to do more than create terrain height variation.\" The real craton/orogen/basin province field drives gProvince in place of the three FBM noise fields of position that gate ~48 features' amplitude and albedo today. This is the largest single step from 'tectonics makes bumps' to 'tectonics makes places', and the source already calls it out at planet-lod-lab.html:1500 as a separate job.",
      "verifyVia": {
        "input": "On a plate-path preset, compare feature placement against the province field shown in the (currently hidden) debug overlay; then plant a change in the province field and re-render.",
        "observable": "Feature placement follows the real craton/orogen/basin partition and moves when it is perturbed; the FBM province fields are no longer read by the gated features. Blast radius is the risk here -- ~48 features change behaviour, so the 10 out-of-scope presets must be checked for unintended movement."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-STRIKE",
      "statement": "Mountain belts run ALONG their plate boundaries. Today the grain cube that orients mountains, canyons, scarps, tessera and lava on EVERY dispatch path -- including the plate path -- is baked from the despun latitude-band stress model, which knows nothing about plate boundaries. The per-edge boundary normal and tangent needed to fix this are computed at plates.js:289-291 and discarded. Correctly placed, arbitrarily oriented ranges are the clearest symptom of the two disconnected tectonic models.",
      "verifyVia": {
        "input": "On a plate-path preset, measure the local structure-tensor orientation of the height field along an extracted orogenic belt and compare it to the belt's own boundary tangent.",
        "observable": "Belt strike correlates with boundary tangent rather than with latitude. Ranges visibly run along their belts instead of across them."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-PLATESCALE",
      "statement": "INVERTED 2026-07-29 after the literature falsified this AC's original premise; see amendments[1]. Plate structure is ANGULARLY RADIUS-INVARIANT, and the shipped plateCount = 7-13 is CORRECT AS IS -- do not add a radius term. Two independent routes converge: plate width is set in units of MANTLE DEPTH by an aspect ratio that is a function of rheology alone (Mallard+2016 Nature 535:140; Hoink & Lenardic 2010 GJI 180:23), and D/R is constant for fixed composition since D ~ M^0.28 against R ~ M^0.27 (Valencia+2006); independently Valencia+2007 ApJ 670:L45 measure L/R going 0.29 -> 0.30 across a factor of TEN in mass. Bird 2003 G3 4:1027 writes the plate-size distribution in STERADIANS, so it is dimensionless and radius-free by construction. Net N ~ R^-0.07: under one plate across the whole slider band. THE UNDECLARED LAW IS REAL BUT LIVES ELSEWHERE: Slice C sets fbmd's base frequency via uDispDomainScale = sVis (planet-lod-height.glsl.js:765, written at planet-lod-lab.html:5932), giving the SYNTH body an angular wavelength ~ R^-0.5 -- exactly a plate-scaling law of exponent 1, with no derivation, no citation and no name, arriving as an algebraic consequence of the camera-framing constant VIS_SCALE_EXP = 0.5. So the honest reading of the bake crossover is: the baked body has exponent 0 (what the literature says is correct) and the display layer wants exponent 1 (invented); rather than reconcile them it fades the correct body into the invented one. THAT is the patched-on law parent goal 3 is aimed at.",
      "verifyVia": {
        "input": "Sweep radius across the band on the plate-path presets; inspect the emitted partition's ANGULAR statistics; run the registry entries through auditLaw.",
        "observable": "(1) Plate angular size and count are radius-invariant within the literature's ~13% band, pinned by a registry entry with hand-duplicated literals -- and plate LINEAR size therefore scales as R for free, because the partition is angular. (2) The exponent-1 law in the display path is either NAMED and derived with a citation, or removed. It must not remain an unnamed side effect of a camera constant. (3) bakeReliefCrossover(sVis) is RETIRED and replaced by the derived Nyquist gate: n_s(R) = theta_feature / max(meshPitch, cubePitch) with a Shannon floor of 2, where meshPitch = sqrt(2*(4*PI/TARGET_N)/sqrt(3)) at TARGET_N = 40000 and cubePitch = 2/RELIEF_CUBE_SIZE at 256. NOTE THE MESH BINDS, NOT THE CUBE (1.9046e-2 rad vs 7.8e-3) -- the 256^3 texture was never the constraint. Every term reads an existing constant and none of them is sVis. Closes 'tectonics become noise at 3.26 R-earth' at its cause."
      },
      "layer": "unit",
      "live": false,
      "itemStatus": {
        "item1_noRadiusTermInPlateCount": "HELD — nothing added. plateCount stays the seeded draw at src/worldengine/base/plates.js:227.",
        "item2_nameOrRemoveTheDisplayLaw": "DONE 2026-07-30 (removed). The frame-loop write of uDispDomainScale is deleted; the uniform is pinned at its 1.0 initializer. Byte-identity at the anchor VERIFIED by two-page GPU-readback hash equality, not merely derived. Evidence: evidence/platescale-B/AC-PLATESCALE-item2-BEFORE.md §4b.",
        "item3_retireBakeReliefCrossoverForNyquistGate": "OPEN — deliberately not bundled. See amendments[2]."
      },
      "observableStatus": {
        "1_registryEntryPinningRadiusInvariance": "OPEN — deferred to AC-PLATECOMP on purpose; a radius-only entry would be tautological. Reasoning recorded in the evidence file.",
        "2_lawNamedOrRemoved": "MET — removed.",
        "3_nyquistGateReplacesBakeReliefCrossover": "OPEN — and the AC's own meshPitch literal needs correcting first. See amendments[2]."
      }
    },
    {
      "id": "AC-PLATECOMP",
      "statement": "Plate count is driven by WHAT THE PLANET IS MADE OF, not left as a bare seeded draw. The literature's strong count driver is the mantle-depth fraction: N ~ 4*pi*(R/D)^2 / Gamma^2 with R/D = 1/(1 - R_core/R), i.e. set by CORE MASS FRACTION -- which compositionClass already carries in the condition vector. Earth checks out: R/D = 2.204 with Gamma = 2.56 gives N = 9.3 against the 7-9 large plates actually observed. A low-core body at R_core/R = 0.30 gives 2.4x fewer plates from the same cited physics. Max ruled this channel OPEN (2026-07-29), accepting that non-Earth rocky presets visibly change: replacing a constant with a physical cause is the point of parent goal 3, and planets differentiating by composition rather than only by size is the payoff.",
      "verifyVia": {
        "input": "Derive plate count across the rocky plate-path presets from their condition-vector core fractions; run the registry entry through auditLaw.",
        "observable": "Plate count varies with core mass fraction by the cited relation, with Gamma declared as a calibration (it is anchored on Earth ONLY -- no source measures it on another body) rather than dressed as a derivation. Earth stays BYTE-IDENTICAL: driversToTune's AC2 identity guard must still return null at the anchor. The moved presets are enumerated with their before/after counts, because Max accepted a visible change and is owed the specific list rather than a general warning."
      },
      "layer": "unit",
      "live": false,
      "status": "IMPLEMENTED 2026-07-30 — pending Max UAT on Ocean (temperate)",
      "premiseCorrections": [
        "FALSE AS WRITTEN: 'core mass fraction -- which compositionClass already carries in the condition vector'. compositionClass is a FUNCTION (src/worldengine/base/e1Regime.js:66) returning one of four STRINGS; inside deriveConditionVector its result is a local `_class` (body-condition-vector.js:107) used only to pick the gravity law and never returned. A live vector dump has exactly 14 keys and `'compositionClass' in cv === false`. No core mass fraction, core radius fraction or core density exists anywhere in the repo; body-condition-vector.js:36 states outright that 'no CMF / iron-fraction plumbing is needed'. driversToTune also never reads the condition vector at all -- three FLAT keys only (plates.js:131-133), fenced by tests/worldengine-base-condition-vector.test.js:199.",
        "MASS vs RADIUS CONFLATION: the AC says 'core MASS fraction' but its formula R/D = 1/(1 - R_core/R) needs the core RADIUS fraction. Earth's are 0.325 and 0.546 respectively. Implementing the prose literally gives Earth R/D = 1.481 and N = 4.2, less than half the intended 9.3 -- a plausible-looking wrong answer. RESOLVED by authoring R_core/R directly as reviewable preset DATA, so nothing in the shipped path inverts a CMF and no core-density constant (a SECOND undeclared calibration) is needed.",
        "GAMMA = 2.56 IS INCOMPATIBLE WITH THIS AC'S OWN BYTE-IDENTITY REQUIREMENT if N is applied absolutely: N(Earth) rounds to 9 against DEFAULTS.PLATE_COUNT_MIN = 7 (plates.js:51), so the AC2 guard's fourth conjunct (plates.js:156) fails and EARTH MOVES. Only Gamma ~2.95 would land 7. RESOLVED by the ratio form, in which 1/Gamma^2 cancels identically.",
        "'Earth checks out: N = 9.3 against the 7-9 large plates actually observed' IS NOT A CHECK. Gamma is fitted on Earth, so that comparison cannot fail -- it restates the fit. And 9.31 is 3.4% ABOVE the top of 7-9, i.e. outside the band it is said to match.",
        "SCOPE OVERSTATED SIXFOLD: 'non-Earth rocky presets visibly change' (plural). Only TWO of 18 presets reach the plate writer -- Rocky (Earthlike) and Ocean (temperate) -- measured over 18 presets x 320 radii x 3 seeds, 0/960 cells for the other sixteen. Rocky IS the byte-identity anchor. So the enumerated list Max is owed has exactly ONE row."
      ],
      "howItWasImplemented": "GAMMA-FREE EARTH-ANCHORED RATIO. The shipped form is compFactor = (mdf/mdf_Earth)^PLATE_COUNT_MDF_EXP with PLATE_COUNT_MDF_EXP = -2, so the aspect ratio Gamma cancels identically and APPEARS NOWHERE IN THE CODE -- there is no calibrated constant for a future reader to mistake for a measurement, which is a stronger honesty guarantee than any comment. The absolute normalization is the shipped DEFAULTS.PLATE_COUNT_MIN = 7, which IS the Earth calibration expressed in the file's own units, so the law contributes its EXPONENT only. R_core/R is authored per preset as data (opt-in, one literal at a time); any preset omitting it falls back to the anchor via `?? D_EARTH.coreRadiusFraction`, giving a factor of exactly 1 and byte-identity by construction.",
      "verifiedBy": [
        "Earth anchor EXACT: plateCountTarget(1 - EARTH_CORE_RADIUS_FRACTION) === 7 with toBe (x/x on the identical double, then pow(1,k)); driversToTune(D_EARTH) === null unchanged.",
        "NON-CIRCULAR golden carve-out: Ocean's 5 rows are asserted (a) MOVED off the immutable v2-0-carrier-goldens.json, and (b) forcing coreRadiusFraction back to the anchor reproduces that immutable golden BYTE-FOR-BYTE -- which proves the new driver is the SOLE cause and nothing else leaked. Frozen's pattern (equality against an independent writer) was unavailable because Ocean runs the same plate writer, so a fresh re-run would have been circular and a re-capture is forbidden.",
        "Registry entry plate-count-vs-mantle-depth-fraction: PASS, exponent -1.9999999999999971 +/- 1.283e-15, r2 = 1, 11 laws allPass. Positive controls both FAIL as required (composition-blind constant -> exponent 0; exponent -1 variant -> -1.0000000000000073).",
        "Enumerated table committed at plate-count-before-after.json and RECOMPUTED by tests/worldengine-base-plate-composition.test.js so it cannot drift: Rocky 7->7 with counts 10,11,12,7,9 unchanged; Ocean 7->6 with counts 10,11,12,7,9 -> 9,10,11,6,8.",
        "Radius invariance asserted under the LAB's radius-aware wiring (massGravity = condition.surfaceGravity), not the radius-blind canonical gravity which would pass vacuously: PLATE_COUNT_MIN constant across R in {0.5 ... 16.0} for both presets. AC-PLATESCALE preserved.",
        "Full suite 20684 passed / 4 failed; the 4 are pre-existing (KnownObjects x3, GalacticFeatures x1) plus 13 vendor/motion-test-kit collection errors, all baselined by stashing the edit."
      ],
      "knownTensionsNotClosed": [
        "clamp(5, 14) at plates.js:148 survives and is in direct tension with AC-NOCLAMP. It cannot move Earth (7 is interior) and does not bind Ocean (6 is interior), so it is byte-safe today, but it would saturate any body authored outside R_core/R in roughly [0.49, 0.68].",
        "N diverges as mdf -> 0 (N ~ mdf^-2): ~16 plates at R_core/R = 0.70 and ~50 at Mercury's real ~0.83. Handled by a DECLARED validity domain that THROWS rather than by the clamp -- returning NaN would slip past the AC2 guard, since clamp(5,14,NaN) is NaN, and yield a silent all-NaN planet.",
        "The value is hand-mirrored in THREE places (preset composition, body-drivers.js, the lab's explicit buildBodyDrivers literal) because driversToTune is fenced from reading the nested condition. 'In the condition vector' is true of the DATA only; nothing reads it there. A tripwire test pins the preset copy against the anchor constant.",
        "ironFraction was deliberately NOT reused as a CMF source: its shipped values are detuned away from physics (driver-presets.js documents Mars at 0.10 as 'DELIBERATELY under Mars's true ~0.2' to suppress an aurora artifact, and Lava carries 0.7 above the generator's own 0.6 clamp), and it has live consumers in the aurora and giant-metallicity paths."
      ]
    },
    {
      "id": "AC-ONEBODY",
      "statement": "The rivers are routed on the body the planet displays. Today route() gates its height source on a BOOLEAN -- bakedOn = uReliefBakeStrength > 0 (planet-lod-rivers.js:1399) -- and then takes 100% carrier, while the renderer BLENDS CONTINUOUSLY: baked*s + synth*(1-s) with s = base * bakeReliefCrossover(sVis) (planet-lod-lab.html:385, :5942). Those agreed while strength was only ever 0 or 1; Slice D made it continuous and silently invalidated the Phase-D comment at planet-lod-rivers.js:1393-1397 that still asserts 'single source, no surface-vs-rivers split'. So at R = 2 the drainage is cut into 100% carrier while the planet shows 50/50 -- a different body at every radius except 1. The Slice-D crater fix conserved the crater term across that split and did not conserve the body. EXPECTED TO CLOSE AS A CONSEQUENCE OF AC-PLATESCALE rather than by its own fix: if plate structure becomes radius-responsive and the crossfade is retired, uReliefBakeStrength sits at 1, renderer and router both take the baked/carrier field, and uCraterBakeRestore (= base - effective) falls to 0 and goes inert. That chain is READ FROM SOURCE, NOT TESTED -- if it does not hold, this AC needs its own fix at the route() gate.",
      "verifyVia": {
        "input": "At R = 0.5, 1, 2 and 3 on a plate-path preset, compare the field route() routes on against the field the planet renders.",
        "observable": "The two agree at every radius in the band, not only at R = 1. Rivers sit in valleys that are visibly there. If a residual difference remains by design, it is declared with its grounds rather than left as an undocumented split."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-EXAG",
      "statement": "Max's words: \"I want honest numbers and honest renders.\" The render's vertical exaggeration becomes ONE declared, derived, tested constant, stated in the readout next to the true km -- exaggerating on the record rather than by drift. Today it is ~160x, reached through state.perturb = 0.55, a GUI slider with no derivation, no justifying comment and no test pinning it. HYPOTHESIS TO TEST FIRST, NOT ASSUME: that the 160x is compensating for absent structure (real province material, correct coastlines, terminator shadowing), in which case the needed factor falls once AC-PROVINCE and AC-HYPSO land. Pick the number after the structure, not before.",
      "verifyVia": {
        "input": "Render a plate-path preset at a terminator sun angle with the structure ACs landed, at several candidate exaggeration factors; and separately render the same body through both shading paths.",
        "observable": "One named constant governs implied relief; the readout states both true km and the exaggeration factor. The analytic path (strength*0.6 on the combiner field) and the finite-diff regression path (strength*0.025 on computeHeight) agree -- they currently disagree by ~24x, so a calibration valid for one is wrong for the other. NOTE: there is NO pixel-level regression test anywhere in this repo, so this AC has no automated net; a fixed-camera fixed-seed before/after capture set is the substitute."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-NOCLAMP",
      "statement": "Max's words: \"Plate tectonics work across all sizes of planets\" and \"make sure these are all being programmatically generated and aren't being patched on.\" No magic clamp stands in for physics in the tectonic chain. clamp(0.4, 2.5, (g/g0)^-0.5) appears at plates.js:136, magmatism.js:120 and shellRelief.js:144; its lower bound is reached at g = 5.625, i.e. R = 2.762, above which plate uplift and rifting stop responding to planet size entirely. Also in scope: stagnantLid.js:144 carries the same shape with K_G = 0, i.e. the gravity term switched off as a byte-safety placeholder.",
      "verifyVia": {
        "input": "Sweep radius across the band on each in-scope preset and measure the delivered tectonic gain and the end-to-end relief exponent.",
        "observable": "Tectonic and volcanic content keeps responding to radius across the whole band -- no plateau at R = 2.762. Every bound that survives is DERIVED with its physical basis stated, or explicitly declared as a modelling choice with its grounds; neither is left as a bare magic number."
      },
      "layer": "unit",
      "live": false
    },
    {
      "id": "AC-DERIVED",
      "statement": "Rule 15 spine conformance for everything this workstream adds: driver connectivity, a named consumer, taxonomy registration. Every derived law lands with a LAW_REGISTRY entry so a future retune fails a named test instead of drifting silently.",
      "verifyVia": {
        "input": "Run the spine-conformance and registry audits over the new laws; mutation-test every new guard.",
        "observable": "Each new law has a registry entry that auditLaw RESOLVES (not UNRESOLVABLE); each new guard discriminates under mutation; no guard derives its expectation from the thing it guards. That last one is the recurring defect of this program -- five instances in one prior session, and fixing each did not prevent the next."
      },
      "layer": "unit",
      "live": false
    },
    {
      "id": "AC-NOREGRESS",
      "statement": "The 10 presets outside the tectonic set keep rendering as they do today, and the test baseline holds. IMPORTANT CORRECTION carried into this contract: golden hash 40c18aad is a fixed-timestep accumulator + Mulberry32 RNG hash over a synthetic moving anchor, importing only from vendor/motion-test-kit. It CANNOT see relief or shading. Prior sessions cited 'golden untouched' as evidence that relief changes were safe; that claim was vacuous. Relief is baselined by fence-baseline.json and l0-baseline.json instead -- those are the artifacts that matter here.",
      "verifyVia": {
        "input": "Run the full suite, verify-golden, and a render pass over all 18 presets.",
        "observable": "Suite at EXACTLY the 4 named failures (GalacticFeatures x1, KnownObjects x3) = 17 failed FILES (15 vendor + the 2 holding the real 4); anything else is ours. verify-golden passes at 40c18aad with NO re-capture. The CPU relief baselines either hold or their movement is declared with grounds. The 10 out-of-scope presets show no unintended change from the gProvince rewire."
      },
      "layer": "integration",
      "live": false
    },
    {
      "id": "AC-UAT",
      "statement": "Max's criterion, verbatim: \"success here means simply that we can model earth-like planets (or any planet that has tectonic activity) in a way that makes the features read realistically as they would from space. This is the basis for all sorts of downstream systems, e.g., water cycle.\" Holds when zooming in -- \"from space we can zoom in and look more closely at these features.\" MAX'S GATE ALONE. No agent drives or PASSes this; it is always deferred-to-max, and it presupposes every objective AC above is GREEN.",
      "verifyVia": {
        "input": "Lab at :5175. Rocky (Earthlike) and Ocean (temperate) first, then the shell/volcanic/stagnant presets. Drag 'planet radius (log)' -- it is a 0-to-1 log POSITION t, NOT radius; watch the 'radius (RE)' readout. Reference stops: t = 0.128 / 0.303 / 0.477 / 0.579 = R 0.5 / 1.0 / 2.0 / 3.0. Wait >= 500 ms after the slider settles (220 ms debounce + 220 ms river reroute). Do NOT touch the 'gravity (g)' override slider -- it desynchronises the writers from the readout. Zoom to the camera floor (1.1 radii from centre, ~637 km altitude at Earth scale).",
        "observable": "Max judges whether the features read as they would from space -- coastlines and shelves in believable places, mountain belts with believable plan-form running the right way, the whole thing cohering as a planet rather than as noise with a height map. Carve-out stated up front so he is not sent hunting: everything in the lab is orbital; the camera floor is low orbit, not aerial, so sub-km surface detail is out of frame by construction."
      },
      "layer": "uat"
    }
  ],
  "amendments": [
    {
      "date": "2026-07-29",
      "added": [
        "AC-PLATESCALE",
        "AC-ONEBODY"
      ],
      "grounds": "The AC-SAMPLER adversarial gate surfaced a blocker outside that AC's scope: the router and the renderer take different bodies across the whole interior radius band. Working-Claude's first recommendation was to FILE it and keep the workstream on measurement. Max rejected that framing -- 'why not fold it in at the most appropriate place (given inputs outputs of the system)?' -- and he was right on the project's own rule: that recommendation was justified on blast radius, i.e. ECONOMY, which the scoping reference says is a tiebreaker AFTER the feature question and never the framing that shapes it. Rivers cut into a body the viewer is not looking at is a direct hit on 'features read realistically as they would from space'.",
      "seamAnalysis": "The appropriate seam is NOT the route() gate. It is driversToTune, where radius is missing from the driver -> plate-parameter map. The crossfade is a display-side compensation for that missing term, and it is the common cause of three separately-observed defects: Max's 'tectonics become noise at 3.26 R-earth', the router/renderer body split, and the unmet half of parent goal 1 ('plate tectonics work across all sizes of planets'). Fixing the symptom at the router gate would have left the cause in place -- the patch-on-top behaviour parent goal 3 forbids.",
      "confirmedBy": "Max, 2026-07-29"
    },
    {
      "date": "2026-07-29",
      "inverted": [
        "AC-PLATESCALE"
      ],
      "added": [
        "AC-PLATECOMP"
      ],
      "grounds": "THE PREMISE OF AC-PLATESCALE WAS FALSIFIED BY THE LITERATURE IT ASKED FOR. Working-Claude's reasoning -- 'driversToTune has no radius term, that is the gap, and the crossfade is a display-side patch for it' -- was wrong, and Max had confirmed amendment[0] on the strength of it. Two independent routes give plate count as radius-INVARIANT (N ~ R^-0.07, under one plate across the band), and Bird 2003's plate-size law is written in steradians, hence dimensionless. The shipped plateCount = 7-13 is correct as it stands.",
      "whatSurvived": "The DIRECTION was right and the research relocated it. There IS an undeclared, uncited scaling law patched on -- exponent 1, in the DISPLAY path via uDispDomainScale = sVis, arriving as an algebraic side effect of the camera constant VIS_SCALE_EXP = 0.5. The crossfade therefore fades a physically-correct exponent-0 body into an invented exponent-1 one. The research also produced a DERIVED replacement for the crossover (a Nyquist gate on delivered samples per feature) and established that the MESH pitch binds, not the 256^3 cube.",
      "alsoRejected": "The workflow's own proposed replacement law was rejected by adversarial review, 6 blockers. The sharpest: its belt term would have driven belts below the mesh Nyquist limit across most of the band -- re-creating 'tectonics become noise at 3.26 R-earth' in geometry -- while the registry entry kept reporting PASS.",
      "confirmedBy": "Max, 2026-07-29 — chose 'invert it fully' and 'open the composition channel', both over the alternatives"
    },
    {
      "date": "2026-07-30",
      "resolves": [
        "AC-PLATESCALE item 2"
      ],
      "ruling": "Max chose outcome B — REMOVE the undeclared exponent-1 display law — over outcome A (name it and derive it with a citation), after being shown that A's only available derivation is a CAMERA-FRAMING one and that the law is unauditable by the instrument built to audit laws: the registry's measure() must call a real CPU function, and this law exists only in GLSL plus one JS write.",
      "whatLanded": "planet-lod-lab.html: the sole write `uniforms.uDispDomainScale.value = sVis` is deleted; the uniform holds its 1.0 initializer at every radius, making all 5 GLSL read sites exact no-ops. The GLSL string is deliberately UNTOUCHED so the compiled program stays bit-identical. tests/vis-scale-fence.test.js: the Slice C block is INVERTED — it previously REQUIRED the write, i.e. the fence was pinning the invented law in place.",
      "verification": "Two-page recipe (git show HEAD:planet-lod-lab.html served alongside the edited page), both driven to the same state with the animation clock PINNED to 0 and canvases equalised at 1750x1375, compared by GPU readback hash of the centre 256x256. R=1 BYTE-IDENTICAL (4d76e86e, sum 33185803 on both). R=4 and R=16 differ as required. Quantitative invariance signature: AFTER pixel sums at R=4 and R=16 agree to 0.01% where BEFORE differed by 8%. Tests 80/80 on the four relevant suites; the 4 full-suite failures are pre-existing and were baselined by stashing the edit.",
      "methodologyCorrectionForFutureSessions": "freezeAnimation(true) stops the clock ADVANCING but leaves it at an arbitrary reached value (measured 18.2175). Clouds read uTime, so ANY cross-session byte comparison of this lab is invalid unless the clock is PINNED via _lab.setAnimationClock(0). The first AFTER pass failed all three byte predictions for this reason alone. Also: a 2px canvas mismatch changes the aspect ratio and shifts a centred readback window — equalise canvas dimensions before comparing.",
      "correctionsToThisACsOwnText": [
        "meshPitch: the AC asserts 1.90463e-2 rad and calls it the binding pitch. That is the IDEALIZED hex-lattice closed form and it equals the MEASURED MEAN (1.93776e-2, over 119994 edges of the real buildIrregularSphere(40000,4) carrier). The measured WORST-CASE local edge angle is 2.53520e-2 — 33% coarser. For a Nyquist FLOOR the worst case governs: a feature sitting exactly on the AC's Shannon floor of 2 is delivered at n_s = 1.50 in the coarsest patches while the gate reports PASS, which is the same defect class already rejected once (amendments[1].alsoRejected). sqrt(4*pi/N) = 1.77245e-2 is MORE optimistic, so there is no cheap closed-form fix; use a measured constant and record the measurement.",
        "The mesh is correctly identified but is NOT where the AC implies: TARGET_N=40000 / LLOYD_ITERS=4 come from the frozen DEFAULT_PARAMS at planet-lod-rivers.js:59-60, consumed at :1423 inside createRiverOverlay's ensureMesh. rivers-lab.html:38 is a different lab. The lab's render mesh, SphereGeometry(R,256,256) at planet-lod-lab.html:1484, is NOT the relevant grid at all.",
        "Relief is SHADED, NOT DISPLACED (planet-lod-lab.html:1603 states it; the vertex shader at :204-226 leaves `position` untouched). The render mesh therefore never samples the height field, and the ANALYTIC body has no fixed angular Nyquist limit — it is pixel-rate and camera-driven via fwidth, with uOctaves swinging the top frequency 32x on camera distance alone. The gate formula can only apply to the BAKED body. The cube term survives intact: 2/256 = 7.8125e-3 rad is the face-centre value and a genuine upper bound (3.68e-3 at a corner).",
        "The 256x256 render mesh is NOT inert, though: it binds the E5 gas-giant per-vertex fields aBand/aShear/aMush/aStorm, CPU-baked from geometry.attributes.position and linearly interpolated. A render-mesh Nyquist term is meaningful for that field — just not for rock relief."
      ],
      "whyItem3IsSeparate": "Retiring bakeReliefCrossover with base = 1 makes uReliefBakeStrength == 1 at EVERY radius, which silently widens fieldSampler.js:764's l2AnchorGate from 'opens at R = 1 only' to 'opens across the whole band' — and NO TEST TURNS RED. It also strands the crater cube as a dead per-route GPU bake. Both need handling deliberately. Note item 3's ORIGINAL rationale is now partly moot: with the display law gone, the baked body was already exponent 0 and the analytic body becomes so too, so there is no radius-driven noise-ification left from this mechanism. The surviving justification for a gate is a FEATURE-SCALE guard (the carrier mesh's worst-case pitch is radius-independent), not a radius guard.",
      "sideEffectOnAC_ONEBODY": "Measuring the crossover band quantified the renderer/router split: in 1 < R < 4 the renderer draws a BLEND while route() picks ONE source and picks the pure baked carrier throughout — worst at R=3, where rivers route onto a body contributing 11.1% of what is drawn. At R >= 4 they agree on the relief BODY but NOT on craters: the router takes sampler.read() ('bare fbmd, no baked-cube blend, no crater restore', fieldSampler.js:771) while the renderer shows fbmd + C*base. A third case — a stale latched heightSource — was REFUTED: the 'route() never re-runs' comment at planet-lod-rivers.js:1542-1544 predates the radius slider; the real wiring re-routes on radius through a 220ms+220ms debounce chain, making it a bounded transient.",
      "confirmedBy": "Max, 2026-07-29 — \"go with b, your rec\""
    },
    {
      "date": "2026-07-30",
      "resolves": [
        "AC-PLATECOMP"
      ],
      "grounds": "Implemented after four read-only mapping agents and three independent design agents, each adversarially verified by a refuter that APPLIED the design in a scratch clone and ran the real suite. All three designs converged independently on the Gamma-free Earth-ratio form.",
      "correctionFoundDuringImplementation": "The monotonic DIRECTION inverts easily and was initially written backwards in the new test (and in working-Claude's report to Max): since N ~ mdf^-2, a THINNER mantle (bigger core) gives MORE, smaller plates, and a THICKER mantle gives fewer. Ocean has a SMALLER core than Earth (0.506 vs 0.546), hence a thicker mantle, hence FEWER plates -- which is why it drops 7 -> 6. The AC's own worked example runs the same way ('a low-core body ... gives 2.4x fewer plates'). The assertion caught it; the direction is now spelled out in plateCountTarget's docblock and pinned by a test that also checks the 2.38x magnitude.",
      "testDesignFlawFixedInPassing": "tests/worldengine-v2-4-province.test.js asserted controlEta2 <= nullP99, but province.js:217-226 draws the control from the SAME spatialNullPartition generator as the null ensemble -- so that assertion compares one sample against its own distribution's 99th percentile and fails ~1% of the time BY CONSTRUCTION (~10% across the suite's 10 cells). Ocean's re-partition duly tripped it (control 0.0878 vs p99 0.0797). The derivation was NOT weakened: real eta^2 at that cell measured 0.5429, HIGHER than the 0.4986 it gives with the composition term anchored off, and `pass` stayed true. Fixed by asserting the MEDIAN of five control draws is inside the null and that a MAJORITY are rejected -- strictly more evidence than one draw -- rather than by nudging a threshold on an honesty-critical gate.",
      "confirmedBy": "Max, 2026-07-29 chose 'Implement it anyway' after being shown that the payoff is one preset rather than the family the AC claimed"
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "body-condition-vector.js (REPO ROOT, not src/worldengine/base/) -- surfaceGravity, radiusEarth, compositionClass, volatileFraction, T_eq",
      "body-drivers.js (REPO ROOT) -- massGravity, repointed to the radius-aware condition gravity at 6779af3",
      "driver-presets.js -- the 18 presets; 8 reach a tectonic writer and are in scope",
      "src/worldengine/base/e1Regime.js computeE1 -- the geodynamic regime tuple that selects the writer",
      "planet-lod-rivers.js:575-607 -- the regime -> writer dispatch (NOT modified by this workstream; the Mars reroute is deliberately deferred)",
      "planet-lod-lab-core.js reliefEnvelope -- the v2 relief law (g^-0.58 below the seam, g^-1.678235294117647 above), shipped at 85cf3f0 and NOT reopened here",
      "macroSeed and the alea seed namespaces"
    ],
    "outputs": [
      "kmPerUnitOf(radiusEarth, surfaceGravity, path) -- the derived vertical calibration, plus its LAW_REGISTRY entry",
      "a corrected createHeightSampler that reads the rendered (baked + crater-restored) field",
      "km-honest readouts for mountain relief, crater depth, edifice height and canyon depth, replacing four dead constants",
      "a bimodal-hypsometry validator with a centre-of-figure re-reference step and a despun-preset negative control",
      "gProvince fed by the real craton/orogen/basin field from src/worldengine/base/province.js instead of FBM noise",
      "range strike derived from the plate boundary tangent computed at plates.js:289-291 (currently discarded)",
      "one declared vertical-exaggeration constant, stated in the readout beside the true km"
    ],
    "mustStayWorking": [
      "Suite baseline of EXACTLY 4 named failures (GalacticFeatures x1, KnownObjects x3) = 17 failed FILES (15 vendor + 2 real)",
      "verify-golden 40c18aad -- NO re-capture ever; note it guards fixed-timestep determinism, NOT relief",
      "fence-baseline.json and l0-baseline.json -- these are the artifacts that actually baseline relief",
      "Q_RELIEF = 0.58 must never be edited in place; reliefBudget.js:30 imports it and :91 pairs it with C_RELIC, a fit solved AT 0.58",
      "K_DT = 3.1 and D_D_SIMPLE = 0.20 -- correct as shipped, do not touch",
      "The 10 out-of-scope presets (despun and gas paths) -- highest regression risk is the gProvince rewire, which touches ~48 gated features globally",
      "uCraterBakeRestore = reliefBakeStrength - uReliefBakeStrength, the complement that keeps craters present as the bake fades (fixed at f88c2c6)",
      "The river router (routeAndOrder reads height/grad/isOcean) and the passive-margin shelf channel, both of which consume carrier.height",
      "tests/vis-scale-fence.test.js greps visScaleOf | sVis | VIS_SCALE_EXP INCLUDING COMMENTS across planet-lod-height.glsl.js, planet-lod-rivers.js, run-golden.mjs, canonical-scenario.js and all of src/worldengine/** -- three of four failures in one prior session were the word sVis in a comment",
      "The two standing NOT-OURS working-tree mods (src/auto/CameraChoreographer.js, src/debug/LabMode.js) -- never stage, never revert; use explicit paths in git add",
      "Lab :5175 page 2 is Max's tab -- never touch it; use an isolated context and close pages afterwards"
    ]
  }
}
