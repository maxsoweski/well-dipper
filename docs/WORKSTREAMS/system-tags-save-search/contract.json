{
  "workstream": "system-tags-save-search",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "building",
  "phase": "Phase 1 — debug/QA surface (player-facing UI is Phase 2, out of scope)",
  "acceptanceCriteria": [
    {
      "id": "AC1-derive-tags",
      "statement": "A pure tag-derivation pass turns a generated systemData into a tag set: isBinary, primary/secondary spectral type, planetCount, hasRings, hasHabitable, archetype.",
      "verifyVia": {
        "input": "Call deriveSystemTags(systemData) on known fixtures (e.g. Sol via KnownSystems, a seeded binary, a seeded ringed-giant system).",
        "observable": "Returned tags match the fixture's actual contents — Sol: not binary, planetCount=8, hasHabitable=true; a binary fixture: isBinary=true with both star types; a ringed fixture: hasRings=true."
      },
      "layer": "unit"
    },
    {
      "id": "AC2-cheap-fast-path",
      "statement": "A cheap fast-path derives the early tags (isBinary, spectral type(s), planetCount, archetype) WITHOUT running the per-planet loop, and never perturbs the full generator's RNG draw order.",
      "verifyVia": {
        "input": "Call the cheap derivation for a seed+galaxyContext; separately call full StarSystemGenerator.generate(seed, ctx).",
        "observable": "Cheap tags equal the full-generation values for those same fields; PlanetGenerator.generate is not invoked by the cheap path (spy/instrument); and a full generate(seed) before vs. after a cheap call produces an identical system (RNG order preserved)."
      },
      "layer": "unit"
    },
    {
      "id": "AC3-probe-shallow-search",
      "statement": "A probe aimed at an arbitrary galactic region (prism/cube/radius via HashGridStarfield) sweeps that volume and returns systems matching a cheap-tag filter, deterministically.",
      "verifyVia": {
        "input": "Run a probe at a fixed center+volume with a cheap filter (e.g. isBinary=true), twice.",
        "observable": "Both runs return the identical result list; every result lies within the requested volume and matches the filter; each result carries a navStarData snapshot + its tags."
      },
      "layer": "integration"
    },
    {
      "id": "AC4-deep-scan-modes",
      "statement": "Selectable scan depth: a 'deep' scan cheap-filters the region then fully generates the survivors to confirm expensive tags (hasRings, hasHabitable); 'shallow' does not.",
      "verifyVia": {
        "input": "Run the same probe+region with a hasRings filter at scan depth 'shallow' vs 'deep'.",
        "observable": "Deep results contain only systems whose full generation confirms a ringed planet (verified by re-generating and scanning planets[]); shallow returns the cheap-tag candidate set (a superset or proxy), and the two result sets differ as expected for that mode."
      },
      "layer": "integration"
    },
    {
      "id": "AC5-save-persists",
      "statement": "Saving the current system writes a navStarData snapshot ({worldX,worldY,worldZ,seed,type}) + derived tags to a persisted store that survives a page reload.",
      "verifyVia": {
        "input": "Invoke 'save current system' while in a system, then reload the page and read the saved-systems store.",
        "observable": "After reload, the store contains an entry with the system's navStarData + tags; the snapshot fields are non-empty and match the system that was active when saved."
      },
      "layer": "integration"
    },
    {
      "id": "AC6-faithful-reload",
      "statement": "Selecting a saved entry reproduces the IDENTICAL system via the existing Priority-1 navStarData warp path — not a near-miss.",
      "verifyVia": {
        "input": "Save a system, then reload it from the saved entry by feeding its navStarData back through the warp resolution path.",
        "observable": "The regenerated systemData deep-equals the original on identity fields: same seed, same star.type/star2.type, same planetCount, same per-planet types and rings (determinism proof)."
      },
      "layer": "integration"
    },
    {
      "id": "AC7-search-saved-list",
      "statement": "The persisted saved-systems list is itself filterable by tag.",
      "verifyVia": {
        "input": "Populate the saved list with mixed systems, then filter by a tag (e.g. hasHabitable=true).",
        "observable": "Only the saved entries whose tags match the filter are returned."
      },
      "layer": "unit"
    },
    {
      "id": "AC8-debug-panel-uat",
      "statement": "Max, with his hands in the debug panel, can see the current system's tags, save it, aim a probe at a region, run a tag-search, and warp to a result.",
      "verifyVia": {
        "input": "Max opens the debug panel in the running game, reads the current system's tags, clicks save, opens the probe, sets a region + tag filter, runs the search, and selects a result to warp to.",
        "observable": "He sees the tags rendered; the saved entry appears; the probe returns a matching list; selecting a result warps him into that system — all without console errors."
      },
      "layer": "uat"
    },
    {
      "id": "UX-1-live-tag-refresh",
      "statement": "While the panel is open, the SYSTEM TAGS grid, the Save-button enabled state, and the probe Center label re-render whenever the active system changes (warp / jump), without requiring a panel close+reopen.",
      "verifyVia": {
        "input": "Open panel in system A; trigger a system change (probe-jump or warp) while the panel stays open; read #debug-tags-grid.",
        "observable": "The grid shows system B's tags (matching window._lab.systemInfo()), not A's, with no reopen."
      },
      "layer": "uat"
    },
    {
      "id": "UX-2-save-guard-visible",
      "statement": "When there is no faithful nav snapshot (_currentNavStar null), the Save button is visibly disabled with a persistent reason, instead of looking clickable and rejecting on click.",
      "verifyVia": {
        "input": "Enter a system via _lab.enterSol() (no warp); inspect #debug-save-system; then arrive via probe-jump and inspect again.",
        "observable": "Disabled (greyed, not pointer, reason shown) before a faithful arrival; enabled after."
      },
      "layer": "uat"
    },
    {
      "id": "UX-3-fidelity-marking",
      "statement": "Tag summaries distinguish shallow (unconfirmed rings/hab) from confirmed sets; a pure tagSummary()/isShallowTags() pair produces the string and the flag. The '~' marker appears on unconfirmed probe result rows only.",
      "verifyVia": {
        "input": "Call tagSummary() on a cheap-tag set (hasRings:null) vs a full set; call isShallowTags() on each.",
        "observable": "Shallow set is flagged and its summary carries the '~' marker + omits rings/hab; full set is not flagged and shows rings/hab."
      },
      "layer": "unit"
    },
    {
      "id": "UX-4-distinct-save-affordance",
      "statement": "The result-row save control is visually/labelled distinct from the top 'Save current system' control, and saving a result CONFIRMS it to full fidelity (regenerates the system) so saved entries are never shallow.",
      "verifyVia": {
        "input": "Run a SHALLOW probe; inspect a result row's save button; save a '~'-marked row; inspect the resulting saved entry.",
        "observable": "Result save reads 'save' (text), not a bare '★'; the saved entry shows confirmed rings/hab tokens with NO '~' marker even though the probe row was shallow."
      },
      "layer": "uat"
    },
    {
      "id": "UX-5-result-count-truth",
      "statement": "Probe reporting tells the truth about truncation: the status names the matched count and whether the sweep hit its star cap, and the results list shows 'showing N of M'.",
      "verifyVia": {
        "input": "probeRegionDetailed() on a region denser than the sweep cap; run the probe in-panel.",
        "observable": "Returns {results, sweptCount, sweepCapped}; status notes the cap; results header reads 'showing 50 of <matched>'."
      },
      "layer": "unit"
    },
    {
      "id": "UX-6-human-filter-labels",
      "statement": "Probe filter labels match the human labels used in SYSTEM TAGS (Binary / Star type / Has rings / Habitable), not raw field names.",
      "verifyVia": {
        "input": "Inspect probe filter row labels.",
        "observable": "No 'isBinary'/'primaryType'/'hasRings'/'hasHabitable' code names shown to the user."
      },
      "layer": "uat"
    },
    {
      "id": "UX-7-polish",
      "statement": "Empty archetype renders a labelled '(none)' not a bare em-dash; removing a saved entry requires a confirm/arm step; new probe+saved form fields carry accessible labels (no a11y warning for them).",
      "verifyVia": {
        "input": "Inspect Sol's archetype row; click ✕ once then again; audit new fields for associated labels.",
        "observable": "'(none)'; first ✕ arms, second removes; new selects/inputs have aria-label."
      },
      "layer": "uat"
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "HashGridStarfield.findStarsInPrism / findStarsInCube / findStarsInRadius (region sweep over arbitrary center coords)",
      "StarSystemGenerator.generate / generateAsync and its _generateIterator (full system; basis for the cheap fast-path)",
      "GalacticMap.deriveGalaxyContext(position) — pure position->context (metallicity, age, starWeights, features)",
      "navStarData shape {worldX,worldY,worldZ,seed,type} and the Priority-1 warp resolution path (main.js ~2920)",
      "PlanetGenerator (rings @520, habitabilityScore @571) — the source of the expensive tags for deep scan"
    ],
    "outputs": [
      "DebugPanel.js — new controls: show current-system tags, save system, probe region + scan-depth + tag filter, jump-to-result",
      "a persisted saved-systems store (localStorage) keyed by navStarData snapshot + tags",
      "deriveSystemTags() + cheap-tag API — the seam a Phase 2 player-facing share UI will consume"
    ],
    "mustStayWorking": [
      "StarSystemGenerator determinism: same seed+galaxyContext -> identical system. The cheap fast-path MUST be read-only / replay the exact early Alea draws and must not change the full generator's draw order (Alea is sequential; out-of-order consumption changes everything downstream).",
      "Existing warp resolution Priority 1 (navStarData) and Priority 2 (starIndex) — unchanged behavior",
      "KnownSystems.findAt overrides (Sol etc.) still take precedence at their positions",
      "spawnWithSeed debug path continues to work (it is the lossy bare-seed path; not the save/reload path)",
      "scene-naming / _systemSeed body-id derivation unaffected"
    ]
  }
}
