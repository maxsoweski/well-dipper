# Seam map — orrery-coherence-2026-07-15 build grounding

Produced 2026-07-15 by a read-only opus Explore pass at the start of the build session
(branch `feature/supercruise-freelook`, post-8f1d8e8). Line numbers are point-in-time;
re-locate by symbol. All paths `src/main.js` unless noted. NOTE: `cameraController` in
main.js is a **ShipCameraSystem** (main.js:170); `CameraController.js` is dead-but-kept —
the live `viewSystem`/`focusOn`/`trackTarget` live in **ShipCameraSystem.js**.

## Ratified regime bit
- `_scManual` decl main.js:516 (`false` = ORRERY, `true` = HELM); `setScManual(on)` :529-540
  is THE universal flip point (except the commitBurn inline swap, §5).
- `window._regime()` :1854-1865 returns `{scManual, freeLookLatched, handsOn, flightMode,
  tour: autoNav.isActive, pilot: scPilot.isActive, pilotPhase, camBypassed}`.
- Regime derived inline as `_scManual ? 'helm' : 'orrery'` at 406, 8647, 8666, 9943, 9995.

## 1. Boot-skip branch (`_pendingBootReveal`)
- decl :2652; `_pendingBootMode` :2645 (via `bootModeAction`); consumed :6288-6291.
- HELM boot branch :6292-6316 (setScManual(true), autoNav.start, beginTour, returns).
- ORRERY boot-skip :6336-6345 — `if (_isBootReveal && !_autopilotEnabled)` → restore orbit
  cam at origin, focus indices -1, returns BEFORE the fly-in. Does NOT call viewSystem
  (no framed distance) — AC2 needs `cameraController.viewSystem(systemRadius)` added.

## 2. ORRERY system entry today (AC2)
- Selection: bg-star click `trySelect` :10100 → `trySelectWarpTarget` :10213 sets warpTarget;
  NavComputer search-hit → `dispatchNavAction` :2861 warp branch → `_setWarpTargetFromNavStar`
  :2939 + `beginWarpTurn()` (500ms timeout, :2894).
- Commit → cinematic: `commitSelection` :6652 → portal preview :6660-6710 →
  `warpEffect.start` :6708. WarpEffect: `onPrepareSystem` :3436 (snapshots
  warpTarget.navStarData at FOLD, :3515-3517) → `onSwapSystem` :3611 → `onComplete` :3809 →
  `warpRevealSystem()` :6237.
- Reveal → fly-in + shake: mid-session ORRERY warp misses the boot-skip branch → falls to
  :6347-6371 `_dispatchTourLeg(firstStop)` (pilot fly-in) + `shipChoreographer.beginTour
  ({fromWarp:true})` :6371 (the shake). = Max's "shaking cam and unskippable auto fly-in."
- **AC2 fork (build decision):** boot-skip reuse runs AFTER warpEffect played; AC2 verify
  demands NO warpEffect activation → ORRERY entry must bypass the cinematic via the
  instant-spawn path `spawnSystem({forWarp:false})` :4227 (camera setup :4258; precedent
  callers `_debugEnterKnownSystem` :2596, `window._lab.enterSol` :2065) + `viewSystem`.
- `viewSystem`: ShipCameraSystem.js:581-599 — target (0,0,0), distance systemRadius*1.5,
  SNAPS. Current caller: `focusPlanet(-1)` overview only (:6834, :6838).

## 3. Auto-warp timers (AC3)
| timer | warp site | gated? |
|---|---|---|
| title-end ~3:16 | :2546-2559 → `autoSelectWarpTarget(); beginWarpTurn()` :2554 | NO (only !_portalLabMode) |
| deep-sky/nebula linger 15s | `_deepSkyLingerTimer` :2472; set :5980/:8688/:2706; warp-away :8697-8717 | NO regime gate |
| mobile double-tap | touchend :10770-10773 `beginWarpTurn()` | NO |
| idle→tour | :8641-8651, :8662-8670 | YES — `idleFiresTour` |
- The mode-ownership "title-timer HELM gate" (:2707) gates the fallback TOUR-ARM in
  `dismissTitleScreen`, NOT the warp at :2554.

## 4. Tab / number-key cycling (AC1)
- One keydown handler :9509-10087. Autopilot-active branch :10020-10047 (HELM tour):
  Tab → `autoNav.advance(±1)` + `_beginTourLegMotion`; digits → jumpToStar/jumpToPlanet — fly.
- Normal-mode branch :10050-10086: Tab :10067-10076 → `focusPlanet((focusIndex+1)%n)`;
  digits :10081-10085 → `focusPlanet(idx)`. `focusPlanet(i>=0)` :6823 FLIES:
  `cameraController.bypassed=true` :6850 → `scControls.flyTo({linger:Infinity})` :6866.
  Only `focusPlanet(-1)` (overview) is view-only. **Tab/number cycling flies in ORRERY today.**

## 5. BURN button + commit path (AC4)
- Button `commit-burn-btn`; state fn `_updateCommitBurnButton` :6723-6743, called EVERY
  FRAME (:9247) + on select/deselect/commit. Visibility :6736-6738:
  `burning = flythrough.active || warpEffect.isActive || warpTarget.turning ||
  scPilot.isActive || _scManual; visible = !!_selectedTarget && !burning` — in ORRERY
  the button SHOWS for any selection. ORRERY hide goes here (every-frame = self-healing).
- `commitBurn` :6609-6642; the known silent swap :6628-6634 via
  `commitBurnSwapsToHelm(t.kind,_scManual,_isMobile)` (flightModes.js:185) →
  `setScManual(true)` inline. commitBurn also reachable via Space→`commitSelection`→
  `commitBurn` :6712-6713 and `dispatchNavAction` burn :2865-2883 — gate those too or the
  swap still triggers with the button hidden.
- Other ORRERY→HELM/flight paths: R-key engage :9887-9908 → `_enterFlightInternal` :9322 →
  `setScManual(true)` :9337 (EXPLICIT station action — sanctioned, keep); mobile double-tap
  warp :10772 (ship-flight leak, overlaps AC3); `_doModeSwap` :9436-9471 (the sanctioned
  M/HUD/options switch AC4 preserves).

## 6. NavComputer AUTOPILOT button (AC1)
- Drawn NavComputer.js:2792-2809; hit-test :2968-2979 → `_onAutopilotToggle(!active)` :2976.
- Callback wired UNGATED main.js:2794-2799: `enable ? startFlythrough() : stopFlythrough()`.
- `startFlythrough` :5935 in ORRERY still sets `_autopilotEnabled=true` :5970, autoNav.start,
  `_dispatchTourLeg` → pilot FLIES. Gate the callback on regime.
- **Lane C guard:** the search overlay (DOM input + `_searchFocused` capture) is a distinct
  surface from this canvas-drawn button — edit the callback only.

## 7. Mid-boot nav selection vs boot tour warp (AC6)
- Pending-warp state = ONE mutable `warpTarget` object; NO queue. `onPrepareSystem`
  snapshots `warpTarget.navStarData` at FOLD-start :3515-3517 — whichever write is present
  then, wins. Player dispatch during in-flight warp: `beginWarpTurn` no-ops
  (:10352 warpEffect.isActive / :10353 turning); N blocked during warp :9572; the write
  just overwrites `warpTarget` (pre-FOLD = accidental win; post-FOLD = ignored/stray).
- Hook options: `dispatchNavAction` :2861 (stash explicit pending-player-warp when a warp
  is in flight) + `warpRevealSystem` :6237 end (if stash set → skip tour arming, dispatch
  player warp). Teardown to respect: `beginWarpTurn` drops `_scPendingRealTarget` :10383;
  `onTourComplete`/AutopilotNavSequence :2427 is the boot tour's own re-warp source.

## 8. Tour-complete re-arm loop (AC7)
- `autoNav.onTourComplete` set :2427-2468, fired from AutoNavigator.js:149 on queue
  exhaustion → AutopilotNavSequence :2467 → onWarpReady :2437 → dispatchNavAction warp →
  new system → (HELM) reveal → next tour. Handler is UNGATED today (correct-by-construction
  in ORRERY, not pinned). Pin at :2427 top: re-arm IFF helm && hands-off (reducer).
- F-takeover :9951-9964 stops autoNav+scPilot → tour never completes → loop broken (Z
  re-arms via `_beginHandsOffTour` :10011). AC7 pins this explicitly.

## 9. Click-2 view glide substrate (AC5)
- Click: mouseup :10657-10723 (drag gate :10695/:10721), touchend :10761 → `trySelect`
  :10100 → `hitTestBodies` → `scControls.selectTarget(bodyHit)` :10168 → `selectTarget`
  :6463-6528. Selection state `_selectedTarget` :355. On select (when !bypassed): eases
  ORBIT PIVOT only (:6514-6516 `_targetGoal` + `_transitioning`), no distance/yaw change.
- Second click on same body today: re-runs selectTarget (sound + pivot re-ease). AC5 edit
  point: `trySelect` bodyHit branch :10166-10169 — same-body compare → glide-focus.
- `focusOn(position, viewDistance=8)` ShipCameraSystem.js:548-579 — SNAPS smoothedYaw/
  Pitch/Distance (:559/:562/:564). Debug-only callers: main.js:5159, :9837. Glide variant =
  set yaw/pitch/distance targets, LEAVE smoothed* alone → per-frame smoothing :937-944
  (`smoothing` factor lerp; ORRERY TOY_BOX `_applyOrbit` :978 is live) does the ease.
- Weld vs free: welded = `bypassed=true` + pilot leg (focusPlanet/flyTo/tour); free ORRERY
  vantage = `bypassed=false` TOY_BOX orbit. AC5 keeps bypassed=false, NEVER flyTo.
- Check `camera/__tests__/ShipCameraSystem.test.js` + `modeSwapNoSnap.test.js` before
  changing focusOn semantics; `flightExitAnchor.test.js` re-implements adoptCurrentPose/
  _applyOrbit math inline (ShipCameraSystem.js:732 DRIFT GUARD) — do not alter that math.

## Adjacent pinned tests (stay green)
modeOwnership.test.js (handRouting/zKeyAction/fKeyAction/idleFiresTour cells),
bootMode.test.js, escCascade.test.js (never-mode-switch), flightModes.test.js,
modeSwap.test.js, pointerHud.test.js, playerBurnTakeover.test.js (pins
commitBurnSwapsToHelm PURE — keep byte-identical, gate upstream), forcedProximityDrop/
tourStandoff/SupercruisePilot*/SupercruiseModel* (byte-identical guardrail files),
ShipCameraSystem.test.js, modeSwapNoSnap.test.js, flightExitAnchor.test.js.

## Risk notes
- AC2 must bypass warpEffect (instant-spawn), not just skip the fly-in.
- AC3 = three ungated warp sites (:2554, :8715, :10772), not one.
- Silent-swap surface wider than commitBurn: R-engage (sanctioned-explicit, keep),
  mobile double-tap (leak).
- Mobile: verify AC1/AC4 gates under `_isMobile` (touchend select/double-tap :10761-10783).
