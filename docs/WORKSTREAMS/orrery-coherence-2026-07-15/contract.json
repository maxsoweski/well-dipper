{
  "workstream": "orrery-coherence-2026-07-15",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "verified",
  "statusNote": "VERIFIED_PENDING_MAX 31eae77 — awaiting Max's AC8 UAT. Unit PASS (AC1 3/3-adv, wf_3786dbdc-f25) + live integration closed by working-Claude 2026-07-15 (verdict-live-drives-8b35ec4.json): AC2/AC3/AC4/AC5/AC7 live-GREEN incl. 4 live-caught fixes (entry framing anchor + scene-scale + wide-binary clamp; focusPlanet(-1) overview anchor; boot-aware linger gate — a HELM-boot-stranding regression; boot-flag consume on instant-cut). AC6 unit+static green, live folded into Max's UAT (turn-window not synthetically reachable; nav commit quirk = lane C's pre-existing identity family). FOR MAX'S AC8 UAT, taste items surfaced up front: (1) overview vantage is sparse — sub-pixel planets, no orbit rings at wide framing (pre-existing presentation, now player-facing on every entry); (2) click-2 glide reads as a fast snap-zoom across system-scale distances (log-ease ~0.5-1s), smooth at planet-to-planet range; (3) ORRERY boot no longer auto-warps — you enter systems yourself (bg-star click + Space, or nav); (4) post-FOLD nav redirect = brief double-warp (boot system flashes, never settles); (5) gentle auto-rotate inherited after instant-cut entry; (6) the nav AUTOPILOT button still renders in ORRERY (inert + console hint — hiding it is a NavComputer.js edit, lane D coordination). AC6 UAT step: boot HELM, press N the moment the boot warp TURN starts, pick a different star, commit — console [WARP]/[NAV DISPATCH] lines name the winner. RE-CHECK 2026-07-15 (fresh session, pre-UAT): 2-lens opus workflow wf_32f3fac9-81a + independent in-thread trace over 8f1d8e8..8b35ec4 — 1 HIGH fixed at e23050b (boot-window HELM downgrade: the four systemEntryStyle entry sites read raw _scManual, dishonest pre-reveal; now boot-aware via _effectiveRegime(), same read as the title-end/linger gates) + 1 LOW comment-corrected (focusPlanet(-1) binary overview pivots on the primary, not the barycenter — wide-binary pivot choice parked to well-dipper-binary-wide-separation). Suites 422/422, build green, byte-guards clean at e23050b. Verdict addendum: verdict-recheck-e23050b.json. NEW UAT STEP for the fix: boot HELM, press a key to dismiss the title, then IMMEDIATELY enter a system yourself (bg-star click + Space, or N + pick + commit) — you should get the CINEMATIC warp and arrive with the tour armed (pre-fix: silent ORRERY instant-cut, HELM pick destroyed). UAT ROUND 1 (2026-07-16): Max reported 4 findings (wonky/inverted mouse, no orbit lines, starfield-only clicks, vantage too far) — ONE root cause, live-diagnosed and fixed at ffea67c: the instant-cut skipped camera-mode housekeeping (persisted FLIGHT mode left the flight integrator owning the camera; framing writes never moved it) + skipped the A1 orbit-line sync. Entry now asserts TOY_BOX, frames at 1.8x outer orbit with pitch 0.7 overview tilt, raises maxDistance to 3x, and runs _syncOrbitsToMode. All four verified fixed through the real boot->pick->dismiss->click->Space circuit with the FLIGHT trap armed; zero console errors. WHY THE 8b35ec4 DRIVES MISSED IT: their entries started from settled post-warp states where the mode was already TOY_BOX — the cold-boot persisted-FLIGHT cell was not in the matrix. Residual taste items for re-UAT: mirrored system label on backside-azimuth entries (one-faced in-world text, pre-existing render, newly exposed); toy-box drag vertical sign is the legacy convention (drag down = camera rises) — flag if it still reads inverted. UAT ROUND 2 (2026-07-17): A1/A2 PASS; Max's 3 new findings (dogleg glide to clicked bodies; moons unapproachable again; planets frame far / stars close) root-caused by a 3-opus investigation workflow (wf_c2ee4336-a9f) to ONE method, the click-2 glideFocus: (F1) three independently-eased channels (pivot 0.06 vs yaw/pitch/distance 0.08) = the dual-lerp-spiral dogleg, intermittency = pivot residual gap at click-2; (F2) absolute floors (0.01 wheel clamp / 0.02 glide floor) vs 0.001-0.006-radius moons = 2.5R-20R lockout (HELM's radius-scaled 2.6R hold is why HELM felt fixed); (F3) flat radius*6 vs star glow 3R = stars at 76% frame / planets 27%. FIXED at 31eae77 via TDD fix workflow wf_635f2767-237 (red tests MEASURED the dogleg 40.67%/21.69% on the old code -> 0.00% after; moving-target 439.83-unit miss -> met at standoff): single-channel glide (one smoothstep t drives camera AND pivot to a live-tracked body vantage; frozen approach dir; no-snap back-solve handback on drag/wheel/select/Esc; post-arrival tracking lock), orreryStandoff table {star 15R, planet 4R, moon 2.6R, 0.002 degenerate-only floor}, focus min-distance radius*1.05 at select+glide with overview + mode-swap resets. 2-lens adversarial review PASS x2; 3 advisories folded in-thread (tiny-moon floor would have re-parked sub-0.0077 moons ~10R; cross-mode floor leak on M-swap; select-site stale floor). Gates at 31eae77: suites 428/428 (422 baseline + 6 new), byte-guards EMPTY, build green, live drive on :5173 (real canvas clicks, Sol system): select floor 1.05R exact x2, Saturn glide arrives 4R exact (1.6103) centered + locked, Esc cascade (deselect then overview) resets floor 0.01 + ends glide, console clean. Live straightness numbers are rebase-contaminated (world-origin rebase mid-glide, pre-existing infra) — path shape is unit-proven 0.00%. RE-UAT (round 2b) OWED: A2-A4 feel re-test (glide directness, moon approach, framing distances — the standoff table is a one-line tune at src/camera/orreryStandoff.js if a number feels off; glide duration 1.1s likewise) + Blocks B5-B7 still owed + open taste calls (drag sign, backside label). NEW pre-existing taste item surfaced: evenly-spaced wide systems (no >5x orbit gap, outer up to ~1367 AU) frame at the full outer orbit = dot-in-a-starfield overview (the entry's gap-break heuristic documents the factor as Max-re-rulable).",
  "phase": "Sibling A of the post-847ab19 pair (sibling B = autopilot-depart-2026-07-15; Max chose two siblings at scope 2026-07-15). Finishes the ORRERY half of the mode-ownership model: ORRERY is god's-eye player-driven viewing — nothing flies in ORRERY. Ready seams verified 2026-07-15: boot-skip branch (_pendingBootReveal, main.js ~6290) + cameraController.viewSystem framing = the non-cinematic entry; ShipCameraSystem.focusOn (camera/ShipCameraSystem.js:548, currently debug-key-only) = the click-2 primitive; NavComputer AUTOPILOT button (ui/NavComputer.js ~2795) calls startFlythrough ungated; commitBurnSwapsToHelm reducer family in flightModes.js. GUARDRAILS: SC_TUNING + SupercruisePilot/AutoNavigator/tourStandoff byte-identical (pilot motion belongs to sibling B); HELM behavior unchanged (boot screensaver, F/Z, warp cinematic); Esc-cascade first-branch + escCascadeAction pinned invariants stay green; lane C's NavComputer search overlay (DOM input + _searchFocused guard) untouched.",
  "acceptanceCriteria": [
    {
      "id": "AC1-orrery-no-ship-authority-reducers",
      "statement": "A pure reducer layer answers, for ORRERY, that no input or timer may produce ship flight: Tab/number-key body cycling, the BURN path, the NavComputer AUTOPILOT action, and the title-end/nebula-linger auto-warp timers all resolve to view-only or inert in ORRERY, while their HELM resolutions are unchanged. Built TDD in flightModes.js alongside the existing reducers.",
      "verifyVia": {
        "input": "Headless Vitest on the new/extended pure reducers in src/flight/flightModes.js: every (station=orrery, input/timer) cell vs its (station=helm) twin.",
        "observable": "All ORRERY cells return view-only/inert routings; all HELM cells byte-match today's routing table; suite green."
      },
      "layer": "unit"
    },
    {
      "id": "AC2-instant-framed-entry",
      "statement": "From ORRERY, choosing a star system enters it as an instant framed cut: the whole system framed in view, no warp cinematic, no camera shake, no auto fly-in, and the pilot never leaves idle. This is the production non-cinematic entry Max asked for ('how am I supposed to get into a system other than by...').",
      "verifyVia": {
        "input": "Live drive on :5173 (chrome-devtools :9223): boot ORRERY, select a system from the galaxy context.",
        "observable": "Next settled frame shows the target system framed (viewSystem-scale vantage); no warpEffect activation, no choreographer shake, PilotPhase stays idle throughout (window._regime()/_lab probes)."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC3-no-auto-warps-in-orrery",
      "statement": "The title-end (~3:16) and nebula-linger (15s) timers never fire a warp while in ORRERY; ORRERY idles indefinitely (the already-ratified 'ORRERY idles forever' ruling, now covering the warp timers).",
      "verifyVia": {
        "input": "Headless where the timer chain is pure; live fallback: boot ORRERY and let title-end/linger windows elapse (timers accelerated via probe where available).",
        "observable": "No warp fires; station remains ORRERY; camera vantage unchanged by any timer."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC4-burn-hidden-no-silent-swap",
      "statement": "In ORRERY there is no BURN FOR workflow at all: the button never renders for a selected body, and no code path silently swaps the station to HELM. Station changes happen only by the explicit station switch. (Max's pick: hide entirely, no crossover button.)",
      "verifyVia": {
        "input": "Live drive: boot ORRERY, select bodies incl. SHIP-target and planet targets; attempt the old burn/commit flows.",
        "observable": "No BURN button in the DOM in ORRERY; station stays ORRERY across all selection flows; HELM still shows BURN exactly as today."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC5-click-click-view-glide",
      "statement": "Click 1 on a body selects it (existing behavior); click 2 on the same selected body smoothly glides the VIEW over to frame that body (ShipCameraSystem.focusOn + glide easing). The camera is never welded to the ship; it works on bodies across the system. Max: 'click 1 selects, click 2 quickly moves us over to that body' — glide feel his pick.",
      "verifyVia": {
        "input": "Live drive: boot ORRERY, click a body once, then again; repeat on a second body.",
        "observable": "First click selects only (vantage unchanged); second click animates the vantage to frame the body over a short ease; camera tracking state is view-focused, not ship-welded."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC6-midboot-nav-selection-wins",
      "statement": "A nav-computer selection dispatched during the boot warp wins: the player arrives at the SELECTED system, not the boot tour's own warp target (the coordinator-flagged collision from lane C's live drives — player intent beats autopilot).",
      "verifyVia": {
        "input": "Live drive: boot HELM, open nav during the boot warp, select a different system before the boot warp completes.",
        "observable": "[WARP] log + arrival system match the player's selection; the in-flight boot tour warp is cancelled or redirected, never completes to its own target."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC7-screensaver-rearm-bounds",
      "statement": "The tour-complete re-arm loop (warp onward + new tour) continues only while hands-off in HELM — that IS the screensaver; it never arms in ORRERY, and taking the stick (F) or Z-stop ends the loop. Pins the boundary so lane-C's 18h-loop observation stays correct-by-design in HELM and impossible in ORRERY.",
      "verifyVia": {
        "input": "Headless reducer cells where pure (idleFiresTour family) + live spot-check: let a tour complete hands-off in HELM; then same in ORRERY; then F mid-loop.",
        "observable": "HELM hands-off: re-arm fires. ORRERY: never. After F: no further re-arm until Z."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC8-orrery-coheres-uat",
      "statement": "ORRERY works as a cohesive whole from Max's view: he can get into systems without the cinematic, browse and click-click around the system, and nothing ever flies him anywhere — the mode finally fits 'the whole point of Orrery.'",
      "verifyVia": {
        "input": "Max boots ORRERY on :5173/well-dipper/ and explores freely: enter a system, select bodies, click-2 glide between them, sit idle, try the old burn/tab habits.",
        "observable": "Max's holistic acceptance that ORRERY is player-driven contemplation with zero ship machinery leaking in."
      },
      "layer": "uat"
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "mode-ownership regime reducers (src/flight/flightModes.js: bootModeAction, idleFiresTour, commitBurnSwapsToHelm family) — shipped 847ab19",
      "boot-skip branch (_pendingBootReveal, main.js ~6290) + cameraController.viewSystem — the ready non-cinematic arrival",
      "ShipCameraSystem.focusOn (camera/ShipCameraSystem.js:548) — the ready click-2 view-move primitive",
      "NavComputer AUTOPILOT button (ui/NavComputer.js ~2795 -> startFlythrough) — the ungated seam AC1 closes",
      "title-end / nebula-linger auto-warp timers (main.js warp chain)"
    ],
    "outputs": [
      "ORRERY as a pure player-driven viewing mode (the ratified 'nothing flies in ORRERY' rule) — the register lane D's nav/deep-sky rendering work inherits",
      "Production non-cinematic system entry — first non-warp arrival path in the game",
      "Click-2 view-glide semantics available as an ORRERY-native navigation idiom"
    ],
    "mustStayWorking": [
      "HELM boot screensaver tour + F/Z semantics + warp cinematic in HELM (mode-ownership AC12, Max-UAT'd 2026-07-11)",
      "Esc-cascade first-branch + flightModes.escCascadeAction pinned invariants (lane B's two NavComputer invariants)",
      "Lane C NavComputer search overlay (DOM input, _searchFocused capture guard, search-hit warp) and catalog-true star positions",
      "SC_TUNING + SupercruisePilot/AutoNavigator/tourStandoff byte-identical (sibling B owns pilot motion)",
      "Full flight+auto suite (497/497 at 847ab19) + build green"
    ]
  },
  "greenlight": "Max, 2026-07-15 (scoping session, recovered from the interrupted 7/11 VS Code seam): both siblings greenlit; build order = orrery-coherence first, autopilot-depart second; builds run in fresh sessions via opus-pinned workflows."
}
