# world-engine-v2-1-e1-shadow-2026-07-03 — intent

## Why we care

Max's motivating concern for the whole condition-first re-founding (ROADMAP-v2 §0, verbatim):
**catalog-bounded variety — "every stagnant-lid world is a re-rolled Venus."** He wants condition
*combinations* to produce **predicted-but-never-observed** landforms, not re-rolls of the catalog.
And the standing process ask (SPINE-CONFORMANCE, verbatim): *"How can we always be operating in such
a way as to be procedurally building this system, as opposed to me having to constantly ask 'are we
tacking something on?'"*

V2-1 is the increment that makes both structurally possible: **E1, the derived regime selector**, run
in **SHADOW mode** — it computes every body's geodynamic regime from its condition vector and proves
(via the conformance oracle) that condition-derivation reproduces today's routing, while **dispatch
still reads `PRESET_ARCHETYPE` and no rendered world changes by a single byte**. Archetype is demoted
from load-bearing input to emergent label — spine §4b, executed on paper. Deliberately **zero
player-visible change**: the payoff lands at V2-2 (the pilot reads `L/Φ/n/m_hp`) and V2-3 (dispatch
flips).

**Program context:** serves the north star (*distinct, history-coherent worlds visible per minute*)
by standing up the selector the whole condition-first architecture routes through; JOURNEY milestone =
the SCREENSAVER heart's world-variety arc; no PLAYER_EXPERIENCE tier is touched this increment
(data-only — the tier lands when V2-2/V2-3 read what E1 emits).

## Success criteria (Max's language, extracted from the signed roadmap + gate briefs)

- **Shadow, zero-clobber:** E1 runs data-only on every lab body; **byte-identical everywhere** — the
  standing 75/75 carrier goldens, tune-null anchors, and baseStep goldens all stay green; dispatch
  still reads `PRESET_ARCHETYPE`.
- **The oracle:** `writerUnder(e1) === writerUnder(PRESET_ARCHETYPE)` over the **15 archetype-mapped
  presets**, minus the **enumerated allow-list** Max adopted (Frozen(airless)→dead-lid,
  Mars→stagnant/dead as *recorded expected divergences to fix at V2-3*, never silently matched);
  **Lava/Magma/Venus writer-equal** (the byte-identical trio).
- **Physics per the resolved gates:** `L` implements gate-1's pinned form exactly; `Φ` is resolved
  in-build (delegable #4) and **separates Mars from Venus on real field values**; `n` per gate-2's
  pinned form; `m_hp` keyed on the raw Io-ratio.
- **The contested middle is a seeded pick, labeled as such:** frozen weights + lab-only override
  (mirrors `_driverAbMode`), **no hysteresis input** (§7a), emitting a *resolved* regime +
  `positionWithinRegime` — and a seeded-`'stagnant'` pick sets an **effective `L` in the
  strong-mixed band** (the gate-2 §4 hand-up; the wet-stagnant falsification §5.4 #1 stays open
  without it).
- **Label is diagnostic output only** — nothing anywhere branches on it.

## DOES / UNLOCKS card (Rule 15; edges read from the ROADMAP-v2 §3.1 DAG, not invented)

**What it DOES** (each output × what sets it × what the player sees — *nothing yet, shadow by design*):

| E1 output | set by (driver → derivation) | read by (named consumer) |
|---|---|---|
| `compositionClass` | density/composition (D2/D9/D10) → Stage-A terminals + §1 label carve-out | oracle now; V2-3 dispatch tuple |
| `geodynamicRegime` | edges physics-derived (dead-lid/heat-pipe/icy); hot-surface edge data-placed; middle seeded (`'e1:regime:'`) | oracle now; V2-2 router, V2-3 dispatch |
| `label` | emergent from the above | diagnostic/probe ONLY (never an input) |
| `L` (+ `effectiveL` on stagnant picks) | gate-1 pinned form: z(T_surf,age)·μProxy(V,T_surf)·gMod | V2-2 router + `localYield(L,i)` |
| `Φ` | delegable #4, resolved in-build: radiogenic×mass + d³ + raw tidal (SH-F2: `d` ≠ `z` ≠ `shellThickness`-as-is) | V2-2 pierce boolean + `n` |
| `V` | D2 passthrough (`composition.volatileFraction`) | V2-2 stagnant response; pick weights |
| `n` | gate-2 pinned form: `f(Φ, 1/L)` | V2-2 SP-CENTERS count |
| `m_hp` | raw Io-ratio − peg (~0.45, tunable; delegable #6) | V2-2 heat-pipe hard gate |
| `e1Seed`, `positionWithinRegime` | macroSeed draw + band position | V2-2 within-region continua |

**What it UNLOCKS:** V2-2 (the pilot consumes `L/Φ/V/n/m_hp/effectiveL` — all three pre-code gates
resolved 2026-07-03 and feeding the V2-2 contract); V2-3 (dispatch flip to the derived 2-tuple +
Mars/Frozen reroutes + `PRESET_ARCHETYPE` retirement path); V2-5 (dead-lid regime home for
Moon/Mercury); V2-9a (Stage-A terminals for carbon/crystal). Per §3.1: *"the whole condition-first
architecture, zero-clobber; executes spine §4b on paper."*

## Deliberate non-goals (scope fence)

No dispatch change (V2-3). No writer changes, no lid-response code (V2-2). No game wiring (E1
lab-only until V2-10, §7a). No hysteresis/history-token input (§7a: seeded pick suffices). No
retirement of `PRESET_ARCHETYPE` (V2-3, only after every misroute is adjudicated). Mars + Hot
Jupiter stay outside the oracle (no archetype mapping; they join at V2-3).
