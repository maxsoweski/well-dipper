{
  "workstream": "real-universe-overlay-2026-07-12",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "verified",
  "phase": "Increment 5 (AC1 + AC6 + snum-pin + fill ruling) BUILT 3e58fac (2026-07-14; design 4775f5f w/ 2 read-only explorer passes) -> VERIFIED_PENDING_MAX. AC1: committed neighborhood-reference.json (19 Sol / 15 Sirius named neighbors within 5 pc, shipped-catalog distances, absent-famous gap documented: Wolf 359 / Lalande 21185 / Luyten 726-8 / Ross 154 / Ross 248 / Lacaille 9352) via deterministic gen-neighborhood-reference.mjs; NavComputer._queryYRange position-snap FIX (matched real stars now render at CATALOG positions + player-relative distances, per interview ruling 1 -- pre-fix a matched star could display >100% distance error); window._navComputer read-only handle. AC6: process-harris-catalog.mjs parses Harris Part III (r_t = r_c*10^c at rSun) -> 152 distinct globular radii 1.9-180.2 pc replace the uniform 0.03 placeholder; committed audit (audit-structures.mjs + structures-reference.json + structures-audit.md): 37/37 profiles within tolerance AFTER correcting 3 real position errors (M13 1.53 kpc off, M57 0.22 kpc, M45 z-sign); 152/152 globular positions self-consistent. snum==1 single-pin implemented (adopted deviation, AC4 case (e) amendment): RealSystemOverlay.resolve() no-table branch synthesizes {kind:'single', source:'archive-snum'}; one-directional, table-wins by construction; AC3 immunity vehicle moved TRAPPIST-1 -> 55 Cnc. Alpha-Cen A/B fill ruled FILL-ON by Max 2026-07-14 conditioned on an astronomy-plausible planetless rate -- both resolve to documentation (representation-cap section 6: the existing 8% empty roll + cited occurrence-rate basis + named-not-built AC8-safe ctx calibration seam). Suite 1340 -> 1404 (0 new failures); ProcgenSnapshot 24/24 (the enlarged-globular-radius hazard did not manifest); ZERO main.js edits (lane-B flag count stays 4); NavComputer seam record extended for lane D (increment-5-design.md: merge position fix + self-registration). verify-workstream full at 3e58fac (wf_1eab0d7b-3eb, 39 agents): AC4 + AC6 + AC7 + AC10 PASS 3/3 adversarial (AC6 flipped from FAIL-as-scheduled); AC1/AC2/AC3/AC5/AC8 static-INSUFFICIENT (live-evidence-only) CLOSED same day by chrome-devtools drives on :5176 (verdict-live-drives-3e58fac.json, TWO circuits): AC1 both vantages -- all 19 Sol + all 15 Sirius reference entries present by exact shipped name, worst distance error 0.03% vs the +/-2% tolerance, TRAPPIST-1 visible at 12.43 pc after the nav's bg Y-expansion; TRAPPIST-1 arrival under the pin = M single (isBinary false), exactly 7 knowns b-h; authored Alpha Centauri arrival = G2V+K1V @23.5 AU + Proxima far companion (b, d); Sirius = A1V+DA2 @19.8 AU (table-wins over pin live-confirmed); Sol suite 19/19 x3; console zero errors/warns. FLAG FOR AC9: Alpha Cen A/B carry ZERO fill planets on the authored seed (fill-on active; the deterministic seed lands the documented empty branch / circumbinary >=58.75 AU geometry) -- astronomy-plausible but Max may prefer the flagship system to carry worlds (small authoring knob if so). AC9 RUN 2026-07-15: FAIL (Max, verbatim + corrected mechanism in ac9-uat-findings.md finding #2) -- PRISM partially works minus label legibility; SYSTEM view does not match PRISM view for binary/trinary systems. Mechanism (live-verified on the 36 Oph trio, correcting the first same-day write-up): (1) seed identity is pipeline-dependent -- search/catalog quantize position to 0.1 pc (the trio collapses to ONE seed), the prism merge retains replaced hash-grid seeds, its unmatched branch uses a degenerate x^z XOR -- so the same named star generates different systems by selection path; (2) SYSTEM view for browsed systems is an overlay-less locally-generated preview (no companion table / no snum pin; live: Guniibuu preview 6 planets vs arrival K+K binary + 4); (3) un-tabled, un-hosted real stars roll fabricated procgen companions (pin reach = archive hosts only). Supersedes the ship-without-blocking disposition: BLOCKING for AC9; fix scope = drafted successor scope (oracle + companion table/dedup + N-dot glyph + label-declutter) PLUS three interview questions: seed-identity unification, preview honesty, fabrication-reach ruling. AC9 re-runs after it ships to :5176. Alpha-Cen fill resolved SHIP-AS-IS (empty-is-realistic, Max 2026-07-15; populate knob stays available as authoring content). Objective sweep record stands green (verdict-ac9-objective-sweep-3e58fac.json). Branch UNPUSHED (merge Max-gated; deploy = Pages). PRIOR: Inc 1 (AC7 ingest) 77723c2; Inc 2 (AC10+AC5) d106181; Inc 3 (AC3+AC4 bulk merge) c68c1fb; Inc 4 (AC2 search) 44c7075 -- each verified + live-driven, see increment designs + verdict addenda.",
  "acceptanceCriteria": [
    {
      "id": "AC1-neighborhood-fidelity",
      "statement": "In the neighborhood of Sol or another famous star, the Nav screen shows the stars Max would expect to see: real neighbors with real names at true positions, including newly ingested dim hosts (e.g. Proxima Centauri, TRAPPIST-1) that are not naked-eye but are nav-instrument-visible.",
      "verifyVia": {
        "input": "Enter Sol, open the nav computer; repeat from Sirius. Assert against the committed closed reference table docs/WORKSTREAMS/real-universe-overlay-2026-07-12/neighborhood-reference.json (authored in Increment 5: the exact expected named neighbors from Sol and from Sirius, each with a reference distance).",
        "observable": "Every neighbor in the reference table appears in the nav view/list with its real name, at a distance within \u00b12% of the table's reference value; newly added dim hosts (Proxima Centauri, Barnard's Star if below the HYG cut, TRAPPIST-1) appear despite being below naked-eye magnitude; zero table entries missing. The open-ended 'does the neighborhood look right' judgment stays in AC9."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC2-player-search",
      "statement": "A player-facing search in the nav computer finds a system the player knows about \u2014 real star systems (proper name or designation), named settled/notable catalog systems (the ac5 settled-catalog UI ask folds in here), and deep-sky structures \u2014 and selecting a result targets it for travel.",
      "verifyVia": {
        "input": "Open the nav computer search; query 'TRAPPIST-1', 'Sirius', 'M42', and one settled-catalog name from the gazetteer; select each result and travel/warp.",
        "observable": "Each query resolves to the right object; selection targets it; warp arrival lands on a system whose name matches the query (structures: nav/travel points at the structure's true position). Search is reachable in normal play without the debug panel. Typing in the search field does not trigger the nav map's W/A/S/D/R/F bindings (the capture-phase keydown handler is reconciled with text focus)."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC3-real-characteristics",
      "statement": "Systems Max knows about match the characteristics already observed (type of star, known planets, etc.): arriving at a known real system spawns its real structure from the ingested data (exoplanet archive + curated companion table) via the overlay merge \u2014 NOT via new KnownSystems registry entries (findAt at Sirius stays null; authoring stays scoped to Alpha Centauri in AC5).",
      "verifyVia": {
        "input": "Warp to Sirius and to TRAPPIST-1 (any targeting path).",
        "observable": "Sirius: A-class primary plus its white-dwarf companion present as a binary, sourced from the curated companion table (companion class D per AC10 engine support). TRAPPIST-1: M-dwarf with its seven known planets carrying their real designations (b\u2013h) and archive-derived parameters. Star class matches the sky/catalog on every arrival path."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC4-procgen-fill",
      "statement": "When real data runs out, the system fills the rest with procgen (Elite as the guide): known planets appear per the ingested data, procgen deterministically completes the remainder; real star type always comes from the catalog; real structure (multiplicity) applies exactly where the companion table covers it (the table may also pin famous singles), and procgen fills structure elsewhere. ADOPTED 2026-07-13 (deviation to post-review ruling 6): an archive snum==1 on a non-table host is an implicit single-pin \u2014 a real host whose archive record says the system has exactly one star suppresses the procgen companion roll. One-directional (it only suppresses fabrication, never adds structure; snum>=2 hosts keep the live roll) and the curated table still wins wherever both apply.",
      "verifyVia": {
        "input": "Headless: generate contents twice for (a) a real system with partial known planets, (b) a real star with zero known planets and no companion-table entry, (c) a companion-table binary, (d) a pinned-single star, (e) a snum==1 non-table real host on a seed that would roll a binary, through the real arrival pipeline (StarSystemGenerator + overlay merge).",
        "observable": "(a) all known planets present with archive-derived parameters, remainder procgen-filled, both runs identical (deep-equal on systemData; revisit-stable); (b) catalog star type preserved, structure and planets fully procgen, identical across runs; (c) table-specified companion class present, identical across runs; (d) singleness honored \u2014 procgen may not invent a companion for a pinned single; (e) the snum==1 non-table host produces NO companion (isBinary false, star2 null) on a seed that WOULD roll a binary absent the pin, with any known planets intact and both runs identical \u2014 while a snum>=2 non-table host keeps its live roll (one-directional boundary) and a table-covered host ignores snum (curated table wins)."
      },
      "layer": "integration",
      "live": false
    },
    {
      "id": "AC5-authoring-path",
      "statement": "The ability to author systems and place them over procgen ones exists as a data-driven capability: a KnownSystems registry entry defined as structured data (not a hand-written generate() function) produces full system contents \u2014 proven by registering Alpha Centauri as the A+B binary claiming all three catalog stars, with Proxima as a far companion carrying its known planets (A+B structure drawn from the Increment-1 companion table, Proxima's planets from the Increment-1 archive ingest \u2014 the registry entry duplicates no multiplicity data; exact far-companion mechanism is build latitude within AC10's representation rules).",
      "verifyVia": {
        "input": "Register Alpha Centauri as a data-defined entry; arrive via all three paths (nav-warp, sky-click warp, teleport), including an arrival targeted at Proxima's position; run the KnownSystems test suites.",
        "observable": "All three arrival paths spawn the authored system (A+B binary + Proxima far companion with its known planets present \u2014 no procgen impostor at any of the three stars); findAt(RIGIL_POS) resolves to Alpha Centauri (the a1d2d4c successor-flag test flips from 'does NOT match Sol'); alias derivation claims Rigil Kentaurus, Toliman, AND Proxima Centauri; belt/sweep tests stay green with the multi-star entry."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC6-structures-audit",
      "statement": "Famous structures (nebulas and so on) match observation: the shipped deep-sky objects (37 KnownObjectProfiles) and real globular clusters (152, Harris) sit at observed positions and sizes, with discrepancies fixed or explicitly documented.",
      "verifyVia": {
        "input": "Run a committed headless audit comparing profile/catalog positions and radii against reference catalog values.",
        "observable": "Audit report committed to this workstream dir; the famous set (M42, Horsehead/IC434, M78, 47 Tuc, ...) within documented tolerance; each out-of-tolerance entry either corrected or carries a written rationale."
      },
      "layer": "unit",
      "live": false
    },
    {
      "id": "AC7-ingestion-pipeline",
      "statement": "Build-time ingestion delivers the data everything else consumes: re-runnable scripts ingest (i) a public confirmed-exoplanet archive (candidate: NASA Exoplanet Archive Planetary Systems table \u2014 source, license, and counts verified in this increment), (ii) dim famous hosts as new real catalog stars at true positions, and (iii) a small curated stellar-companion table (famous systems first: Sirius, Procyon, Alpha Centauri; may pin famous singles) \u2014 with build-time verification of exactly the fields downstream ACs consume.",
      "verifyVia": {
        "input": "Run the ingestion scripts twice; run the existing naming/catalog test suites against the enlarged data.",
        "observable": "Two runs byte-identical; ingested counts reported (systems, planets, new host stars, companion entries); no duplicate star identities; REQUIRED-FIELD coverage asserted: every ingested host carries position + spectral type; every ingested planet carries its designation letter, at least one orbital parameter (period or semi-major axis), and at least one physical parameter (mass or radius); system star/planet counts present; companion-table entries carry an explicit kind discriminator \u2014 multiple entries carry component class + separation, single-pin entries carry a singleness marker and no companion fields. The companion table is the SINGLE multiplicity source of truth: consumed by both the overlay merge (AC3/AC4 \u2014 Sirius, Procyon) and the AC5 registry authoring (Alpha Centauri's A+B structure), never duplicated. The enlarged real-proper-name set feeds the procgen blocklist and the named-systems build-time checks; if any new real name collides with a shipped settled/notable catalog name, regenerating namedSystemsCatalog.js via gen-named-systems.mjs under the enlarged blocklist is IN-SCOPE remediation (shape disjointness does not protect bare-word proper names). The full-catalog sweep test stays green (new hosts near known-system entries resolve via alias membership, not swallow)."
      },
      "layer": "unit",
      "live": false
    },
    {
      "id": "AC8-sol-and-regression-guardrail",
      "statement": "The universe that already works stays working: Sol still resolves as Sol with handcrafted contents (always-test-Sol), the full test suite stays green, and purely procedural systems \u2014 those touched by no real data \u2014 generate identical contents before vs after this workstream (seed-stream isolation holds).",
      "verifyVia": {
        "input": "Per increment: window._lab.enterSol() + window.__wd.runIntegrationSuite(); full vitest run from the repo dir; a pinned snapshot test over sample procgen-only seeds/positions captured before Increment 1 and RE-FILTERED after Increment 1 against the ingested position set (so positions that legitimately became real-covered are excluded rather than reported as false regressions).",
        "observable": "Scene suite 19/19 in Sol; vitest fully green (baseline 1123/1123 grows, never shrinks); the re-filtered procgen-only snapshot identical (deep-equal on systemData) across the workstream."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC9-known-universe-uat",
      "statement": "Max's holistic acceptance: the universe he knows is actually there and findable as a cohesive whole \u2014 the nav neighborhood looks right from home, searching finds what he expects, arriving matches what astronomy says, structures included.",
      "verifyVia": {
        "input": "Max explores freely: nav from Sol, searches systems and structures he knows, warps to Sirius / Alpha Centauri / TRAPPIST-1, checks the fill against his Elite intuition.",
        "observable": "Max's pass/fail-with-findings judgment, recorded in this workstream dir. The workflow never marks this AC \u2014 deferred-to-max."
      },
      "layer": "uat"
    },
    {
      "id": "AC10-engine-structural-support",
      "statement": "The engine can represent observed stellar structure (it currently cannot: star types stop at O/B/A/F/G/K/M and systemData carries at most star + star2): a degenerate/white-dwarf star class works end-to-end (generation contents + rendering parameters, no G-type fallback warning), the generation context gains overlay fields (companion spec, known-planet list), and a far-companion representation covers the Alpha Centauri family. Representation cap is explicit and documented: at most 2 close stars per system; wide companions are represented as far companions or adjacent real systems; closer higher-order multiples collapse to the brightest pair with a documented caveat.",
      "verifyVia": {
        "input": "Unit tests over StarSystemGenerator + the overlay merge: generate a system with a class-D companion; generate with injected known planets; generate the far-companion configuration.",
        "observable": "Class-D companion emerges with white-dwarf parameters (no fallback warn); injected known planets appear with their designations while procgen fills the rest; far companion present in systemData in the documented shape. Overlay fields (far companion, known-planet injection) are OMITTED from systemData when the overlay supplies none \u2014 never emitted as always-present nulls (unlike the star2:null pattern) \u2014 so purely-procgen systemData stays deep-equal to the pre-Increment-1 AC8 baseline. The representation-cap rules are written down in this workstream dir and cited by the tests."
      },
      "layer": "unit",
      "live": false
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "Confirmed-exoplanet archive (candidate: NASA Exoplanet Archive Planetary Systems table) \u2014 NEW external data source, fetched at build time only; source/license/counts verified in Increment 1; required fields per AC7",
      "Curated stellar-companion table (NEW, small, authored in-repo: famous binaries/multiples + pinned famous singles) \u2014 the ONLY multiplicity source; bulk double-star catalogs (e.g. WDS) deliberately NOT ingested (noisy optical pairs)",
      "HYG real-star catalog (public/assets/data/hyg-stars.json: 15,599 entries, 15,560 usable names, 39 empty, 0 quote-artifact) + a dim-host supplement at true positions (same processing pattern as scripts/process-hyg-catalog.mjs)",
      "KnownSystems substrate from a1d2d4c: associate()/claimedStars (built as this overlay's foundation, currently unused), findByAlias (alias index + 3 pc belt), MATCH_RADIUS/NAME_JOIN_RADIUS/POSITION_MATCH_TOL ordering pinned by test",
      "StarSystemGenerator + galaxyContext.starTypeOverride arrival path (main.js ~3405) \u2014 the seam where overlay contents replace/augment procgen; grows the AC10 overlay fields",
      "Naming precedence chain (KnownSystems > real-star names > named-systems catalog > procgen) \u2014 precedence unchanged; the named-systems catalog's CONTENTS may regenerate under the enlarged blocklist per AC7's contingency",
      "NavComputer real-star overlay (setRealStarCatalog / findInVolume, src/ui/NavComputer.js ~2396) \u2014 AC1's existing substrate. NOTE the AC2 seam honestly: NavComputer is a canvas-2D component (custom fillText render loop, NO DOM inputs) with a capture-phase document keydown handler that preventDefaults W/A/S/D/R/F \u2014 the search input must be a DOM overlay or custom canvas widget PLUS keyboard-capture reconciliation; only the resolution logic ports from DebugPanel. Larger than a trivial seam; coordinator-flagged (lane-D adjacency)",
      "DebugPanel search (src/ui/DebugPanel.js ~298, searchKnownObjects) \u2014 prior art for search RESOLUTION logic only (it is DOM; NavComputer is canvas); debug panel itself stays as-is",
      "KnownObjectProfiles (37) + RealFeatureCatalog (152 Harris globulars) \u2014 AC6 audit subjects and AC2 search targets",
      "namedSystemsCatalog enumeration (enumerateNamedSystems) \u2014 AC2's settled/notable search feed (ac5 addendum ruling 2)"
    ],
    "outputs": [
      "Shipped overlay data: exoplanet/system-contents catalog + curated companion table + enlarged real-star catalog with dim hosts \u2014 consumed by arrival generation, nav, and search",
      "Data-driven KnownSystems authoring path (structured entry -> full system) \u2014 the durable capability for future story-location/creative authoring",
      "Engine structural capabilities (degenerate star class, known-planet injection, far-companion representation) \u2014 reusable beyond this workstream",
      "Player-facing nav search surface (real + settled/notable + structures) \u2014 also the designated future home of the parked system-tags-save-search idea",
      "Enlarged real-proper-name blocklist feeding the naming uniqueness invariants",
      "Structures audit report (AC6) \u2014 evidence base for lane-D deep-sky successor work"
    ],
    "mustStayWorking": [
      "Naming uniqueness invariants with the enlarged real-name set: procgen injectivity, real-proper-name blocklist, catalog/procgen shape disjointness (NameGenerator.injective tests + census baseline); if the AC7 contingency regenerates the named-systems catalog, its build-time checks (duplicate/blocklist/round-trip) must re-pass in full",
      "Sol full override + 19/19 scene suite (always-test-Sol; Sol IS the known-system code path)",
      "Real-name + real-spectral-type precedence on all three arrival paths (nav-warp main.js ~2833/~3405, sky-click, teleport ~7f5fd1e)",
      "Procgen systems untouched: any system covered by no real data generates identical contents pre/post (content RNG stream isolation, AC8 re-filtered snapshot; overlay systemData fields omitted \u2014 not null \u2014 on untouched systems per AC10)",
      "Tolerance-ordering invariant (POSITION_MATCH_TOL < MATCH_RADIUS < NAME_JOIN_RADIUS, pinned by test) and belt accept/reject semantics \u2014 the multi-star Alpha Centauri entry (Proxima 0.06 pc from the A/B pair, inside MATCH_RADIUS) must resolve via alias membership per the d8d6b63 identity-agreement exemption, never via radius shrinking",
      "Lane boundaries: main.js warp-arrival/nav-arm blocks = minimal surgical edits flagged in the seam handoff (lane B adjacency); NavComputer internals are lane-D-owned \u2014 the AC2 search UI is a coordinator-flagged seam sized per the inputs note; no edits to lane-D GalacticMap regions (density/clouds/features)",
      "Full vitest suite green from the repo dir (baseline 1123/1123; vendor/motion-test-kit collection errors are pre-existing noise)"
    ]
  }
}