# world-engine-v2-3-dispatch-flip — intent

## Why we care (Max's words, 2026-07-11)

> The name tag stops being load-bearing. After this, what a world looks like comes from its
> derived conditions, and the archetype is just a diagnostic label — the condition-first bet
> is live in the real dispatch, not just the lab.

This is the milestone the condition-first re-founding (ROADMAP-v2, signed 2026-07-03) has been
building toward: V2-0 surfaced the conditions, V2-1 derived the regime in shadow, V2-2 built and
falsification-tested the response space — V2-3 makes the derivation *the* production routing.
It's the last step before the program returns to expression work (V2-4+) on an honest foundation.

**Orientation:** serves the JOURNEY world-engine objective ("condition combinations produce
predicted-but-never-observed landforms"); PLAYER_EXPERIENCE-wise still lab-only by charter —
the screensaver sees this at V2-10 game-port.

## Success criteria (Max's language, confirmed 2026-07-11)

- Flipping the switch changes nothing I can see: every preset except Frozen renders exactly as
  before, byte-provable, across seeds. *(Amended same day, adversarial-lens finding M2: Hot
  Jupiter joins Frozen as a second adjudicated reroute — today it wrongly rides the icy-shell
  writer via the locked-fallback; the fix is byte-real but visually masked by the gas-row
  relief gating. Surfaced to Max in the amendment report.)*
- Frozen is the one deliberate change, and it reads as the correct "dead frozen ball" fix,
  not a bug.
- No label routing anywhere: dispatch reads only derived conditions; the archetype string
  survives only as a cross-check oracle.
- All 17 presets adjudicated in the oracle (Mars + Hot Jupiter join), with the old routing
  deleted only after post-V2-3 verify + my UAT.

## DOES / UNLOCKS (Rule 15 card)

**DOES:** flips `writeBodyRelief` production dispatch from the `PRESET_ARCHETYPE` string chain
to E1's derived `{compositionClass, geodynamicRegime}` (+ dispatch-level locked-awareness),
routing the unbroken-lid family through the V2-2 router; fixes the adjudicated Frozen(airless)
misroute; adds Mars + Hot Jupiter to the conformance oracle; threads `tidalState.locked` into
the condition vector; resolves the Neptunian/Sub-Neptune short-key taxonomy collision with zero
visible change; promotes `MIXED_LO` to a shared `e1Regime` export; reconciles the router
weak-branch `appliedTune` probe parity.

**UNLOCKS:** type-as-derived-label live (ROADMAP §3.1 V2-3 row) — every later increment
(V2-4 substrate, V2-5 bombardment/dead-lid home, V2-5s shell-MULTIPLY, V2-7 epochs, V2-8
sculpting) keys expression on conditions a production selector actually emits; the archetype
predicates (`isVolcanicPath` et al.) become deletable (post-verify + Max UAT, follow-up).

## Deliberate non-goals (fences)

- No game-port wiring (`_pickType` untouched; E1 stays lab-only per charter — V2-10).
- No new expression work (no writer/composer feature changes; V2-4+ owns that).
- No lab-GUI reorganization inside this contract — the lab UX pass runs alongside as
  main-session work, judged by Max, not gated by these ACs.
- The seeded multistable pick does NOT go production-live for named presets (modal collapse
  at dispatch — designDecision #1); per-seed regime variety arrives with seeded bodies at V2-10.
- `PRESET_ARCHETYPE` and the old dispatch predicates are NOT deleted here (fallback oracle
  until the retirement trigger: post-V2-3 verify-workstream green + Max UAT).
