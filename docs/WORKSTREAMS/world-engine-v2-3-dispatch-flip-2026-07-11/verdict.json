{
  "workstream": "world-engine-v2-3-dispatch-flip-2026-07-11",
  "commit": "b08cd01",
  "perLayer": {
    "unit": "PASS",
    "integration": "INSUFFICIENT",
    "uat": "deferred-to-max"
  },
  "perAC": [
    {
      "id": "AC-0",
      "result": "PASS",
      "evidence": "Driver connectivity confirmed by direct source-slice read of writeBodyRelief's condition-bearing branch (rivers.js:472-557): zero reads of PRESET_ARCHETYPE/.label/stagnantLidRegimeOf(/isVolcanicPath(/isEarthlikePlatePath(/shellRegimeOf(/bare `archetype`; named readers of tidalState.locked and MOBILE_L confirmed; no new taxonomy key added (Neptune Option B = comment-only). 8 targeted test files (331/331) + full suite (1945/1949, 4 pre-existing baseline failures, unchanged) all pass.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC-FLIP-LABEL-FREE",
      "result": "PASS",
      "evidence": "tests/worldengine-v2-3-dispatch-oracle.test.js (24/24) plus independent manual read of the dispatch block confirm routing derives purely from computeE1's tuple + locked + modal collapse, is seed-invariant across 7 seeds, distinguishes Europa/Titan, and survives a PRESET_ARCHETYPE garble test unchanged. Guardrail quartet (68/68) + full suite (1945/1949, baseline-only failures) green.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC-ORACLE-17",
      "result": "PASS",
      "evidence": "tests/worldengine-v2-3-dispatch-oracle.test.js (24/24) adjudicates all 17 DRIVER_PRESETS against the pinned table: exactly 15 writer-identical, exactly 2 named/dispositioned reroutes (Frozen, Hot Jupiter). V2-1's 15-preset shadow oracle (worldengine-e1-conformance-oracle) unchanged and green (9/9), pinning that only dispatch changed, not E1. Guardrail suites 142/142; full suite 1945/1949 (baseline-only failures).",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC-PLUMB-RECONCILE",
      "result": "PASS",
      "evidence": "(a) condition.tidalState.locked present/consumed as specified, tested for all 17 presets and byte-safety across 3 tune builders. (b) MOBILE_L promoted to single exported source of truth in e1Regime.js, local duplicate literal deleted, comment-stripped-source test confirms. (c) .appliedTune set unconditionally on both router corners, structurally confirmed by source read AND an ad-hoc live exercise of the real dispatch on Lava/Magma/Venus/Rocky bundles (cleaned up after) -- flagged as a genuine coverage gap: no permanent automated test asserts .appliedTune presence on this path. 8 relevant files/362 tests pass; full suite baseline-only failures.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC-TAXONOMY-NEPTUNE",
      "result": "PASS",
      "evidence": "driver-presets.js change is comment-only (Option B: shared 'sub-neptune' key kept, not split) -- explicitly permitted by the AC's own verifyVia OR-clause. New tests/worldengine-v2-3-taxonomy.test.js (8/8) confirms identical archetype key + radius-range reference for both Ice-giant/Sub-Neptune presets, neither resolves gas-giant range, both route 'despun', and PRESET_ARCHETYPE deep-equals the frozen 15-key golden fixture. Full suite (1653/1653 non-vendor tests) and byte-safety guardrails (161/161) green. Note: AC prose reads as Option A ('distinct identity') but contract.json's own design-decision explicitly gates Option A out of scope; Option B taken and tested is the sanctioned path.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC-ZERO-CLOBBER",
      "result": "PASS",
      "evidence": "All 7 sub-clauses (a-g) independently verified with real command output: byte-identity harness 83/83 (70 bit-identical rows + 5 despun-carve-out rows for Frozen); byte-anchor tests for Lava/Magma/Venus/Eyeball green; migration-bridge mechanism confirmed present verbatim carrying ~8 legacy condition-less callers; both guardrail repurposings (e1-shadow-audit, lid-router-audit) present and green with no other guardrail file touched; full suite 4 failed/1945 passed matches the documented pre-existing baseline exactly, did not grow; verify.js's regime enum unchanged; diff touches only the AC's allow-listed files -- the only working-tree noise (CameraChoreographer.js, LabMode.js) is explicitly fenced-off unrelated V2-7d work, confirmed uncommitted and untouched by any V2-3 commit.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC-LIVE-SWEEP",
      "result": "INSUFFICIENT",
      "evidence": "Objective live-integration AC requiring a real chrome-devtools session against a running dev server -- not closable by static/CLI analysis. Static grounding done (diff read, headless oracle 115/115 green, lab UI plumbing confirmed unchanged/reusable via window._lab.setPreset + family probes), but no browser was actually driven. GAP FLAGGED: grepped the full V2-3 diff for new console.log additions in the touched files -- zero matches, so the AC's 'logged fallback-oracle comparison' artifact does not exist in the code as built; only probe-inference is available live. Deferred to working-Claude (main thread) to drive: start `npm run dev -- --port 5175`, attach chrome-devtools debug session, load planet-lod-lab.html, sweep >=9 DRIVER_PRESETS (must include Rocky, Ocean, Lava, Magma, Venus, Frozen, Eyeball, Mars, plus one giant) via window._lab.setPreset(name), and cross-check routes against the pinned 15-identical/2-reroute table using the existing plateProbe/shellProbe/magmaProbe/stagnantLidProbe/mixedProbe/e1Probe functions.",
      "adversarialVotes": "0/3 sufficient"
    },
    {
      "id": "AC-UAT-SWEEP",
      "result": "deferred-to-max",
      "evidence": "UAT is Max-only by design; agents cannot PASS it. Currently not yet actionable: contract.json status is still 'verifying' (no verdict.json exists), and this AC explicitly presupposes AC-LIVE-SWEEP plus all unit/integration ACs green -- AC-LIVE-SWEEP has not been driven yet (see above). Underlying unit/integration test health re-confirmed as corroboration (4 files/124 tests + guardrail quartet 4 files/98 tests + full suite matching documented baseline), but this does not substitute for the live sweep or produce acceptance.",
      "adversarialVotes": "n/a — UAT is Max-only"
    }
  ],
  "overall": "INSUFFICIENT — unit layer PASS (5/5 ACs, 3/3 adversarial consensus each). Integration layer PARTIAL: AC-ZERO-CLOBBER PASS (3/3), but AC-LIVE-SWEEP is INSUFFICIENT (0/3 adversarial) because no live chrome-devtools browser drive has occurred yet — this is an objective gap, not an acceptance gap, and is deferred to working-Claude (main thread), not Max. UAT (AC-UAT-SWEEP) is deferred-to-max as always, and is currently blocked on AC-LIVE-SWEEP going green first. Workstream cannot move to VERIFIED_PENDING_MAX until the live sweep is driven and passes.",
  "summaryForMax": "Line of sight: this is the V2-3 dispatch flip in the World Engine history-systems program (ROADMAP-v2.1) — it's the step right after V2-2b-2b that makes planet surface writers actually route off the derived E1 driver state instead of the old archetype/label lookup, which is the load-bearing mechanism the rest of the roadmap (V2-4+) builds on.\n\nWhere it stands: unit layer is fully green — all 5 headless ACs (driver connectivity, label-free routing, the 17-preset dispatch oracle, plumbing/probe reconciliation, Neptune taxonomy) verified by real vitest runs at b08cd01, each independently checked 3/3 by adversarial review. AC-ZERO-CLOBBER (the byte-safety/no-clobber guardrail) also passed 3/3.\n\nWhat's blocking: AC-LIVE-SWEEP has not been driven yet at all — nobody has opened the lab in a real browser and swept the presets since Slice C landed. This is not your gate; it's working-Claude's next job (start the dev server on :5175, attach chrome-devtools, sweep >=9 presets including Rocky/Ocean/Lava/Magma/Venus/Frozen/Eyeball/Mars plus one giant, and cross-check the routes against the pinned 15-identical/2-reroute table using the existing probe functions). One thing to be aware of when that's done: the review found the diff never actually added a console-logged fallback-oracle comparison (grepped, zero hits) — so the live check will have to rely on probe-inference rather than a literal log line the AC's wording implies. Worth a quick call on whether that's fine or whether a console log should get added.\n\nYour part (AC-UAT-SWEEP) is not actionable yet — it explicitly waits on AC-LIVE-SWEEP going green. Once working-Claude reports that live sweep clean, your UAT gate will be: open the planet-lod lab yourself, run the same preset sweep, and confirm the whole thing behaves coherently end-to-end (no dispatch errors, every archetype renders plausibly) before you call it Shipped."
}