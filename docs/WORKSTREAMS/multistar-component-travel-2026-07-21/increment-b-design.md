# Increment B design — build-now tranche (2026-07-21)

Workstream `multistar-component-travel-2026-07-21`. Produced by grounding workflow
`wf_e688b0f3-a4e` (4 read-only seam traces + synthesis; per-agent evidence in the
session workflow journal). Contract: `contract.json` in this dir.

## Working-Claude rulings on the design's open questions (2026-07-21, flagged to Max, vetoable)

1. **AC1 transport = NAME-CHANNEL.** The component arrival address rides
   `action.star.name` -> `warpTarget.name` -> the resolver's `displayName` param —
   the only channel surviving main.js's 6-field whitelist copy unchanged. The
   explicit-index alternative costs ~3 gated main.js lines PLUS extending lane B's
   `_pendingPlayerWarp` stash shape — strictly worse cross-lane coupling for an
   identical player experience. Component resolution is an explicit resolver
   parameter (NavComputer's parent-preview call site opts OUT; never keyed on
   displayName alone). `_knownSystemNames` decoration is MANDATORY so titles stay
   'Alpha Centauri' (clause 1). The implicit main.js contract this rests on
   (knownWarp consumers read only position/seed/name) is PINNED by a BN1 test +
   module comment.
2. **Blazing tier threshold = appMag < -3.** New top size/halo tier for
   very-negative magnitudes fires ONLY below -3, so Sirius-from-Sol (-1.44)
   renders exactly as today; the change confines to component-scene siblings
   (A+B from Proxima = -6.90). Max judges the look at UAT.
3. **AC8 observable scoped per its own escape hatch:** 'sibling visible from each
   component' is fully satisfiable for Alpha Centauri only — every other authored
   sibling was dedup-absorbed at catalog regen (no catalog row; fix needs scripts/,
   contract-untouchable). Recorded for triage with Max; the audit doc enumerates.
4. **AC1 live-check expectation fix:** from Proxima, A+B render as ONE blazing
   point (shared HYG row; 23.5 AU sub-resolution) — physically honest, not a fail.
5. **Formal verify-workstream (full) runs at increment completion (post-lane-B).**
   The build-now tranche gets per-unit adversarial verification in the build
   workflow + the full-suite/snapshot gate; live drives for AC1 are GB7 (gated).
6. **Design docs anchor on FUNCTION NAMES, never line numbers** (lane B still
   moving; the contract's `~6371` citation is already stale -> `:6462`).

## Coordinator relay items (add to the cross-lane relay)

- AC2 (SC_TUNING ceiling) collides with autopilot-depart's AC7 byte-guard
  ('SupercruiseModel.js not in diff') — sequence AC2 AFTER depart ships, or
  renegotiate the guard.
- GB2-GB4 are DOUBLE-gated: autopilot-depart has zero code on lane B's branch yet
  (SupercruisePilot.js byte-identical to master) — landing the current branch
  delivers only the ORRERY seams.
- Step zero of all gated units: merged build + full suite green (merge-tree is
  textually clean but never built together).


## Build-now units (full specs — builders read these verbatim)

```json
[
 {
  "id": "BN1",
  "title": "Component-addressable arrival resolution in arrivalResolution.js (+ section-6 amendment and pin updates)",
  "acRefs": [
   "AC1-component-warp-arrival",
   "AC9-regression-guardrail"
  ],
  "files": [
   "/home/ax/projects/well-dipper-sysdetails/src/generation/arrivalResolution.js",
   "/home/ax/projects/well-dipper-sysdetails/src/generation/__tests__/arrivalResolution.test.js",
   "/home/ax/projects/well-dipper-sysdetails/src/ui/componentIdentity.js",
   "/home/ax/projects/well-dipper-sysdetails/docs/NAMING_AND_REAL_OBJECTS.md"
  ],
  "approach": "Extend the shared resolver (arrivalResolution.js:55-131, FIX-2 module) with an OPT-IN component-resolution mode \u2014 explicit parameter, never keyed on displayName alone, because resolveArrivalSystem is also NavComputer's PARENT-system preview path (NavComputer.js:2000-2008 passes member-marker names like 'Proxima Centauri'; auto-switching on name would break the drill-in flow whose _systemData must stay the parent, trace 1 risk). When the flag is set and the name resolves to a component (findComponentIndexByName pattern, componentIdentity.js:27-31; plus a member-name\u2192STELLAR_COMPANIONS-entry bridge for overlay systems, since RealSystemOverlay.resolve joins by ENTRY name only at RealSystemOverlay.js:156 \u2014 'HD 156026' must bridge to 'Guniibuu' inside the module), return componentSystems[k].systemData decorated with _knownSystemNames {system:'Alpha Centauri', star:componentName} so spawnSystem titles it from main.js:4355-4357/:4747 unchanged, plus a knownWarp WRAPPER {...ksEntry, position: componentCatalogPos} that steers the main.js:3580-3589 realignment (which reads only position/seed/name) to the component \u2014 landing playerGalacticPos and the sky prep (main.js:3602) at the component with zero main.js edits. This wrapper is the load-bearing bridge that also un-gates AC8's in-scene half (trace 3's 'gated' item resolved by trace 1's finding). Transport for the component address is the NAME channel \u2014 the only nav\u2192arrival channel surviving main.js's whitelist copies (dispatchNavAction :2894-2897, navStarData :2969-2976). ALWAYS attach _knownSystemNames on the component path (name-channel feeds generateSystemNames fallback at main.js:4367 otherwise). Amend NAMING_AND_REAL_OBJECTS.md \u00a76 ('one system identity, component-addressable arrival point') and the shipped primary-only pins (multistar-components AC7 pin + grammar-WS pins \u2014 grep for D2 'primary arrival' assertions FIRST, per trace 1 risk) in the same commit, cited to this workstream.",
  "testPlan": "TDD against the existing arrivalResolution.test.js harness (preview==arrival deep-compare over shipped catalog JSON). Red-first tests: (1) component-flagged resolve for 'Proxima Centauri' returns exactly parentSystemData.componentSystems[k].systemData (deep-equal to the NavComputer drill-in preview payload \u2014 preview==arrival by construction, NavComputer.js:2603-2615) with _knownSystemNames {system:'Alpha Centauri', star:'Proxima Centauri'}; (2) knownWarp wrapper .position === Proxima supplement coords, .seed/.name preserved; wrapper position within POSITION_MATCH_TOL 0.1 pc of the registry position (0.0554 pc actual \u2014 pins revisit/_isCurrentSystem recognition, trace 1 risk); (3) no component selection \u2192 primary payload byte-identical to current behavior; (4) non-component systems (Sirius, procgen seed) \u2192 byte-identical passthrough; (5) member-name bridge: 'HD 156026' \u2192 Guniibuu entry + correct component index; (6) sync resolveArrivalSystem WITHOUT the flag, displayName='Proxima Centauri' \u2192 PARENT payload (preview regression guard); (7) both alpha Cen marker paths \u2192 F1 seed 1816942132 (verified both catalog positions hash to it, trace 1); (8) amended pins pass with citation text present, not deleted (AC1 observable).",
  "mainJsDelta": "None under the name-channel design. IF Max rejects name-channel overloading (it conflates display label with arrival address): ~3 one-line additions at dispatchNavAction's 6-field copy (main.js:2894-2897), _setWarpTargetFromNavStar navStarData (:2969-2976), and the resolveArrivalSystemAsync call site (:3562-3570) \u2014 then gated on lane B landing. Either way, lane B's _pendingPlayerWarp mid-warp stash ({wx,wy,wz,seed,name,type}, trace 4) needs the component address threaded post-merge \u2014 deferred to GB7."
 },
 {
  "id": "BN2",
  "title": "NavComputer component-view WARP commit affordance + component-name commit plumbing",
  "acRefs": [
   "AC1-component-warp-arrival"
  ],
  "files": [
   "/home/ax/projects/well-dipper-sysdetails/src/ui/NavComputer.js",
   "/home/ax/projects/well-dipper-sysdetails/src/ui/__tests__/"
  ],
  "approach": "The component drill-in view is VIEW ONLY today (_commitButtonRect=null at NavComputer.js:2606; component-mode click pops back to system at :3927-3932) and _buildCommitAction (:744-759) never reads _selectedComponentIdx. Add the WARP commit affordance to _renderComponentDetail and make the committed action.star.name carry the COMPONENT's name. Two entry paths differ (trace 1): the PRISM member-marker path ALREADY commits the member name (member click sets _systemStar=member at :3990/:4001) \u2014 arrival today resolves it via alias to the parent and lands at the primary, the exact D2 behavior AC1 supersedes; the far-chip path (:3937-3948) leaves _systemStar as the SYSTEM marker, so its commit name is wrong for components \u2014 this fix is load-bearing, not polish, because dedup-absorbed siblings (HD 156026, Zet-2 Ret) have NO PRISM marker and the far chip is their only entry point (trace 1 risk). The commit must also set the opt-in component flag consumed at BN1's single clearly-marked interpretation site. NavComputer.js is byte-guarded in lane B's orrery-entry-orbits contract mustStayWorking, and lane B touches no src/ui/* (trace 4) \u2014 safe lane-C surface.",
  "testPlan": "Red-first against the existing NavComputer headless test patterns: (1) component drill-in view exposes a commit rect (non-null) where today it is null; (2) _buildCommitAction from a drilled component view yields action.star.name === componentSystems[idx].name for BOTH entry paths (PRISM member-marker AND far-chip \u2014 the far-chip case fails red today since _systemStar is the system marker); (3) A+B marker commit unchanged: name = marker name, no component flag (regression \u2014 'warping from the A+B marker still lands at A+B'); (4) non-component system commit payload byte-identical (AC9); (5) click handling: commit-button hit in component mode commits instead of popping back, clicks elsewhere still pop; (6) stale _pendingComponentSelect clearing behavior (627d072 fix) unregressed.",
  "mainJsDelta": "None \u2014 main.js's dispatchNavAction whitelist copy passes name/seed/pos/spectral through unchanged; extra action fields are dropped harmlessly (trace 1 seam fact)."
 },
 {
  "id": "BN3",
  "title": "OrbitArc geometry class + pure component-orbit derivation helper",
  "acRefs": [
   "AC6-component-orbit-render"
  ],
  "files": [
   "/home/ax/projects/well-dipper-sysdetails/src/objects/OrbitArc.js",
   "/home/ax/projects/well-dipper-sysdetails/src/generation/componentOrbit.js",
   "/home/ax/projects/well-dipper-sysdetails/src/generation/__tests__/componentOrbit.test.js"
  ],
  "approach": "Naive OrbitLine reuse at R=13e6 scene units (Proxima: 13,000 AU \u00d7 AU_TO_SCENE 1000) is triply broken (trace 2): 115,378 vertices from the segment formula, float32 quantization ~1-2 units at 1.3e7 magnitude (bigger than an M-dwarf's ~0.7-unit radius \u2192 visible jitter), and the camera far plane (200,000 units, main.js:131) clips all but a ~\u00b1200 AU near-arc anyway (~0.44\u00b0 of lateral curvature \u2014 Max's predicted 'straight line'). So: (a) pure helper deriveComponentOrbitSpec(parentSystemData, idx, {siblingDirection}) following the componentIdentity.js lane-C precedent \u2014 converts wrapper separationAU (it lives on the componentSystems WRAPPER, not inner systemData, StarSystemGenerator.js:888-895) to scene radius, clip-aware near-arc extent, and arc vertices in COMPONENT-LOCAL coordinates (small values near local origin \u2014 the float32 fix is load-bearing, not an optimization); returns null for non-component systems (the AC6 headless observable). Direction is an explicit PARAMETER because the payload carries only a scalar separationAU and orientation depends on AC1's sky/scene decision (trace 2 risk) \u2014 RealStarCatalog positions can supply the true 3D direction at consumption time. (b) OrbitArc class in src/objects/ (NOT a gated surface \u2014 only main.js and src/flight/ are) mirroring OrbitLine's mesh/addTo/dispose contract so the eventual spawnSystem consumption is a drop-in next to main.js:4414. Design must decide the far-plane edge treatment (opacity fade along the arc vs cap inside far) or it reads as a bug in UAT. Document the barycenter approximation (radius=separationAU, fine for Proxima, less exact for near-equal-mass 36 Oph C) in tests so AC9's diff review doesn't flag it as accidental.",
  "testPlan": "Template: tests/orbit-ring-rebase.test.js (OrbitLine + WorldOrigin rebase interaction). Red-first: (1) all three authored separations (Proxima 13,000 AU, HD 156026 4,400 AU, Zet-2 Ret 3,750 AU \u2192 radii 13e6/4.4e6/3.75e6) produce specs with every vertex coordinate magnitude bounded well under float32 hazard (< ~4e5, inside camera.far); (2) non-component systemData \u2192 null; (3) vertex count capped (no 115k-vertex geometry; do NOT pin OrbitLine's segment formula against huge radii, per trace 2 risk); (4) direction parameter rotates the arc as specified; (5) OrbitArc dispose/addTo contract parity with OrbitLine; (6) WorldOrigin rebase leaves arc-local vertices unchanged; (7) arc chord sagitta over the visible span matches the analytic ~1,540-unit deviation for R=13e6 (the 'near-straight' property, pinned numerically).",
  "mainJsDelta": "Deferred to GB6: ~5-10 line consumption in spawnSystem (construct via helper, _placeInRebasedFrame, push into system.starOrbitLines \u2014 after which toggle/visibility/removal/disposal are free since all four paths null-guard-iterate starOrbitLines, main.js:7017-7021/5247/4206-4208/4319-4321) PLUS per-instance setProximityFade override (lane B's OrbitRingSDF fade makes R=13M orbits invisible under defaults, trace 4)."
 },
 {
  "id": "BN4",
  "title": "AC8 starfield-honesty audit doc (committed workstream artifact)",
  "acRefs": [
   "AC8-starfield-honesty-audit"
  ],
  "files": [
   "/home/ax/projects/well-dipper-sysdetails/docs/WORKSTREAMS/multistar-component-travel-2026-07-21/starfield-honesty-audit.md"
  ],
  "approach": "Docs-only, all evidence already gathered by trace 3 (see auditDraft field). Structure: (1) how brightness works today (player-relative recompute, verified correct); (2) the one defect in-surface (0.1 pc blanket self-skip); (3) what the numbers become post-fix (appMag 4.16 / \u22126.90); (4) the render-cap gap ('blazing' needs size/halo work); (5) record-and-triage items OUTSIDE the surface (dedup-absorbed siblings, \u03b6 Ret resolvability \u2014 scripts/ is contract-untouchable); (6) pre-empt the Sol-view confusion (one point, no Proxima = physically honest, trace 3 risk); (7) the live-vantage protocol via the existing DebugPanel teleport (main.js:4970-5060, no code edit \u2014 it regenerates sky from raw coords without registry realignment).",
  "testPlan": "Every quantitative claim in the doc is backed by a headless computation in BN5's test suite (appMag values via findVisible over fs-fed catalog JSON) \u2014 the doc cites test names, not hand math. Review checklist: all file:line citations resolve; the triage list enumerates every dedup-absorbed sibling (Toliman, HD 155886, HD 156026, HD 201092, Zet-2 Ret); the Sol expectation is stated before the fix section."
 },
 {
  "id": "BN5",
  "title": "AC8 sky fixes: findVisible self-skip scoping + blazing-sibling brightness/size differentiation",
  "acRefs": [
   "AC8-starfield-honesty-audit"
  ],
  "files": [
   "/home/ax/projects/well-dipper-sysdetails/src/generation/RealStarCatalog.js",
   "/home/ax/projects/well-dipper-sysdetails/src/rendering/sky/StarfieldLayer.js",
   "/home/ax/projects/well-dipper-sysdetails/src/generation/__tests__/"
  ],
  "approach": "Two fixes inside AC8's declared minimal sky surface. (1) Shrink RealStarCatalog.findVisible's blanket self-skip (dist < 0.0001 kpc = 0.1 pc, RealStarCatalog.js:221) to a small epsilon (~1e-6 kpc \u2248 206 AU) \u2014 safe because every arrival/teleport path lands playerGalacticPos EXACTLY on catalog/registry coords so self is dist=0 (trace 3). Scope to RealStarCatalog ONLY: the same 0.0001 literal in HashGridStarfield.js:193/:371 must NOT change (would newly reveal procgen stars in every system's sky \u2014 regression surface beyond AC8). Do NOT touch POSITION_MATCH_TOL \u2014 same number, different job (identity matching; KnownSystems.MATCH_RADIUS ordering invariant, trace 3 risk). (2) 'Unmistakably blazing': today size buckets cap at 12 px below appMag \u22121 (RealStarCatalog.js:240) and the shader clamps to white for anything \u2264 ~appMag 2.5 (StarfieldLayer.js:295), so mag \u22126.9 renders pixel-identical to Sirius-from-Sol. Add a very-negative-magnitude size/halo tier in RealStarCatalog's mapping + shader support in StarfieldLayer. Do NOT design around uBrightnessMin/Max \u2014 dead uniforms, never used in shader math (trace 3). Flag to Max: this changes how ALL very bright stars look, within the retro pipeline's constraints (Bayer dither, 1/3-res compositor) \u2014 aesthetic sign-off before finalizing. Live demo TODAY without AC1: DebugPanel teleport to Proxima's supplement coords (8.000902, 0.024956, \u22120.000937).",
  "testPlan": "Red-first via RealStarCatalog.ingestCatalogData with fs-fed JSON (catalogDedup.test.js pattern): (1) from Rigil's coords, Proxima IS in findVisible output (fails red today \u2014 0.0554 pc < 0.1 pc skip) at appMag \u2248 4.16, size 4; (2) from Proxima's supplement coords, Rigil present at appMag \u2248 \u22126.90 with the NEW top size/halo tier; (3) from exact catalog coords the star itself is still excluded (dist=0 self-skip works); (4) pin HashGridStarfield's 0.0001 skip UNCHANGED (grep/behavioral pin \u2014 the scope guard); (5) POSITION_MATCH_TOL unchanged pin; (6) from Sol: Rigil visible at appMag \u2248 \u22120.01, Proxima cut at 6.5 threshold (physical-honesty regression pin); (7) size/brightness mapping: mag \u22126.9 output attributes measurably distinct from mag \u22121.44 (the anti-saturation assertion). Shader change verified live via teleport screenshots (headless ceiling for GLSL).",
  "mainJsDelta": "None for the fixes. The 'sky generated FROM the component after warp arrival' half is delivered by BN1's knownWarp wrapper (steers main.js:3580-3589 realignment lane-C-only) \u2014 the trace-3 'gated' framing is superseded by trace 1's wrapper finding; the implicit contract (main.js reads only position/seed/name off knownWarp) must be pinned by a BN1 test + module comment."
 }
]
```

## Gated units (built after lane B lands; seam contracts)

```json
[
 {
  "id": "GB1",
  "title": "AC2 supercruise ceiling raise (CAP_MAX ~20,000 \u2192 ~75,000)",
  "acRefs": [
   "AC2-supercruise-ceiling"
  ],
  "seamContract": "Edit surface: SupercruiseModel.js SC_TUNING (CAP_MAX at :16), byte-identical in both worktrees today \u2014 lane B does NOT touch it. Invariants to hold: ETA_K 3.0 \u2265 9/4 chain (:11-13), ACCEL_TAU \u2264 ETA_K/4 (:17-19); speedCap() = clamp(min over bodies of max(CAP_MIN_ABS, radius*0.5, surfaceDist/ETA_K), CAP_MAX) (:85-94). Trace-4-verified arithmetic: at CAP_MAX 75,000 the distance-proportional term governs within 225,000 units = 225 AU of any body \u2014 the contract's 'below ~225 AU unchanged' claim checks out; today's 20,000 ceiling binds only beyond 60 AU. SEQUENCING CONSTRAINT: autopilot-depart's AC7 regression gate pins 'SupercruiseModel.js not in diff' \u2014 this unit must build AFTER depart ships, or the byte-guard must be renegotiated with the coordinator first."
 },
 {
  "id": "GB2",
  "title": "AC3 nav BURN inter-component leg: DEPART-radial \u2192 cruise \u2192 ETA-decel + mid-leg scene handover",
  "acRefs": [
   "AC3-burn-transition"
  ],
  "seamContract": "DOUBLE-GATED: DEPART has ZERO code on feature/supercruise-freelook (SupercruisePilot.js byte-identical to master, phases IDLE/ALIGN/CRUISE/HOLD only) \u2014 design against the autopilot-depart CONTRACT shape (radial climb v/vmax-gated \u2192 orbit-to-horizon alignment \u2192 TRANSIT handoff at horizon-clear \u2192 commit-and-hold \u2192 ETA-scheduled decel replacing the HOLD position-lerp \u2192 DEPART-own timeout isolated from the CRUISE-keyed stall detector), re-sync when its build lands. Handover primitive: warpSwapSystem's sequence (flythrough.stop \u2192 autoNav.stop \u2192 scPilot.stop \u2192 clear pending/music/reticle \u2192 spawnSystem({forWarp:true}) \u2192 camera teleport to billboard-derived standoff \u2192 MANDATORY cameraInterp.resync or the interpolator's prev/curr blend erases the teleport). spawnSystem side effects the handover must survive: _resetWorldOrigin (zeroes origin every spawn, no listeners \u2014 ship pose/velocity must be re-expressed across the discontinuity), warpTarget full wipe, camera.far reset to 200,000, scPilot.stop() (pilot must re-arm post-swap). Nav BURN slots behind flightModes.js burnWorkflowAvailable (:436, HELM-only). Cite function names, never line numbers \u2014 lane B is still moving (round-4 UAT possible)."
 },
 {
  "id": "GB3",
  "title": "AC4 BURN-to-planet: compose component crossing with the in-system body leg",
  "acRefs": [
   "AC4-burn-to-planet"
  ],
  "seamContract": "Builds on GB2's handover. Critical seam: spawnSystem wipes ALL warpTarget fields on every spawn (supercruise main.js:4455-4463) \u2014 the continue-to-planet leg's target MUST be stashed before the swap and re-armed after, or the composed sequence loses its destination at handover. One continuous autopilot sequence (no manual re-targeting) is the observable; the post-swap re-target must resolve the planet in the NEWLY spawned component payload (real designations from componentSystems[k].systemData, matching BN1's spawned identity)."
 },
 {
  "id": "GB4",
  "title": "AC5 manual-cruise crossing: handover as a flight-model property",
  "acRefs": [
   "AC5-manual-cruise-crossing"
  ],
  "seamContract": "The handover trigger must live in the shared flight model / simStep path, not the pilot, so manual full-throttle HELM flight fires it too (D3 ruling: manual and autopilot share the model). simStep is a lane-B-claimed main.js region (merge-base 8695-8877 \u2192 9283-9488) \u2014 zero pre-build possible. Shares GB2's handover primitive and its spawnSystem-discontinuity obligations; mode-ownership law: player input present \u21d2 no autopilot, but the crossing itself is mode-independent. Handover point selection is by-construction sub-pixel on both sides (contract) \u2014 the detector needs only distance/midpoint logic testable headless once the seam exists."
 },
 {
  "id": "GB5",
  "title": "AC7 ORRERY log-zoom-out + sibling brackets + cross-component fly-over",
  "acRefs": [
   "AC7-orrery-cross-component"
  ],
  "seamContract": "Anchor on FUNCTION NAMES: _frameSystemForOrrery is at supercruise main.js:6462 \u2014 the contract's '~6371' is STALE. Its gap-stop (walk sorted orbitRadiusScene, break at >5\u00d7 gap; far companions deliberately excluded; 5\u00d7 factor marked Max-re-rulable) frames viewSystem(systemRadius*1.2), pitch 0.7, maxDistance = max(50000, systemRadius*3). AC7 must raise maxDistance past 3\u00d7 AND manage camera.far (default 200,000 << the ~13M-unit component orbit); precedent: _beginOrreryArrivalZoom raises far = max(200000, spawn*1.15) and restores via ShipCameraSystem.beginArrivalZoom's onSettle. Fly-over carriers: beginArrivalZoom(spawnDistance,{onSettle}) at ShipCameraSystem.js:630, two-phase AIM/APPROACH glide (GLIDE_AIM_SECONDS_PER_RAD 0.45, cap 0.7s), focusOn :662; orreryStandoff {star 15R}. Pure twins to extend headlessly pre-merge if desired: orreryEntryGeometry.effectiveOuterOrbit/orbitVisibilityFactor/arrivalSpawnDistance (:102-163). All routing resolves through lane B's flightModes.js reducer register (systemEntryStyle :515, bodyClickAction :568, navDispatchDuringWarp :597...); the swap under the hood is _enterSystemInstantOrrery (:6596-6641), which post-merge resolves identity through resolveArrivalSystemAsync inside lane B's extracted _generateWarpDestinationData \u2014 so BN1's component extension serves the ORRERY instant-cut automatically. ORRERY never flies the ship: camera + scene swap only."
 },
 {
  "id": "GB6",
  "title": "AC6 consumption: spawnSystem creates the component orbit arc + proximity-fade override",
  "acRefs": [
   "AC6-component-orbit-render"
  ],
  "seamContract": "~5-10 lines in spawnSystem next to the binary starOrbitLines block (master main.js:4414-4422): construct via BN3's helper (direction supplied from RealStarCatalog sibling position per AC1's orientation decision), _placeInRebasedFrame, push into system.starOrbitLines \u2014 toggle/visibility/removal/disposal then work free since all four downstream paths null-guard-iterate that array (main.js:7017-7021, 5247, 4206-4208, 4319-4321); star orbit lines are not hover/click targets, so no interaction wiring. MUST apply per-instance setProximityFade overrides (OrbitRingSDF.js:316-321): lane B's proximity fade (near = max(0.35, 0.02\u00d7R), far = near\u00d73) gives near \u2248 260,000 units at R=13M \u2014 the giant orbit fades to ZERO from anywhere in the component's neighborhood under defaults. Also gated on AC1's spawn path existing at all (componentSystems is referenced nowhere in main.js today) and on how the wrapper's separationAU/name reach the scene builder. AC9 note: pushing a component line into starOrbitLines ends the de-facto 'starOrbitLines \u21d2 isBinary' invariant \u2014 grep for binarity inference off that field at diff-review time."
 },
 {
  "id": "GB7",
  "title": "AC1 completion: mid-warp stash threading + live :5176 integration drives",
  "acRefs": [
   "AC1-component-warp-arrival",
   "AC9-regression-guardrail"
  ],
  "seamContract": "Lane B's dispatchNavAction warp path has THREE routes (navDispatchDuringWarp, flightModes.js:597): 'stash' (post-FOLD: _pendingPlayerWarp = {wx,wy,wz,seed,name,type}, consumed at warpRevealSystem), 'overwrite', 'normal'. Under BN1's name-channel design the component address rides in .name and survives the stash as-is \u2014 verify post-merge that warpRevealSystem's consumption preserves it and re-applies the component opt-in flag; under an explicit-index design BOTH navStarData and the stash shape need a component field or a mid-warp component pick silently resolves to the primary. First post-merge action regardless: full build + 1,616-baseline suite run \u2014 the master+lane-B merge is textually clean (git merge-tree exit 0) but never built/tested together; lane C's resolveArrivalSystemAsync call lands inside lane B's extracted _generateWarpDestinationData with zero shared coverage. Then the AC1 live drives (PRISM \u2192 Proxima \u2192 WARP; A+B marker \u2192 WARP; [WARP] log seed 1816942132 both ways, title 'Alpha Centauri')."
 }
]
```

## Post-build verifier triage → gated-unit addenda (2026-07-21, minors pass)

Triage of the build workflow's 21 MINOR findings (wf_16801d1c-86a journal;
lane B not yet landed, so only follow-ups were buildable). Comment/pin/accessor
fixes landed in the same commit as this section; audit-covered items (epsilon
blast radius, StarFlare staleness, mobile point-size cap) already live in
`starfield-honesty-audit.md` §5. Three findings need OWNERS in the gated units
— recorded here so they aren't orphaned:

- **GB7 addendum — preview ≠ arrival after a component arrival (was unowned).**
  `_isCurrentSystem` (NavComputer.js, POSITION_MATCH_TOL identity radius) plus
  the current-system preview shortcut (`_systemData = _currentSystemData`)
  means: warp-arrive at Proxima → browse the A+B/Rigil marker → SYSTEM view
  previews Proxima's single-M payload titled 'Alpha Centauri' (no drill chip),
  while committing WARP there delivers the pair. The design's tension #8 only
  half-covered this: recognition IS tested (AC1(b)); the preview-content
  consequence is not. GB7 must either route same-identity sibling previews
  through resolveArrivalSystem (parent semantics) or rule the current behavior
  correct-per-§6 with Max — either way it's GB7's call to close, with a test.
  A cross-reference comment now sits at the cause site (`_isCurrentSystem`).
- **GB7 addendum — sky-click component arrival live check.** Sky-click
  (hasNavStar:false) arrival on Proxima's own catalog row now
  component-resolves via the findAt path — consistent with amended §6 'on
  every path' and headless-tested, but never driven live. Add one line to the
  GB7 :5176 circuit: sky-click a component's own sky point → component scene.
- **GB2 note — foreign-binary star-B selection resets on drill round-trip.**
  `_renderComponentDetail` unconditionally overwrites `_selectedBody` with
  star 0 every frame; pop-back + system-view re-arm leave the ring on star A
  even if star B was selected pre-drill (pre-BN2 it survived). Cosmetic —
  warp dispatch ignores starIndex — but GB2 rebuilds the commit affordance
  for BURN legs, so fix or re-pin it there.

## AC8 audit draft (trace-3 evidence, BN4 formalizes)

WHAT THE SKY DOES TODAY (verified, trace 3): Catalog-star brightness IS player-relative — the AC8 audit question's core premise holds. RealStarCatalog.findVisible (RealStarCatalog.js:207-271) recomputes apparent magnitude per position: appMag = absMag + 5*log10(d_pc/10) from the live player→star distance (:224-225); the catalog's Earth-relative mag field is never used for rendering. Color = SPECTRAL_COLOR × max(0.1, 1.5 − appMag/5) (:236), size buckets (:239-245), sky direction is the true player→star unit vector (:229-232). StarfieldLayer renders this baked vertex data verbatim with a per-channel white clamp (StarfieldLayer.js:295). From third systems positions are catalog-true: from Sol, Rigil renders at appMag −0.01 (matches reality) in the correct direction; Proxima's separate supplement row is correctly cut at the 6.5 naked-eye threshold (real mag 11.01). Note for UAT: the Sol check will show Alpha Cen as ONE point (A+B share one HYG row) and NO Proxima — physically honest, not a defect.

THE DEFECT (in-surface, fix in this workstream): findVisible's self-exclusion is a blanket 0.1 pc distance skip — `if (dist < 0.0001) continue; // skip self` (RealStarCatalog.js:221). Rigil↔Proxima separation is 0.0554 pc — INSIDE the skip. The ENTIRE authored component-separation regime (ceiling ≈ 0.1 pc per intent) sits inside the skip radius, so from inside Alpha Cen the sky renders NO Proxima and from Proxima's coords NO blazing A+B. Self-exclusion works today only by coincidence: every arrival/teleport path lands playerGalacticPos exactly on catalog/registry coords (dist=0).

SECONDARY GAP (in-surface): if un-skipped, the math lands where AC8 wants it — Proxima from A+B: appMag 4.16 (dim point, correct); A+B from Proxima: appMag −6.90 — but 'unmistakably blazing' is capped: size buckets stop at 12 px below appMag −1 (RealStarCatalog.js:240) and color saturates to white for anything ≤ ~appMag 2.5, so mag −6.9 renders pixel-identical to Sirius-from-Sol (mag −1.44). Needs a very-negative-magnitude size/halo tier — an aesthetic change affecting all very bright stars; Max sign-off required. Also: uBrightnessMin/uBrightnessMax are dead uniforms (declared StarfieldLayer.js:246-247, never used in shader math) — the documented brightness budget is unenforced; do not design against it.

PROPOSED FIX SURFACE (all lane-C, headless-testable via ingestCatalogData): (1) shrink the findVisible skip to ~1e-6 kpc (≈206 AU) or identity-based, RealStarCatalog.js ONLY — the same 0.0001 literal in HashGridStarfield.js:193/:371 must NOT change (would reveal procgen stars within 0.1 pc in every sky) and POSITION_MATCH_TOL must NOT shrink (different job: arrival identity; KnownSystems.MATCH_RADIUS ordering invariant). (2) Blazing-tier size/halo mapping in RealStarCatalog + StarfieldLayer shader. (3) Live vantage TODAY with zero code: DebugPanel teleport (main.js:4970-5060) regenerates the sky from raw coords without registry realignment — teleport to Proxima's supplement coords (8.000902, 0.024956, −0.000937) demonstrates the missing-sibling defect and verifies the fix. (4) The 'sky generated FROM the component after real warp arrival' half is delivered by BN1's knownWarp position wrapper (main.js:3580-3589 reads only position/seed/name), not by a main.js edit.

RECORD-AND-TRIAGE WITH MAX (outside surface — scripts/ is contract-untouchable): all component siblings EXCEPT Proxima have no independent catalog row — Toliman, HD 155886, HD 156026 (36 Oph B/C), HD 201092 (61 Cyg B), Zet-2 Ret were alias-absorbed at catalog regen (process-hyg-catalog.mjs dedup :114-127). They can never render as separate sky points from any vantage, including inside their own systems' component scenes — Guniibuu and Zeta Ret component scenes will show NO sibling sky point even after the skip fix. Real ζ¹/ζ² Ret are both naked-eye and ~5 arcmin apart (genuinely resolvable); fixing requires catalog regen in scripts/ — a lane-D / future-workstream item. The componentSystems payload carries no galactic position/direction, so the sky pipeline cannot synthesize sibling points from it either. AC8's 'sibling visible from each component' observable is therefore fully satisfiable ONLY for Alpha Centauri in this workstream.

## Contract tensions (verbatim from synthesis)

```json
[
 "GATE UNDER-SPECIFICATION (AC3/AC4/AC5): the contract gates build on 'lane B's in-flight branch landing', but autopilot-depart \u2014 whose radial-climb + ETA-decel ARE the BURN leg's endpoint behaviors per the contract's own architecturalConnections \u2014 has ZERO code on feature/supercruise-freelook (SupercruisePilot.js byte-identical to master). Landing the current branch delivers the ORRERY seams only; GB2-GB4 are additionally gated on autopilot-depart's build shipping on that lane. Design against the depart contract shape now, re-sync at its landing.",
 "AC2 vs AUTOPILOT-DEPART BYTE-GUARD: depart's AC7 regression gate pins 'SupercruiseModel.js not in diff' while our AC2 edits exactly that file. Concurrent builds collide; sequence AC2 after depart ships or renegotiate the guard with the coordinator. The contract does not mention this ordering.",
 "STALE SEAM CITATION: the contract cites orrery-coherence's _frameSystemForOrrery at 'supercruise main.js ~6371'; it is now at :6462 and lane B is still moving (round-4 UAT possible). All Increment-B design docs must anchor on exported function names, never line numbers, and re-verify seams at merge.",
 "AC6 vs LANE-B ROUND 3: OrbitRingSDF's new proximity fade (near = max(0.35, 0.02\u00d7R)) \u2014 which ALL orbit lines inherit via OrbitLine extends OrbitRingSDF \u2014 zeroes out a ~13M-unit component orbit from anywhere inside the component's neighborhood under default knobs (near \u2248 260,000 units). The contract's AC6 says nothing about fade overrides; per-instance setProximityFade is a mandatory part of the GB6 design or the giant orbit never renders.",
 "AC1 TRANSPORT AMBIGUITY: the only zero-main.js channel for the component address is overloading action.star.name / warpTarget.name \u2014 which conflates display label with arrival address (it also becomes the HUD warp-target label and _warpTargetName; probably desired, but it feeds generateSystemNames fallback, so _knownSystemNames decoration is mandatory). The contract doesn't rule on name-channel vs explicit-index; explicit-index needs ~3 main.js lines (gated) AND extending lane B's _pendingPlayerWarp stash ({wx,wy,wz,seed,name,type} \u2014 no component slot), or a mid-warp component pick silently resolves to the primary. Needs a Max ruling before BN1 finalizes.",
 "AC8 OBSERVABLE PARTIALLY UNSATISFIABLE IN-SURFACE: 'sibling visible from each component at plausible relative brightness' is achievable only for Alpha Centauri \u2014 every other authored sibling (Toliman, HD 155886, HD 156026, HD 201092, Zet-2 Ret) was dedup-absorbed at catalog regen and has no catalog row; fixing requires scripts/ (contract-pinned untouchable) catalog regen. The contract's own escape hatch ('larger findings are recorded and triaged with Max') applies, but the AC's observable wording overpromises for Guniibuu/Zet Ret component scenes.",
 "AC1 OBSERVABLE WORDING: 'A+B as sky points' (plural) from Proxima \u2014 A+B share one HYG catalog row and 23.5 AU is sub-resolution anyway, so the sky will show ONE blazing point. Physically honest; the live-verification checklist should expect one point so it isn't logged as a failure.",
 "TRACE DISAGREEMENT RESOLVED IN LANE C'S FAVOR: trace 3 classed 'sky generated from the component's position at warp arrival' as lane-B-gated (main.js:3541/3580), but trace 1 verified the knownWarp realignment reads only position/seed/name \u2014 a module-returned wrapper steers playerGalacticPos lane-C-only. This rests on an IMPLICIT main.js contract (a future change reading other knownWarp fields breaks it silently); pin with a BN1 test + module comment. Related: the component position is 0.0554 pc from the registry position vs POSITION_MATCH_TOL = 0.1 pc \u2014 inside tolerance today but >half of it; _isCurrentSystem recognition from a component scene needs an explicit test.",
 "MERGE IS CLEAN BUT UNPROVEN: git merge-tree of master + feature/supercruise-freelook exits clean, and lane C's resolveArrivalSystemAsync rewiring nests inside lane B's _generateWarpDestinationData extraction without textual overlap \u2014 semantically correct by construction but never built/tested together. The contract's build gate should include 'merged build + full 1,616 suite green' as step zero of all gated units (captured in GB7)."
]
```

## Trace summaries (evidence provenance)

### Orbit-line rendering is inline in src/main.js's spawnSystem() (main.js:4225), no...

Orbit-line rendering is inline in src/main.js's spawnSystem() (main.js:4225), not in a module: binary-star barycentric circles at main.js:4393-4423, planet orbits at 4553-4561, moon orbits at 4541-4547, all instantiating the pure OrbitLine class (src/objects/OrbitLine.js, LineLoop of Float32 points, segments = max(128, ceil(sqrt(r)*32))). The O toggle is toggleOrbits() (main.js:7002) -> _applyOrbitVisibility() (7012-7027), reached from KeyO handlers (10073, 10105-10106), a settings observer (3114-3115), and the HUD button (10897-10898). Critically, every downstream path — visibility apply (7017-7021), _setSystemVisible (5247), scene removal (4206-4208), disposal (4319-4321) — already iterates system.starOrbitLines generically behind a null guard, so a component barycentric line pushed into that array inherits toggle/cleanup/visibility for free; the only main.js edit AC6 strictly needs is the creation call inside spawnSystem. componentSystems entries carry {name, class, type, separationAU, seed, systemData} (StarSystemGenerator.js:888-895) but the inner systemData has NO component marker or separationAU, and componentSystems is referenced nowhere in main.js — component scenes cannot spawn today, so AC6's live half is doubly gated (needs AC1's spawn path AND the lane-B branch landing). Scale reality: AU_TO_SCENE=1000, so Proxima's 13,000 AU orbit is a 13,000,000-scene-unit-radius circle against a camera far plane of 200,000 units (main.js:131) — the projection matrix still clips beyond far even with logarithmicDepthBuffer:true (RetroRenderer.js:49), so only a ~±200 AU near-arc renders (lateral curvature ~1,540 units over that span: reads as Max's 'straight line'). A naive OrbitLine(13e6) would also carry 115,378 vertices and float32 quantization of ~1-2 scene units at 1.3e7 magnitude — bigger than an M-dwarf's ~0.7-unit scene radius — so the correct design is a lane-C helper emitting a near-ARC in component-local coordinates, following the established pure-derivation precedent of src/ui/componentIdentity.js.

Key seam facts:
- Orbit lines are created INLINE in main.js spawnSystem() (main.js:4225): binary star circles main.js:4414-4422 (OrbitLine(r1/r2, 0x00dd00), _placeInRebasedFrame, pushed to starOrbitLines), planet lines main.js:4557-4561 (OrbitLine(entry.orbitRadiusScene, 0x00ff00)), moon lines main.js:4542-4547.
- The O toggle chain: KeyO (main.js:10073 autopilot branch, 10105-10106 normal) -> toggleOrbits() (main.js:7002-7008) -> _applyOrbitVisibility() (main.js:7012-7027); also settings observer case 'showOrbits' (main.js:3114-3115), HUD button (main.js:10897-10898), init orbitsVisible = settings.get('showOrbits') (main.js:311), mode sync _syncOrbitsToMode (main.js:7032-7036).
- system.starOrbitLines is iterated with a null guard in ALL four downstream paths: visibility toggle main.js:7017-7021, _setSystemVisible main.js:5247, scene removal main.js:4206-4208, disposal main.js:4319-4321 — so a component orbit line pushed into starOrbitLines inherits toggle/cleanup with ZERO extra main.js changes beyond the creation site.
- Star orbit lines are NOT hover/click targets — _orbitLineTargets only registers planet/moon lines (main.js:4731-4737) — so no interaction wiring is needed for the component line.
- componentSystems entry shape (StarSystemGenerator.js:888-895): { name, class, type, separationAU, seed, systemData }; separationAU lives on the WRAPPER, and the inner systemData (from the recursive _generateIterator) carries NO component marker — the spawn path (AC1, gated) must hand the wrapper's separationAU/name to the scene builder.
- grep confirms 'componentSystems' appears NOWHERE in src/main.js — component scenes cannot spawn today; the drill-in (NavComputer.js:106, 2596, 3940) is nav-view-only.
- Authored separations: Proxima Centauri 13,000 AU (stellarCompanions.js:102), HD 156026 4,400 AU (:130), Zet-2 Ret 3,750 AU (:172). AU_TO_SCENE = 1000 (ScaleConstants.js:39) -> orbit radii of 13,000,000 / 4,400,000 / 3,750,000 scene units.
- Camera: near=1e-9, far=200,000 scene units (main.js:131; reset guard 4252-4254; rationale comment 8882-8901). logarithmicDepthBuffer:true (RetroRenderer.js:49) fixes depth PRECISION but the projection far plane still CLIPS geometry beyond 200,000 units — only the ~±200 AU near-arc of a 13M-unit circle renders; over that visible span the arc deviates ~1,540 units laterally (~0.44 deg), i.e. it genuinely reads as a near-straight line, matching Max's stated expectation.
- Naive reuse of OrbitLine at r=13e6 is hazardous: segments = ceil(sqrt(13e6)*32) = 115,378 vertices (~708-unit spacing), and Float32 vertex quantization at 1.3e7 magnitude is ~1-2 scene units — larger than an M-dwarf star's ~0.7-scene-unit radius — producing star-radius-scale jitter on the visible arc. The helper should emit a NEAR-ARC with vertices in component-local coordinates (small values near the local origin).
- No precedent exists for multi-million-unit scene geometry: largest current orbit lines are ~30,000 units (OrbitLine.js:8-9 comment); far companions today have NO scene body — their sky presence is their own real catalog star (StarSystemGenerator.js:854-856 comment), rendered by the sky layer, not scene geometry.
- The payload gives only a SCALAR separationAU — no direction to the sibling and no masses. Orbit orientation (which way the barycenter lies from the component) and barycenter position (approximating it at the A+B pair, radius = separationAU) are design inputs the helper must take as parameters; direction ultimately depends on AC1's scene-orientation/sky decision (catalog positions in RealStarCatalog can supply the true 3D direction).
- Lane-C helper precedent is established twice: src/ui/componentIdentity.js (pure (parentSystemData, idx) derivations with __tests__, consumed by NavComputer) and src/generation/arrivalResolution.js (logic extracted from main.js into a module with a thin main.js call site).

### TRACE 3 verified the full starfield brightness path. Catalog-star brightness IS ...

TRACE 3 verified the full starfield brightness path. Catalog-star brightness IS player-relative: RealStarCatalog.findVisible recomputes apparent magnitude from the catalog's absolute magnitude plus the live player→star distance (RealStarCatalog.js:224-225), bakes it into vertex color/size, and StarfieldLayer renders that data verbatim with a per-channel white clamp (StarfieldLayer.js:295) — the Earth-relative catalog `mag` is never used. However, AC8's central scenario fails today for a different reason: findVisible's self-exclusion is a blanket 0.1 pc distance skip (RealStarCatalog.js:221, `dist < 0.0001` kpc), and the Rigil↔Proxima catalog separation is 0.0554 pc — so from inside Alpha Centauri the sky renders NO Proxima, and from Proxima's coords NO blazing A+B; the entire authored component-separation regime (ceiling ≈ 0.1 pc) sits inside the skip. If un-skipped the math already lands right (Proxima from A+B: appMag 4.16 dim point; A+B from Proxima: appMag −6.90) — but 'blazing' is additionally capped because size buckets stop at 12 px below appMag −1 and color saturates to white for anything brighter than ~appMag 2.5, making a mag −6.9 star pixel-identical to Sirius-from-Sol. From third systems, positions ARE catalog-true (Rigil renders from Sol at appMag −0.01 in the correct direction; Proxima's separate true-position row is correctly cut at the naked-eye 6.5 threshold, matching its real mag 11). Own-entry exclusion works only by coincidence of exact-coordinate arrival (dist=0), and component siblings other than Proxima (Toliman, 36 Oph B/C, 61 Cyg B, ζ² Ret) were dedup-absorbed into alias arrays at catalog regen, so they have no independent sky representation from anywhere. The minimal fix (shrink/identity-scope the findVisible skip) plus the blazing-star render work are lane-C feasible in RealStarCatalog.js + StarfieldLayer.js with headless tests and a zero-code live vantage via the existing debug teleport; only the 'sky generated FROM the component's position after component-addressable arrival' half is gated on lane B's main.js surface.

Key seam facts:
- Brightness is PLAYER-RELATIVE, recomputed per position: RealStarCatalog.findVisible (RealStarCatalog.js:207-271) computes `const d_pc = dist * 1000; const appMag = s.absMag + 5 * Math.log10(d_pc / 10)` (lines 224-225) where dist is playerPos→catalog-star true x/y/z; the catalog's Earth-relative `mag` field is never used for sky rendering. Fed from StarfieldGenerator._finalizeFromGridData line 150: `this.realStarCatalog.findVisible(playerPos, 6.5, radius)`.
- Rendered star color = SPECTRAL_COLOR[spect] × max(0.1, 1.5 − appMag/5) (RealStarCatalog.js:236, 262; identical formula for procgen stars at HashGridStarfield.js:205); StarfieldLayer's fragment shader multiplies by shape and clamps `min(col, vec3(1.0))` (StarfieldLayer.js:292-295). Channels saturate to white for any appMag ≤ ~2.5; size buckets top out at 12 px for appMag < −1 (RealStarCatalog.js:240). Therefore a mag −6.9 star renders pixel-identically to Sirius-from-Sol (mag −1.44).
- THE AC8 DEFECT: findVisible skips ALL catalog stars within 0.0001 kpc = 0.1 pc of the player (`if (dist < 0.0001) continue; // skip self`, RealStarCatalog.js:221). Rigil↔Proxima catalog separation is 5.54e-5 kpc = 0.0554 pc = ~11,400 AU — INSIDE the skip. From inside Alpha Cen the sky renders NO Proxima; from Proxima's coords it renders NO Rigil/Toliman. The entire authored-component separation regime (ceiling ≈ 0.1 pc per intent.md) sits inside this skip radius. Same 0.1 pc skip for procgen stars at HashGridStarfield.js:193 (sync) and :371 (async).
- If un-skipped, the numbers land exactly where AC8 wants them: Proxima from A+B = appMag 4.16 (dim point, size 4, brightness 0.67); A+B from Proxima = appMag −6.90 (brightness mult 2.88, size 12, clamps to white — 'blazing' needs the additional size/halo work).
- playerGalacticPos inside Alpha Cen is ALWAYS Rigil's HYG record (8.000948, 0.024984, −0.000924): warp sets it from resolvedStar (main.js:3541) then knownWarp realigns to KnownSystems' registered position (main.js:3580; KnownSystems.js:106-107), and sky generation reads it at main.js:3602 via skyRenderer.prepareForPositionAsync. Component selection does not change it today (AC1 unbuilt).
- Sky positions ARE catalog-true post-rework: skyX/Y/Z = (player→star unit vector) × skyRadius (RealStarCatalog.js:229-232). From Sol (GalacticMap.getStartPosition = {SOLAR_R, SOLAR_Z, 0}, GalacticMap.js:1693-1699), Rigil renders at appMag −0.01 (matches real), Proxima's row is separate and 2.39° away but correctly cut (appMag 11.01 > 6.5 naked-eye threshold).
- Own-entry exclusion is DISTANCE-based, not identity-based — it works only because every arrival/teleport path lands playerGalacticPos exactly on catalog/registry coords (dist=0): warp main.js:3541/3580, teleport main.js:5006-5012 (seeds by canonical F1 of the CATALOG position).
- Component siblings' catalog status is asymmetric: Proxima Centauri is the ONLY sibling with its own catalog row (real-star-supplement.json, absMag 15.44, hostname 'Proxima Cen'). Toliman, HD 155886, HD 156026 (36 Oph B/C), HD 201092 (61 Cyg B), Zet-2 Ret were alias-absorbed at catalog regen (process-hyg-catalog.mjs DEDUP lines 114-127; verified `aliases` arrays in hyg-stars.json) — they cannot render as separate sky points from ANY vantage, including inside their own system's component scenes.
- The sky pipeline has ZERO componentSystems awareness: the payload (componentSystems.js validateComponentPayload, lines 87-114) carries name/class/seed/type/separationAU/systemData but no galactic position or direction — nothing for StarfieldGenerator to place.
- StarfieldLayer's uBrightnessMin/uBrightnessMax uniforms (set from SkyRenderer._brightnessConfig.stars {min:0.15, max:0.65}, SkyRenderer.js:57-61) are declared in the fragment shader (StarfieldLayer.js:246-247) but never referenced in any computation — the documented per-layer brightness budget is unenforced dead code; actual formula is vColor × uBrightness(1.0) × shape, clamped.
- A no-code live-audit vantage exists: DebugPanel teleportToPosition (main.js:4970-5060) sets playerGalacticPos to raw coords and regenerates the sky WITHOUT realigning to the known-system registry — teleporting to Proxima's supplement coords (8.000902, 0.024956, −0.000937) today demonstrates the missing-sibling defect live.
- findVisible is headless-testable without a browser: RealStarCatalog.ingestCatalogData (RealStarCatalog.js:98-106) accepts fs-read JSON — the established pattern in src/generation/__tests__/catalogDedup.test.js and RealSystemOverlay tests.

### Trace 1 complete, all claims verified against code. The nav→warp pipeline is: Na...

Trace 1 complete, all claims verified against code. The nav→warp pipeline is: NavComputer commit (_buildCommitAction NavComputer.js:744-759 or _selectSearchResult :485-515) fires _onCommit with action.star={wx,wy,wz,seed,name,spectral}; main.js stores it (_pendingAction :2790), dispatchNavAction (:2865-2900) whitelist-copies exactly those 6 fields into _setWarpTargetFromNavStar (:2943), which stores 5 of them as warpTarget.navStarData (:2969-2975) plus warpTarget.name (:2976). The component drill state (_systemMode==='component', _selectedComponentIdx, NavComputer.js:104-107) does NOT flow into the warp target: the component view is VIEW ONLY (no commit button, :2606; component-mode click pops back to system, :3927-3932) and _buildCommitAction never reads _selectedComponentIdx. The shared arrival module is src/generation/arrivalResolution.js (FIX-2 of real-star-identity-unification, shipped f6b3eff): resolveArrivalSystem (sync, consumed by NavComputer._renderSystem preview :2000-2008) and resolveArrivalSystemAsync (consumed by main.js onPrepareSystem :3562-3570); main.js consumes ONLY _arr.systemData and _arr.knownWarp (:3571-3572, grep-verified exhaustive). Spawn position is decided by warpSwapSystem (main.js:6152-6253): it spawns pendingSystemData verbatim and places the camera in front of system.star (the spawned payload's primary, :6216-6236) — so "which star you appear in front of" is entirely determined by WHICH systemData payload the resolution module returns. Because Alpha Centauri is a declarative KnownSystems entry (KnownSystems.js:105-110) generated via KnownSystemAuthoring→StarSystemGenerator, its arrival payload already carries systemData.componentSystems (StarSystemGenerator.js:857-898); main.js has ZERO componentSystems references (Increment A data-only confirmed — consumers are NavComputer/componentIdentity/farCompanionChips only). An "arrive at component K" request IS expressible lane-C-only: NavComputer controls action.star.name → warpTarget.name → displayName param of the module, the module can resolve a component by name (findComponentIndexByName pattern, componentIdentity.js:27-31) and return componentSystems[k].systemData decorated with _knownSystemNames (spawnSystem reads it at main.js:4355-4357 and titles the system from it at :4747), and even the knownWarp-branch galactic realignment (main.js:3580-3589) reads only knownWarp.position/seed/name so a module-returned wrapper object can steer it. I computed and confirmed both Rigil Kentaurus and Proxima Centauri catalog positions hash to F1 seed 1816942132 (same 0.1 pc bin), so the [WARP] log seed invariant holds on both marker paths.

Key seam facts:
- Commit payload shape: action = {type:'warp'|'burn', target, starIndex, planetIndex, moonIndex, star:{wx,wy,wz,seed,name,spectral}} built in NavComputer._buildCommitAction (NavComputer.js:744-759) from _selectedBody + _systemStar; _selectedComponentIdx is NOT read there.
- main.js dispatchNavAction warp branch (main.js:2894-2897) whitelist-copies ONLY {wx,wy,wz,seed,name,spectral} into _setWarpTargetFromNavStar; any extra field NavComputer attaches to the action is dropped. _setWarpTargetFromNavStar (main.js:2969-2976) stores warpTarget.navStarData={worldX,worldY,worldZ,seed,type} + warpTarget.name. So the ONLY NavComputer→arrival channels surviving main.js unchanged are: position, seed, spectral type, and the NAME string.
- onPrepareSystem passes displayName: warpTarget.name into resolveArrivalSystemAsync (main.js:3562-3570) and consumes ONLY _arr.systemData (→ pendingSystemData, :3571) and _arr.knownWarp (:3572-3591). pendingSystemData is spawned verbatim by warpSwapSystem→spawnSystem (main.js:6173).
- Arrival spawn position: warpSwapSystem places the camera in front of system.star — the spawned payload's primary — at billboard-derived standoff (main.js:6216-6236: starPos = system.star.mesh.position; camera.position.set(starPos.x, starPos.y+2, starPos.z + travelDist + orbitDist + coastDist)). Which star you appear in front of == which systemData payload the module returns. No per-component position logic exists in main.js.
- knownWarp realignment (main.js:3574-3591) reads only knownWarp.position, knownWarp.seed||knownWarp.name, knownWarp.name — a module-returned wrapper {...ksEntry, position: componentCatalogPos} would steer playerGalacticPos (and hence skyRenderer.prepareForPositionAsync at :3602) to the component with zero main.js edits.
- spawnSystem titles the arrived system from systemData._knownSystemNames when present (main.js:4355-4357), _currentSystemName = systemNames.system (main.js:4747) — so a component payload decorated with _knownSystemNames={system:'Alpha Centauri', star:'Proxima Centauri', ...} keeps the one-system title with main.js unchanged. Naming-shape precedent: deriveAuthoredNames (KnownSystemAuthoring.js:134-173).
- Component preview payload: NavComputer._renderComponentDetail renders deriveComponentView(...).systemData === parentSystemData.componentSystems[idx].systemData (NavComputer.js:2603-2615, componentIdentity.js:43-54) — preview==arrival for AC1 means arrival must spawn exactly this payload.
- componentSystems emission: StarSystemGenerator.js:857-898, gated on Array.isArray(galaxyContext?.farCompanions) — procgen contexts never carry farCompanions, so the ProcgenSnapshot componentSystems-absent invariant holds structurally. Entry shape: {name, class, type, separationAU, seed: componentSeed(canonicalSeed,idx), systemData} (validated by validateComponentPayload, componentSystems.js:87-114).
- componentSystems reach today: main.js has ZERO references (grep-verified); consumers are NavComputer.js, componentIdentity.js, farCompanionChips.js + tests only. Increment A data-only confirmed.
- Alpha Cen arrival takes the knownWarp branch: declarative entry (KnownSystems.js:105-110) → generateAuthoredSystem (KnownSystemAuthoring.js:186-195) → StarSystemGenerator.generate('alpha-centauri', ctx-with-farCompanions) → payload HAS componentSystems. 'Proxima Centauri' is an eager alias of the entry (KnownSystems.js:148-151), so findByAlias('Proxima Centauri', proximaPos) → Alpha Centauri entry (3 pc belt :232-237 passes; members ~0.06 pc apart).
- Non-known multiples (Guniibuu/36 Oph, Zet Ret) take the overlay branch: RealSystemOverlay.resolve joins STELLAR_COMPANIONS by ENTRY name only (RealSystemOverlay.js:156, e.name===starName). A member/component name ('HD 156026') does NOT join → ctx gets no farCompanions → no componentSystems. Component-name-keyed arrival for these requires a member-name→entry lookup (e.g. scan STELLAR_COMPANIONS farCompanions), placeable inside the lane-C module.
- Seed identity computed and confirmed: realStarSeed(Rigil 8.000948,0.024984,-0.000924) === realStarSeed(Proxima 8.000902,0.024956,-0.000937) === 1816942132 (same 0.1 pc F1 bin: 80009/250/-9). Both marker paths log the AC1-pinned seed. [WARP] log line: main.js:3604 prints the marker seed (generation seed for knownWarp is 'alpha-centauri', ignored by the log).

### Lane B (feature/supercruise-freelook, worktree /home/ax/projects/well-dipper-sup...

Lane B (feature/supercruise-freelook, worktree /home/ax/projects/well-dipper-supercruise, HEAD 0ad88df, merge-base with master 847ab19) carries two ORRERY workstreams (orrery-coherence at VERIFIED_PENDING_MAX 802cceb close-out-open, orrery-entry-orbits at VERIFIED_PENDING_MAX ee1747b awaiting Max UAT) — and ZERO autopilot-depart code: SupercruisePilot.js is byte-identical to master (phases IDLE/ALIGN/CRUISE/HOLD only), so AC3's DEPART radial-climb/ETA-decel endpoints exist purely as contract text; DEPART builds on this branch AFTER the orrery pair ships, which lengthens our build gate beyond "current branch merges". The good news: `git merge-tree --write-tree master feature/supercruise-freelook` is CLEAN (exit 0) — file overlap between lanes is only src/main.js + docs/NOW.md, and within main.js the two lanes' hunks interleave without textual collision. The one semantic composition point is the destination-resolution seam: lane B extracted onPrepareSystem's body into `_generateWarpDestinationData()` (shared with the ORRERY instant-cut), while lane C (master) rewired that same body through `resolveArrivalSystemAsync` (arrivalResolution.js) — post-merge, lane C's shared resolver lands inside lane B's extracted function, which means an AC1 component-arrival extension made in arrivalResolution.js automatically serves warp cinematic AND ORRERY instant-cut. Key seams verified: warpSwapSystem's stop-spawn-teleport-resync sequence (incl. the mandatory cameraInterp.resync after any teleport), spawnSystem's _resetWorldOrigin + warpTarget wipe + far-plane reset (all of which the AC3 handover must survive), _frameSystemForOrrery's >5x gap-stop (now at :6462, not the contract's stale ~6371) with maxDistance = max(50000, 3×gap-stopped radius) that AC7 must extend, and lane B's new reducer register in flightModes.js that all our ORRERY/nav routing must resolve through. Two lane-B changes materially threaten contract assumptions: (1) OrbitRingSDF's new proximity fade (near = max(0.35, 0.02×R)) makes AC6's giant component orbit (R≈13M units → near≈260k units) invisible from inside the component's own neighborhood unless per-instance setProximityFade overrides are part of the design; (2) dispatchNavAction's warp path now has three routes (stash/overwrite/normal), and the `_pendingPlayerWarp` stash carries only {wx,wy,wz,seed,name,type} — no slot for a component index, so AC1 must extend both navStarData and the stash shape.

Key seam facts:
- MERGE IS TEXTUALLY CLEAN TODAY: `git merge-tree --write-tree master feature/supercruise-freelook` exits 0 with no conflicted files. File-level overlap between lane B and master-since-847ab19 is ONLY src/main.js and docs/NOW.md (comm -12 of --name-only diffs). Lane C touches no src/flight/*; lane B touches no src/ui/* (NavComputer.js byte-guarded in orrery-entry-orbits contract mustStayWorking).
- DEPART IS NOT BUILT: lane B's entire src/flight diff vs merge-base is flightModes.js (+239) + two test files; SupercruisePilot.js and SupercruiseModel.js are byte-identical to master (diff -q silent). Pilot phases today: IDLE/ALIGN/CRUISE/HOLD (SupercruisePilot.js:88-213). AC3's 'DEPART-style radial escape -> cruise -> ETA-decel' must be designed against autopilot-depart contract text only: radial climb v/vmax-gated, orbit-to-horizon alignment, TRANSIT handoff at horizon-clear, commit-and-hold, ETA-scheduled decel replacing the HOLD position-lerp, DEPART-own timeout isolated from the CRUISE-keyed stall detector (contract.json:6,10-13,28-31,46-51).
- warpSwapSystem (supercruise src/main.js:6352-6453) sequence AC3's handover extends: flythrough.stop() -> autoNav.stop() -> scPilot.stop() -> _scPendingRealTarget=null -> _cancelSystemMusic() -> _deepSkyLingerTimer=-1 -> _clearReticleTargets() -> spawnSystem({forWarp:true, systemData:pendingSystemData}) -> camera teleport to standoff (orbitDist = billboardSwitchDistance(fov,innerHeight) x window._warpArrivalMargin default 1.3; binaries take max over both stars + separation, :6423-6433) -> cameraInterp.resync(camera) at :6449 — MANDATORY after any camera teleport or the render interpolator's prev/curr blend erases it (:6438-6448).
- spawnSystem (supercruise src/main.js:4455): wipes ALL warpTarget fields (:4457-4463 — AC4's continue-to-planet leg must stash its target before the swap), _resetWorldOrigin() at :4477 (zeroes world origin on EVERY spawn, fires no listeners — AC3 must re-express ship scene-local position/velocity across this discontinuity), camera.far reset to 200000 (:4482-4485), then shipSpawner.clear/lodManager.clear/_hideCurrentSystem/systemMap.dispose/old-system disposal.
- _frameSystemForOrrery is now at supercruise src/main.js:6462 (contract's '~6371' is STALE — drifted across lane B rounds; cite function names, not line numbers). Gap-stop at :6501-6509: sort planets' orbitRadiusScene ascending, walk outward, `if (orbits[i] > orbits[i-1] * 5) break` — far companions deliberately excluded; comment (:6496-6500) parks wide-separation presentation to well-dipper-binary-wide-separation and marks the 5x factor Max-re-rulable. Frames viewSystem(systemRadius*1.2) = net 1.8x outer orbit, pitch 0.7, maxDistance = Math.max(50000, systemRadius*3) (:6524-6527). AC7's log-zoom-out must raise maxDistance past 3x AND manage camera.far (default 200000 << the ~13M-unit alpha Cen component orbit); precedent: _beginOrreryArrivalZoom raises far = max(200000, spawn*1.15) and restores 200000 via beginArrivalZoom onSettle (:6573-6582). Pure twin of the gap rule: effectiveOuterOrbit (orreryEntryGeometry.js:102-111).
- DESTINATION-RESOLUTION SEAM COMPOSES CLEANLY: lane B extracted onPrepareSystem's resolution body into `async function _generateWarpDestinationData()` (supercruise main.js:3649; onPrepareSystem wrapper :3815 sets _foldSnapshotTaken synchronously then awaits it) so _enterSystemInstantOrrery (:6596-6641) resolves byte-identical identity. Master/lane C rewired the INSIDE of that same body through resolveArrivalSystemAsync (sysdetails src/generation/arrivalResolution.js:124; sets pendingSystemData = _arr.systemData). Hunks don't overlap -> git auto-merges; post-merge, AC1's component-index extension in arrivalResolution.js automatically covers BOTH warp cinematic and ORRERY instant-cut entry.
- dispatchNavAction 'warp' now has THREE routes (lane B): navDispatchDuringWarp (flightModes.js:597) -> 'stash' (post-FOLD: _pendingPlayerWarp = {wx,wy,wz,seed,name,type} consumed at warpRevealSystem) | 'overwrite' (pre-FOLD: _setWarpTargetFromNavStar with turning save/restore) | 'normal' -> systemEntryStyle(_effectiveRegime()) picks _enterSystemInstantOrrery vs beginWarpTurn. AC1 must thread a component index through warpTarget.navStarData AND the _pendingPlayerWarp stash shape, or a mid-warp component pick silently loses its component. dispatchNavAction 'burn' is gated by burnWorkflowAvailable (HELM-only; inert in ORRERY) — AC3's nav BURN slots behind this gate.
- SC_TUNING facts (SupercruiseModel.js, byte-identical both worktrees): CAP_MAX 20000.0 at :16; ETA_K 3.0 with documented invariant >= 9/4 (:11-13); ACCEL_TAU <= ETA_K/4 (:17-19); speedCap() = clamp(min over bodies of max(CAP_MIN_ABS, radius*0.5, surfaceDist/ETA_K), CAP_MAX) (:85-94). Verified arithmetic: at CAP_MAX 75000, the distance-proportional term governs within 75000*3 = 225,000 units = 225 AU of any body (contract's 'below ~225 AU unchanged' claim checks out); today's 20000 ceiling binds only beyond 60 AU. Lane B does NOT change CAP_MAX or any neighbor.
- Lane B's mode-ownership reducer register our AC7 must route through (flightModes.js): bodyCycleAction:408, burnWorkflowAvailable:436, burnButtonRegimeVisible:462, navAutopilotToggleAction:482, autoWarpTimerFires:495, systemEntryStyle:515, tourRearmAllowed:535, bodyClickAction:568, navDispatchDuringWarp:597, bootSkipDecision:228; boot-aware regime read _effectiveRegime() at main.js:2705. ORRERY camera carriers for the AC7 fly-over: ShipCameraSystem.beginArrivalZoom(spawnDistance,{onSettle}) :630, two-phase AIM/APPROACH glide (GLIDE_AIM_SECONDS_PER_RAD 0.45 :92, cap 0.7s), focusOn :662; orreryStandoff table {star 15R, planet 4R, moon 2.6R} (orreryStandoff.js:32).
- AC6 HAZARD — OrbitRingSDF proximity fade (lane B round 3, ee1747b): OrbitLine extends OrbitRingSDF (OrbitLine.js:35; used for ALL orbit lines, main.js:4644/4648/4772/4787); vertex-stage fade by camera 3D distance to the ring's CIRCLE with near = max(uProxNearAbs 0.35, uProxNearRel 0.02 x R), far = near x uProxFarMul 3.0 (OrbitRingSDF.js:139-141,174-175). For a component orbit R ~ 13M units (alpha Cen ~13,000 AU x 1000 u/AU): near = 260,000 units — a camera anywhere in the component's neighborhood is ON the circle, so the giant orbit fades to ZERO under defaults. AC6's ring must apply per-instance setProximityFade overrides (OrbitRingSDF.js:316-321 — nearAbs/nearRel/farMul, all clamped positive) or bypass the fade.
- Lane B's claimed src/main.js regions (merge-base -> HEAD line ranges; ANY pre-merge edit here conflicts): imports 17-54; autoNav.onTourComplete ~2425->2432; startIntroSequence ~2550->2572; boot flags/_pickBootMode/_bootSkipToSol/_effectiveRegime 2651-2790->2683-2838; _initNavComputer callback + dispatchNavAction burn/warp 2791-2898->2928-3097; onPrepareSystem extraction 3433-3610->3636-3833; hitTestOrbits ~3894->4117; warpSwapSystem tail + ORRERY entry block 6224-6238->6452-6742; warpRevealSystem 6238-6346->6693-6813; selectTarget/commitSelection/_updateCommitBurnButton/focusPlanet 6514-6842->6981-7368; _syncOrbitsToMode 7007->7533-7601; simStep 8695-8877->9283-9488; keydown 10069-10086->10679-10720; trySelect 10132-10172->10764-10848; touchend/mobileControls 10768-10886->11448-11621.
