{
 "workstream": "nav-restorations-2026-09-20",
 "intentRef": "intent.md",
 "project": "well-dipper",
 "status": "building",
 "seamRef": "SEAM.md",
 "tier": "shipping game UI; the 240p pictures are prototype-tier and Max judges them by eye. Bars are observable facts (a string in the text stream, a rect published, a field moved); the only numeric bars are derived — 417 texels (the buffer at his window), 3260 ly/kpc (legacy's own constant), the wheel's 1.15/0.87 and [0.3, 5.0] (legacy's own clamp).",
 "waves": {
  "1": [
   "AC-1",
   "AC-2",
   "AC-3",
   "AC-7",
   "AC-8",
   "AC-9"
  ],
  "2": [
   "AC-4",
   "AC-5",
   "AC-6",
   "AC-10",
   "AC-11"
  ]
 },
 "acceptanceCriteria": [
  {
   "id": "AC-1",
   "pageItem": 13,
   "wave": 1,
   "statement": "\"Hover-inspect on every screen: sector name at GALAXY, coordinates at SECTOR and REGION, the star tooltip at PRISM, the body callout at SYSTEM.\" MEASURED: under a design the hover is ALREADY RESOLVED — navViewModes/picking.js:53 HOVER_FIELD writes nav._hoveredTile / _hoveredLocalStar / _hoveredBody from the designs' own published geometry inside resolveHover (index.js:508-532, called at the tail of render(), index.js:346) — but no painter can read it: S and D carry no hover field (state.js:283-438) and the painters close over S/D only (designs.js:66-68). Legacy's tooltip contents are the spec: GALAXY the sector name (NavComputer.js:1856-1861); SECTOR/REGION the tile centre as a kpc pair `(x.x, z.z)` (:1677-1679); PRISM name, `<spectral> class`, `<pc> pc (<ly> ly)`, `<pc> pc above|below plane` (:2200-2215); SYSTEM a planet's type · R⊕ · AU · K · moons, or the star's class · R☉ · age · planet count (:2858-2893).",
   "verifyVia": {
    "input": "In each design, at each of the five levels, move the pointer onto a drawn thing (a sector cell, a tile, a star mark, a planet pip / ladder stop / the star) and render; then move it onto empty map and render.",
    "observable": "The next frame paints a callout beside the pointer, inside the map pane, with that thing's facts in the legacy content (name and class and distance and height for a star; type, R⊕, AU, K, moon count for a planet; class, R☉, Gyr, planet count for the star; the sector name; the kpc pair for a tile). On empty map no callout paints. The callout never overprints the rail, the status line, the tabs or the commit row, and the mark guard stays silent. The legacy look is untouched."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 1b (LAB half; the DRIVER half landed wave 1a). hoverCallout (designs.js:1998-2050), calloutLines :2064-2110 (branches on hv.kind then ref.type), CALLOUT_GAP = 7 (:1996 — the 6 first tried was a real bug: the plate's edge sat on the mark's half-extent, so every 2D callout silently drew nothing), S.hoverCalloutRect published by the paint (cleared :796/:2149), design 1's call :839 gated on !S.search.open, design 2's :2216 inside the legend gate. R☉ is not in the face → the star callout prints R SUN (R⊕ is). 13 cases: 10 screens swept with real hoverAt, the rect inside the pane and disjoint from the mark and every chrome region, the empty-map frame byte-identical to the pointer-less frame, legacy's four PRISM lines and the SYSTEM body block read off the callout's rows; mutants noop/unclamped/on-null/name-only all red. Guard silent. ⚠ FINDING (integrator, measured live-visible): design 1 paints a callout for a RAIL-ROW hover — the hover point is in the rail, so the plate clamps into the map's corner ~35 texels from the pointer (design 2 already suppresses it in list mode). FIX = gate the call on the hover point lying inside REGIONS.map → wave 2a LAB. ⚠ S.hoverCalloutRect has no state.js default → wave 2a DRIVER. ✅ wave 2a: the rail-row-hover callout is FIXED — hoverCallout (lab :2524 / designs.js:2233) returns unless the hover point lies inside REGIONS.map, both designs; a real rail sweep at PRISM and SYSTEM hashes to the pointer-less frame. hoverCalloutRect default landed in state.js:341-352."
  },
  {
   "id": "AC-2",
   "pageItem": 14,
   "wave": 1,
   "statement": "\"The prism's depth cues: drop lines to the plane, the plane grid, far-first drawing; plus spectral colour and the catalogue halo in design 1.\" MEASURED (the page's prism table): legacy draws a dashed drop line per star, green above / red below the plane (NavComputer.js:2074-2079), a 1 pc plane grid (:1994-2014), sorts far-first (:2029), colours by spectral class (star.color, :2104) and rings catalogue stars (star.isReal && star.name, :2109-2113). Both designs draw none of the first three; design 1 colours catalogue-vs-procedural only and draws no halo. The page: \"every cue in the 'none' cells has its two endpoints already computed in the design pipeline (the foot dot and the mark share them), so the drop line and the plane grid are lab-side drawing, not new data\" — projectPrism (designs.js) returns both the mark and its foot.",
   "verifyVia": {
    "input": "Open each design at PRISM in Sol at 417x240; record the frame's fill stream. Rotate the prism (Q/E + arrows) and zoom (wheel) and record again.",
    "observable": "For every drawn star mark there is a one-texel-wide line from its foot on the plane to the mark, in one ink above the plane and another below; a grid of plane lines at 1 pc spacing, clipped to the map pane, underneath the marks; marks are painted in far-to-near order (the recorded fill order of two overlapping marks puts the nearer one last). In design 1 each catalogue star's mark takes its spectral ink (the SPECTRAL table in designs.js) and carries a one-texel halo ring; procedural stars keep the low-fi mark. The mark cap (240 design 1 / 584 design 2) and the marks themselves are unchanged; the mark guard is silent; frame hashes at levels 0-2 and 4 are byte-identical before/after."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 1a (LAB, lab → extract). Four INK entries GRID/ABOVE/BELOW/HALO (designs.js:186-189); lineTexels :334; prismPlane :635 (PLANE_CELL_KPC 0.001 = NavComputer.js:1191); prismDrop :711; d1Prism far-to-near sort + plane + stems + spectral/halo (lab :1505-1521); d2Prism (lab :2452-2456). Tests red on mutants: stem ink swapped, plane call deleted/moved, sort removed, spectral dropped. ⚠ DEPARTURE (measured): the lattice is 1 pc at the default camera and doubles (2/4/8 pc) as the camera pulls back — a true 1 pc lattice at the widest stop draws 13,093 texels, a wash. ⚠ LIVE CHECK PENDING: frame time at design 2 PRISM zoomed fully out (the cues multiply the drawing ~5×); headless the two 5 s-budget cases are unchanged (2928 vs 2968 ms). Frame hashes at levels 0/1/2/4 byte-identical."
  },
  {
   "id": "AC-3",
   "pageItem": 15,
   "wave": 1,
   "statement": "\"Names on the prism in design 1 (in place of the eight digits), and the height numbers in both: HEIGHT with its region, PLAYER Y, Y RANGE\" → names on the marks; numbers into design 1's detail block and design 2's status line. MEASURED: legacy labels only star.isReal && star.name (NavComputer.js:2109-2133) with a placement solver (:2337-2387); design 1 prints the eight-digit seed and no name (the page's prism table: NAMES ON THE MAP 0); design 2 already prints up to ten names. Legacy's numbers (:4276-4296): HEIGHT = _localCenter.y in pc with region thin disk (<0.3 kpc) / thick disk (<1.0) / halo, above|below plane; PLAYER Y = _playerY kpc and pc; Y RANGE = playerY ± 2.0 kpc in pc. All three inputs are already mirrored: S.cam.y (state.js:394) and D.player.y (state.js:796).",
   "verifyVia": {
    "input": "Open design 1 at PRISM in Sol; read the text stream. Then design 2 at PRISM. Then move the camera height with R/F (or the y-gauge) and read both again.",
    "observable": "Design 1: each catalogue star mark that had a name in legacy carries that NAME (upper-case, fitted) and the eight-digit seed is gone from the map; the selected star's name always draws; where two names would overlap, the lower-priority one (selected > current > nearest first) yields to its letter or is dropped, so the mark guard is silent. Design 1's detail block at PRISM prints three lines — `HEIGHT <n> PC ABOVE|BELOW · <THIN DISK|THICK DISK|HALO>`, `PLAYER Y <n> PC`, `Y RANGE <a> TO <b> PC` — and design 2's PRISM status line prints at least HEIGHT with its region, all fitting 417 texels without the fit() eating any existing clause. After R/F the HEIGHT number moves by the same pc the camera moved. Legacy unchanged."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 1a (LAB). Names replace the eight index digits on design 1's marks, selected > current > nearest (lab :1546-1577); prismNumbers() serves both designs (designs.js:1804); design 1's PRISM detail block (lab :1905-1919); design 2's PRISM status (lab :2312-2316). Mutants: rank sort removed → 'expected ALTAIR to be SIRIUS'; height from D.player.y → red at 450 pc. ⚠ DEPARTURE 1: design 1's HEIGHT clause is split over two rail rows (HEIGHT 0 PC ABOVE / THIN DISK) — the joined form is 179 texels in a 149-texel rail (the guard fires on it). ⚠ DEPARTURE 2: design 2's PRISM bar was ALREADY overflowing (69 chars in 60; R/F UP had never been visible) — its star count and three key hints moved to a legend row on the map (designs.js:1886-1900); the bar reads PRISM · VIEW n LY · HEIGHT n PC ABOVE · REGION. CONSEQUENCE: design 1's PRISM rail list is 22 rows (was 27); navDefects2026w2.driver.test.js:85-95 now reads the page size off S.listGeom.rows (integrator; red on a paging mutant). Loose thread, no AC: the rail's 1-8 digit column at PRISM while marks carry names."
  },
  {
   "id": "AC-4",
   "pageItem": 16,
   "wave": 2,
   "statement": "\"Planet detail with moons, and moons as burn targets\" — \"needs a design-side sub-view, not a switch back to the old one.\" MEASURED: legacy enters _systemMode='planet' on a click of a planet with moons (NavComputer.js:4574-4592) and draws an in-scene orrery of its moons (_renderPlanetDetail :3256-3537) where a moon click makes it the burn target (:4515-4519, _buildCommitAction :1124-1139 emits target 'moon' with planetIndex+moonIndex, which main.js:5994/5999 already consumes). Under a design the pin at :4585/:4590 is skipped by design (AC-3 of the close pass) and stays; the designs draw the whole system and a moon pip or rail row selects the PARENT (the page's ladder table). D.bodies rows already carry each planet's moon count (state.js:544-546).",
   "verifyVia": {
    "input": "In each design at SYSTEM in the CURRENT system (Sol): click a planet that has moons (it selects); click it AGAIN (the detail opens); hover a moon (the callout names it) and click it; press Enter (or the commit row / chip). Then right-click, or click empty map, from the detail. In design 1 also click a moon's rail row from the whole-system view. A FOREIGN system is outside this AC (rewritten 2026-09-20 after wave 2b): under a design the host's foreign-system planet branch arms no selection (NavComputer.js:4589-4592 sets _selectedPlanetIdx only), so no planet can be the selection there and the sub-view's entry condition cannot be met — logged as a follow-on (HOST work, a wave of its own).",
    "observable": "The second click opens a design-side sub-view drawn in that design's own language — design 1 a moon ladder with the parent at its head and the rail listing the moons; design 2 a moon orrery with rings and pips — with every moon on its orbit, named (the same name the rail prints). A moon click selects it: the commit row / chip arms for the MOON and Enter (or the commit click) fires _onCommit with target 'moon', planetIndex and moonIndex, which the running game turns into a burn to that moon. Right-click and an empty-map click each return to the whole-system picture without closing the nav or changing level (the glass says RIGHT CLICK BACK; the empty click also clears the selection, as it does everywhere). Esc keeps closing the WHOLE nav (rewritten 2026-09-20: Max's 2026-07-29 ruling — 'after we bring up the nav computer, esc should just dismiss' — guarded by NavComputer.escape.test.js; a real Esc never reaches handleEscape. One host fold in _onKeyDown at :349 flips it to 'step back out of the sub-view first' if he wants that — ASK, carried). In design 1 a moon's rail row selects the moon directly, without the sub-view. Nothing here sets nav._systemMode; the legacy look and its own planet detail are unchanged."
   },
   "layer": "integration",
   "live": true,
   "progress": "✅ BUILT wave 2b, all three lanes MEET (integrator's end-to-end walk navRestorations4.design.test.js:651, both designs, no stubs: select → drill → hover a moon (the plate names it with ORBIT n R⊕) → click it (_commitAction {type:'burn', target:'moon', planetIndex, moonIndex}) → BURN TO <MOON> on design 1's commit row / [BURN] + the moon on design 2's bar → right-click through the canvas's own contextmenu handler back to the whole system, selection kept → a second right-click pops to PRISM as before). LAB: d1MoonLadder (parent at the head, moons by orbit radius sorted — the axis separation pass assumes ascending order — tag = parent letter + lower-case ordinal, names, selected framed, ship/trajectory suppressed) + d1Rail's MOONS list, MOON OF <PARENT>, the moon's facts, hint SELECT A MOON · RIGHT CLICK BACK; d2MoonSystem (parent at the centre, a ring + pip per moon at its startAngle, names, selected framed, zoom + gauge still live, S.orbitRings empty on purpose so an empty click stays the way out) + d2Status <PLANET> · N MOONS · RIGHT CLICK BACK; moon hits published with the moon's own row and { type:'moon', planetIndex, moonIndex }; calloutLines' moon branch; S.railBodies = the rows the sub-view rail actually drew. DRIVER: S.sysView/S.detailPlanet (state.js:435-457), reset on level ≠ 4 (an invariant, every frame) and on a design change (:893); ENTER/INSIDE/EXIT in routeSystem (index.js ~:1385) + onEscape(); bodyIdentity's 4th arg S so a moon is the pick only inside the sub-view (picking.js:402-412); pickFromRow reads S.railBodies by the DRAWN row (integrator, index.js:497-507 — measured before the fix the five drawn rows resolved to planets 0,1,2,2,3); D.selBody already carried type 'moon'. HOST: ONE fold at handleEscape() :1441 (`if (this.viewMode && this._viewDriverInst?.onEscape?.() === true) return true;`; trap 11 discharged — no test scans that signature; 4711 kept). Integrator: ESC BACK replaced by RIGHT CLICK BACK on the glass (Esc never reaches handleEscape — see the observable), two VOCABULARY entries with real probes in navAffordances (:487-548). Byte-identity: every level of both designs and the whole-system SYSTEM hash identically to HEAD; legacy incl. its own planet detail unchanged. Suites: src/ui 918/44 at --testTimeout=60000 (the same 17 five-second contention timeouts at the default), cockpit 698. OPEN: (1) ASK Max — Esc in the sub-view: step back, or keep dismissing the nav (his July ruling)? Recommend keep. (2) FOLLOW-ON (HOST): foreign-system planet selection under a design. (3) FOLLOW-ON (HOST+DRIVER, small): V from a design through LEGACY and back leaves a sub-view open (RAIL↔BARS resets; the driver has no frame under legacy) — one statement on the host's V clause (:349). (4) FOLLOW-ON (minor): S.ladderScroll is not restored on leaving the sub-view in a system dense enough to scroll (Sol does not). ⚠ LIVE CHECK PENDING: a real burn to a moon through the running game."
  },
  {
   "id": "AC-5",
   "pageItem": 17,
   "wave": 2,
   "statement": "\"The ship's real position at SYSTEM, and the trajectory line to the selected body.\" MEASURED: legacy places the ship from nav._currentFocusIndex / _currentMoonIndex (NavComputer.js:280-281, set by the game through the setter at :1174-1175) — at the star for -2/-1, else on the focused planet's orbit point (and its moon's, :2909-2929) — draws a diamond + SHIP (:2935-2956) and a dashed line to the hovered-or-selected body (:2959-3005), all gated on _isCurrentSystem() (:2905, mirrored as D.isCurrent). Neither field reaches D today; design 2 draws its diamond at a fixed offset, design 1 draws none (the page's ladder table, 'See where the ship is').",
   "verifyVia": {
    "input": "In the live game, in the current system (Sol), open each design at SYSTEM with the ship parked at a planet (the game's focus index set); read the frame; select another body; read again. Then open a foreign system from the prism.",
    "observable": "The ship diamond paints AT the focused body's drawn position — on that planet's orbit point in design 2's orrery, at that planet's stop on design 1's ladder (at the star for -2/-1; offset onto the moon's ring when a moon is focused) — labelled SHIP, and a dashed trajectory runs from it to the selected body (or the hovered one while the pointer is on a body), ending in an arrowhead. In a foreign system neither draws. Design 2's fixed-offset diamond is gone. Legacy unchanged."
   },
   "layer": "integration",
   "live": true,
   "progress": "✅ BUILT wave 2a (LAB; the DRIVER's D.ship landed wave 1a). shipHit/trajectoryHit/drawShip (lab :2401/:2417/:2452), INK.SHIP = legacy's #00ff80 (lab :310), called from d1Ladder (:1883) and d2System (:3411); design 2's fixed-offset diamond REMOVED. The diamond stands on the focused body's DRAWN point (the painter's own hits: planetIndex < 0 → the primary; moonIndex ≥ 0 → that moon's pip); SHIP goes through placeLabel and yields to a body's name; the trajectory is a dashed lineTexels (every other texel) to the hovered body (S.hover kind 'body') else D.selBody, ending in a 3-texel chevron; nothing when the target is the ship's own body or null; D.ship null → nothing. Six cases driven through nav.setCurrentBody(planetIndex, moonIndex) (NavComputer.js:1173, the game's own setter); mutants ship-fixed-offset and trajectory-to-centre red. DEPARTURES, declared: the line stops 4 texels short of BOTH marks (at 240p there is no alpha; a solid run erases a 3-texel planet); dashes every other texel, not [6,4]; a 3-texel chevron, not a filled triangle. A rail-row hover re-aims the trajectory (legacy's own hovered-or-selected rule). ⚠ LIVE CHECK PENDING: the diamond on the body the running game is actually at; vanishing in a foreign system."
  },
  {
   "id": "AC-6",
   "pageItem": 18,
   "wave": 2,
   "statement": "\"Wheel zoom at SYSTEM: design 2 yes; design 1 no, its ladder is a scroll, not a zoom.\" MEASURED: NavComputer._handleWheel (:4701-4710) already scales nav._systemZoom by 1.15/0.87 in [0.3, 5.0] at level 4 with no viewMode gate, and resets it to 1.0 on open / tab / drill (:607, :4470, :4618); only the legacy orrery reads it (:2531); no design does, so 'the wheel still changes a value; nothing draws it'. The close pass's AC-9 principle: an indicator that shows a position along a range is a handle.",
   "verifyVia": {
    "input": "Live, design 2 at SYSTEM in Sol: send wheel events over the map (both directions, past both clamps); read the orrery's outer ring radius from the frame before and after; then drag the zoom gauge; then Tab away and back. Design 1 at SYSTEM: wheel over the ladder.",
    "observable": "Design 2's orrery scales with nav._systemZoom (the outer ring radius grows by the wheel's factor per notch and stops at the clamps), the same picture at every zoom re-centres on the star, and a zoom gauge in design 2's SYSTEM chrome draws the value as a mark along its range; dragging the gauge sets the same zoom. Tab away and back returns to 1.0 as legacy does. In design 1 the wheel at SYSTEM changes nothing drawn (the ladder is unchanged, no gauge appears). Legacy unchanged."
   },
   "layer": "integration",
   "live": true,
   "progress": "✅ BUILT wave 2a, all three lanes MEET. LAB: d2System's rOf × S.sysCam.zoom (lab :3234), re-centred on the star by construction; rings/bodies/belts/pips/labels scale; a body pushed off the pane is neither drawn nor published (AC-19); zoomGauge() (lab :2355) draws a vertical track + mark + X<n.n> readout (the face has no ×) inside the pane at x = W-10 and publishes S.zoomGaugeRect = {x: W-10, y: mapY+10, w: 6, h: mapH-24}; the mark is clamped to the track's last row so t reads 1/h at the very bottom (0.007× in a range the host clamps anyway). DRIVER: zoomGrab/zoomDragTo (index.js:1055-1103, exported :1512; design 2 + level 4 + the rect with gaugeGrab's 1-texel skirt; t = clamp((y+h−py)/h), zoom = exp(ln 0.3 + t·(ln 5 − ln 0.3))), default state.js:340, cleared by resetPicks index.js:201. HOST: four /* */ folds at NavComputer.js:156/4355/4405/4415 exactly as the seam spelled them (4711 kept; the counter's own arming intact after an in-flight slip the HOST lane caught itself). End-to-end case navRestorations3.design.test.js:458: a real press+move on the DRAWN gauge changes nav._systemZoom and the next frame's outer ring radius to 6 dp. The real wheel (nav._handleWheel) scales the ring by its own factor and the star does not move; design 1's ladder unmoved by the wheel. ~20 named mutants red across the three lanes. ⚠ LIVE CHECK PENDING: a real wheel and a real drag in Max's Chrome."
  },
  {
   "id": "AC-7",
   "pageItem": 19,
   "wave": 1,
   "statement": "\"Body names on the ladder and the orrery, placed like the prism labels, in place of or beside the letter and roman tags.\" MEASURED: both SYSTEM pictures show ordinal tags only (the page's lost #4); legacy prints the generated or known name under every body (NavComputer.js:2833 via _planetDisplayName :916-920 / _moonDisplayName :923-927) and suppresses them at 240p (:2825-2832, seven names overprinted). D.bodies rows carry `name` (state.js:544). Sol has 15 stops in 226 texels on the ladder (the page's ladder table).",
   "verifyVia": {
    "input": "Open each design at SYSTEM in Sol; read the text stream; select a body; read again; sort the rail by name (design 1) and read again.",
    "observable": "Every body whose name fits beside its stop / pip carries its NAME (the rail's spelling); the selected body's name always draws; where names would collide the lower-priority body (selected > current > by AU) keeps only its letter tag, so no two labels overlap and the mark guard is silent; the letter tags stay where a name is dropped, and the ladder's AU order is unchanged (the 2026-09-18 decision). Belts keep their belt label (AC-8). Legacy unchanged."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 1b (LAB). bodyLabelText :1935, bodyLabelOrder :1957 (selected > ship > BELT > by AU — belts outrank planets because a belt has no rail row and no sprite; without it ASTEROID BELT was refused every slot on an eight-planet system), d1Ladder's label pass :1556-1563 (the NAME beside the letter — the letter is load-bearing for AC-20), d2System :2796-2803 (the name IN PLACE of the roman numeral, the numeral as the fallback when no slot fits). A 15-character cap was tried and reverted (procedural names differ only in their last character; fit() truncates from the right). Tests: names in the map text, S.ladderStops non-decreasing, one single-character tag per stop (AC-20), pairwise plate-vs-glyph overlap and plate-vs-foreign-mark, the outermost planet named once selected, a name plate selects the body it names (kind 'body' in S.labelHits). Mutants red ×11. Departure, declared: the overlap test is plate-against-GLYPHS (plated() pads 7 rows for 5 rows of glyphs; adjacent plates abut on padding without erasing a word)."
  },
  {
   "id": "AC-8",
   "pageItem": 20,
   "wave": 1,
   "statement": "\"Belt names, and a line of facts about the star\" (the wide-binary member list is PARKED). MEASURED: legacy labels each belt ASTEROID BELT / KUIPER BELT at 45° mid-radius (NavComputer.js:2644) and prints the header fact line `<class> · N planets · N.N Gyr` (:966-969; binary → `<c1>+<c2> binary`). Belts are already in D.bodies (state.js:602) as anonymous stops/annuli; D.sys carries star.type, planets, ageGyr, isBinary, star2. With nothing selected design 1's detail block prints NO BODY SELECTED and design 2's status line NOTHING SELECTED (AC-2 of the defects batch).",
   "verifyVia": {
    "input": "Open each design at SYSTEM in Sol with nothing selected; read the text stream; select a planet; read again. Open a system with a Kuiper belt and one with a binary star.",
    "observable": "Each belt stop (design 1) / annulus (design 2) carries ASTEROID BELT or KUIPER BELT, fitted. With nothing selected, design 1's detail block and design 2's status line carry the star line — class (`G2`, or `G2+M4 BINARY`), planet count, age in Gyr — in place of the empty-selection text; once a body is selected the block/status show that body as before. Everything fits 417 texels with the mark guard silent. Legacy unchanged."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 1b (LAB). beltLabel :1901 (the generator's isKuiper when the row carries it, else geometry: the belt beyond every planet is the Kuiper belt — exactly how shouldOuterBeltExist places it), sysStarClauses :1921 (design 2's bar gets legacy's sentence verbatim; design 1's rail prints the class on the KEY row and the two numbers on the next — G2+M4 BINARY · 8 PLANETS · 4.6 GYR is 34 characters in a 25-character rail), d1Rail :1720-1723, d2Status :2351. Tests: two belts named with the outer one Kuiper, the flag winning over geometry, the star line with nothing selected in both designs, a binary fitting the rail, the selected body shown exactly as before. Integrator: navDefects2026.design's two AC-2 no-selection cases (:235-244, :258-259) now assert the star line in place of NO BODY SELECTED (0.0 GYR is legacy's own (ageGyr||0).toFixed(1) for that fixture). ⚠ state.js buildBodies belt rows do not carry isKuiper (the generator sets it; legacy reads it) → wave 2a DRIVER adds `isKuiper: !!b.isKuiper`; until then the geometry path runs in the game and agrees on Sol. ✅ wave 2a: belt rows carry isKuiper: !!b.isKuiper (state.js:675-689); navRestorations2.design:265 now expects false (integrator; mutant buildBodies-drop-isKuiper red)."
  },
  {
   "id": "AC-9",
   "pageItem": 21,
   "wave": 1,
   "statement": "\"One zoom readout for all three looks, same quantity and unit\" — the old nav's radius in light-years. MEASURED: legacy prints `VIEW: <n> ly` = round(_localRadius × 1000 × 3.26) (NavComputer.js:4272-4273); design 1 prints nothing; design 2 prints `VIEW n.n PC ACROSS` = radius×2×1000 (the page's prism table). _localRadius is already S.cam.radius (state.js:394).",
   "verifyVia": {
    "input": "Open each design at PRISM in Sol; read the text stream; zoom one wheel notch in and out; read again. Compare with legacy's number at the same radius.",
    "observable": "Both designs print `VIEW <n> LY` where n = round(S.cam.radius × 3260), the same integer legacy prints for the same radius, in design 1's PRISM detail block and design 2's PRISM status line; design 2's PC ACROSS text is gone; the number changes with the wheel. Legacy unchanged."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 1a (LAB). VIEW <n> LY, n = round(S.cam.radius × 3260) (designs.js:1814) in design 1's PRISM block and design 2's PRISM status; PC ACROSS gone from every drawn string. Test drives nav._handleWheel and asserts against legacy's own integer before and after; mutant (PC ACROSS kept) red."
  },
  {
   "id": "AC-10",
   "pageItem": 23,
   "wave": 2,
   "statement": "\"Grabbable handles in design 2 to match design 1.\" Design 1 has the prism y-gauge (drag sets the camera height) and the ladder counter scrubber; design 2 has none (the page's Gained table, row 6: 'none yet'). MEASURED: the host's gauge routing at NavComputer.js:4402 already accepts ANY design (`this.viewMode && drv.gaugeGrab(x,y)`), and the driver's gaugeGrab/gaugeDragTo (index.js:878-898) read whatever S.yGaugeRect the paint publishes — so design 2 only has to draw a gauge and publish the rect. Design 2's PRISM corner widget today shows a scale mark only (the page's prism table). The SYSTEM zoom gauge is AC-6's.",
   "verifyVia": {
    "input": "Design 2 at PRISM: press on the gauge, drag up/down, release; read S.cam.y / nav._localCenter.y before and after; press on the map beside the gauge and drag (must pan / rotate, not the gauge). Repeat with the pointer one texel outside the gauge rect.",
    "observable": "Design 2's PRISM draws a y-gauge (a track with a mark at the camera's height, in design 2's chrome) and publishes S.yGaugeRect from its draw site; a press inside it arms the gauge and dragging moves nav._localCenter.y by the same mapping design 1 uses (the HEIGHT number of AC-3 follows); a press one texel outside it starts the map gesture instead. Design 1 unchanged; legacy unchanged."
   },
   "layer": "unit",
   "progress": "✅ BUILT wave 2a (LAB). Design 1's inline gauge lifted VERBATIM into a shared yGauge(g, gx, gy, gh) (lab :2218) — ONE publisher of S.yGaugeRect for both designs, so the eight fields the driver inverts cannot drift; d1Prism calls it with its old values (D1 PRISM hash identical), d2Prism at x = W-20 inside the map pane spanning the pane's height (the old 24-texel scale column is gone — over 24 texels the ±2 pc mapping was 0.17 pc per texel, unresolvable by a drag; cost, stated: six columns of design 2's prism starfield stop answering a click, the trade design 1 already made). A real drag through the existing :4402 routing moves nav._localCenter.y to the gauge's own inverse and AC-3's HEIGHT follows; a press 3 texels outside moves nothing. Integrator: navPicking.test.js:1325-1365's 'design 2 publishes no gauge' premise rewritten to the new behaviour (key set equals design 1's rect, read off a real frame; a press beside it still rotates — kept verbatim); stale one-publisher comments at index.js:1015/:1264 corrected. Mutants d2Prism-no-gauge and gaugeGrab-widen-skirt red."
  },
  {
   "id": "AC-11",
   "pageItem": null,
   "wave": 2,
   "statement": "The whole: with the old nav's information back — what a thing is on hover, the prism's height cues and names, the moons, the ship, the zoom you can see and grab — the two new menus read as one instrument. Max's walk.",
   "verifyVia": {
    "input": "Max opens the nav in his own browser, cycles V through both designs, and walks GALAXY → SYSTEM hovering, zooming, opening a planet's moons and burning to one.",
    "observable": "His call: it makes sense as a whole. No agent PASSes this; the workflow marks it deferred-to-max."
   },
   "layer": "uat",
   "progress": "not started"
  }
 ],
 "architecturalConnections": {
  "inputs": [
   "nav-240p-lab.html → scripts/extract-nav-designs.mjs → src/ui/navViewModes/designs.js (GENERATED, byte-checked by --check; never hand-edited). Design 1 block from lab :887, design 2 from :1841; the painters close over S and D by identity; `fit()` :326, `plated()` :423, `assertFits` :81 (the mark guard, inside every T() draw).",
   "src/ui/navViewModes/state.js — S (:283-429) and D (:430-438) with a default for every field a painter reads; refresh() mirrors nav._localCenter/_localRadius → S.cam, nav._playerY → D.player.y, nav._systemData → D.sys/D.bodies (buildBodies :513-…, rows carry kind, name, au, cls, moons, pd), nav._isCurrentSystem() → D.isCurrent. NEW this workstream (SEAM.md): S.hover, D.ship, S.sysCam.zoom, S.sysView/S.detailPlanet, S.zoomGaugeRect.",
   "src/ui/navViewModes/index.js — the driver: render() (:289-348) paints then resolves hover at its tail (resolveHover :508-532 writes the HOST's legacy hover fields from the designs' published geometry via picking.js:53 HOVER_FIELD); remapClick (:1089-1297) routes chrome before the host's per-level picks; gaugeGrab/gaugeDragTo (:878-898) read S.yGaugeRect.",
   "src/ui/NavComputer.js — the host, LINE-COUNT-FROZEN at 4711; new statements FOLD onto existing lines with /* */ comments; lines 1, 19, 255, 586, 613, 1434, 2029, 4345, 4420, 4442 end in // and are POISONED. Folds this workstream may need: handleEscape() (:1441, check for a test that source-scans the signature line before folding on it) for the sub-view's Esc/right-click; 4405 / 4355 / 4415 for the design-2 zoom gauge (mirroring the counter's three call sites); NOTHING else. _handleWheel (:4701-4710) is untouched.",
   "src/main.js — FROZEN at 15161 and untouched: its commit consumer already handles target 'moon' (:5994 burn, :5999 focus).",
   "docs/FEATURES/nav-menu-map-old-vs-new.html — the spec in Max's language: items 13-23, the prism table, the ladder table; his rulings are in intent.md.",
   "docs/WORKSTREAMS/nav-screens-close-pass/INTERFACE.md (+ nav-menu-elements-functional/INTERFACE.md) — the seam conventions this workstream's SEAM.md extends: LAB publishes at the draw site, DRIVER hit-tests, HOST folds; geometry comes out of the paint; branch on a published kind; the plate rule; labelHits before prismHits/bodyHits."
  ],
  "outputs": [
   "Both designs identify what the pointer is on before a click, at every level, in the legacy tooltip's content.",
   "Design 1's prism reads height the way legacy's did (drop lines, grid, far-first, spectral ink, halo, names) while keeping its low-fi marks; both designs print the height numbers and the same VIEW radius in ly.",
   "SYSTEM in both designs: every body and belt named on the picture, the star's fact line when nothing is selected, the ship where it really is with a trajectory to the target, and a design-side moon view from which a moon can be burned to.",
   "Design 2 gains its handles: the prism y-gauge and a SYSTEM zoom gauge the wheel also drives."
  ],
  "mustStayWorking": [
   "wc -l src/ui/NavComputer.js == 4711 and wc -l src/main.js == 15161.",
   "node scripts/extract-nav-designs.mjs --check exits 0.",
   "viewMode === null draws byte-for-byte what HEAD draws at every level on a fresh open AND after a V cycle (nothing in this workstream touches legacy behaviour; AC-6/AC-7/AC-8 of the defects batch are the only legacy changes on record).",
   "Every AC of nav-defects-batch-2026-09-18 still holds — in particular AC-2 (a real no-selection state: D.selBody null must be handled by every NEW reader too), AC-4 (a press on chrome — now including a callout, a gauge or a belt label — arms no map gesture unless it is a handle), AC-12/AC-18 (every legend/status/hint string still fits 417 texels after the new clauses; the mark guard is silent across both designs × five levels × the sub-view), AC-13 (a drag released over any new rect fires nothing), AC-20 (one letter per body, the ladder in AU order).",
   "Every AC of nav-screens-close-pass and nav-menu-elements-functional: the map is a picker at all five levels; Tab/Enter/[ ]/- =// stay bound where they draw; the y-gauge, counter and ladder drag in design 1 work as before; rings/labels/tags pick as before; S.labelHits is tested before S.prismHits/S.bodyHits.",
   "Suites at baseline: src/ui 767 passed / 35 files (+ this workstream's new tests), src/cockpit 698, tests/ 20 failed across the same 8 files; run vitest with --root /home/ax/projects/well-dipper --exclude '**/.claude/**' --exclude '**/node_modules/**'; never overlap two vitest runs.",
   "Hover resolves at the tail of the driver's render(); S.hover published there is read by the NEXT frame's paint (one frame of lag, as legacy's own mousemove-then-draw has). S and D are MUTATED, never replaced; every new field has a default in state.js; PanelHost freezes the glass on the first painter throw.",
   "The cockpit panel instance (activate() gate) is untouched; nothing here reaches the ORRERY/HELM screens."
  ]
 },
 "baselineAfterWave1a": "src/ui 797 passed / 37 files (2 navViewModes load flakes green alone), src/cockpit 698, extract --check 0, NavComputer.js 4711, main.js 15161",
 "baselineAfterWave1b": "src/ui 810 passed / 38 files (six 5 s-budget timeouts under whole-suite contention, all green alone — navPicking alone takes ~243 s), src/cockpit 698, extract --check 0, NavComputer.js 4711, main.js 15161",
 "baselineAfterWave2a": "src/ui 855 passed / 41 files (17 five-second-budget timeouts under the whole-suite run, 855/855 with --testTimeout=60000 — contention, not product; the un-budgeted cases in navPicking/navSearch/navViewModes now sit near the default), src/cockpit 698, extract --check 0, NavComputer.js 4711 (4 folds: 156/4355/4405/4415), main.js 15161",
 "followOns": [
  "HOST: under a design a FOREIGN-system planet click arms no selection (NavComputer.js:4589-4592), so neither a planet burn/warp target nor the moon sub-view is reachable there; legacy drills straight into its planet detail. A wave of its own — it changes what a foreign planet click means across the screen.",
  "HOST+DRIVER (small): V from a design through legacy and back to a design keeps an open sub-view open; RAIL↔BARS resets it (state.js:893). One statement on the host's V clause at :349 calling a driver reset when entering a design.",
  "DRIVER (minor): S.ladderScroll is clamped to the moon ladder's range inside the sub-view and not restored on leaving; only visible in a system whose whole-system ladder overflows (Sol fits).",
  "TEST HYGIENE: the whole src/ui run at vitest's 5 s default now shows 17 contention timeouts (6 before this workstream); 918/918 at --testTimeout=60000. Either raise the budget in navPicking/navSearch/navViewModes or set testTimeout in the vitest config.",
  "LAB (no AC): design 1's PRISM rail still prints a 1-8 digit column while the marks carry names (wave 1a loose thread).",
  "LAB (no AC): design 2's SYSTEM companion strip draws a » that is not in the face (tofu) — pre-existing, seen while building wave 1b."
 ],
 "baselineAfterWave2b": "src/ui 918 passed / 44 files at --testTimeout=60000 (17 contention timeouts at the 5 s default — the same set as wave 2a), src/cockpit 698, extract --check 0, NavComputer.js 4711 (5 folds this workstream: 156/1441/4355/4405/4415), main.js 15161"
}