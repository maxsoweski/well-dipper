{
  "workstream": "agent-camera-api-2026-08-10",
  "intentRef": "intent.md",
  "project": "well-dipper",
  "status": "verified",
  "verification": {
    "at": "34046f4",
    "date": "2026-08-10",
    "driver": "working-Claude via chrome-devtools against localhost:5173, both front-ends, seed lab-procedural-6",
    "serverProvenance": "confirmed serving THIS tree before any result was trusted — `_lab.frameBody` is present and did not exist minutes earlier",
    "results": {
      "AC-1": "PASS both front-ends, each with a positive control that MOVED. Game: framed pose drift 0.000000 units over 40 frames; camera-only write of the same magnitude erased, 6928.204 units, snapping back to within 0.001225 of the framed pose. Lab: 0.0000% vs a control erased at 66.67%.",
      "AC-2": "PASS as amended. Lab clamp 1.05->1.10 reported; plain moon asked 3 achieved 2.9999; achieved matches shotState's independent route to 0.0000% at 12/4/1.5 radii. Game near-clamp absent — recorded as a finding.",
      "AC-3": "PARTIAL. Agreement branch verified live (4dp at 9 rungs); no-uniform branch verified live with the reason named; `agrees:false` branch unit-tested only, NOT witnessed live.",
      "AC-4": "PASS. 9-rung tables from both front-ends, identical asked values, achieved agreeing to 4dp, diverging only at the clamped last rung.",
      "AC-5": "PASS. Star framed against its AUTHORED radius 4.720087; plain moon at 2.9999 of 0.012134; planet-class moon reachable by name; star LOD uniforms null rather than 0.",
      "AC-6": "PASS with five hand-authored mutants, each run RED and restored GREEN."
    },
    "headline": "Saturation onset in (6.62, 4.58] body radii. Five of nine rungs resolve nothing new — the disc grows ~4.4x from 4.58 to 1.05 while the octave budget stays pinned at 9.",
    "instruments": "all four green; citations 320",
    "methodNote": "The first AC-1 control run reported 'did not move' because it sampled through shotState, which spends 500ms measuring fps BEFORE reading geometry — both samples landed post-erasure. The instrument, not the code. cameraPose() is synchronous and gave the real answer."
  },
  "acceptanceCriteria": [
    {
      "id": "AC-1",
      "statement": "Framing a body STICKS. After asking for a body at a given multiple of its radius, the camera is still there many frames later — on the game's ORRERY and on the lab page. The claim is only accepted alongside a positive control proving the old direct-write path drifts over the same interval, because the failure this replaces reports success while the camera walks away.",
      "verifyVia": {
        "input": "In the game: `_lab.spawnProceduralSystem('lab-procedural-6')` then `_lab.frameBody({kind:'planet', p:3}, {radii:2})`. In the lab: `_lab.frameBody({radii:2})`. Wait >=30 rendered frames on each, then re-measure.",
        "observable": "Achieved distance-in-radii after >=30 frames is within 1% of the value the call returned, on BOTH front-ends. POSITIVE CONTROL, same page, same interval: the pre-existing path (game `_lab.setCameraPose`, lab direct `camera.position.set`) drifts well outside that band — proving the stability is a real null and not an unmoved instrument."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-2",
      "statement": "The call reports what it ACHIEVED, not what was asked. The returned number is the achieved one, measured in the renderer's own frame. ⚠ AMENDED AFTER MEASUREMENT (2026-08-10): this AC originally asserted that BOTH front-ends clamp the near end. Only the lab does — `minCameraDistance(sVis)` raised an ask of 1.05 to 1.10. The game applied NO clamp on the focusOn path: asked 0.4, achieved 0.4, camera inside the body. The radius*1.05 floor belongs to ORRERY focus min-distance, not to focusOn. The AC is therefore verified on the lab's clamp plus two independent no-echo demonstrations, and the game's missing near clamp is recorded as a finding for Max rather than treated as a pass or a fix.",
      "verifyVia": {
        "input": "Ask for a radius BELOW the near-end clamp on each front-end (e.g. `{radii: 1.0}`), and for one comfortably above it (`{radii: 8}`).",
        "observable": "For the below-clamp ask, the returned `achieved.radii` differs from the asked value and equals an independent measurement taken from the live scene graph via `getWorldPosition`/`getWorldScale` (the rebase-immune method `_lab.shotState().geometry.distanceRadii` already uses) to within 1%. For the above-clamp ask, asked and achieved agree to within 1%. No call ever echoes the asked value back as though it were achieved."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-3",
      "statement": "The call reports the LOD state it produced, with the renderer's LIVE uniform values and the values predicted from the achieved distance reported SEPARATELY — so a disagreement between them is visible rather than averaged away. This is what turns a side-by-side into two pictures with numbers attached.",
      "verifyVia": {
        "input": "`_lab.frameBody({kind:'planet', p:3}, {radii:2})` on a generated system, then the same against a PLANET-CLASS MOON.",
        "observable": "The return carries live `uOctaves`/`uLodRamp` read off the body's material AND `predicted.octaves`/`predicted.ramp` computed by `lodRampOf`/`autoOctaves` (imported from `planet-lod-lab-core.js`, the shared law) from the achieved radii — as separate fields, so a mismatch is visible rather than averaged away. ⚠ AMENDED AFTER MEASUREMENT (2026-08-10): the planet-class-moon prediction in this AC was WRONG. Measured on `body.planet.f5791a`, such a moon carries NO `uOctaves` uniform to be frozen at 4.0 — it is not on the lab material at all (71 uniforms, isLabPlanetMaterial false, carries `uReliefOctaves`/`lodLevel` instead), so the LODManager registration gap is downstream of a material gap. VERIFIED: ordinary planet agrees to 4dp at all 9 rungs; no-uniform bodies report `agrees: null` with the reason named, and the sweep now refuses to present the law's octave column as a measurement of such a body. ⛔ RESIDUAL: the `agrees: false` branch (uniform present but not driven) is NOT witnessed live — no body in the scanned seeds is both lab-material and unregistered. It is unit-tested only, and AC-3 must not be quoted as fully closed."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-4",
      "statement": "The whole approach comes back in one call. Walking a body from far out down to nearly touching it produces a table — one row per step, each with achieved radius and the LOD state at that radius — on both front-ends. This is the row of numbers that answers 'does detail keep resolving, or am I flying at a beach ball?'",
      "verifyVia": {
        "input": "`_lab.approachSweep({kind:'planet', p:3}, {from:20, to:1.2, steps:8})` in the game, and the lab-page equivalent.",
        "observable": "Returns 8 rows, each carrying achieved radii (monotonically decreasing) plus live and predicted LOD state. The table PRINTS the saturation: rows at and below 6 body radii all show predicted octaves pinned at 9.0 while achieved radii keep shrinking. Both front-ends produce a table of the same shape over the same ladder, so rows can be paired."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-5",
      "statement": "Stars and moons are reachable, not just planets — and widening body resolution to reach stars does not disturb any existing planet or moon resolution.",
      "verifyVia": {
        "input": "On a fixed seed: `_lab.frameBody({kind:'star', s:0}, {radii:4})`, `_lab.frameBody({kind:'moon', p:1, m:0}, {radii:3})`, plus `_lab.resolveBody({kind:'planet', p:0})` and `_lab.shotState()` run before and after the change.",
        "observable": "Star and moon framings succeed and report achieved radii against the correct body's own radius (stars carry radius on `data.radius`, not derived from geometry — the two paths must not be conflated). REGRESSION: `resolveBody` for every existing `{kind:'planet'|'moon'}` subject returns identical `name`/`kind`/`p`/`m`/`backLink`, and `shotState()`'s `body` and `geometry` blocks are unchanged field for field."
      },
      "layer": "integration",
      "live": true
    },
    {
      "id": "AC-6",
      "statement": "The interpolator resync cannot be silently removed. The defect this workstream exists to end is a missing `cameraInterp.resync(camera)` whose absence produces a confident success report, so its presence is held by a gate that fails when it is deleted.",
      "verifyVia": {
        "input": "Run the test suite; then run it again against a mutant with the `resync` call deleted from the game-side framing path.",
        "observable": "A headless source-scan gate (the repo's existing `source-scan.mjs` idiom) asserts the game framing path calls `cameraInterp.resync`. It PASSES on the real source and FAILS on the mutant — a moved control, not an assertion that merely holds."
      },
      "layer": "unit",
      "live": false
    }
  ],
  "architecturalConnections": {
    "inputs": [
      "src/camera/ShipCameraSystem.js:662 `focusOn(position, viewDistance)` — the supported framing path; snaps `distance` AND `smoothedDistance` so `_applyOrbit()` places the camera FROM that state instead of fighting it. Has no `window._lab` hook today, which is exactly why it was unreachable.",
      "src/core/CameraInterpolator.js:91 `resync(camera)` — the module's documented announcement for a teleport discontinuity; collapses both snapshots so the next blend is a no-op.",
      "src/main.js `_lab.resolveBody` — NAME-keyed body resolution over the SIM tree. The single resolution path, deliberately: two index spaces are live at once and resolving through the scene walk instead frames body X while driving body Y, with both hooks reporting success.",
      "planet-lod-lab-core.js `lodRampOf` / `autoOctaves` — the shared LOD law, already imported by both `BodyRenderer.js:11` and `LabPlanetMaterial.js`. Predicted LOD state is computed from these, never re-derived.",
      "world-engine-lab.html state.distance / state.yaw / state.pitch — the lab's frame loop reads these every frame; `state.distance` is ALREADY expressed in planet radii (logical = state.distance / sVis)."
    ],
    "outputs": [
      "`_lab.frameBody(subject, opts)` on BOTH front-ends — returns `{ok, achieved:{radii, units}, lod:{live:{octaves,ramp}, predicted:{octaves,ramp}}, body:{name,kind,p,m}}`.",
      "`_lab.approachSweep(subject, {from, to, steps})` on both front-ends — the per-step measurement table.",
      "Paired A/B stills framed by identical code on both front-ends, with the LOD state that produced them attached — the input PLAN §LAYER 7 (approach detail) needs before it can start.",
      "Evidence of the unregistered-planet-class-moon defect, as a live/predicted disagreement rather than a claim."
    ],
    "mustStayWorking": [
      "`_lab.shotState()` — its `body`, `geometry` and `roi` blocks are the sidecar contract `scripts/shot-diff.mjs` refuses to run without. Widening `resolveBody` to stars must leave every existing field byte-identical for planet and moon subjects.",
      "`_lab.setCameraPose` / `cameraPose` — the existing A/B pose-replay pair. Keep working; they are how prior shots are reproduced. (`setCameraPose` should gain the missing `resync`, which CHANGES its behaviour from broken to correct — that is a fix, and it must be called out rather than slipped in.)",
      "ORRERY <-> HELM mode ownership. `focusOn` is an orbit-camera method; the M-swap already clears ORRERY focus state (`_gliding`, `resetFocusMinDistance()`) so a focus min-distance cannot leak into HELM's supercruise-exit clamp. A framing API that pins focus state must not defeat that clearing.",
      "The arrival-zoom path — `focusOn` calls `_endArrival(false)` by design (a focus-snap cancels a live arrival zoom, AC4). Framing during a spawn's arrival zoom must not leave the zoom half-cancelled.",
      "All four instruments green (`npm run check:instruments`), citations at 320. Any new file carrying symbol-anchored citations lands inside the fence's coverage.",
      "The known-failure baseline of 24 failed / 17 failed FILES / 22685 passed is GREEN — do not read those as damage."
    ]
  }
}
