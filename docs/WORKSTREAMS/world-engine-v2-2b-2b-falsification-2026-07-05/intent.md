# world-engine-v2-2b-2b-falsification-2026-07-05 — intent

## Why we care

> ⚠ **This section is Claude's paraphrase of the program's established rationale (ROADMAP §5.4) —
> Max, rewrite it in your own words if it's off before you greenlight.**

V2-2b-2a built the mixed-interior machinery and proved it on **Tharsis** — but Tharsis is the *easy*
case, the derivable end-member the condition-first premise was always going to produce. V2-2b-2b is the
**honesty half**: the two worlds that can *falsify* the whole condition-first bet.

- The **wet-stagnant world** fires E1's seeded-'stagnant' pick machinery at the genuinely contested band.
  If a wet body seeded 'stagnant' just comes out as "Venus with water," the premise failed exactly where
  the research says determinism breaks down. `effectiveL` is the hand-up that places it in the strong-lid
  *mixed* interior instead of letting it pierce everywhere ("Io-with-water").
- The **corona-pierced world** is the new-landform test — a shield emerging *from* a corona at ONE center,
  a compound expression no catalog world (Io or Venus) contains, and the place we finally **assert** (not
  just measure, as 2b-2a did) that pierce and tent *interpenetrate* rather than *tile* into two catalog patches.

This is also the increment that earns the pilot's **holistic UAT** — the first "do these read as coherent,
distinct, never-observed worlds?" judgment that only Max makes. It closes at `VERIFIED_PENDING_MAX → Max UAT
→ Shipped`, not the objective-terminal pattern 2b-2a used.

## Success criteria (Max's language)

- **The wet-stagnant world reads as a coherent strong-lid world that is _not_ Venus and _not_ a re-rolled
  Venus** — structurally distinct, organized by its own plumes, different per seed. (Basis-level: no literal
  "wet" albedo/hydrology this increment — that's a later expression increment, deliberately out of scope.)
- **The corona-pierced landform is present and crisp** — a shield emerging from a corona at one center, the
  compound expression Tharsis (shields-on-plains) can't produce.
- **The falsification holds:** on the corona-pierced world, pierce and tent *interpenetrate* (the Π=C·F
  statistic fires, M≤0.70, ≥2 legible pierce components), they do *not* tile into separable Io-patch /
  Venus-patch regions.
- **Holistic gate (Max only):** both worlds read **coherent, distinct, and never-observed** — not a faded
  catalog member, not two catalog landforms placed side by side.

## Out of scope (deliberate)

- No literal "wet" expression (albedo/hydrology/shorelines) — basis-level only this increment.
- No production dispatch flip (PRESET_ARCHETYPE → derived E1 regime) — that is V2-3; the mixed path stays lab-only.
- No `e1Regime.js` edit — it already emits `out.effectiveL` (:198,:229); this increment only *reads* it.
