{
  "workstream": "world-engine-v2-0-plumbing-2026-07-03",
  "commit": "0461463",
  "perLayer": {
    "unit": "PASS",
    "integration": "INSUFFICIENT",
    "uat": "N/A"
  },
  "perAC": [
    {
      "id": "AC-0",
      "result": "PASS",
      "evidence": "All three spine checks pass. (1) Driver connectivity: BUILD-PLAN §4 conformance table covers all 13 surfaced/read scalars, cross-checked against code — baseStep.js names rawTidalIoRatio as D12 raw (pre-calibrateTidal) and exports 7 named helpers each mapped to a D-slot or named derivation; body-condition-vector.js annotates every field (D2/D9/D10/D16/D12-input/D12-raw, Slice-B shellThickness with R4 no-d3-transform, D13 + metallicity data-only as mandated by ROADMAP-v2 §3.1 V2-0 row); archetype-string routing declared accepted debt with deriving increments named (V2-1 shadow / V2-3 flip); no new archetype routing added. (2) Named consumer: every table row cites a verified-to-exist ROADMAP-v2 increment (V2-1 E1 §3.1, m_hp §2.3, Φ d³ §7b#4 with SH-F2 triple-duty flag, V2-6 D13, V2-9a D10; presets module → lab + tests + V2-1 conformance oracle); no dead fields. (3) Taxonomy: lab diff adds zero GUI controls (grep of added lab lines for .add(/addFolder = no hits); npx vitest run tests/planet-archetypes.test.js → 21/21 PASSED including the two new lab↔module coupling guards; supporting byte-safety + condition-vector suites 52/52 PASSED pinned to ad156cc snapshots. Non-blocking nit: BUILD-PLAN §2 cites magmatism.js under src/worldengine/history/; actual path is src/worldengine/base/ — code correct, plan text wrong.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC1",
      "result": "PASS",
      "evidence": "Refactor gate holds at ad156cc..0461463 (21 files, zero touches under src/generation/ or vendor/). BYTE-IDENTITY: 130/130 tests pass across the three gate suites; 75/75 SHA-256 carrier hashes (15 archetype-mapped presets × seeds {1,2,3,7,42}, real writeBodyRelief run on deterministic N=700 sphere) equal the committed golden — which git log shows was captured ONCE at 7441c92 and never re-captured, so Slices B and C had to reproduce it; the gate-time bundle attaches the nested Slice-C condition against the condition-less golden, so byte-equality doubles as the widened-bundle inertness proof (BUILD-PLAN §3/R1). Independent chain check: DRIVER_PRESETS + PRESET_ARCHETYPE literals extracted from the real ad156cc lab HTML deep-equal both the committed snapshot fixtures and the current module. TUNE-NULL ANCHORS: driversToTune(D_EARTH)===null and magmaDriversToTune(MAGMA_REF)===null both hold. FULL SUITE at HEAD: 1512 passed / 4 failed; the 17 failing files decompose exactly into 15 vendor/motion-test-kit node:test collection errors (contract-excluded, vendor untouched) + 2 src/generation/__tests__ files whose same 4 assertions fail identically at pre-change base ad156cc in a worktree → pre-existing, outside blast radius. No pre-existing test dropped/skipped: 80 pre-change test files → 85 (+5 additive AC files per R7 amendment), zero dropped, no .skip/.only added; scoped tests/ run 1220/1220 green. Honest caveat: suite is not absolutely green, but the failure set is byte-identical pre vs post.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC2",
      "result": "PASS",
      "evidence": "baseStep.js exports pure grid-free deriveBodyScalars plus all AC-named helpers (bodyRawTidal, bodyShellThickness, bodyThermalState, bodyRadialStrain, bodyLiquidStability, bodySurfaceGravity, bodyAgeNorm), single-source — makeBaseStep calls deriveBodyScalars once then does only the crustalThickness grid loop + reassembly. Golden provenance non-vacuous: git diff ad156cc 7441c92 on baseStep.js is EMPTY and Slice A touched neither relief-presets nor src/worldengine/base/, so tests/fixtures/v2-0-basestep-goldens.json was captured with inputs byte-identical to ad156cc and is unchanged through 0461463. Coverage: 6 golden bundles each pinning all 13 drivers scalars, crust fields, crustalThickness SHA-256, and raw-tidal. Runs: golden + scalar-helpers suites 77/77 passed; all 24 worldengine-base-* files 355/355 passed; no pre-existing worldengine-base-* suite file modified. Oracle: calibrateTidal(bodyRawTidal(b)) === drivers.tidalHeat holds against both live output and frozen golden, plus forward-drift pin on rawTidal.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC3",
      "result": "PASS",
      "evidence": "driver-presets.js exports exactly 17 DRIVER_PRESETS entries including Mars and Hot Jupiter; PRESET_NAMES matches key order; PRESET_ARCHETYPE has 15 entries with Mars + Hot Jupiter correctly absent (data-but-no-mapping). Lab imports the module (planet-lod-lab.html:164); zero residual assignment literals in the HTML. tests/planet-archetypes.test.js imports the module (line 10) and builds panelPresetKeys from Object.keys — preset string-scrape gone (the test still reads labSrc as text, but only for the unrelated *Enabled-key scrape and for the new lab↔module coupling assertions that enforce this AC's own clause — no preset DATA sourced from HTML). Deep-equal: independent brace-matched extraction of both literals from pre-extraction commit eaa6a07 deep-equals the module exports (17 + 15 entries, key order identical); repo guard test additionally pins both to ad156cc fixtures. Runs: planet-archetypes 21/21; combined with v2-0-slice-a-byte-safety 45/45 passed.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC4",
      "result": "PASS",
      "evidence": "New body-condition-vector.js exports pure deriveConditionVector(fp, derived, radiusEarth) emitting every named field — density/composition passthrough, age, radiusEarth (drawn value with fp fallback), eccentricity, rawTidalIoRatio (RAW/un-calibrated, with bodyRawTidal fallback), shellThickness (baseStep helper, no d³ transform), plus data-only magneticField/metallicity. Threading traced: lab buildBodyDrivers attaches condition NESTED under bodyDrivers → route() call site → planet-lod-rivers.js route → writeBodyRelief receives bodyDrivers verbatim; gate fixture attaches the same single-source condition. Runs: condition-vector suite 28/28 (all fields present; _fp-derived NON-default values for Magma/Europa/Ocean; rawTidalIoRatio === deriveUniforms(fp).tidalHeat, !== calibrateTidal(raw), >1 so m_hp computable on the production derive; instrumented spy over the REAL writeBodyRelief asserts nested condition arrives at the seam; tune builders byte-equal with vs without condition for ALL presets); byte-identity suites 102/102 (condition-bearing bundle still matches condition-less goldens). Honest deviation from verifyVia wording: writer instrumented directly at the writeBodyRelief seam rather than injected into route() (writeBodyRelief is statically imported, not injectable); static trace confirms route() forwards bodyDrivers unmodified, so the observable is equivalent. Full suite: 4 pre-existing unrelated failures reproduce identically at parent commit.",
      "adversarialVotes": "3/3 sufficient"
    },
    {
      "id": "AC5",
      "result": "INSUFFICIENT",
      "evidence": "Live browser pass not yet driven — static/CLI checks all clean but cannot substitute per the live-integration rule (0/3 adversarial votes sufficient). What IS established: diff classification shows body-condition-vector.js is pure; lab change is one import + one nested condition attach; planet-lod-rivers.js change is comment-only; worldengine-fieldviz.html is untouched and its single baseStep import (makeBaseStep) is still exported after Slice B — no static breakage vector. node import smoke from repo root resolves and returns all 9 named fields. Headless gate suites 170/170 passed. Live-read surface confirmed in source: state._lastBodyDrivers set per-route and surfaced via window._lab, so the nested condition will be inspectable live. REMAINING (working-Claude, chrome-devtools): load planet-lod-lab.html on the dev server, step 4+ archetype presets, verify zero console errors + presets render, inspect _lab state for the new condition-vector fields in the live bundle; load worldengine-fieldviz.html, verify zero console errors and render.",
      "adversarialVotes": "0/3 sufficient"
    }
  ],
  "overall": "INSUFFICIENT — unit layer PASS (5/5 ACs, 3/3 adversarial each), live integration pending: AC5 deferred to working-Claude to drive in the browser. No UAT-layer ACs in this contract.",
  "summaryForMax": "Line of sight: V2-0 is the plumbing increment of the World Engine v2 condition-first program (ROADMAP-v2 §3.1) — it surfaces the body condition-vector (D12 raw Io-ratio, eccentricity, D13, D16, metallicity, shellThickness, etc.) to the writer seam and extracts baseStep scalars + DRIVER_PRESETS into importable modules, so V2-1's E1 regime selector, the m_hp heat-pipe margin, and the Φ size-proxy have real inputs instead of lab-only fallbacks. It is deliberately a ZERO-behavior-change refactor. Status at 0461463: all five headless unit ACs PASS with strong evidence — 75/75 SHA-256 carrier hashes byte-identical to the once-captured golden across all 15 archetype presets × 5 seeds, both tune-null anchors hold, baseStep outputs equal ad156cc-captured goldens exactly, presets deep-equal the pre-extraction literals, and the condition vector demonstrably arrives at the writeBodyRelief seam un-calibrated. The 4 failing src/generation tests and 15 vendor collection errors all reproduce identically at the pre-change base — pre-existing, outside this workstream. WHAT REMAINS: AC5 (live integration) is INSUFFICIENT and is deferred to working-Claude, NOT to you — it is an objective wiring check: chrome-devtools load of planet-lod-lab.html (step 4+ presets, zero console errors, _lab shows the new condition fields) and worldengine-fieldviz.html (loads + renders clean). This contract has no UAT-layer ACs, so there is no acceptance gate for you on this increment; once working-Claude greens AC5, the workstream can move to VERIFIED_PENDING_MAX only if you want a look, otherwise integration-complete per contract."
}
