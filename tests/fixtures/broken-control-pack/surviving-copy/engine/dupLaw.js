// tests/fixtures/broken-control-pack/surviving-copy/engine/dupLaw.js
//
// ⛔⛔ BROKEN ON PURPOSE, AS A PAIR with ../lab-copy.html. DO NOT RECONCILE THEM.
//
// The executed control for one-pipeline-fence registration 3 — "no surviving lab copy". This is the
// engine-side declaration; the lab-side copy declares the SAME name with a DIFFERENT value, which is
// the `craterRelief.glsl.js` precedent the plan names: both were kept, and the copy became a law that
// quietly disagrees. The values differ deliberately so the fixture also demonstrates the drift the
// fence exists to prevent.
export const DUPLICATED_LAW = 2.0;
