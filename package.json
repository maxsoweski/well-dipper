{
  "name": "well-dipper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync-ships": "bash scripts/sync-ships.sh",
    "generate-glow": "node scripts/generate-galaxy-glow.mjs",
    "generate-glow:test": "node scripts/generate-galaxy-glow.mjs --test",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "vite": "^7.3.1",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "alea": "^1.0.1",
    "lil-gui": "^0.21.0",
    "simplex-noise": "^4.0.3",
    "three": "^0.183.1"
  }
}
